<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.dromara.permission.mapper.PcRoleResourcePermissionMapper">

    <resultMap id="RoleResourcePermissionResultMap" type="org.dromara.permission.domain.PcRoleResourcePermission">
        <id column="id" property="id"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="abstract_role_id" property="abstractRoleId"/>
        <result column="resource_entity_id" property="resourceEntityId"/>
        <result column="operation_permission_id" property="operationPermissionId"/>
        <result column="can_manage" property="canManage"/>
        <result column="condition_id" property="conditionId"/>
        <result column="created_by" property="createdBy"/>
        <result column="updated_by" property="updatedBy"/>
        <result column="deleted_by" property="deletedBy"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="deleted_at" property="deletedAt"/>
        <result column="delete_flag" property="deleteFlag"/>
    </resultMap>

    <select id="selectByTenantAndRoleIds" resultMap="RoleResourcePermissionResultMap">
        SELECT id, tenant_id, abstract_role_id, resource_entity_id, operation_permission_id,
               can_manage, condition_id, created_by, updated_by, deleted_by,
               created_at, updated_at, deleted_at, delete_flag
        FROM role_resource_permission
        WHERE tenant_id = #{tenantId}
          AND delete_flag = 0
          AND abstract_role_id IN
          <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
              #{roleId}
          </foreach>
    </select>

    <select id="selectByTenantAndRoleIdsAndTarget" resultMap="RoleResourcePermissionResultMap">
        SELECT id, tenant_id, abstract_role_id, resource_entity_id, operation_permission_id,
               can_manage, condition_id, created_by, updated_by, deleted_by,
               created_at, updated_at, deleted_at, delete_flag
        FROM role_resource_permission
        WHERE tenant_id = #{tenantId}
          AND delete_flag = 0
          AND abstract_role_id IN
          <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
              #{roleId}
          </foreach>
          AND resource_entity_id = #{resourceEntityId}
          AND operation_permission_id = #{operationPermissionId}
    </select>
</mapper>
