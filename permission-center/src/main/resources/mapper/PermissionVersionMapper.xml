<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ac.fage.accessmesh.permission.mapper.PermissionVersionMapper">

    <select id="selectLatestByRole" resultType="cn.ac.fage.accessmesh.permission.entity.PermissionVersion">
        SELECT id, tenant_id, abstract_role_id, version_no, created_at
        FROM permission_version
        WHERE tenant_id = #{tenantId} AND abstract_role_id = #{roleId}
        ORDER BY version_no DESC
        LIMIT 1
    </select>

    <select id="selectAllByRolesOrdered" resultType="cn.ac.fage.accessmesh.permission.entity.PermissionVersion">
        SELECT id, tenant_id, abstract_role_id, version_no, created_at
        FROM permission_version
        WHERE tenant_id = #{tenantId} AND abstract_role_id IN
        <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
            #{roleId}
        </foreach>
        ORDER BY version_no DESC
    </select>

</mapper>
