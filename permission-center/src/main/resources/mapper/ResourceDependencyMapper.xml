<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ac.fage.accessmesh.permission.mapper.ResourceDependencyMapper">

    <update id="softDeleteBatch">
        UPDATE resource_dependency
        SET delete_flag = id, deleted_at = #{deletedAt}
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND delete_flag = 0 AND tenant_id = #{tenantId}
    </update>

    <update id="softDeleteBatchByOwnerService">
        UPDATE resource_dependency
        SET delete_flag = id, deleted_at = #{deletedAt}
        WHERE tenant_id = #{tenantId}
          AND owner_service_code = #{ownerServiceCode}
          AND maintain_source = #{maintainSource}
          AND delete_flag = 0
    </update>

</mapper>
