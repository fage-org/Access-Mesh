<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ac.fage.accessmesh.permission.mapper.AbstractRoleMapper">

    <select id="selectRoleTreeByGroupIds" resultType="cn.ac.fage.accessmesh.permission.entity.AbstractRole">
        WITH RECURSIVE role_tree AS (
            SELECT * FROM abstract_role
            WHERE id IN
            <foreach collection="groupRoleIds" item="id" open="(" separator="," close=")">
                #{id}
            </foreach>
            AND tenant_id = #{tenantId} AND delete_flag = 0

            UNION ALL

            SELECT r.* FROM abstract_role r
            INNER JOIN role_tree rt ON r.parent_id = rt.id
            WHERE r.tenant_id = #{tenantId} AND r.delete_flag = 0
        )
        SELECT * FROM role_tree
    </select>

</mapper>