<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ac.fage.accessmesh.permission.mapper.UserRoleMapper">

    <update id="softDeleteBatch">
        UPDATE user_role
        SET delete_flag = id, deleted_at = #{deletedAt}
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND delete_flag = 0 AND tenant_id = #{tenantId}
    </update>

    <select id="selectValidByUserIds" resultType="cn.ac.fage.accessmesh.permission.entity.UserRole">
        SELECT * FROM user_role
        WHERE tenant_id = #{tenantId}
          AND abstract_user_id IN
        <foreach collection="userIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
          AND delete_flag = 0
    </select>

    <select id="selectValidByUserIdsAndTargetIds" resultType="cn.ac.fage.accessmesh.permission.entity.UserRole">
        SELECT * FROM user_role
        WHERE tenant_id = #{tenantId}
          AND abstract_user_id IN
        <foreach collection="userIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
          AND target_id IN
        <foreach collection="targetIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
          AND target_type = #{targetType}
          AND delete_flag = 0
    </select>

    <select id="selectValidByUserIdsAndTargetId" resultType="cn.ac.fage.accessmesh.permission.entity.UserRole">
        SELECT * FROM user_role
        WHERE tenant_id = #{tenantId}
          AND abstract_user_id IN
        <foreach collection="userIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
          AND target_id = #{targetId}
          AND target_type = #{targetType}
          AND delete_flag = 0
    </select>

    <select id="selectValidByUserIdsTypeAndTargetIds" resultType="cn.ac.fage.accessmesh.permission.entity.UserRole">
        SELECT * FROM user_role
        WHERE tenant_id = #{tenantId}
          AND abstract_user_id IN
        <foreach collection="userIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
          AND target_type = #{targetType}
          AND target_id IN
        <foreach collection="targetIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
          AND delete_flag = 0
    </select>

    <select id="selectValidByUserIdWithValidity" resultType="cn.ac.fage.accessmesh.permission.entity.UserRole">
        SELECT * FROM user_role
        WHERE abstract_user_id = #{userId}
          AND tenant_id = #{tenantId}
          AND delete_flag = 0
          AND (valid_from &lt;= #{now} OR valid_from IS NULL)
          AND (valid_to &gt;= #{now} OR valid_to IS NULL)
    </select>

    <select id="selectValidByUserIdsWithValidity" resultType="cn.ac.fage.accessmesh.permission.entity.UserRole">
        SELECT * FROM user_role
        WHERE tenant_id = #{tenantId}
          AND abstract_user_id IN
        <foreach collection="userIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
          AND delete_flag = 0
          AND (valid_from &lt;= #{now} OR valid_from IS NULL)
          AND (valid_to &gt;= #{now} OR valid_to IS NULL)
    </select>

    <select id="selectValidByTargetIdAndType" resultType="cn.ac.fage.accessmesh.permission.entity.UserRole">
        SELECT * FROM user_role
        WHERE tenant_id = #{tenantId}
          AND target_id = #{targetId}
          AND target_type = #{targetType}
          AND delete_flag = 0
    </select>

    <select id="selectValidByTargetIdsAndType" resultType="cn.ac.fage.accessmesh.permission.entity.UserRole">
        SELECT * FROM user_role
        WHERE tenant_id = #{tenantId}
          AND target_id IN
        <foreach collection="targetIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
          AND target_type = #{targetType}
          AND delete_flag = 0
    </select>


    <select id="selectValidByTargetAndRelation" resultType="cn.ac.fage.accessmesh.permission.entity.UserRole">
        SELECT * FROM user_role
        WHERE tenant_id = #{tenantId}
          AND target_type = #{targetType}
          AND target_id = #{targetId}
          AND relation_id = #{relationId}
          AND delete_flag = 0
    </select>

    <select id="selectByTargetTypeAndTargetId" resultType="cn.ac.fage.accessmesh.permission.entity.UserRole">
        SELECT * FROM user_role
        WHERE tenant_id = #{tenantId}
          AND target_type = #{targetType}
          AND target_id = #{targetId}
          AND delete_flag = 0
    </select>

    <select id="selectValidByUserTargetRelation" resultType="cn.ac.fage.accessmesh.permission.entity.UserRole">
        SELECT * FROM user_role
        WHERE tenant_id = #{tenantId}
          AND target_type = #{targetType}
          AND abstract_user_id IN
        <foreach collection="userIds" item="uid" open="(" separator="," close=")">
            #{uid}
        </foreach>
          AND target_id IN
        <foreach collection="targetIds" item="tid" open="(" separator="," close=")">
            #{tid}
        </foreach>
          AND relation_id IN
        <foreach collection="relationIds" item="rid" open="(" separator="," close=")">
            #{rid}
        </foreach>
          AND delete_flag = 0
    </select>

    <!-- 孤儿 user_role 查询：用户已软删但 user_role 仍存活（全租户，延迟补偿用） -->
    <select id="selectOrphansByCutoff" resultType="cn.ac.fage.accessmesh.permission.entity.UserRole">
        SELECT ur.*
        FROM user_role ur
        JOIN abstract_user au ON ur.abstract_user_id = au.id AND au.tenant_id = ur.tenant_id
        WHERE ur.delete_flag = 0
          AND au.delete_flag != 0
          AND ur.updated_at &lt; #{cutoff}
    </select>

</mapper>