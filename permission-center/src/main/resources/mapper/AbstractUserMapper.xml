<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ac.fage.accessmesh.permission.mapper.AbstractUserMapper">

    <!-- selectUserIdsByBizDomain 已移除：abstract_role.biz_domain_id列已删除，域过滤改为DomainClassifyService按类型码过滤 -->

    <update id="softDeleteBatch">
        UPDATE abstract_user
        SET delete_flag = id, deleted_at = #{deletedAt}
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND delete_flag = 0 AND tenant_id = #{tenantId}
    </update>

    <select id="selectValidById" resultType="cn.ac.fage.accessmesh.permission.entity.AbstractUser">
        SELECT * FROM abstract_user
        WHERE id = #{id}
          AND tenant_id = #{tenantId}
          AND delete_flag = 0
    </select>

    <select id="selectByTypeAndExternalId" resultType="cn.ac.fage.accessmesh.permission.entity.AbstractUser">
        SELECT * FROM abstract_user
        WHERE tenant_id = #{tenantId}
          AND user_type = #{userType}
          AND external_id = #{externalId}
          AND delete_flag = 0
    </select>

    <select id="selectValidByIds" resultType="cn.ac.fage.accessmesh.permission.entity.AbstractUser">
        SELECT * FROM abstract_user
        WHERE tenant_id = #{tenantId}
          AND id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
          AND delete_flag = 0
    </select>

    <select id="selectUserListPaged" resultType="cn.ac.fage.accessmesh.permission.entity.AbstractUser">
        SELECT * FROM abstract_user
        WHERE tenant_id = #{tenantId}
          AND delete_flag = 0
        <if test="matchNone">
          AND id = -1
        </if>
        <if test="!matchNone and userType != null">
          AND user_type = #{userType}
        </if>
        <if test="!matchNone and keyword != null and keyword != ''">
          AND (name LIKE CONCAT('%', #{keyword}, '%') OR external_id LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        ORDER BY id
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="selectUserListCount" resultType="long">
        SELECT COUNT(*) FROM abstract_user
        WHERE tenant_id = #{tenantId}
          AND delete_flag = 0
        <if test="matchNone">
          AND id = -1
        </if>
        <if test="!matchNone and userType != null">
          AND user_type = #{userType}
        </if>
        <if test="!matchNone and keyword != null and keyword != ''">
          AND (name LIKE CONCAT('%', #{keyword}, '%') OR external_id LIKE CONCAT('%', #{keyword}, '%'))
        </if>
    </select>

    <select id="selectByTypeAndExternalIds" resultType="cn.ac.fage.accessmesh.permission.entity.AbstractUser">
        SELECT * FROM abstract_user
        WHERE tenant_id = #{tenantId}
          AND user_type = #{userType}
          AND external_id IN
        <foreach collection="externalIds" item="eid" open="(" separator="," close=")">
            #{eid}
        </foreach>
          AND delete_flag = 0
    </select>

</mapper>
