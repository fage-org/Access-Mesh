<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ac.fage.accessmesh.permission.mapper.TypeDefinitionMapper">

    <update id="softDeleteBatch">
        UPDATE type_definition
        SET delete_flag = id, deleted_at = #{deletedAt}
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND delete_flag = 0 AND tenant_id = #{tenantId}
    </update>

    <select id="selectByTypeKeyAndCode" resultType="cn.ac.fage.accessmesh.permission.entity.TypeDefinition">
        SELECT * FROM type_definition
        WHERE tenant_id = #{tenantId}
          AND type_key = #{typeKey}
          AND type_code = #{typeCode}
          AND delete_flag = 0
    </select>

    <select id="selectByTypeKeyAndValue" resultType="cn.ac.fage.accessmesh.permission.entity.TypeDefinition">
        SELECT * FROM type_definition
        WHERE tenant_id = #{tenantId}
          AND type_key = #{typeKey}
          AND type_value = #{typeValue}
          AND delete_flag = 0
    </select>

    <select id="selectByTypeKeyAndCodes" resultType="cn.ac.fage.accessmesh.permission.entity.TypeDefinition">
        SELECT * FROM type_definition
        WHERE tenant_id = #{tenantId}
          AND type_key = #{typeKey}
          AND type_code IN
        <foreach collection="codes" item="code" open="(" separator="," close=")">
            #{code}
        </foreach>
          AND delete_flag = 0
    </select>

    <select id="selectByTypeKeyAndValues" resultType="cn.ac.fage.accessmesh.permission.entity.TypeDefinition">
        SELECT * FROM type_definition
        WHERE tenant_id = #{tenantId}
          AND type_key = #{typeKey}
          AND type_value IN
        <foreach collection="values" item="value" open="(" separator="," close=")">
            #{value}
        </foreach>
          AND delete_flag = 0
    </select>

    <select id="selectValidById" resultType="cn.ac.fage.accessmesh.permission.entity.TypeDefinition">
        SELECT * FROM type_definition
        WHERE id = #{typeDefId}
          AND tenant_id = #{tenantId}
          AND delete_flag = 0
    </select>

    <select id="selectByTenantId" resultType="cn.ac.fage.accessmesh.permission.entity.TypeDefinition">
        SELECT * FROM type_definition
        WHERE tenant_id = #{tenantId}
          AND delete_flag = 0
    </select>

    <select id="selectValidByIds" resultType="cn.ac.fage.accessmesh.permission.entity.TypeDefinition">
        SELECT * FROM type_definition
        WHERE tenant_id = #{tenantId}
          AND delete_flag = 0
          AND id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <select id="selectByTenantAndTypeKey" resultType="cn.ac.fage.accessmesh.permission.entity.TypeDefinition">
        SELECT * FROM type_definition
        WHERE tenant_id = #{tenantId}
          AND type_key = #{typeKey}
          AND delete_flag = 0
    </select>

</mapper>
