---
doc_type: task
id: T-FE-040
title: 4.1 权限授予弹窗 v3.1 记录级聚焦编辑（决策记录 permission-grant-record-level-editing-proposal 已确认）
status: done
plan: docs/plans/frontend-phase2-plan.md
domain: frontend
design_refs:
  - docs/design/frontend/permission-grant.md
  - docs/design/permission-center/api-contract.md#§6.5.1
  - docs/design/permission-center/api-contract.md#§6.5.2
  - docs/plans/permission-grant-record-level-editing-proposal.md
depends_on:
  - T-FE-039
blocks:
  - T-FE-018
acceptance:
  - >-
    S1 记录级聚焦编辑：弹窗打开自动聚焦 initial 完整授权键（resourceTypeCode + resourceCode + codeType + operationCode + scopeMode，未授权也聚焦、设置区只读展示默认值）；复选框切换勾选（取消已有节点生成 remove、新勾选生成 add 且属性=新增默认值 conditionCode=null/canGrant=false/children=[]），点击行其余区域聚焦该记录；焦点为空时首次勾选自动聚焦，焦点非空时批量勾选不抢焦点；聚焦记录的条件/canGrant 修改只生成该记录自己的 update，不产生其他记录变更；取消勾选焦点记录时焦点清空；选择 ALL 聚焦 ALL 槽位，取消 ALL 回 INSTANCE 清空焦点；设置区不继承显示值到新勾选
  - >-
    S2 焦点生命周期：焦点=MANUAL 编辑槽位（资源、操作、范围），与来源记录解耦；AUTO_DEP/继承来源只读、不阻碍聚焦（仅 AUTO_DEP 来源的资源行可聚焦只读，勾选创建并存 MANUAL）；取消勾选草稿 suspended 暂存（同弹窗重新勾选恢复；提交时 baseline 记录生成 remove、待新增 add 草稿取消整个变更组、取消弹窗恢复进入前页面草稿）；未授权资源可聚焦（设置区只读，勾选后启用）
  - >-
    S3 显式复制：设置区提供"复制当前设置到…"入口，源=聚焦记录（条件+再授予），目标=已勾选 MANUAL 记录多选+确认覆盖，不复制子权限；启用条件=焦点为有效 MANUAL 或待新增 add 草稿；目标排除源、跳过同值、add 就地改属性、update 合并单条、禁止同一 id 追加多条 update、合并后 after==before 归一化删除（复用 applyDialogResultToDraft 无差异清理）；条件非空时目标 canGrant=false（20041 口径）；源条件已停用时整个复制入口禁用（提示"请先选择启用条件或清除条件后再复制"，避免隐式清空目标条件导致权限扩大）
  - >-
    S4 停用条件（20042 同口径）：条件选择器仅允许选择启用条件（停用灰显不可选）；已绑定后停用的条件回显并标注"条件已停用"，保存时未修改 conditionCode 允许保留；修改 conditionCode 时新值必须启用；条件选择/复制条件无 CONDITION:VIEW 门禁（条件规则全租户开放）；错误码提示：20042 条件已停用、20043 子权限属性不允许
  - >-
    S5 条件管理页与授权页 CONDITION:VIEW 门禁移除（2026-08-08 产品确认全租户开放）：条件管理页路由 auths 门禁、loadList 短路、canView 空态、角色矩阵 VIEW 列移除；GrantDialog canCondition 禁用逻辑与 tooltip 移除（条件选择器始终可用）；permission-condition.md §权限接线/矩阵同步
  - >-
    S6 子权限记录级入口：子权限入口位于授权设置区，标题展示挂载目标（"配置当前授权的子权限 / 挂载到：A资源 · VIEW · 条件 C"）；单父记录上下文、父权限下拉退役；启用条件=焦点为有效 MANUAL 或待新增 add 草稿（未聚焦/未授权聚焦/仅 AUTO_DEP 槽位时禁用）；资源类型选择器按 sub-perm-allowed-types 允许集过滤：ALLOW_ALL 不限制、ALLOW_LIST 仅列表内类型（其余禁用）、ALLOW_NONE 按 reason 区分（PARENT_NOT_CONFIGURED/CHILD_TYPES_EMPTY=业务不允许"该资源类型不允许配置子权限"，CONFIG_MISSING/EMPTY/INVALID=配置异常"请联系管理员"）；候选三态：加载中=loading、加载失败或无 TYPE_DEFINITION:VIEW=失败+重试（不得误报为 SUB_PERM 配置错误）、成功且交集为空=配置/数据不一致空态；子权限属性固定 conditionCode=null/canGrant=false（20043 后端强制，界面不可编辑）；不允许前端硬编码允许集
  - >-
    S7 节点紧凑摘要：已勾选 MANUAL 节点与 baseline MANUAL 标记 remove（待撤销）的节点在行内展示紧凑摘要（条件名称、再授予状态、子权限数量、草稿状态）；无条件记录显示浅色[无条件]；待撤销节点红色"待撤销"；未勾选节点（除待撤销外）不显示摘要
  - >-
    S8 验收场景（permission-grant.md §11 S3 场景全集）：矩阵无权限单元格打开自动聚焦 initial、聚焦记录条件/canGrant 修改只生成自身 update、显式复制覆盖确认与归一化、停用条件选择/复制/保存全链路、子权限允许集过滤与三态、suspended 双路径提交、取消弹窗不泄漏草稿；提交走 apply-grant-plan 单事务（错误码映射 20041/20042/20043/20033/20034/20036 提示文案）
  - >-
    S9 实施文件与测试门禁（提案 §12 第 3 步要求）：组件改造——GrantDialog.vue（焦点槽位状态迁移、复选框/整行聚焦分离、ALL 焦点迁移、复制弹窗、停用条件选择器、20042/20043 错误提示）、GrantChildConfigurator.vue（记录级入口、SUB_PERM 允许集过滤、候选三态）；草稿纯函数——grant/utils/grant-plan.ts 记录槽位 reducer（focus/suspend/resume/复制合并归一化 after==before 清理，纯函数可单测）+ grant-plan.spec.ts；API 与 mock——src/api/permission-grant.ts 接入 sub-perm-allowed-types 与 20042/20043 错误映射（**路径为 src/api，不在 grant/utils 下**），mock 更新（子权限条件冲突从 20041 改 20043、停用条件数据、sub-perm-allowed-types 各 mode/reason 场景）；条件管理页门禁移除（路由 auths、loadList 短路、canView 空态、GrantDialog canCondition/tooltip）；**测试门禁：纯 reducer 单测 + mock 人工验收，不引入组件测试基础设施**——焦点生命周期/suspended 双路径/复制归一化/ALL 迁移等核心逻辑全部在 grant-plan.ts 纯 reducer 上以 vitest node 环境单测覆盖（现有配置可跑，不新增 jsdom/@vue/test-utils）；组件层为绑定薄壳不单测；S1~S8 场景在 mock 上人工验收全过后再进真实接口联调
  - >-
    design_writeback：permission-grant.md v3.1 各节实现状态标注、api-contract §6.5.1/§6.5.2 契约核对、任务验收描述逐项人工核对，任一不同步不通过
design_writeback:
  required: true
  status: done
last_updated: 2026-08-09
---

# T-FE-040 4.1 权限授予弹窗 v3.1 记录级聚焦编辑

> 状态：done（2026-08-09 S1~S8 mock 人工验收完成；记录槽位 reducer、mock 契约、错误映射与跨标签页 20042 竞态均已复验通过）
> 依赖：**mock-first（已确认）**——mock 阶段不依赖后端任务；真实接口联调（T-FE-018）前需完成 T-PERM-034（apply-grant-plan 扩展：20042/20043、sub-perm-allowed-types、SubPermissionPolicy）与 T-PERM-041（主权限条件不变量）
> 前置验收：见 acceptance（DoD 门禁）

## 完成记录（2026-08-09）

- **纯函数层**：`grant/utils/grant-plan.ts` 新增记录槽位 reducer——`FocusSlot/SlotAttributes/SuspendedSlot/SlotDraftState` 类型 + `slotKeyOf/findSlotRecord/uncheckSlot/resumeSlot/applyFocusAttributes/copySlotAttributes/expandSuspended/normalizeNoopUpdates/createSlotDraftState`；`applyDialogResultToDraft` 改为记录级语义（勾选仅表达存在性、add 默认属性、focusSlot+focusAttributes 驱动属性修改、条件不可转授兜底保留）；`grant-plan.spec.ts` 新增 15 个 reducer 用例 + 更新 6 个 v3 语义用例（用户已确认可更新）
- **组件层**：`GrantDialog.vue` v3.1 改造（焦点模型 focusSlot/focusedRecord、复选框勾选与点击行聚焦分离、@check 差量实时驱动 uncheckSlot/resumeSlot、ALL 勾选焦点迁移与取消 suspended、设置区仅聚焦记录编辑、D4 有效来源提示、显式复制弹窗、节点紧凑摘要、确认时 expandSuspended 双路径、canCondition 门禁与 tooltip 移除）；`GrantChildConfigurator.vue` 单父上下文（父=聚焦记录，父下拉退役）+ sub-perm-allowed-types 允许集过滤 + 候选三态（loading/失败/交集空含各 reason 文案）；`GrantDialog` 新增 subjectKey prop（index.vue 从 grantStore.context 传入）
- **API/mock 层**：`src/api/permission-grant.ts` 接入 sub-perm-allowed-types（SubPermAllowedTypesReq/SubPermissionPolicy + getSubPermAllowedTypes）+ 20042 CONDITION_DISABLED / 20043 SUB_PERMISSION_ATTRIBUTE_INVALID 错误码与文案；`mock/_shared/sub-perm-policy.ts` 按后端 fail-closed 规则统一解析 SUB_PERM（配置缺失/空/非法、父类型未覆盖、子类型空、顶层/嵌套通配、大小写合并）；`mock/permission-grant.ts` 用显式配置数据生成 DATA→ALLOW_ALL、MENU→ALLOW_LIST[BUTTON]、REPORT→ALLOW_LIST[DATA,REPORT]、API→PARENT_NOT_CONFIGURED、BUTTON→CONFIG_MISSING，并执行 20042/20043 校验；`mock/_shared/permission-condition-store.ts` 将条件 CRUD 与授权校验状态挂载到 fake-server 服务进程 `globalThis`（生产 mock 兼容同源 localStorage），解决独立 mock 文件分别打包导致的状态副本问题，确保跨标签页“选中后停用再保存”稳定触发 20042；BASIC_202 增加 MENU·VIEW 的无条件/启用条件复制源/归一化目标和 MENU·DELETE 的停用条件存量绑定夹具；`mock/login.ts` 角色矩阵 CONDITION:VIEW 列移除（hr/auditor 不再持有条件读取串）
- **门禁移除（S5）**：条件管理页路由 auths、loadList 短路、canView 空态；授权页 canCondition 门控与 GrantDialog 禁用/tooltip
- **验证**：vitest 全量 206 tests 通过（8 文件，含 grant-plan.spec.ts 72、SUB_PERM 策略 9、mock 授权契约 6、错误码映射 36）；vue-tsc / eslint（max-warnings 0）/ prettier / vite build 全过；设计回写 permission-grant.md v3.1 状态标注 done
- **实现校核（2026-08-09）**：
  - ALL 勾选按 resumeSlot→同步草稿→聚焦执行；全局操作切换类型时撤销旧类型并恢复新类型，ALL 主权限可独立配置属性与子权限
  - suspended 保存完整 add/属性/子权限快照；恢复时保留原 changeId，兜底重建会同步重映射子权限 parentChangeId；baseline 主记录 update 按快照相对基线重建且不会重复
  - suspended 槽位不进入预填、编辑或复制候选；页面 add/remove 草稿分别显示“待授权/待撤销”，待撤销 baseline MANUAL 节点保留条件、再授予和子权限摘要
  - 未授权 initial 仅按完整授权键聚焦，不伪造勾选或 add；首次新勾选在焦点为空时自动聚焦，未授权焦点的条件与再授予控件保持只读
  - 显式复制合并既有 update 并保留 before/changeId，复制回 baseline 时通过 noop 归一化删除；停用条件不能作为复制源
  - 子权限候选区分加载中、失败可重试、成功但交集为空和 ALLOW_NONE 原因；mock 按契约执行 20041/20042/20043 优先级及 update null 三态
- **首轮验收问题修复（2026-08-09）**：修复 API 父类型未配置被误判 CONFIG_MISSING、条件页与授权 mock 状态割裂、复制/回退/停用条件缺少稳定夹具、错误码只能依赖手工造竞态四类问题；本地页面已复验 BASIC_202 三种授权摘要/停用条件回显与禁用复制/API 的 PARENT_NOT_CONFIGURED 文案，mock 契约测试覆盖 20042/20043。
- **最终复签（2026-08-09）**：S1 修复 Element Plus 内部节点 id 与业务资源 id 混用导致的 initial 高亮缺失，并将行聚焦事件绑定到自定义行内容、显式关闭 check-on-click-node；S2~S7 逐项人工通过；S8 在两个浏览器标签页完成“授权页选中 office-hours → 条件页停用 → 返回保存”，页面正确提示 20042 且保留全部变更可重试；20043 继续由非法子权限 payload 契约测试覆盖。
- **后续**：真实接口联调归 T-FE-018（依赖 T-PERM-034 + T-PERM-041）。

## 验收场景清单（mock 人工验收，账号 admin/sec 可编辑、hr/auditor 只读）

1. **S1 焦点与 initial**：矩阵无权限单元格（DATA·data:r1·VIEW）打开 → 自动聚焦该行（行高亮）+ 设置区显示该记录属性；未授权单元格打开 → 聚焦 + 设置区只读默认值
2. **S1 勾选/聚焦分离**：复选框取消已授权节点 → 待撤销 + 焦点清空；同弹窗重新勾选 → 恢复（无 remove 残留）；新勾选 → add 默认（无条件/不可转授/无子权限）
3. **S2 suspended 双路径**：取消勾选持久化记录（如 data:r1）→ 确定 → 变更清单出现 remove；取消勾选本弹窗新增记录 → 确定 → 无变更；取消弹窗 → 页面草稿不变
4. **S2 ALL 迁移**：勾选「全量」→ 自动聚焦 ALL 槽位，设置区可改 ALL 记录属性；取消「全量」→ 焦点清空；原有 ALL 记录取消后确定 → remove
5. **S3 显式复制**：选择高级用户（BASIC_202）+ MENU·VIEW；聚焦 role（office-hours）→ 「复制设置到已勾选授权」→ 目标 res-op → 确定 → 目标属性同步（update 单条）；再聚焦 user（无条件+可转授）复制到 res-op → 回到 baseline、清单无幽灵条目；MENU·DELETE 聚焦 sys-mgmt（temp-access）→ 复制按钮禁用
6. **S4 停用条件**：条件选择器列表中 temp-access 灰显不可选；选择高级用户（BASIC_202）+ MENU·DELETE，sys-mgmt 回显「临时开放窗口（temp-access）/条件已停用」；未修改 conditionCode 可保留，改选启用条件后保存不触发 20042
7. **S6 子权限单父**：聚焦 DATA 主权限 → 「配置子权限」→ 资源类型下拉显示全部类型（ALLOW_ALL）；聚焦 MENU 主权限 → 仅 BUTTON（ALLOW_LIST）；聚焦 API 主权限 → 显示「SUB_PERM 业务配置未覆盖此父类型」（PARENT_NOT_CONFIGURED），不得显示“配置缺失”
8. **S7 节点摘要**：已授权节点行内显示条件名/「可转授」/「N 子权限」标签；待撤销节点红色「待撤销」；无条件记录浅色「无条件」
9. **S5 条件页**：hr/auditor 登录 → 「权限条件」菜单可见可读；条件页无「无权限」空态；sec/admin 条件写按钮正常
10. **错误码**：20042 通过“授权弹窗选中启用条件 → 条件页停用该条件 → 返回保存”人工竞态复验；20043 因正常 UI 固定子权限属性而不可手工构造，使用 mock 契约测试提交非法子权限 payload；20041/20042/20043/20033/20034/20036 页面文案映射由 grant-store 单测覆盖，均须提示正确且不崩溃

## 背景

T-FE-036 实现的是 v3 共享设置模式（弹窗条件/再授予统一应用于已勾选授权）。v3.1 决策（2026-08-08 确认）改为**记录级聚焦编辑**：复选框只控制授权新增/保留/撤销，点击资源行聚焦后设置区只读取和修改该聚焦记录；新勾选资源使用固定默认值；无任何隐式或批量覆盖。交互细节（焦点生命周期、显式复制、停用条件、子权限入口、节点摘要）以 permission-grant.md v3.1 与决策记录 §11 为唯一权威。

## 范围

1. 授权弹窗记录级聚焦编辑（§S1/S2）：焦点生命周期、initial 完整键、复选框/整行分离、ALL 焦点迁移、suspended 双路径
2. 显式复制（§S3）：复制弹窗、合并规则、归一化删除、源条件停用禁用
3. 停用条件与条件门禁移除（§S4/S5）：选择器限启用、存量标注、CONDITION:VIEW 全量移除（条件管理页+授权页）
4. 子权限记录级入口（§S6）：SUB_PERM 允许集过滤、候选三态、20043 提示
5. 节点紧凑摘要（§S7）

不在范围：后端 20042/20043 校验（T-PERM-034/T-PERM-041）、sub-perm-allowed-types 后端实现（T-PERM-034）。
