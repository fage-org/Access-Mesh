---
doc_type: task
id: T-GW-005
title: Gateway 失效标记与订阅恢复策略设计（S-006 规范产出）
status: done
plan: docs/archive/2026-06-28/gateway-fail-mode-plan.md
domain: gateway
design_refs:
  - docs/design/services/gateway.md#§快照失效标记与订阅恢复
  - docs/design/permission-center-v3.5-design.md#§9.4
depends_on: [T-PERM-006]
blocks: [T-PERM-008, T-GW-003]
acceptance:
  - "失效标记语义、维度、数据结构、生命周期定义完整"
  - "订阅恢复策略明确（重连即全量清空）"
  - "与 T-GW-003 stale-allow 集成契约清晰"
  - "T-PERM-008 代码落地映射表已列出"
  - "gateway.md 和 v3.5-design.md 已回写"
design_writeback:
  required: true
  status: done
last_updated: 2026-06-28
---

# T-GW-005 Gateway 失效标记与订阅恢复策略设计

> 来源：[gateway-fail-mode-plan](../archive/2026-06-28/gateway-fail-mode-plan.md) 任务 C-5 / 设计审计 S-006
> 依赖：T-PERM-006（`PermInvalidateEvent` 广播载荷已落地）
> 下游：T-PERM-008（代码落地）+ T-GW-003（stale-allow 实现）

## 设计产出

本任务为**纯设计规范产出**，不包含代码改动。设计规范已回写至：

- [`docs/design/services/gateway.md`](../design/services/gateway.md) §快照失效标记与订阅恢复
- [`docs/design/permission-center-v3.5-design.md`](../design/permission-center-v3.5-design.md) §9.4（S-006 状态更新）

## 设计摘要

### 1. 失效标记

Gateway 本地快照有两种失效方式：

| 方式 | 触发 | stale-allow 可续命？ | 语义 |
|---|---|---|---|
| 自然过期 | 主缓存 `expireAfterWrite` TTL 到期，条目转入 stale store | ✅ 可以 | 快照陈旧但未被主动撤销 |
| 显式失效 | 收到 `perm:invalidate` Redis 广播 | ❌ 不可以 | 权限中心明确告知权限已变更 |

**核心原则：权限主动撤销 > 服务不可达兜底。**

#### 标记维度

与当前 `InterfaceSnapshotCacheInvalidator.evict()` 驱逐维度保持一致：

| 事件载荷 | 标记/驱逐范围 | 说明 |
|---|---|---|
| `serviceCodes` 非空 | tenantId + serviceCode 下所有用户快照 | API mapping / 资源 / 条件规则变更 |
| `userIds` 非空 | tenantId + userId 下所有服务快照 | 用户角色关系变化 |
| 仅 `roleIds` 非空 | tenantId 级（所有用户快照） | Gateway 无法反查角色→用户，安全过驱 |

#### 三层数据结构

```
interfaceSnapshotCache: Cache<String, InterfaceSnapshotResp>   // L1 主缓存（expireAfterWrite = ttlSeconds，默认 30s）
staleSnapshotCache:   Cache<String, StaleEntry>               // L2 陈旧快照缓存（expireAfterWrite = ttlSeconds + staleGraceSeconds，默认 60s）
invalidationMarker:   InvalidationMarker                       // invalidatedKeys + keyGeneration + globalEpoch
```

**StaleEntry 包装**：

```java
record StaleEntry(InterfaceSnapshotResp snapshot, Instant staleUntil) {}
```

- `staleUntil` = 快照首次写入主缓存的时刻 + `ttlSeconds` + `staleGraceSeconds`（默认 30+30=60s）
- stale-allow 续命时检查 `Instant.now().isBefore(entry.staleUntil())`，超时不再续命
- **为什么不用 RemovalListener 触发时刻**：Caffeine 过期清理/RemovalListener 不是严格 TTL 到点触发，可能延迟到后续访问或维护周期。若 `staleUntil` 基于触发时刻计算，实际 stale 窗口 = TTL 延迟 + grace，旧快照可能远超配置窗口仍被续命

**选择理由**：

- 主缓存使用 `expireAfterWrite`（当前行为不变），过期条目不可通过 `getIfPresent()` 获取
- Caffeine 的"过期但未驱逐"不可靠用于续命——`getIfPresent()` 对已过期条目直接返回 null
- 独立 stale store 使续命语义明确可控：主缓存过期 → 写入 stale store → stale store 再过 grace period 后自动淘汰
- `invalidatedKeys` 与 stale store 解耦，标记生命周期独立管理

#### 三层操作

| 触发场景 | 主缓存 | stale store | InvalidationMarker |
|---|---|---|---|
| 回源拉取新快照成功且 `LoadToken` 仍有效 | `put(key, snapshot)` | `put(key, StaleEntry(snapshot, staleUntil))`，`staleUntil = now + ttlSeconds + staleGraceSeconds` | `unmarkIfCurrent(token)` |
| 回源完成但 `LoadToken` 已失效 | 不写入 | 不写入 | 不清标记；丢弃结果并重试一次或转 fail-close |
| 主缓存条目过期（Caffeine 自然淘汰） | 自动淘汰 | 已有数据，无需操作 | — |
| `perm:invalidate` 事件 | `invalidate(key)` | `invalidate(key)` | `mark(key)`：`invalidatedKeys.add(key)` + `keyGeneration++` |
| stale-allow 续命检查 | — | `getIfPresent(key)` → 检查 `staleUntil` + 检查 `!invalidatedKeys.contains(key)` | `contains(key)` |
| 订阅重连全量清空 | `invalidateAll()` | `invalidateAll()` | `clearAndBumpGlobalEpoch()` |

**关键设计决策：回源成功时同步写 stale store（而非依赖 RemovalListener）**

原因：Caffeine `RemovalListener` 触发时机不确定——TTL 过期后首次 `getIfPresent` 返回 null 时，RemovalListener 可能尚未执行。若此时 permission-center 不可达，stale store 中没有续命快照，stale-allow 会错误 fail-close。

回源成功时同步写 stale store 保证：
- stale store 始终**先于**主缓存过期就有数据，无竞态窗口
- `staleUntil` 基于回源成功时刻计算（`now + ttl + grace`），不依赖 RemovalListener 触发时刻
- 主缓存自然过期时 stale store 无需操作（数据已在）
- `perm:invalidate` 仍同时驱逐主缓存 + stale store + 标记 invalidatedKeys

**关键安全约束**：`perm:invalidate` 事件必须**同时驱逐主缓存和 stale store**，并**标记 invalidatedKeys**。仅驱逐主缓存而遗漏 stale store 会导致 stale-allow 续命使用已撤销权限。

#### 回源提交与失效并发防护

回源请求可能与 `perm:invalidate` 并发。必须避免"回源先发起 → 失效事件先到 → 旧回源结果后返回"时把已撤销权限重新写回缓存。

`InvalidationMarker` 需要封装：

- `invalidatedKeys: Set<String>`：stale-allow 续命时的显式失效门禁
- `keyGeneration: ConcurrentHashMap<String, AtomicLong>`：每个 key 的失效代际
- `globalEpoch: AtomicLong`：订阅重连全量清空代际

回源落地流程：

1. 回源开始前调用 `LoadToken token = invalidationMarker.beginLoad(key)`，记录当前 `keyGeneration` 与 `globalEpoch`
2. `perm:invalidate` 命中 key 时调用 `mark(key)`：写入 `invalidatedKeys` 并递增该 key 的 `keyGeneration`
3. 订阅重连全量清空时调用 `clearAndBumpGlobalEpoch()`：清标记并递增 `globalEpoch`
4. 回源完成后先校验 `invalidationMarker.isCurrent(token)`：
   - 仍有效：写主缓存 + 写 stale store + `unmarkIfCurrent(token)`
   - 已失效：丢弃该回源结果，不写任何缓存、不清除 marker；当前请求重试一次或按 fail-close 处理

失效匹配 key 集合必须包含：主缓存 key、stale store key、以及 in-flight 回源 key。否则某个 key 在缓存中不存在但正在回源时，失效事件无法 bump generation，旧回源结果仍可能写回。

#### 标记生命周期

- **创建**：`perm:invalidate` 事件触发时写入
- **清除**：
  1. 回源拉取新快照成功时清除该 key 的标记
  2. 定期清理：每 `stale-grace-seconds × 2`（默认 60s）扫描，移除不在 stale store 中的孤立 key
  3. 订阅重连全量清空时一并清除，并递增 `globalEpoch`
- **上限保护**：标记集合 > stale store 条目数 2 倍时，触发全量清理（仅保留 stale store 中存在的 key 对应的标记）

### 2. 订阅恢复策略：重连即全量清空

Gateway 与 Redis 断线重连后执行：

1. **清空主缓存**：`interfaceSnapshotCache.invalidateAll()`
2. **清空 stale store**：`staleSnapshotCache.invalidateAll()`
3. **清空失效标记并递增全局代际**：`invalidationMarker.clearAndBumpGlobalEpoch()`
4. **后续请求按需回源**：首次请求触发 `PermissionClient.interfaceSnapshot()` 拉取新快照

**设计理由**：

- **安全优先**：断线期间错过的 `perm:invalidate` 事件无法追回（pub/sub 无持久化），全量清空确保不存在陈旧权限
- **实现最简**：一次全量清空（主缓存 + stale store + marker reset）完成，无需 Redis Stream 或 offset 管理
- **惊群可控**：重连后并发回源通过 per-key in-flight 去重缓解（见 §3），避免同 key 重复远端调用；TTL 30s 兜底已默认接受同量级延迟

**重连检测**：`PermInvalidationSubscriber` 需增加重连回调：

- Reactive Redis `listenTo()` 的 `onError`/`onComplete` 触发时标记订阅断开
- 重连成功后（`subscribe` 重建）触发全量清空
- 清空操作需在主线程执行，避免并发安全问题

### 3. 与 T-GW-003 stale-allow 集成契约

T-GW-003 实现 stale-allow 时，需遵循本规范定义的三层结构和标记语义：

1. **回源成功时同步写 stale store**：回源拉取新快照成功时同时写入主缓存和 stale store，`staleUntil = now + ttlSeconds + staleGraceSeconds`，保证 stale store 先于主缓存过期就有数据
2. **续命前双重检查**：从 stale store 取出 `StaleEntry` 后：
   - 检查 `Instant.now().isBefore(entry.staleUntil())` → 超时不再续命
   - 检查 `!invalidatedKeys.contains(key)` → 已显式失效不续命
3. **续命不写回主缓存**：续命使用的快照不写回 `interfaceSnapshotCache`（避免陈旧数据覆盖未来新快照）
4. **显式失效三层同步**：`perm:invalidate` 事件必须同时驱逐主缓存 + stale store + 标记 invalidatedKeys
5. **回源提交需校验代际**：回源完成时必须通过 `LoadToken` 校验；若期间发生显式失效或订阅重连全清，旧结果必须丢弃，禁止写缓存或清 marker

### 4. Per-key 回源去重

当前 `PermissionFilter` 使用手动 `getIfPresent` + 手动回源，**不存在** per-key 并发去重。重连全量清空后同 key 并发请求会打出多次远端回源。

**T-PERM-008 / T-GW-003 落地要求**：

采用 `ConcurrentHashMap<String, Mono<InterfaceSnapshotResp>>` 作为 in-flight 去重表：

```java
// 伪代码
ConcurrentHashMap<String, Mono<InterfaceSnapshotResp>> inFlight = new ConcurrentHashMap<>();

Mono<InterfaceSnapshotResp> fetchMono = inFlight.computeIfAbsent(cacheKey,
    k -> permissionClient.interfaceSnapshot(...)
        .doFinally(signal -> inFlight.remove(k))
        .cache());  // Reactor cache() 确保多个订阅者共享同一结果
```

- 同 key 并发请求共享同一 `Mono`，只有首个触发远端调用
- `doFinally` 确保完成后清理（无论成功/失败）
- 不使用 Caffeine `AsyncLoadingCache`（与当前手动 `put`/`invalidate` 逻辑冲突，迁移成本高）

### 5. T-PERM-008 代码落地映射表

| 组件 | 改动 |
|---|---|
| 新增 `InvalidationMarker` | 封装 `invalidatedKeys`、`keyGeneration`、`globalEpoch`、`LoadToken` 校验、定期清理 + 上限保护 |
| 新增 `staleSnapshotCache` Bean | `Caffeine.newBuilder().expireAfterWrite(ttlSeconds + staleGraceSeconds).maximumSize(maxSize).build()` |
| 新增 `StaleEntry` record | `(InterfaceSnapshotResp snapshot, Instant staleUntil)` |
| `CacheConfig` | 注册 stale store Bean |
| `InterfaceSnapshotCacheInvalidator` | `evict()` 同时驱逐主缓存 + stale store，并对命中的主缓存 key、stale store key、in-flight key 调用 `invalidationMarker.mark(key)` |
| `PermissionFilter` | 回源开始前获取 `LoadToken`；回源成功且 token 仍有效时同步写主缓存 + stale store（`StaleEntry(snapshot, staleUntil = now + ttl + grace)`）+ `invalidationMarker.unmarkIfCurrent(token)`；token 失效则丢弃结果；增加 per-key in-flight 去重（`ConcurrentHashMap`）；**stale-allow 读取留给 T-GW-003**，当前仅落地 stale store 写入基础设施 |
| `PermInvalidationSubscriber` | 增加重连检测 + 全量清空逻辑（主缓存 + stale store + marker） |
| `GatewayProperties` | 新增 `gateway.cache.l1.stale-grace-seconds`（T-GW-001 定义） |

## 关联决策

- design-review §4.3 工作单 C（方案 C1+C2）：stale-allow 与显式失效不续命原则
- 审计 S-006（=D 待设计 → 本任务完成设计）
