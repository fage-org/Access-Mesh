---
doc_type: task
id: T-FE-019
title: Phase 3 联调：权限查询/校验（4.2 mock→真实收口 + Gateway 端点注册 + explain 扩展展示接线 + mock 整删）
status: done
plan: docs/plans/frontend-phase3-plan.md
domain: frontend
design_refs:
  - docs/design/permission-center/api-contract.md#§6.6-6.8
  - docs/design/frontend/permission-query.md
  - docs/design/access-service-rebuild-runbook.md
depends_on:
  - T-FE-013
  - T-PERM-033
acceptance:
  - "Gateway bootstrap 管理 API 清单 +3 端点（permission-view/effective-permissions、auth/query-scopes、permission-view/explain）；permission-view 其余端点（effective-permission-codes/resource-users/role-permissions/effective-roles/resource-tree/recent-changes）本页不消费不注册，后续消费页届时按页注册"
  - "业务门禁零新增：explain/effective-permissions 的被查目标实例 USER:VIEW/ROLE:VIEW 与 query-scopes 无排查门禁均为 T-PERM-033 已收口实现，USER:VIEW/ROLE:MANAGE（掩码含 VIEW）固定图原持"
  - "AccessBootstrapPgIT 计数断言：API 资源 54→57、映射 53→56、授权 101→104（+3 路由各派生一条 API:ACCESS 实例授权）、scopeAll 47 不变"
  - "契约零漂移确认：三端点请求/响应前端类型 ↔ 后端 DTO（UserPermissionViewReq/QueryScopesReq/PermissionExplainReq 及对应 Resp 含 SourceRole/ScopeGroup/RecentChangeResp/ConditionEvaluation/ConflictDrop 嵌套）逐字段比对一致"
  - "api/permission-query.ts 三端点本地 mock 路径 /permission-query/* 切换为 Gateway 外部路径（permission-view 两端点 + auth/query-scopes）——修法唯一的事实性修正"
  - "explain 契约扩展前端展示接线（T-PERM-033 设计文档指派本任务）：Tab3 表单补模拟 IP 输入（context.clientIp，可选）；ExplainPanel 补条件评估上下文（evaluationContextSource/evaluatedClientIp）、条件评估明细（状态/逻辑/整体/逐项脱敏参数）、互斥规则丢弃明细三区块"
  - "表单示例默认值清空（设计定案）：Tab2/Tab3 的 REPORT/report:sales/DATA_READ 等 mock 时代示例清为空（真实库必查空、误导排查者），结构性默认（LOCAL_USER/BASIC_ROLE/example 域码）保留，编码写法由 placeholder 引导"
  - "mock/permission-query.ts 整删（设计定案）：仅默认导出 fake 路由、无数据导出、无外部消费方（全仓 grep 核实），与 T-FE-020 conflict-rule.ts 同情形"
  - "联调冒烟通过（重建库后浏览器实测三 Tab，DB/API 硬证据）"
  - "文档回写：看板行、phase3-plan 进度、permission-query.md（联调注记 + mock 终态化 + explain 展示接线收口 + last_reviewed）、runbook 端点计数"
design_writeback:
  required: true
  status: done
last_updated: 2026-09-02
---

# T-FE-019 Phase 3 联调：权限查询/校验

> 状态：done（2026-09-02 单日收口；任务卡为启动时补建——phase3-plan 任务行只留标题，详细范围入卡）

## 背景

Phase 3 第五个联调任务。前置事实（调研核实，2026-09-02）：

- 页面消费 3 端点（effective-permissions / query-scopes / explain），Gateway 注册 0/3，全 fail-closed 403
- api/permission-query.ts 仍用 Phase 1 本地 mock 路径 `/permission-query/*`（头注预留的联调切换事项）
- 后端 T-PERM-033 已收口：explain DTO 扩展（context.clientIp/conditionEvaluations/conflictDrops/evaluatedClientIp）「前端展示随 T-FE-019 联调接线」（permission-query.md §8 明记）
- 业务门禁已在位且固定图已持（USER:VIEW L248、ROLE:MANAGE 掩码含 VIEW），与 T-FE-020 同为零新增

## 设计定案（2026-09-02）

1. **mock 处置 = 整删**：mock/permission-query.ts 仅默认导出 fake 路由、无数据导出、无外部消费方。备选（保留加注记=死代码）弃用
2. **表单示例默认值清空**：REPORT/report:sales/DATA_READ 等 mock 示例在真实 bootstrap 库不存在，默认值直接查询必得 DENIED/空态误导排查者；清空后编码写法由 placeholder 引导。备选（保留示例值/改真实示例——解释力差）弃用
3. **联调验证 = 重建库 + 浏览器冒烟**：固定图 +3 端点变更，既有库重启 fail-fast；dev 密钥沿用 T-FE-017 留存值不换新

## 范围

### 后端（access-service）

- `BootstrapGraphDefinition.apiRoutes()` +3（T-FE-019 块，注明 permission-view 其余端点不消费不注册）
- `AccessBootstrapPgIT` 计数断言 54/53/101 → 57/56/104

### 前端（frontend）

- `api/permission-query.ts` 三端点路径切换 + 头注更新
- `mock/permission-query.ts` 删除
- `utils/hook.ts` Tab2/Tab3 示例默认值清空（初始 + onReset 同步）+ Tab3 补 clientIp 表单字段与请求 context 装配
- `components/ExplainPanel.vue` 补三区块（条件评估上下文 / 条件评估明细 / 互斥规则丢弃）
- `index.vue` Tab3 表单补「模拟 IP」输入

### 不做什么

- 不注册 permission-view 其余端点与 recent-changes（本页不消费，explain 响应内联 recentChanges）
- 不动业务门禁（已在图）
- 不动契约 §6.6-6.8（联调零契约漂移、无协议变更）

## 联调验收场景（实测记录，2026-09-02）

1. 重建库 bootstrap（启动日志 apiResources=57, mappings=56, grants=104, menus=15）→ 登录 admin → 权限排查菜单可见、hash 直达 + F5 渲染 ✅
2. Tab1 effective-permissions：targetType=USER、LOCAL_USER、subjectExternalId=1（admin 主体）→ 共 72 条权限事实（USER/ROLE/SERVICE/TYPE_DEFINITION/RESOURCE/OPERATION 等全量授权行，sourceRoles=Bootstrap Admin）。观察：保留的结构性默认域码 `example` 作为可选筛选传入时全局域授权全被过滤（0 条）——筛选语义正确非缺陷，排查者按需清空域码 ✅
3. Tab2 query-scopes：主资源 USER/1 + 主操作 VIEW、范围类型 ROLE,ORG × 操作 VIEW → 匹配主操作 VIEW、主权限 ID 回显、四态矩阵 ROLE/ORG 均为全量授权 ✅
4. Tab3 explain：USER + USER:VIEW + ALL → allowed=true、权限键/来源角色（Bootstrap Admin）展示；条件评估上下文双来源实证——不填模拟 IP 时「当前请求回退 + 评估 IP 0:0:0:0:0:0:0:1」，填 10.20.30.40 后「管理员输入 + 10.20.30.40」（context.clientIp → evaluationContextSource=ADMIN_INPUT 链路打通）✅

**开发环境备注**：Gateway 重启须带 `GATEWAY_CORS_ALLOWED_ORIGINS` 含 `http://localhost:8890`（runbook 前置条件既有登记，缺省时浏览器请求 403 空体、curl 无 Origin 头不受影响，首轮冒烟误判为接口故障）。

## 完成记录（2026-09-02 收口）

**后端**：+3 端点（业务门禁零新增）+ PgIT 计数订正 57/56/104/47。

**前端**：路径切换（3 端点）+ mock 整删 + 示例默认清空 + explain 扩展展示接线（模拟 IP 输入 + 三展示区块）。

**回归**：后端全仓 mvn test 1303 项 0 失败 0 错误（7 个 surefire 模块执行合计；AccessBootstrapPgIT 15/15 新计数 57/56/104 过、DualInstanceContainerTest 3/3——本轮 dev 服务先停故无端口干扰）；前端 eslint / vue-tsc / vitest 213（与基线一致，mock 无 spec）/ vite build 全绿。

**文档回写**：看板行 ✅、phase3-plan 进度、permission-query.md（联调注记 + mock 终态化 + explain 展示收口 + last_reviewed）、runbook 端点计数。
