---
doc_type: task
id: T-FE-018
title: Phase 3 联调：权限授予（4.1）- 角色联调（首期）
status: proposed
plan: docs/plans/frontend-phase3-plan.md
domain: frontend
design_refs:
  - docs/design/frontend/permission-grant.md
  - docs/design/permission-center/api-contract.md#§5.5
  - docs/design/permission-center/api-contract.md#§6.4
  - docs/design/permission-center/api-contract.md#§6.5
  - docs/design/permission-center/api-contract.md#§6.5.1
  - docs/design/permission-center/api-contract.md#§6.5.2
depends_on:
  - T-FE-036
  - T-FE-038
  - T-FE-039
  - T-FE-040
  - T-PERM-040
  - T-PERM-041
  - T-PERM-034
  - T-PERM-022
  - T-PERM-028
  - T-PERM-029
  - T-PERM-031
blocks: []
acceptance:
  - "**角色联调（首期）门禁**：T-PERM-031（已 done）、T-PERM-034（已 done）、**T-PERM-040 done（单资源类型后端：operation-permission/list 类型查询合并 + role-resource-permission/list.resourceTypeCode + 20008 全覆盖）；T-FE-038 done（单类型矩阵上下文）**（T-ADMIN-021 不在本任务依赖，组织联调归 T-FE-037）"
  - "**角色联调（首期）**：角色入口 mock 切真实后端，list + apply-grant-plan 联通（apply-grant-plan 唯一写入口，单事务原子 + 受影响行数断言，无 CAS/幂等表/clientRequestId）"
  - "**多类型联调验收（2026-08-05 评审补）**：单类型矩阵链路真实后端验证——类型切换操作列变化/操作列配置按 resourceTypeCode 隔离/list 携带 resourceTypeCode 过滤/写入不适用操作返回 20008（含嵌套子权限反例）"
  - "**图标与条件联调验收（2026-08-05 评审补）**：图标正交模型真实数据验证（条纹=条件/粗黑边框=canGrant/红·淡红=撤销/红白条纹/并列规则/分叉精确投影）；条件+canGrant 场景——前端选条件自动关闭 canGrant、直接构造条件+canGrant=true 请求由后端 20041 兜底拒绝、20041 提示不崩溃"
  - >-
    **TYPE_DEFINITION:VIEW 缺口硬门禁（2026-08-07 T-FE-038 评审补）**：授权页类型候选依赖 type-definition/list，真实后端该接口要求 TYPE_DEFINITION:VIEW（TypeDefinitionAppServiceImpl.listTypes），而角色/组织入口权限清单未声明该权限——联调前必须二选一并落地：① 前端在角色/组织入口声明并校验只读依赖 TYPE_DEFINITION:VIEW；② 后端为类型候选提供免权限来源（或移除该接口门控）。联调验收时以真实接口验证候选全集、排序与"已授权类型"标记，未落地视为阻塞项
  - "wire 校验：联调期间前后端响应与 api-contract.md §6.4/§6.5/§6.5.1/§6.5.2 一致（schema 已删）（nullable/BigInt 字符串/响应包装/角色业务键 domainCode+roleTypeCode+roleExternalId 无偏差；结构校验由后端 prevalidateGrantPlan 运行时执行，不做前端机器编译）"
  - "**v3.1 真实接口联调验收（T-FE-040 mock 验收通过后，以下新增真实接口必须在本任务验证）**：① `sub-perm-allowed-types`（§6.5.2）真实接口——ALLOW_ALL（顶层/嵌套通配）/ ALLOW_LIST（并集去重、交集为空三态）/ ALLOW_NONE（PARENT_NOT_CONFIGURED/CHILD_TYPES_EMPTY 业务不允许 vs CONFIG_MISSING/EMPTY/INVALID 配置异常文案区分）、角色定位失败 20001 与无 ROLE:VIEW 统一访问拒绝；② **20042/20043 真实后端兜底**——主权限改绑停用条件 -> 20042、存量停用绑定未改 conditionCode 允许保留、子权限 create 带条件/再授予 -> 20043、update 目标为子权限 -> 20043（mock 与真实响应一致）；③ 停用条件全链路（新选限启用、复制入口禁用、保存保留+标注）；④ 子权限属性拒绝场景 UI 不崩溃且提示正确；⑤ 旧写端点删除确认（save/revoke/children/add-child/remove-child 已随 T-PERM-034 移除，网关 404）"
  - "超时联调：前端 saving 期间按钮 disabled 防重复提交；超时提示刷新确认（不做自动 list 对比 + 重放，收窄定稿）"
  - "故障注入联调：apply-grant-plan 任一步失败后单事务回滚，数据库无部分状态（查询确认）"
  - "前端来源链与引擎 golden fixtures 一致（S6 场景全过）"
  - "**角色联调（首期）**：S1 角色部分 + S2~S11 在角色入口真实后端通过（组织入口联调归 T-FE-037）"
design_writeback:
  required: true
  status: pending
last_updated: 2026-08-08
---

# T-FE-018 Phase 3 联调：权限授予（4.1）

> 状态：proposed（2026-07-26 取消；2026-08-01 T-FE-036 v3 重设计立项后恢复待排期）
> 依赖：T-FE-036 + T-FE-038 + T-FE-039 + **T-FE-040** + T-PERM-040 + T-PERM-041 + T-PERM-034 + T-PERM-022/028/029/031（2026-08-05 评审补：汇合图标模型 T-FE-039 与条件转授 T-PERM-041；2026-08-08 补记录级聚焦编辑 T-FE-040；组织联调归 T-FE-037，依赖图在 frontend-phase3-plan.md 与 tasks/README.md）
> 门禁：见 acceptance 第一条（2026-08-01 评审方案六 DoD 固化；2026-08-02 授权写链路收敛为 list + apply-grant-plan 单入口）

## 背景

v3 权限授予页（T-FE-036，mock 驱动）与后端改造（T-PERM-034，七项 + 工程加固 + 全链路迁移，单入口收敛）并行推进，本任务在二者完成后汇合：前端 mock 层切换真实后端，按 S1 角色部分 + S2~S11 场景联调验收。

## 联调范围

- 接口：`role-resource-permission/list` + `apply-grant-plan`（唯一写入口，§6.5.1）+ `role-resource-permission/sub-perm-allowed-types`（§6.5.2，子权限类型只读契约）+ `extra-roles/list`
- 数据源：全局操作解析（已落地：专属优先+全局 fallback，T-PERM-028/040 覆盖）、线格式（T-PERM-028）；组织入口 org-tree 数据源归 T-FE-037
- 主通道：全部写操作统一走 `apply-grant-plan`（§6.5.1；旧 save/children-save/rebuild/add-child/update-child/remove-child 已移除）

## 完成记录

（待实现后填写）
