---
doc_type: task
id: T-FE-018
title: Phase 3 联调：权限授予（4.1）- 角色联调（首期）
status: done
plan: docs/plans/frontend-phase3-plan.md
domain: frontend
design_refs:
  - docs/design/frontend/permission-grant.md
  - docs/design/permission-center/api-contract.md#§5.5
  - docs/design/permission-center/api-contract.md#§6.4
  - docs/design/permission-center/api-contract.md#§6.5
  - docs/design/permission-center/api-contract.md#§6.5.1
  - docs/design/permission-center/api-contract.md#§6.5.2
depends_on:
  - T-FE-036
  - T-FE-038
  - T-FE-039
  - T-FE-040
  - T-PERM-040
  - T-PERM-041
  - T-PERM-034
  - T-PERM-022
  - T-PERM-028
  - T-PERM-029
  - T-PERM-031
blocks: []
acceptance:
  - "**角色联调（首期）门禁**：T-PERM-031（已 done）、T-PERM-034（已 done）、**T-PERM-040 done（单资源类型后端：operation-permission/list 类型查询合并 + role-resource-permission/list.resourceTypeCode + 20008 全覆盖）；T-FE-038 done（单类型矩阵上下文）**（T-ADMIN-021 不在本任务依赖，组织联调归 T-FE-037）"
  - "**角色联调（首期）**：角色入口 mock 切真实后端，list + apply-grant-plan 联通（apply-grant-plan 唯一写入口，单事务原子 + 受影响行数断言，无 CAS/幂等表/clientRequestId）"
  - "**多类型联调验收（2026-08-05 评审补）**：单类型矩阵链路真实后端验证——类型切换操作列变化/操作列配置按 resourceTypeCode 隔离/list 携带 resourceTypeCode 过滤/写入不适用操作返回 20008（含嵌套子权限反例）"
  - "**图标与条件联调验收（2026-08-05 评审补）**：图标正交模型真实数据验证（条纹=条件/粗黑边框=canGrant/红·淡红=撤销/红白条纹/并列规则/分叉精确投影）；条件+canGrant 场景——前端选条件自动关闭 canGrant、直接构造条件+canGrant=true 请求由后端 20041 兜底拒绝、20041 提示不崩溃"
  - >-
    **TYPE_DEFINITION:VIEW 缺口硬门禁（2026-08-07 T-FE-038 评审补；2026-09-02 定案落地=理解 A，方案①变体）**：类型候选依赖 type-definition/list，真实后端该接口要求 TYPE_DEFINITION:VIEW（TypeDefinitionAppServiceImpl.listTypes）。**定案**：① 前端在授权页声明 TYPE_DEFINITION:VIEW 只读依赖（软依赖，不进路由 meta.auths——无类型管理权的配权用户仍可进入页面）+ hasPerms 探查（登录权限串白名单已含 TYPE_DEFINITION，T-PERM-025）+ 缺权限时只禁类型下拉与矩阵区、明确提示并允许重试（不误报「暂无资源类型配置」空态）；② 后端免权限来源不采用。联调验收时以真实接口验证候选全集、排序与"已授权类型"标记，以及无 TYPE_DEFINITION:VIEW 账号的降级态与重试，未落地视为阻塞项
  - "wire 校验：联调期间前后端响应与 api-contract.md §6.4/§6.5/§6.5.1/§6.5.2 一致（schema 已删）（nullable/BigInt 字符串/响应包装/角色业务键 domainCode+roleTypeCode+roleExternalId 无偏差；结构校验由后端 prevalidateGrantPlan 运行时执行，不做前端机器编译）"
  - "**v3.1 真实接口联调验收（T-FE-040 mock 验收通过后，以下新增真实接口必须在本任务验证）**：① `sub-perm-allowed-types`（§6.5.2）真实接口——ALLOW_ALL（顶层/嵌套通配）/ ALLOW_LIST（并集去重、交集为空三态）/ ALLOW_NONE（PARENT_NOT_CONFIGURED/CHILD_TYPES_EMPTY 业务不允许 vs CONFIG_MISSING/EMPTY/INVALID 配置异常文案区分）、角色定位失败 20001 与无 ROLE:VIEW 统一访问拒绝；② **20042/20043 真实后端兜底**——主权限改绑停用条件 -> 20042、存量停用绑定未改 conditionCode 允许保留、子权限 create 带条件/再授予 -> 20043、update 目标为子权限 -> 20043（mock 与真实响应一致）；③ 停用条件全链路（新选限启用、复制入口禁用、保存保留+标注）；④ 子权限属性拒绝场景 UI 不崩溃且提示正确；⑤ 旧写端点删除确认（save/revoke/children/add-child/remove-child 已随 T-PERM-034 移除，网关 403——2026-09-02 实测为注册清单外拦截「无接口访问权限」，请求不达后端）"
  - "超时联调：前端 saving 期间按钮 disabled 防重复提交；超时提示刷新确认（不做自动 list 对比 + 重放，收窄定稿）"
  - "故障注入联调：apply-grant-plan 任一步失败后单事务回滚，数据库无部分状态（查询确认）"
  - "前端来源链与引擎 golden fixtures 一致（S6 场景全过）"
  - "**角色联调（首期）**：S1 角色部分 + S2~S11 在角色入口真实后端通过（组织入口联调归 T-FE-037）"
design_writeback:
  required: true
  status: done   # 2026-09-02：§10 已知缺口 TYPE_DEFINITION:VIEW 定案收口 + 接口优化登记；api-contract §5.3 tree fail-open 登记（本任务发现）
last_updated: 2026-09-02
---

# T-FE-018 Phase 3 联调：权限授予（4.1）

> 状态：done（2026-09-02 收口；2026-07-26 取消、2026-08-01 按 v3 恢复、2026-09-02 启动并完成）
> 依赖：T-FE-036 + T-FE-038 + T-FE-039 + T-FE-040 + T-PERM-040 + T-PERM-041 + T-PERM-034 + T-PERM-022/028/029/031（全部 done，2026-09-02 核实；组织联调归 T-FE-037）
> 门禁：见 acceptance 第一条；2026-08-02 授权写链路收敛为 list + apply-grant-plan 单入口

## 背景

v3 权限授予页（T-FE-036，mock 驱动）与后端改造（T-PERM-034，七项 + 工程加固 + 全链路迁移，单入口收敛）并行推进，本任务在二者完成后汇合：前端 mock 层切换真实后端，按 S1 角色部分 + S2~S11 场景联调验收。

## 联调范围

- 接口：`role-resource-permission/list` + `apply-grant-plan`（唯一写入口，§6.5.1）+ `role-resource-permission/sub-perm-allowed-types`（§6.5.2，子权限类型只读契约）；只读数据源 `type-definition/list` / `operation-permission/list` / `resource-entity/tree` / `permission-condition/list`（后两者随本任务一并切真实）
- 操作解析口径：**操作位按类型完全隔离、无全局回退**（全局操作概念已随 T-PERM-049 退役，2026-08-30；`includeGlobalFallback` 禁止再传）；线格式 T-PERM-028（binaryBit/inheritMask/grantedBits 十进制字符串）
- `extra-roles/list`：已随 T-PERM-043 退役删除（后端 404），联调动作为「确认授权页无调用」（2026-09-02 核实：SubjectTreePanel 仅 BASIC_ROLE，无引用）
- 主通道：全部写操作统一走 `apply-grant-plan`（§6.5.1；旧 save/revoke/children/rebuild/add-child/update-child/remove-child 已删除，网关 403——2026-09-02 实测）
- TYPE_DEFINITION:VIEW 缺口：**已定案落地（2026-09-02，理解 A）**——前端声明软依赖（perms SSOT TYPE_VIEW，不进路由 meta.auths）+ hasPerms 探查，缺权限时只禁类型下拉与矩阵区并提示可重试，不误报空态；候选仍来自 type-definition/list（详见 acceptance 第 5 条）

## 完成记录

**2026-09-02 收口**（重建库后浏览器全场景冒烟 + vitest 198/198 + typecheck + eslint 全绿；后端零改动）。**同日评审补（用户决策执行）**：前端降级判定扩展 403 捕获（vitest 202/202）+ 后端 bootstrap 授权属性漂移放行（AccessBootstrapPgIT 16/16，dev 库 bootstrap=true 重启实证）+ 停用条件三子项补验（dev 环境真实后端全过）。

### 代码变更（全前端）

- **mock 整删**：`mock/permission-grant.ts`（+spec）、`mock/_shared/permission-condition-store.ts`、`mock/_shared/sub-perm-policy.ts`（+spec）、`mock/type-def.ts`；`mock/resource-operation.ts` 更名 `mock/_shared/resource-fixtures.ts`（+spec 同步，唯一存活消费方 = resource-dependency mock，其路由早随 T-FE-017 退役，本文件本就是纯数据模块）。授权页 6 数据源全真实。
- **TYPE_DEFINITION:VIEW 理解 A 落地**：perms.ts SSOT 增 `TYPE_VIEW`（软依赖，不进 `PERMISSION_GRANT_PERM_LIST`/路由 meta.auths）；hook.ts loadDeps 前置 `hasPerms` 探查，缺权限置 `typePermDenied` 并跳过依赖请求；GrantMatrixPanel 新增降级分支（el-result「无法加载资源类型/缺少 TYPE_DEFINITION:VIEW/重试」），不误报「暂无资源类型配置」；`retryLoadDeps` 重试入口。
- **类型收紧**：`RolePermissionItem.operationCode` 与 `GrantRecordKey.operationCode` 收紧为 `string`（契约必填恒有值；本地域类型的组合位防御分支独立保留不受影响）。

### 评审补变更（2026-09-02，用户决策执行）

- **前端（类型候选 403 同样降级）**：loadDeps 类型候选请求独立捕获 403——探查粒度差异（登录权限串全集含实例级 TYPE_DEFINITION:VIEW，而后端 list 门禁为类型级）下仅实例级授权的账号探查通过但请求被拒，同样进入 `typePermDenied` 降级块（可重试），其余错误（含资源树/操作列 403，无前端前置为既定口径）维持通用提示；新增 `hook.spec.ts` 四用例回归锁；tsconfig include 补 `mock/_shared/*.ts`（恢复 resource-operation 更名 _shared 后丢失的 tsc 覆盖）。**2026-09-03 决策修订**：后端 list 门禁放宽为「类型级或任一实例级 VIEW 均可查询」（`TypeDefinitionAppServiceImpl.requireTypeViewPermission` + mapper `selectValidCodesByTenant`，单测三用例锁定，17/17），两侧口径对齐；前端 403 捕获保留为防御层。注：TYPE_DEFINITION 实例当前无 resource_entity 投影，实例级授权暂不可构造——实例级路径为口径对齐与未来兼容（同日查库核实，api-contract §5.1 已注记）。
- **后端（bootstrap 授权属性漂移放行，口径定案）**：`AccessBootstrapInitializer` 授权匹配拆两级——`GrantIdentity`（资源实体/范围+类型）判缺行 fail-fast；身份行存在而 grantedBits/canGrant/conditionId/dependOn/grantSource 漂移仅 warn 放行、不重种覆盖；PgIT 新增「属性漂移 tolerated 不重种」回归锁（旧实现必红）+ 缺行 fail-fast 用例维持（16/16）；实测 dev 库（id48 condition 漂移）bootstrap=true 重启由拒启改为 warn 放行成功启动；architecture §14.2 与 runbook 语义同步回写。

### 冒烟验证（真实后端，bootstrap 空库种子 + SQL 冒烟数据）

- **S1**：角色树=BASIC_ROLE 真实数据；**S2**：MENU/API 双类型操作列按类型隔离（MENU=CRUD 四列、API=五列含 ACCESS）、66 实例行绿色「直接」图标 + ALL 行图标与 DB 逐行一致、已授权类型下拉标记（「API · 已授权」）；**S4**：ALL·ACCESS 详情抽屉（来源/无条件/createdAt/childCount）；**S3**：弹窗全流程——ACCESS 选中回显 ALL（互斥不勾实例）、取消 ALL 回 INSTANCE（66 实例勾选亮起，computePreset 真实数据验证）、行聚焦、条件选择器（office-hours 可选/cond-disabled 灰显「已停用」）、选条件后 canGrant 自动禁用（20041 前端行为）、子权限配置器（ALLOW_LIST 真实过滤=仅 API/MENU 可选、子权限操作集=子类型专属五操作、挂载目标摘要）；**S5**：三段 plan（removes[ALL·ACCESS]+updates[主权限+条件]+creates[子权限 parentPermissionId]）单请求提交成功，DB 断言 id47 软删/id48 条件+canGrant=false/id114 挂父，矩阵 baseline 整体替换（ALL 行清空）；失败路径 20040（CREATE 无 canGrant 源被拒）草稿保留+标红+整体重试；**S8**：MENU↔API 多次切换正常（代际守卫随上述流程隐式覆盖）。
- **sub-perm-allowed-types 三态**（真实接口逐态 fetch 验证）：MENU→ALLOW_LIST[BUTTON,DATA]；ROLE（未 CLASSIFY 认领）→ALLOW_NONE/CONFIG_MISSING；`NO_SUCH_TYPE`→20007；嵌套通配 `child_types:["*"]`→ALLOW_ALL；20001（角色定位失败）。UI 侧三文案（配置异常/不允许配子权限/允许集内无可用资源类型——BUTTON/DATA 类型零资源实体时的第三态）均按 reason 区分渲染。
- **错误码兜底**（真实后端 fetch 构造）：20041（条件记录置 canGrant=true）/20042（改绑停用条件）/20043（update 目标为子权限 id114）/20033（同键重复 create）/20036（不存在 id）/20005（未知操作码）/20008（混合 plan：合法 MENU·VIEW+非法 MENU·ACCESS 整批拒绝，**查库确认合法项零写入**——单事务回滚）/20040（无 canGrant 源）全部命中且文案正确。
- **超时防重**：XHR 延迟 6s 补丁下 saving 期间保存/放弃/撤销按钮全 disabled；延迟结束后失败提示+草稿保留。
- **降级用户**（理解 A 端到端）：SQL 造 smoke 用户（角色=克隆 admin 授权减 TYPE_DEFINITION 类型那条，权限串 47 条含 ROLE/RESOURCE/OPERATION:VIEW、无 TYPE_DEFINITION:VIEW）登录→授权页可进入、主体树正常可用、类型下拉 disabled、矩阵区降级块「无法加载资源类型…重试」，重试后保持降级、全程不误报空态。
- **停用条件三子项补验（2026-09-02 评审补，dev 环境真实后端）**：SQL 将 id232（smoke-full 角色·API 实例「授权页角色树」·ACCESS）改绑停用条件 cond-disabled 模拟存量停用绑定——① **复制入口禁用**：设置区「复制设置到已勾选授权」is-disabled，悬停提示「源条件已停用，不可复制（需先选择启用中的条件）」；② **回显标注**：详情抽屉「已停用条件（cond-disabled）」+ 弹窗设置区「条件已停用」标签；③ **20042 豁免**：apply-grant-plan `updates[{id:232, conditionCode:"cond-disabled"}]`（同值覆盖=未改）→ code 200 且 DB 断言 condition_id 保留（与既有「改绑停用条件→20042 拒绝」反向用例互补闭环）。
- **S6 来源链**：`source-chain.spec` 17 用例（含组合位/多来源聚合）+ 引擎级 `GoldenFixturePgIT`（T-PERM-034 已锁）为一致性依据；浏览器侧以真实数据的直接/条件图标形态复核。
- **旧写端点**：经网关实测为 **403「无接口访问权限」**（Gateway 66 端点注册清单外拦截，请求不达后端；直连 9100 亦被内部防护 403 先拦）——acceptance 原文「网关 404」按实测修正；后端 Controller 无映射由 T-PERM-034 收口保证。

### 发现与登记（2026-09-02 已决策处置）

1. **SUB_PERM 顶层通配 `"*"` 真实链路不可达（登记已知缺陷）**：domain_config.extra 为 jsonb，`"*"` 落库读回为带引号 JSON 字符串，`parseSubPermissionPolicy` 的裸 `"*"` 判定（L567）永不命中→CONFIG_INVALID；且写入口（DomainConfigAppServiceImpl）仅做 JSON 语法校验，裸 `*` 根本无法经正规入口落库。**嵌套通配 `child_types:["*"]` 可用（已验 ALLOW_ALL）**；单测用裸字符串 fixture 未覆盖真实 jsonb 形态。**决策：登记已知缺陷**——api-contract §6.5.2 判定步骤 1 已注记（ALLOW_ALL 以嵌套通配配置），修复（解析层兼容带引号形态 + 写入口口径）另立任务。
2. **resource-entity/tree 未知类型 fail-open**：已登记 api-contract §5.3（2026-09-02），待办收口对齐 fail-closed。
3. **bootstrap 固定图 fail-fast 与授权页合法写操作冲突（口径定案+本次落地）**：原实现 canGrant/condition 等参与完整匹配键，管理端运营修改固定图授权后 `ACCESS_BOOTSTRAP_ENABLED=true` 重启即拒启（冒烟实证 id48）。**决策：缺行 fail-fast、属性漂移放行**——已本次落地（见评审补变更）并回写 architecture §14.2 / runbook；删行不可撤销边界已登记 architecture §14.2 待议清单（2026-09-03 建立，固定图问题持续登记点）。

### 环境终态

access-service（9100，**bootstrap=true 模式**，新代码：id48 condition 漂移 warn 放行启动）/ gateway（8080，带 secrets+CORS）/ frontend（8890）运行中；DB 含冒烟数据（SMOKE 域/条件×2/角色 3·4/用户 smoke/授权变更 id47、id48 带条件、id114 子权限、id232 停用条件绑定——停用条件补验数据），下次重建库自然清除。
