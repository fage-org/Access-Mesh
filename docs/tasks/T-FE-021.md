---
id: T-FE-021
title: Phase 3 联调：业务域配置（5.1）
plan: docs/archive/2026-09-07/frontend-phase3-plan.md
contract: api-contract
depends: [T-FE-006, T-PERM-026]
status: done
created: 2026-09-02
last_updated: 2026-09-02
---

# T-FE-021：业务域配置页 mock→真实联调

## 范围

- 业务域配置页（`views/system/biz-domain`）9 端点 mock→真实：biz-domain 5（list/detail/create/update/remove）+ domain-config 4（list/detail/save/remove）。
- Gateway bootstrap 固定图端点注册 +9；业务门禁核对零新增（DOMAIN:VIEW 已在图 OPERATION_LOG 先例；SYSTEM_CONFIG:VIEW/MANAGE 已在图系统配置页门禁）。
- 前端 api 双文件路径修正：裸 `/api/perm/*` → Gateway `/perm/api/perm/*` 前缀（T-FE-020 conflict-rule 页同款缺陷——Gateway 仅路由 /admin/**、/perm/**，裸路径必 404）。
- mock 四文件整删：`mock/biz-domain.ts`、`mock/domain-config.ts`、`mock/_bizDomainRegistry.ts`、`mock/_domainConfigRegistry.ts`（仅 mock 间互相消费；`mock/login.ts` 只 import src 的 perms.ts 不受影响）。

## 设计定案（2026-09-02）

- mock 处置=整删（含两个共享注册表）：与 T-FE-019 permission-query 整删先例一致；注册表无 src 消费方、其他 mock 页不引用。
- 端点路径以契约后端路径为叙述权威、前端请求统一 Gateway /perm 前缀——设计文档 §6 补路径说明 blockquote，不逐处改写 §5/§8 表格路径。

## 验收

- [x] DTO 比对零漂移：BizDomainResp（id/tenantId/code/name/description/global/createdAt）、BizDomainCreateReq/UpdateReq/ListReq/DetailReq、DomainConfigResp/Req/GetReq/ListReq 与 TS 类型逐一比对一致（update 空 name=null 不更新、description 空串=清空语义已在 T-PERM-026 收口）。
- [x] bootstrap +9 ApiRoute；PgIT 计数 57→66（API 资源）/ 56→65（映射）/ 104→113（授权），scopeAll 47 不变。
- [x] 前端 eslint / vue-tsc / vitest 213 / vite build 全绿；后端 mvn test 全绿。
- [x] 重建开发库（固定图变更）后浏览器冒烟（见完成记录）。

## 完成记录（2026-09-02）

1. `api/biz-domain.ts` + `api/domain-config.ts` 全 9 端点路径切 `/perm/api/perm/*`，头注释同步（mock 退役注记）。
2. `BootstrapGraphDefinition.apiRoutes()` T-FE-021 块 +9 端点（写明门禁归依：DOMAIN:VIEW 与 SYSTEM_CONFIG 两档已在图）；`AccessBootstrapPgIT` 计数三处 66/65/113 + 注释链补 T-FE-021。
3. mock 四文件 `git rm`；全仓残留扫描仅剩任务卡/设计文档的历史叙述与退役注记。
4. 文档：`design/frontend/biz-domain.md` last_reviewed 2026-09-02 + 联调注记 blockquote + §6 路径说明 + §4/§9 mock 注册表两处改「已退役」；runbook Gateway 端点链 +9 至 66；`tasks/README.md` 与 `plans/frontend-phase3-plan.md` 进度行。
5. 浏览器冒烟（重建库后，admin/bootstrap 种子，七场景全过）：
   - ①主表 list：重建库 biz_domain 0 行，页面「共 0 条」（mock GLOBAL 种子随退役消失——真库全局域缺失为 T-PERM-046 已登记差距，非本任务缺陷）。
   - ②create：新增 HR/人事域/描述 → 「创建成功」，列表回显 HR 行，DB 落库 global=f。
   - ③update：改名「人事域V2」+ description 清空提交 → 「保存成功」，DB description 空串（契约「空串=显式清空」语义，页面展示为空）。
   - ④重复编码：再建 HR → 后端 20052 透出「Biz domain code already exists: HR」。
   - ⑤domain-config save：HR 域新增 CLASSIFY 配置 `{"resourceTypeCodes":["USER","ORG"]}` → 「保存成功」，子表回显。
   - ⑥删除保护：删 HR 域 → 20051 透出「Biz domain has domain configs, remove them first: HR」，行保留。
   - ⑦清理链：删 CLASSIFY 配置（「删除成功」）→ 再删 HR 域成功 → 「共 0 条」，DB 两表干净。
   - 附：bootstrap 种子重建后 DB 实测 API 资源 66 / MANUAL 授权 113 / biz_domain 0，与 PgIT 断言一致。
6. 回归：后端 `mvn test` 首轮唯一失败=DualInstanceContainerTest（端口 9100 被上会话遗留 dev 进程占用，环境性非代码缺陷——停进程后隔离复跑 3/3 绿，其余全绿，PgIT 15/15 容器轨全实跑）；前端 eslint/vue-tsc/vitest/vite build 全绿（评审收口新增竞态回归锁 2 用例，基线 213→215）。
