---
doc_type: task
id: T-FE-013
title: 4.2 权限查询/校验页前端实现（mock 驱动）
status: done
plan: docs/plans/frontend-phase1-plan.md
domain: frontend
design_refs:
  - docs/design/permission-center/api-contract.md#§5.7
  - docs/design/permission-center/api-contract.md#§6.6
  - docs/design/permission-center/api-contract.md#§6.7
  - docs/design/permission-center/api-contract.md#§6.8
  - docs/design/permission-center/core-flows.md
  - docs/design/permission-center/implementation.md
  - docs/design/frontend/permission-query.md
  - docs/design/architecture.md
depends_on:
  - T-FE-001
blocks: []
acceptance:
  - "P0 前端骨架：路由/标题/页面壳（查询表单 + 结果表格/面板），mock 可交互"
  - "多维度查询：按用户/角色，并结合业务域、资源类型、操作和资源关键字，分页查询当前有效权限（permission-view/effective-permissions §6.8）；按用户与主资源查询范围权限四态（auth/query-scopes §6.7）。query-resources §6.6 属运行时 SDK 视角，仅做 API 核对，不做管理端 UI"
  - "权限解释：展示「为什么有/没有」+ 拒绝原因 + 近期影响事件（permission-view/explain §6.8）；关联条件/条件评估/冲突详情登记 T-PERM-033 扩展 explain DTO"
  - "scopeMode 展示：消费 T-PERM-015 已落地的 ScopeMode 四态（frontend/src/utils/scope-mode.ts）"
  - "Step 1.5 组件识别：资源键输入（resourceTypeCode+resourceCode+codeType）候选 -> 登记 T-FE-001 组件池（待 2+ 页确认抽取）；treeMode 结果树不做 UI，登记 T-PERM-033"
  - "Step 3/4 API 核对：核对 effective-permissions / query-scopes / explain / query-resources / permission-view/*，标 ✅/🔧/❌；🔧 项登记 T-PERM-033（7 项）"
  - "P2 权限接线：hasPerms 门控 + 整页无权状态 + hook 短路"
  - "design_writeback：页面级 UI 设计回写至 docs/design/frontend/permission-query.md"
design_writeback:
  required: true
  status: done
last_updated: 2026-07-11
---

# T-FE-013 4.2 权限查询/校验页

> 状态：done
> 复杂度：🔴 高（第 3 批，核心页）
> 后端任务：T-PERM-033（depends_on 本任务）

## 背景

权限排查页：按用户/角色维度查询当前有效权限、范围权限四态、单权限解释（为什么有/没有 + 近期影响事件）。布局：三 Tab（查询表单 + 结果面板）。

## 后端契约锚点

- `api-contract.md §5.7`（L285，`auth/check`/`query-resources`/`query-scopes`）
- `api-contract.md §6.6`（L1171，通用资源权限查询，含 treeMode）
- `api-contract.md §6.7`（L1252，范围权限运行时查询，scopeMode 四态）
- `api-contract.md §6.8`（L1347，权限排查视图 permission-view/* + diff_snapshot）
- `implementation.md §3` PermQueryEngine + `§7.5` query-permission-tree（L825）

## 关联

- treeMode（§6.6 L1249）对应 improvement-plan 附录 A TODO（PermissionQueryAppServiceImpl.java:162），登记 T-PERM-033。
- scopeMode 展示复用 T-PERM-015 已落地类型（`frontend/src/utils/scope-mode.ts` + `api/perm-scope.ts`）。

## 验收标准

见 acceptance。

## 完成记录

### 2026-07-11 Phase 1 实现（mock 驱动）

**实现内容**：
- 路由 `/system/permission-query`，name `SystemPermissionQuery`，title "权限排查"，icon `ep/key`
- 三 Tab 布局：当前有效权限（effective-permissions §6.8）/ 范围权限四态（query-scopes §6.7）/ 单权限解释（explain §6.8）
- 主体模型：Tab1/3 支持 targetType=USER/ROLE，Tab2 仅 USER（核实后端 PermissionQueryAppServiceImpl:126 / PermissionViewAppServiceImpl:698/674）
- 角色类型码对齐 RoleType.java：ORG/POSITION/PERSONAL/GROUP_ROLE/BASIC_ROLE（ORG/POSITION domainCode 必填）
- scopeMode 四态展示（@/utils/scope-mode）：Tab2 矩阵（资源类型×操作笛卡尔积），Tab3 INSTANCE/ALL 切换
- 权限门控：临时复用 SYSTEM_CONFIG:VIEW，整页无权状态 + hook 短路（路由框架不消费 meta.auths）
- reqSeq 请求序号：过期请求静默丢弃（旧请求后返回不覆盖新主体结果，对齐 T-FE-012）
- 前瞻性采用 admin-service 聚合路径 /permission-query/*（Phase 1 mock 模拟，T-PERM-033 后对接真聚合层）

**接口定位纠正**：
- 原验收点名 query-resources（§6.6 运行时 SDK 接口，不分页、仅用户），与"管理端排查"定位冲突
- Tab1 改用 effective-permissions（§6.8 管理端分页排查视图，USER/ROLE + 来源角色）
- query-resources 降为 API 核对登记，不做 UI

**组件识别（Step 1.5，T-FE-001 组件池）**：
- 资源键输入（resourceTypeCode+resourceCode+codeType）：Tab2 主资源 + Tab3 单资源复用，候选登记待 2+ 页确认抽取
- SubjectInputBar：USER/ROLE 主体输入，候选登记
- treeMode 结果树：不做 UI，登记 T-PERM-033

**API 核对（登记 T-PERM-033，7 项；已随 T-PERM-033 于 2026-08-29 全部收口，终态见任务卡与 design/frontend/permission-query.md §9）**：
1. admin-service 聚合入口（PermissionQueryController + 聚合 DTO）——已随 T-ACCESS-012 取消聚合层收口
2. 统一门禁 PERMISSION_QUERY:VIEW 全链路（方案 A/B + 资源类型/种子/默认角色/白名单）——设计定案：不引入独立排查码，门禁=被查目标实例 USER:VIEW/ROLE:VIEW
3. explain DTO 扩展（命中条件/条件评估/冲突详情 + 评估上下文 + 脱敏）——已实现（conditionEvaluations/conflictDrops/evaluationContextSource，IP 掩码脱敏）
4. recentChanges 按完整权限键 6 字段过滤（当前 :735 只按用户/角色取 50 条）——已实现（+USER 目标保留 USER_ROLE_CHANGE）
5. ADMIN_USER/USER 主体语义核对——已核对（LOCAL_USER external_id=sys_user.id 字符串）
6. query-resources API 核对 + treeMode TODO——字段名与 §6.6 一致无差异；treeMode 已从契约移除（2026-08-27）
7. permission-view/* 契约差异核对——唯一差异 resource-users（契约「用户」vs 实现角色维度）已登记待消费方收口

**验证**：pnpm typecheck + lint + build 全过 EXIT:0

**文档**：
- 设计文档 docs/design/frontend/permission-query.md（draft -> adopted）
- 后端任务 docs/tasks/T-PERM-033.md（创建，7 项范围）
- README L27（4.2 待产出->✅）+ L33（7.2 待产出->✅，T-FE-012 评审遗漏修正）
