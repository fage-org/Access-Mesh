---
doc_type: task
id: T-FE-012
title: 7.2 权限变更日志页前端实现（mock 驱动）
status: done
plan: docs/plans/frontend-phase1-plan.md
domain: frontend
design_refs:
  - docs/design/permission-center/api-contract.md#§5.8
  - docs/design/permission-center/api-contract.md#§6.8
  - docs/design/permission-center/core-flows.md
  - docs/design/frontend/permission-change-log.md
  - docs/design/architecture.md
depends_on:
  - T-FE-001
blocks: []
acceptance:
  - "P0 前端骨架：路由/标题/页面壳（时间线/表格 + diff 对比 + 影响分析），mock 可交互"
  - "diff 快照展示：before/after 对比（§6.8 diff_snapshot 轻量规范）"
  - "Step 1.5 组件识别：Diff 对比面板候选 → 登记 T-FE-001 组件池（待 T-FE-005 扩展 diffSnapshot 后确认共用性，2+ 页确认后抽取）"
  - "Step 3/4 API 核对：核对 permission-change-log/list + diff_snapshot，标 ✅/🔧/❌；🔧❌ 项登记 T-PERM-032"
  - "P2 权限接线：hasPerms 门控 + 无权降级"
  - "design_writeback：页面级 UI 设计回写至 docs/design/frontend/permission-change-log.md"
design_writeback:
  required: true
  status: done
last_updated: 2026-07-11
---

# T-FE-012 7.2 权限变更日志页

> 状态：done
> 复杂度：🔴 高（第 3 批）
> 后端任务：T-PERM-032（depends_on 本任务）

## 背景

权限变更日志页：diff 快照展示、变更详情（before/after）、关联影响评估。布局：时间线/表格 + diff 对比 + 影响分析。

## 后端契约锚点

- `api-contract.md §5.8`（L308，`permission-change-log/list`）
- `api-contract.md §6.8`（L1347，含 diff_snapshot 规范 L1590）
- `core-flows.md §13.2` 用户排查链路 + `§13.3` 角色排查链路
- 后端表 `permission_change_log`（permission-center.sql）

## 组件候选

Diff 对比面板：本页 + 7.1 操作日志详情（T-FE-005）共用，登记 T-FE-001 组件池，2+ 页确认后抽取。

## 验收标准

见 acceptance。


## 完成记录

- 2026-07-11：**T-FE-012 实现完成** ✅。分页表格（服务端分页 + entityType/entityId 筛选）+ 详情抽屉（基本信息 + DiffSnapshotPanel 结构化 diff + old/new 原始快照折叠 + 影响范围）；hasPerms 接线复用 SYSTEM_CONFIG:VIEW（无独立权限码），admin/sec/hr/auditor 均可查（审计员必须能查）。UI 设计回写 docs/design/frontend/permission-change-log.md。API 核对：list 端点 ✅、diff_snapshot 规范 ✅、无 detail 设计合理 ✅；契约路径错误 🔧、缺字段契约章节 🔧、筛选维度不足 🔧、Resp 缺操作人 🔧、changeSource 枚举 schema 不符 🔧、独立权限码审计语义 🔧，已登记 T-PERM-032。组件识别：DiffSnapshotPanel 登记 T-FE-001 池待确认（T-FE-005 当前无 diffSnapshot 字段，共用性待后续扩展确认），本页内联不抽取。**第 3 批第 3 项完成，下一项 T-FE-013 4.2 权限查询/校验。**

- 2026-07-11：**评审修复**（commit d327af4b7）。评审发现 1 P1 + 3 P2，全部属实并修复--P1 `loadTable` 并发请求覆盖（引入 `reqSeq` 请求序号，过期请求静默丢弃，最新请求失败时清空 `tableData`/`total`，审计页优先不展示与当前筛选不符的数据）；P2 `entityId` 输入 `:min=1`->`:min=0` 允许筛选批量聚合日志 + mock 补 2 条 `entityId=0` 场景（对齐后端 `RoleManageAppServiceImpl:300` BATCH_DELETE / `UserManageAppServiceImpl:672` BATCH_REMOVE）；P2 `parseDiffSnapshot` 逐项校验 items（仅保留非空对象且 `changeType` 属 ADD/REMOVE/UPDATE，避免 `items:[null]` 在 DiffSnapshotPanel 渲染抛 TypeError 崩溃）；P2 acceptance 措辞调整（「-> 确认」改「待 T-FE-005 扩展 diffSnapshot 后确认共用性」，与 plan 组件池「待确认」状态一致，status 保持 done）。附带发现：后端批量删除角色 `diffSnapshot.eventType` 写 `"ROLE_BATCH_DELETE"` 超出 §6.8 定义的 7 枚举，前端 `EVENT_TYPE_META` fallback 显示原值不崩溃，追加登记 T-PERM-032。
