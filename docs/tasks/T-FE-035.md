---
doc_type: task
id: T-FE-035
title: 扩展V2 transport（多条件+失败模拟）+失格降级+回归验证+设计回写
status: cancelled
plan: docs/plans/archive/2026-07/permission-grant-v2-plan.md
domain: frontend
archived_at: 2026-07-26
archived_reason: 页面交互不满意，v1+v2 两套删除重做，代码与设计文档同步归档/删除
design_refs:
  - docs/archive/2026-07-26/permission-grant-error-flow.md#28-用户失去权限编辑资格e27e31
  - docs/archive/2026-07-26/permission-grant-state-model.md#8-已知缺口与状态模型预留
  - docs/archive/2026-07-26/permission-grant-state-model.md#9-小结
depends_on: [T-FE-034]
blocks: []
acceptance:
  - "扩展 V2 transport（T-FE-029 骨架 + 基础单分支 fixture），支持同单元格多条件分支返回、save 响应按 PermCellKey+conditionCode 回显 id"
  - "mock 支持失败模拟：主失败/子部分失败/reload 失败/超时（localStorage 开关）"
  - "失格降级：失去 MANAGE/角色禁用/canGrant 回收 -> readonly+reason（fail-closed）；401 跳登录"
  - "权限列表返回空时区分'没有配置'与'没有查看权限'"
  - "回归验收场景：多分支展示/逐分支编辑撤销/批量新增分支/子权限 parentVariantId/SAVE_PREVIEW/两子态恢复/STALE_WITH_CHILD_FAILURE 原子刷新重试/条件清除/离开保护"
  - "提交前 pnpm build && pnpm typecheck && pnpm lint && pnpm test 全通过"
  - "设计回写：三份文档状态对齐 V2 实现，缺口表标注 V2 已实现项"
design_writeback:
  required: true
  status: pending
last_updated: 2026-07-17
---

# T-FE-035 扩展V2 transport（多条件+失败模拟）+失格降级+回归验证+设计回写

> ⚠️ 已取消（2026-07-26）：权限授予页面交互不满意，v1+v2 两套整体删除重做，本任务不再推进。

## 目标

扩展 V2 transport（T-FE-029 骨架）补齐多条件分支与失败模拟，完成失格降级、回归验证与设计回写。

## 非目标

- 不联调真实后端（归 T-FE-018 后续）。

## 实现要点

- mock 文件零 src/api 依赖（常量/类型本地声明，遵循 mock 规范）。
- 回归清单覆盖三份文档的关键不变量与恢复流程。

## 验收标准

以 frontmatter `acceptance` 为准；build/typecheck/lint/test 通过。
