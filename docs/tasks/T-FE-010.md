---
doc_type: task
id: T-FE-010
title: 3.3 冲突规则页面前端实现（mock 驱动）
status: done
plan: docs/plans/frontend-phase1-plan.md
domain: frontend
design_refs:
  - docs/design/permission-center/api-contract.md#§5.6
  - docs/design/permission-center/overview.md
  - docs/design/frontend/conflict-rule.md
  - docs/design/architecture.md
depends_on:
  - T-FE-001
blocks: []
acceptance:
  - "P0 前端骨架：路由/标题/页面壳（表格 + 表单弹窗 + 检测结果），mock 可交互"
  - "Step 1.5 组件识别：识别可复用组件（如 Diff 对比面板候选）→ 登记 T-FE-001 组件池 → 确认"
  - "Step 3/4 API 核对：以 mock 请求/响应为基准核对 conflict-rule/* 接口（含 detect 冲突检测），标 ✅/🔧/❌；🔧❌ 项登记 T-PERM-030"
  - "P2 权限接线：hasPerms 门控（perms.ts SSOT）+ 无权降级"
  - "design_writeback：页面级 UI 设计回写至 docs/design/frontend/conflict-rule.md（status: draft → adopted）"
design_writeback:
  required: true
  status: adopted
last_updated: 2026-08-30（头部状态与后端任务行随 T-PERM-030 收口订正）
---

# T-FE-010 3.3 冲突规则页

> 状态：done
> 复杂度：🔴 高（第 3 批）
> 后端任务：T-PERM-030（depends_on 本任务，已收口 2026-08-30——§4 🔧 六项全处置）

## 背景

冲突规则页：角色互斥/权限互斥规则 CRUD + 冲突检测。布局：表格 + 表单弹窗 + 检测结果展示。

## 页面功能（improvement-plan §4.1）

- 角色互斥/权限互斥规则 CRUD
- 冲突检测（调用 `conflict-rule/detect`）
- 检测结果展示

## 后端契约锚点

- `api-contract.md §5.6`（L258，`conflict-rule/*` 含 detect）
- 后端表 `permission_conflict_rule`（permission-center.sql）
- DomainService `PermissionConflictDomainService`（implementation.md §2.4）

## 执行顺序

按 frontend-phase1-plan §任务清单 第 3 批。P0 骨架 → Step 1.5 组件 → API 核对 → P2 权限 → design_writeback。

## 验收标准

见 acceptance。复杂度高，开独立文件跟踪。
