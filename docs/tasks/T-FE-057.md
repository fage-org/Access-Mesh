---
doc_type: task
id: T-FE-057
title: "岗位停用后可发现与恢复"
status: done
plan: docs/plans/iam-task-closure-plan.md
domain: frontend
design_refs:
  - docs/design/iam-task-closure.md#position-lifecycle
  - docs/design/org-user-permission-contract.md
  - docs/design/access-service-api-contract.md
depends_on:
  []
blocks: []
acceptance:
  - "启用岗位禁用后仍能通过管理页找到并重新启用，浏览器完整操作可验证。"
  - "启停后成员/角色权限由后端现有事实链变化，界面不靠本地造状态；恢复失败有可行动提示。"
  - "用于分配的有效岗位候选仍排除禁用项，权限不足/空列表/刷新失败不产生错误恢复动作。"
design_writeback:
  required: true
  status: done
last_updated: 2026-09-22
---

# T-FE-057 岗位停用后可发现与恢复

## 背景

承接[评审证据](../archive/2026-09-20/comprehensive-review.md)的 F007；基线与静态/动态证据强度见该记录。实施与验证见完成记录。

## 范围

- PositionTab管理列表状态筛选、状态显示和恢复编辑；区分管理列表与有效岗位候选。

## 当前口径

方案唯一入口：[IAM闭环方案](../design/iam-task-closure.md#position-lifecycle)（§4.1 已转已实施，2026-09-22）。

**启动拍板（2026-09-22，AskUserQuestion 三问，registry 同日行）**：①恢复入口=编辑弹窗改状态单选（与组织同 OrgForm 先例，不新增行内快捷开关）；②停用岗位上成员挂载/移除/授权/删除等写操作维持可用（后端事实链负责语义，界面不造本地规则）；③验收证据形态=浏览器实测（T-ORG-003 让渡先例不适用于纯前端 UI 卡）。

## 完成记录（2026-09-22）

### 实施

- `PositionTab.vue`：`loadPositions` 查询经新增 `utils/positionList.ts#buildPositionPageQuery` 构造——status 缺省不下发（管理面=全部状态，停用岗位保留可见）；新增状态筛选下拉（服务端参数，MemberTab 同款先例；词汇「禁用」沿组织域 OrgForm/组织信息卡先例）；卡片状态标识=仅停用项红色 `el-tag`（例外标注，ConditionPicker 停用标注先例）；`statusFilter` watcher 变更即重查（重置/清空经同 watcher）；空态文案区分「无匹配岗位（调整关键词或状态筛选）」vs「暂无岗位数据」；八处失败提示换绑 `toErrorMessage`（后端 body 文案优先，T-FE-051 同款——验收②「恢复失败有可行动提示」）；`loadPositions` 补请求代际守卫 `positionReqSeq`（双轨评审 P3-1 处置，用户拍板本卡顺手加——筛选/组织切换并发时旧响应不回写列表与 loading；组件无挂载测试设施，守卫以静态核对+四门为证）。
- 「分配用有效岗位候选排除停用」面（验收③）核实零改动：授权页主体树请求层 `getOrgTree({ includePositions: true, status: 1 })`（SubjectTreePanel.vue:111，2026-09-04 用户决策）；用户详情面板岗位只读（岗位由岗位 Tab 维护）；新增用户表单只选普通组织——全仓无其他岗位候选选择器（双轨代码轨类推扫描 `status: 1` 全仓 18 处逐点定性复核无同款管理列表残留）。
- 后端零改动（`/org/page` status 本就可选，三层契约链〔前端类型→OrgPageReq→mapper 动态条件〕经代码轨评审实证闭合；向停用岗位挂成员本就不拒绝——停用效果走事实链）。

### 验证

- **回归锁（红跑实证）**：`positionList.spec.ts` 4 用例——先以旧形态（固定 status:1）实现被测函数跑红（3 红：默认含 status 键/status:0 透传失败/orgId 键恒下发），修实现后 4/4 绿（代码轨独立推演复核 3 红 1 绿与声明一致）。
- **四道门**：vitest 35 文件 410 全绿（含新增 4 例）、typecheck、lint、build 全绿（评审处置后复跑仍全绿）。
- **浏览器实测（dev 栈，用户拍板证据形态）**：重建 dev 库（runbook 三步）+ bootstrap 种 admin + access-service/gateway（.env 密钥）/前端 8890 起栈；全链路：登录→组织与用户→岗位管理→新增岗位（启用）→列表可见无标签→**编辑禁用→列表保留+红色「禁用」标签**（旧实现此步岗位消失=F007 缺陷本体）→筛选「启用」=空+「无匹配岗位」文案→筛选「禁用」=岗位可见→编辑恢复启用→筛选「禁用」下消失（事实链生效）→重置=全部、无标签；截图入仓 `docs/tasks/evidence/t-fe-057/position-disabled-visible.png`（T-ACCESS-021 惯例）；冒烟数据（测试岗位）已删除清理。

### 双轨评审处置（2026-09-22）

- **代码轨**：P0-P2=0；P3-1 loadPositions 无代际守卫且本卡新增触发源扩大竞态面 → **用户拍板本卡顺手加**（positionReqSeq 局部序号，成功/失败/loading 三回写点全判代际）；P3-2 `loadPositionUsers` catch 同页换绑遗漏（同文件 8 处中唯一漏网，与候选加载双标准）→ 直接修一行；存疑③ 首载失败空态文案维持现状（评审员倾向不修，误导度低，T-FE-051 失败保留旧数据语义下首载失败=空列表为既定形态）。类推扫描通过（getOrgPage 唯一调用方；`status: 1` 全仓 18 处定性无管理列表同款残留；管理面 role hook 不传 enabledOnly 与本修复形态对齐）。
- **文档轨**：P0-P1=0；P2-1 registry 拍板行缺失（本卡 frontmatter 指针悬空）→ 收口补行（本卡 registry 同日行）；P2-2「截图归档」声称无仓库实体 → 截图落盘 `docs/tasks/evidence/t-fe-057/`（T-ACCESS-021 惯例）；P3-1 CHANGELOG 同句「停用/禁用」双词 → 改写统一组织域词；词汇跨 tab 分域注记不补（评审采纳现状——各随资源域有先例锚点）；P3-2 Q-035 行号精确化（PositionTab.vue:246 vs index.vue:159）；P3-3 任务卡补「设计回写」条目（本节即补）。
- 两轨过度设计可裁剪项均=0（positionList.ts 抽取=组件内联不可测的既定解法先例链 T-FE-050/052/055，红跑锁依赖该抽取）。

### claude 外评处置（2026-09-22，模型 deepseek-flash[1m] 本机默认；P0-P2=0、P3×2、过度设计=0，专项清单五项全过、结论逐条代码级核实全成立）

- **P3-1 停用岗位行授权入口死路**（本卡新暴露：入口首次对停用岗位可达→授予页主体树 status=1 定案过滤→preset 必 !found→清空主体+误导提示，且该分支不经 confirmDiscardIfDirty 草稿静默丢失）→ **用户拍板入口侧收敛**：停用行授权按钮置禁用态+tooltip「岗位已禁用，恢复启用后可在授权页选择」（el-tooltip+span 包裹对齐 EP disabled 规范），不触碰 2020-09-04 树过滤定案面；角色管理页/组织信息卡同款存量入口+preset 不确认草稿登记 **Q-037**。
- **P3-2 任务卡两处陈述与产物不符**：①「util 注释已标注」实际只在 spec 用例名 → positionList.ts 头注补分页射程句（直修）；②验收②「✅」证据强度超实测（设计 §4.1 要求权限行为验证，实测九步全为列表可见性）→ 验收②降格为如实（代码级核实+观测让渡），**用户拍板把「停用期成员权限剪枝与恢复自动生效观测」补进 T-ACCESS-055 acceptance① 岗位恢复句**（T-ORG-003 让渡同款）。
- 存量观察核实属实两条登记 **Q-036**（orgTree prop 无人传致位置列恒「-」；loadPositionUsers 失败落「暂无成员」空态混淆）；计数差异按 registry 2020-09-19 T-FE-051 定案不作为缺陷；其余存量（loadPositionUserCount 静默置 0）已有登记不重报。
- 处置后四门复跑全绿。

### 设计回写

- `docs/design/iam-task-closure.md` §4.1 转已实施 + frontmatter last_reviewed（守卫处置后口径同步）。
- `CHANGELOG.md` [Unreleased] Fixed 条目（沿 T-PERM-076 定案②正确性类补条目先例）。
- `docs/pending-problems.md` Q-035 登记（新增岗位弹窗 initialData.parentOrgId 通道失效，存量）。
- `docs/design/decision-registry.md` 2026-09-22 收口定案行（三项启动拍板+双轨处置）。

## 验收对照

- 验收①：✅ 浏览器全链路实测通过（禁用后可见可筛选可恢复，截图入仓）。
- 验收②：界面不靠本地造状态=实测覆盖（全程经后端事实回显）；「成员/角色权限由事实链变化」为**代码级核实**（`OrgWriteAppServiceImpl.projectOrg`→投影同步 `role.status`→引擎有效角色剪枝，本卡零改动、既有链有既有测试锁；浏览器实测岗位无成员，未观测权限剪枝实况——权限剪枝观测待 T-ACCESS-055 组合验收，claude 外评 P3-2 处置）；失败提示透后端文案（toErrorMessage 八处含恢复路径）。
- 验收③：✅ 岗位候选选择器面零改动零回归（授权页主体树 status=1 定案维持）；空态文案区分不误导；加载失败提示后端文案且不造恢复动作。

## 非目标 / 遗留

- 本卡不触及分页闭合（固定 pageNum=1/pageSize=100 属 F008/T-FE-058 射程，util 注释已标注）。
- Q-035（新增岗位弹窗上级组织默认根组织）随下一张触达 PositionTab 的任务顺手收敛。
- 冒烟环境运维注记（复用价值）：gateway 必须与 access-service 同源 `.env` 密钥启动（裸启动时内部密钥不一致→403「鉴权服务暂时不可用」）；TaskStop 停 mvn 任务后 java 子进程残留占端口需 taskkill。
