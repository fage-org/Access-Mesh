# 定案登记表（Decision Registry）

> 全仓定案的**唯一消费入口**：本表只存指针——日期 + 一句话口径 + 出处锚点 + 评审处置，定案正文仍在任务卡/设计文档，不在本表复制（模式同文档治理「入口引用不重复正文」）。会话内拍板、无仓库正文载体的定案，以本表为正文登记点（口径写全）。
>
> 初稿 2026-09-06（初始收编既有散点）；此后用户定案当轮追加，不跨轮补记。

## 写入协议

- 用户每次定案（含 AskUserQuestion 结论、会话内拍板）**当轮登记**新行，按日期顺序追加。
- 口径被新定案推翻：旧条目移入「已推翻」节并标注取代它的定案，**不删除**。
- 评审处置两档：
  - **再报直接撤回**：评审再报该口径即整条撤回，不进存疑队列、不占用用户决策时间；
  - **报了先核出处**：评审报相关问题时先对照出处锚点核实再定性。

## 消费协议

- 评审提示词豁免段：双轨子代理**整段注入本文件当前内容**；外评三通道（2026-09-12 v2 起）不再注入——模板「硬性纪律」首步指令评审自读本文件全文 + 槽位只挑任务相关行 + 报告首行声明已读。
- 评审结论先对照本表：命中「再报直接撤回」即撤回；命中「报了先核出处」先核出处锚点再定性。
- 验收口径：连续两个任务的评审中，已登记定案零进入存疑队列。

## 定案条目

| 日期 | 定案口径 | 出处锚点 | 评审处置 |
|---|---|---|---|
| 2026-08-28 | 产品定位=开源通用 IAM；暂缓能力维持暂缓（自动授权写入口 20048 拒绝 true、动态数据权限端到端验证延后）；执行节奏=维护债→定位落地文档整改→简单页后端，复杂核心链路等用户时间充足再启动 | docs/design/README.md（定位节）、docs/tasks/README.md（后续节奏节） | 报了先核出处 |
| 2026-08-30 | 全局操作概念整体退役（T-PERM-049）：操作位空间按类型完全隔离 + DDL CHECK 焊死；includeGlobalFallback/全局回退/可空键轨全部移除，不得建议恢复 | docs/archive/2026-09-12/tasks/T-PERM-049.md、api-contract.md 头部退役注记、rebuild-runbook.md | 再报直接撤回 |
| 2026-09-10 | T-PERM-059 权限视图/排查删除重设计三项定案（任务内逐项拍板）：①删除边界=全删 8 端点（permission-view 七端点 effective-permissions/resource-users/role-permissions/effective-roles/resource-tree/explain/recent-changes + /auth/query-permission-tree），explain 不保留（codex 四轮外评登记的「USER INSTANCE 候选明细完整性」遗留随之消亡不修），登录串 effective-permission-codes 与运行时 query-scopes 不在删除面；②前端面=页面目录+api/permission-query.ts+api/perm-scope.ts 一并删除，T-FE-043 cancel（重做考虑事项随卡归档、新形态另立任务）；③分期收口=本卡只做删除（新设计方向基于统一引擎结果模型的产出另立任务，需 grill 级产品讨论）。连动面：bootstrap 固定图删 effective-permissions/explain 两行（query-scopes 行保留维持空库 API 映射种子）+「权限排查」菜单种子删；perm-common SDK 三 DTO 副本（UserPermissionViewReq/UserPermissionViewResp/PermissionEffectivePermissionsResp）与 PermissionFeignClient.getEffectivePermissions 同批删除；PermissionConditionDomainService.evaluateDetailed/PermissionConflictDomainService.filterPermMutexWithDrops（explain 专属）删除，STATUS 常量与 ItemDetail 迁入实现类私有载体；④收尾两项（用户拍板）：check-interface `matchedResources[].resourceTypeCode` 契约示例改 null（实现恒 null——T-API-003 行挂靠本卡的示例漂移收口，Gateway 专用端点无人读该字段）；permission-query-unification 计划随本卡归档 + query-engine-unification.md 转 superseded（四任务全 done） | docs/archive/2026-09-12/tasks/T-PERM-059.md、api-contract §5.8/§6.1/§6.7、implementation.md、access-service-architecture §14.4、rebuild-runbook FAQ、BootstrapGraphDefinition | 评审勿再报「permission-view 端点 404/排查页缺失」——预期行为，重做另立任务。claude 外评处置（2026-09-10，6e665334b 后）：P2×4（§6.8 活引用 23 处改指 §5.8+编号锚点/契约表格空行/impactLevel 与排查页暂停注记两处自相矛盾/admin 契约菜单种子行）+P3×7 组（PermMutexContext 三死方法与 fromSnapshot 死方法删除/注释现在时 6 处/mermaid 与遗留清单等文档漏改/归档索引四处/Bootstrap Javadoc「原」字/mock 注释）全处置。codex luna max 复评（2026-09-11，93daea457 read-only，813k tokens）：P0=0/P1=0，P2×2+P3×4 全处置——①归档状态同步（design/README 索引 superseded+query-engine-unification 补 superseded_by+archive/2026-09-10 补批次 README）；②runbook 固定图历史叙述补现值注记+Initializer 菜单注释 15→14；③claude 处置轮自我引入的「（原 §5.8）」双重替换错引修正为「（原 §6.8）」+行号引用清理；④任务卡「整删」表述修正+ScopeModeSupportTest 空方法删；⑤AbstractRoleMapper.selectFilteredByIds 死查询（唯一消费方 filterRoleIds 已删）连 XML 删除；⑥operation-log/change-log 两处前端设计现在时句加删除注记 |
| 2026-08-30 | Locale：裸 toUpperCase/toLowerCase（土耳其 dotted-i）不修，normalizeCode 与全仓同款站点一律不动 | docs/tasks/T-PERM-034.md（P3 定案句）；全仓适用范围以本表为登记点 | 再报直接撤回 |
| 2026-08-30 | 无 resource_entity 投影的类型其实例级门禁=ID 空间错位：CONDITION/CONFLICT_RULE/DEPENDENCY 写门禁一律类型级（T-PERM-029/030/031）——【部分取代：CONDITION update/delete 已随 2026-09-11 T-PERM-048 定案④升实例级（投影落地后解锁）；CONFLICT_RULE/DEPENDENCY 维持类型级（无投影，原则仍成立）】 | api-contract.md（权限条件/冲突规则/资源依赖三节契约要点）、三张任务卡 | 再报直接撤回 |
| 2026-09-02 | BIGSERIAL 主键全仓 number 线格式（序列值物理不可能超 2^53）；字符串线格式仅位值/大数列 | docs/archive/2026-09-07/tasks/T-FE-016.md 验收（BIGSERIAL 条）、project-rules.md §7.4 | 再报直接撤回 |
| 2026-09-02 | 字段清空统一协议=xxxClear Boolean 标志四件套（标志 + UpdateEntity 强制写 null + 前端公式 + 契约条目），禁止「空串=清空」第二语义 | docs/archive/2026-09-07/tasks/T-FE-016.md 验收（extraClear 条）、api-contract.md §5.2/§5.3 extraClear 行 | 报了先核出处 |
| 2026-09-02 | 权限码=「资源:操作」结构：资源名须名词性实体、操作描述不做资源名；先复用目标实例权限检查再造新码 | api-contract.md（独立排查权限码否决句）、docs/tasks/T-PERM-033.md；名词性实体约束以本表为登记点 | 报了先核出处 |
| 2026-09-03 | TYPE_DEFINITION 反向变体：type-definition/list 门禁放宽为「类型级或任一实例级 VIEW」，实例投影登记 T-PERM-051 待做——评审勿报「实例级路径无自动产出」缺陷 | docs/archive/2026-09-12/tasks/T-PERM-051.md、api-contract.md 头部登记 | 再报直接撤回 |
| 2026-09-03 | 固定图（BootstrapGraphDefinition）冲突/边界问题一律登记 access-service-architecture §14.2 待议清单，攒批讨论，不零散改口径 | docs/design/access-service-architecture.md §14.2 | 报了先核出处 |
| 2026-09-04 | 四棵树统一写锁=Redisson（advisory 路径退役），unlock 挂 afterCompletion | docs/archive/2026-09-12/tasks/T-PERM-044.md | 报了先核出处 |
| 2026-09-05 | 资源类型级所有权（T-PERM-052）：每 resource_type 单一所有权（extra.managedMode MANAGED/SYNC + syncSourceService）；USER/ORG/MENU/ROLE 种子声明 SYNC+access-service；syncTypes 资源维度退役（subject/role/source 三维白名单保留）；评审勿再建议行级方案 | AGENTS.md 权限中心实现提醒节、docs/tasks/T-PERM-052.md、access-service-architecture.md | 再报直接撤回 |
| 2026-09-06 | 本地双轨评审与 codex 外评两轨分离：codex 仅用户显式触发，本地收口不自动串联，续跑由用户拍板 | dual-track-local-review / codex-external-review 两 skill 触发纪律节 | 报了先核出处 |
| 2026-09-06 | 决策提问协议：任何提问决策场景必须举例说明 | .claude/rules/decision-question-protocol.md | 报了先核出处 |
| 2026-09-06 | 树写锁不加 tryLock/lock_timeout 等待保险丝（会误杀排在 full-sync 后的合法长等待，与不配 statement_timeout 同向；恢复=重启持锁实例或 lease TTL 30s 兜底） | 本表（会话定案；机制背景见 docs/archive/2026-09-12/tasks/T-PERM-044.md） | 报了先核出处 |
| 2026-09-06 | TaskExecutionLeaseConcurrencyTest 全量负载时序抖动定案不修：多方法轮流红 + 隔离运行恒绿即定性为已知抖动，不调查本任务改动 | 本表（会话定案；历史实证散见各任务卡） | 报了先核出处 |
| 2026-09-06 | 瞬时状态不入记忆（仓库为载体）；待拍板问题当场按决策提问协议直问，不驻留记忆攒议程 | 本表（会话定规） | 报了先核出处 |
| 2026-09-06 | 工作流审计沉淀定案：评审收口已随两轨分离 skill 治理落地；定案登记表=本文件；回归 SOP 与锁协议收编 rule/skill 不立项（能力内容留在评审/回归相关记忆） | 本表（会话定案） | 报了先核出处 |
| 2026-09-06 | 权限排查页（permission-query）暂停待重做（T-FE-043 登记）：页面问题与功能记录后整体重做，后续任务对该页只做编译一致最小改动，不投入页面改造；评审勿报该页 UI/交互缺陷，改登记 T-FE-043（**已被 2026-09-10 T-PERM-059 行取代**：页面已删、卡已 cancel，本条操作指引失效） | docs/archive/2026-09-12/tasks/T-FE-043.md、api-contract.md §6.7 排查页暂停注记 | 再报直接撤回 |
| 2026-09-06 | AGENTS.md 只承载长期稳定事实与路由（外部评审核实后用户拍板）：任务进度/阶段枚举移出、指针化到 `docs/tasks/README.md`（禁以本文件历史任务号推断状态）；所有权段只留当前模型+错误码+防复活护栏，演进史交本表与架构文档；核心编码规范裁到高频硬约束（细则单一权威 project-rules）；新增 Agent 工作协议 5 步 | AGENTS.md | 报了先核出处 |
| 2026-09-06 | 统一响应壳全量更名 PermResult→R（工厂 success/error→ok/fail、Advice→RResponseAdvice、前端 TS 信封类型同步 R<T>）；线格式字段与取值不变（RWireShapeTest 锁）；引擎类 `dto/query/PermResult` 是权限查询内部结果、与响应壳是两个物，禁止合并或混淆 | common/model/R.java、project-rules.md §1.1、docs/design/frontend/service-interface-mapping.md | 报了先核出处 |
| 2026-09-06 | Agent 指令面收敛：删除 `.github/copilot-instructions.md`（第 4 份浓缩副本，漂移实证 3 处），规范入口=AGENTS.md + project-rules.md + skills/rules；勿再为 GitHub Copilot 重建独立指令副本（如需注入面用薄指针文件指向 AGENTS.md） | accessmesh-patterns SKILL.md（权威列表行）、本表 | 报了先核出处 |
| 2026-09-06 | 分页信封统一（T-ADMIN-027 登记）：全仓唯一分页方言=扁平五字段 `{items,total,pageNum,pageSize,hasNext}`，统一类更名 `PaginatedResp`→`PageResp`（线格式零变化）；admin 嵌套方言 `PaginatedResult` 退役删除；5 处 `R<List>` 裸数组收编 `ItemsResp`；分页字段不进 R（信封管传输/载荷管分页正交）；类名 `Page` 因与 MyBatis-Flex Page（19 文件在用）冲突被否 | docs/archive/2026-09-12/tasks/T-ADMIN-027.md、api-contract.md §3.3、project-rules.md §1.3 | 报了先核出处 |
| 2026-09-06 | 分页/列表信封双份重复类收编单源（T-ADMIN-027 执行定案）：`access.permission.dto.resp` 内部 `PaginatedResp`/`ItemsResp` 副本删除，全仓（admin/permission 域、SDK）统一 import perm-common `perm.common.dto.resp.PageResp/ItemsResp`（执行扩面：裸数组收编由建卡 5 处增至 6 处，含 FQ 写法漏盘的 `/notice/my-notices`）——后续演进只改一处，勿再建议恢复域内副本 | perm-sdk/perm-common dto/resp、project-rules.md §1.3 类名指引 | 报了先核出处 |
| 2026-09-06 | admin 分页端点参数面统一（T-ADMIN-026 设计定案：顺带对齐先例）：分页请求 DTO 不再允许裸 Integer 形态——FilePageReq/JobLogPageReq 补 `@Min(1)/@Max(100)` + 默认 getter（1/20），login-log/page 补 `@Valid`；漏传走默认、非法值 400，对齐 PageReq/Oauth2ClientPageReq 先例 | docs/archive/2026-09-12/tasks/T-ADMIN-026.md、admin-service-api-contract.md §4.7.3/§1.3 | 报了先核出处 |
| 2026-09-06 | 容器测试轨道基建与并行口径（T-ACCESS-030）：容器组单例基建 ItInfra + fork 级进程并行——sa-token 的 SaManager 是 JVM 级静态单例，线程级类并发被两轮实证否决，勿再建议 JUnit 线程并行；fork 标记 = user.home 文件锁槽位 1..4（surefire ${surefire.forkNumber} 在 systemPropertyVariables 插值空串、java.io.tmpdir 每 fork 不同，均不可用）；Redis 测试容器开 --databases 64 按槽位独占 16 索引段（利用 Redis 多 database，勿建议恢复共享段/奇偶两段式）；forkCount=${it.forkCount} 默认 2，-Dit.forkCount=1 串行逃生门 | docs/archive/2026-09-12/tasks/T-ACCESS-030.md、access-service/pom.xml、AGENTS.md 常用命令区、access-service/src/test/java/cn/ac/fage/accessmesh/access/it/ItInfra.java | 报了先核出处 |
| 2026-09-06 | E2E 独立模块分轨与 -T 模块并行恢复（T-ACCESS-031，用户决策 B+结构性拆分）：两条跨服务 E2E 验收 IT 迁独立 `e2e` 模块（reactor 末位），gateway 解除对 access-service/example-service 的 test-scope 依赖与容器轨死配置（勿再建议把跨服务 E2E 类放回服务模块——reactor 依赖边会使 mvn -T 对重模块完全失效，-T 未拆分实测 525s≈串行 522s）；日常全仓 `mvn test -T 1C -DskipE2E=true`、收口 `mvn test -T 1C`（E2E 必跑，ci.yml 单测 job 两开关都带）；e2e 模块须随 reactor 构建（-pl e2e 单跑会解析三服务 repackaged jar 必失败）；e2e（access-service 同款）surefire 的 `api.version=1.44` 钉版是 Docker 29+ 与 docker-java 3.3.6 兼容必需（缺它 @Testcontainers 静默跳过）；时序用例禁裸 sleep 余量（-T 负载已击穿两例，改轮询/提交闸门确定性机制） | docs/archive/2026-09-12/tasks/T-ACCESS-031.md、e2e/pom.xml、gateway/pom.xml、AGENTS.md 常用命令区与测试运行纪律、.github/workflows/ci.yml | 报了先核出处 |
| 2026-09-07 | 后端业务键统一入口=perm-common BusinessKeys（19 方法族，A+B 全收：跨类契约键+单文件内部键；格式 BusinessKeysParityTest golden 锁）；防回归仅 golden 锁，不加源码扫描守卫——评审勿再建议裸拼扫描守卫测试 | docs/design/permission-center/implementation.md §8、perm-sdk/perm-common util/BusinessKeys.java、AGENTS.md 核心编码规范行 | 再报直接撤回 |
| 2026-09-07 | BusinessKeys/SyncKeyCodec 名词命名偏离 project-rules §6.2「XxxUtil」：用户已知、计划后续 IDE 统一改名；改名落地前评审勿再报命名不符，规则例外句暂不写 | 本表（会话定案；类见 perm-common util/BusinessKeys.java、access-service permission/util/SyncKeyCodec.java） | 再报直接撤回 |
| 2026-09-07 | operationCodeKey 族不做大小写归一：授权域（先 toUpperCase 再拼）与查询/解析域（裸拼）语义不一致为已登记事实，保持现状待统一；统一方向（raw 严格化/归一宽松化+DTO Pattern）属行为变更需单独立项【已统一：2026-09-14 T-PERM-066 定案 raw 严格化，见同日行】 | docs/design/permission-center/implementation.md §8.2 | 报了先核出处 |
| 2026-09-07 | T-PERM-051 四项执行定案（同批 AskUserQuestion）：①复合键长度溢出（type_key/type_code 各 ≤64 最坏 129 > 旧 code 列宽 128）处置=加宽 resource_entity.code 至 VARCHAR(256)，不加创建入口限长；②类型软删对「投影行下授权行」处置=级联软删（deleteResources 同款：markRoles/markServiceCodes + 投影行/授权行同事务软删），不走删除保护；③「TYPE_DEFINITION 加入保留清单」按机制现状落地=种子声明 SYNC+access-service（清单机制已被 T-PERM-052 收编，不复活字面清单）；④存量投影回填=bootstrap 启动自愈（对齐 T-ADMIN-025 ensureAdminFileFolder 先例）+ runbook FAQ 订正语句兜底，非纯手工 SQL | docs/archive/2026-09-12/tasks/T-PERM-051.md 实现记录、docs/design/schema/access-service.sql（列宽注释+种子声明）、access-service-rebuild-runbook.md §1/§3 | 报了先核出处 |
| 2026-09-08 | BusinessKeys 竖线族补收（codex 复评 P2-1 用户拍板全量收敛）：2026-09-07 首轮收敛遗留的 12 文件约 31 处 `\|` 裸拼点全量收编——八族进 BusinessKeys、sync_key 两族进 SyncKeyCodec；SignatureVerifier 签名载荷维持出界（基础设施键）；golden 锁扩至 30 用例 | implementation.md §8.1 范围补收句、perm-common util/BusinessKeys.java、access-service permission/util/SyncKeyCodec.java | 报了先核出处 |
| 2026-09-08 | Cookie 承载通道双向关闭（codex 外评 P1 修法二选一，用户拍板关闭而非补 CSRF 防护）：令牌仅经 Authorization 头显式传递（前端恒发 Bearer）——access-service `is-read-cookie: false`（sa-token 1.38.0 默认 true，不显式关闭登录响应会种无 SameSite/HttpOnly 标记的 Authorization Cookie）、Gateway `AuthTokenFilter` 删 Cookie 备选只认 Bearer 头 + yml 同步 false；双端 `saTokenConfigMatchesAuthority` 断言 + `cookieOnlyToken_rejected401` 回归锁钉住。评审勿再报「Gateway 存在 Authorization Cookie 认证通道构成 CSRF 面」——修法即关闭 | security-standards.md §3 本仓事实注记、gateway/access-service application.yml sa-token 块、AuthTokenFilter.java extractToken | 再报直接撤回 |
| 2026-09-08 | Feign Header 透传规范按现状对齐（codex 外评 P2，用户拍板仅修文档、不实现拦截器）：§14.2 改为「现状注记 + 目标态契约」双层表述（当前唯一装配=FeignInternalSyncInterceptor 仅注入 X-Internal-Secret/X-Service-Code，X-Tenant-Id 调用方注入，Authorization/链路追踪头未统一透传；日志关联依赖 Gateway X-Request-Id 头链），目标态链路头统一为 X-Request-Id（与 MDC traceId 同源）；实现统一拦截器属新功能需单独立项，仓内无活跃 Feign 消费方 | project-rules.md §4.3 现状行 + §14.2 | 报了先核出处 |
| 2026-09-09 | 全局域 CLASSIFY 声明生效（T-PERM-046 执行期用户三选一定案，选「让 CLASSIFY 生效」而非拒绝保存/容忍死配置）：全局域实际范围=有 CLASSIFY 声明按声明（声明即收窄覆盖动态补集，可为空集）、无声明退动态补集（未被非全局域认领的类型）；GLOBAL_PLUS 隐式段同源对齐（全局域有声明时=声明集，无声明/无全局域=未被认领补集，既有语义不变）；反查 findDomainIdsByTypeCodes 次序不变（非全局域认领优先、全局域兜底）。此前「全局范围维持补集动态计算」的任务卡口径随本定案作废；对存量部署零行为差异（全局域此前物理不可创建） | api-contract.md §5.1/§5.6、DomainClassifyServiceImpl（effectiveTypeCodes/matchesTypeCode）、schema access-service.sql biz_domain/domain_config 表注释、docs/archive/2026-09-12/tasks/T-PERM-046.md | 报了先核出处 |
| 2026-09-09 | T-PERM-046 执行口径两则：①错误码排号 20057 DOMAIN_GLOBAL_EXISTS（全局域已存在）/ 20058 DOMAIN_CONFIG_CONCURRENT_CONFLICT（并发保存提示重试），perm 段顺延 T-PERM-052 的 20055/20056 之后；②remove/save 并发窗口机制按任务卡首选落地=FOR UPDATE 域行锁（mapper 同事务连接，remove 用 selectValidByIdsForUpdate、save 域解析用 selectByCodeForUpdate），不引入 Redisson（T-PERM-044 选 Redisson 是树场景递归 CTE 校验锁不住行集合，本场景精确域行集合 FOR UPDATE 更优且无 Redis fail-closed 依赖）——评审勿再建议本场景换分布式锁 | docs/archive/2026-09-12/tasks/T-PERM-046.md、PermissionErrorCode（20057/20058）、BizDomainMapper（selectByCodeForUpdate/selectValidByIdsForUpdate） | 再报直接撤回 |
| 2026-09-09 | codex luna max 外评 T-PERM-046（commit 869b01a3c，read-only，session 01a083b0）：零 P0-P2、P3×2 均已处置——①getClassifiedTypeCodes 接口方法 Javadoc 旧口径残留当场修正（实现已按声明优先）；②业务域弹窗打开竞态（create 态 await 全局域预查期间点其他弹窗入口→慢网叠弹窗，表单独立提交不串数据）用户定案序列号守卫修法（openBizDomainForm 模块级序号，过期打开丢弃），弃「弹窗先开+异步填充」方案（props 需响应式改造且开关状态打开后突变）。评审勿再报「全局域预查致弹窗叠层」——已修 | frontend/src/views/system/biz-domain/index.vue openBizDomainForm、DomainClassifyService.java | 再报直接撤回 |
| 2026-09-09 | T-PERM-050 三项执行定案：①resource_type 删除=同事务级联软删该类型全部有效操作定义行（对称于创建联动预置 CRUD 四操作位），不走删除保护；②该类型下有效授权行（正常流仅剩 scope_all 类型级行）并入级联 + markRoles + OPERATION_PERMISSIONS_BY_TYPE per-type 失效（T-PERM-047 终态复用）；级联面限定 type_key=resource_type（type_value 仅 tenant+type_key 内唯一，同值不得跨族误伤）；③user_type/role_type 删除零检查面拆分登记 T-PERM-056 | docs/archive/2026-09-12/tasks/T-PERM-050.md 当前口径、api-contract.md §5.1、schema access-service.sql 三表注释 | 报了先核出处 |
| 2026-09-09 | T-PERM-056 user_type/role_type 删除处置定案（用户二选一拍板）：**删除保护**（类型下存在有效 abstract_user/abstract_role 行时整批拒绝 20056，管理员先清数据再删类型），弃级联——用户/角色是业务主体数据，对标 resource_entity 面=守卫先例（而非操作位/投影=级联先例），级联会连带 user_role 关系/授权/投影/缓存广播把一次删类型放大成静默蒸发一批登录主体。并发口径：role_type 面共持 ABSTRACT_ROLE 树写锁（自定义 role_type 角色行唯一创建入口=角色同步通道 sync/full-sync 且持同锁，管理面 updateRole/moveRole 亦可写已存在行但均持同锁、createRole 经 RoleType 枚举校验只接受种子类型，锁闭合完整）；user_type 面用户写入口无锁可复用，best-effort（对齐 T-PERM-050 级联并发先例）+ typeValue 软删不复用兜底。20056 message 措辞扩为「有效引用行——资源行/用户行/角色行」（复用错误码，不新排号） | docs/archive/2026-09-12/tasks/T-PERM-056.md 当前口径、api-contract.md §5.1、schema access-service.sql type_definition 表注释、PermissionErrorCode 20056、TypeDefinitionAppServiceImpl（rejectIfSubjectTypeReferenced + 锁块） | 报了先核出处 |
| 2026-09-09 | 权限查询统一引擎形态定案（grill Q1-Q7+Q15）：一引擎一套入参一个结果模型、多入口=参数预设——现状六套执行形态（query() 六工厂 / getDenied\* 手写管线（目标同单点门禁、评估语义不等价）/ query-resources AppService 展开 / deleteRoles 局部规则 / canGrant 直查管线 / query-scopes AppService 自评管线）全收编；**目标模式三态判别（TYPE_LEVEL 只消费 scopeAll / INSTANCE 下推+闭包 / LIST 按角色全量）——「无实例目标」不得二义映射为清单（类型级门禁会被实例授权误判越权）**；编码目标为三元组（resourceTypeCode/resourceCode/codeType——codeType 参与实体解析不归组装层）；树级展开拆两语义（判定面=查询前目标闭包改变结论、展示面=查询后条目克隆不改结论，清单面无判定面继承）；主语只认 roleIds（userId→角色解析入封装层含缓存）；resourceTypeCodes 用集合；位覆盖常开不可关；操作投影展开归展示面（结果双轨=原始行+覆盖投影）；主资源上下文为一等入参；网关快照架构保留（ACCESS 单一位维持）。T-PERM-045 随之取消（范围并入 T-PERM-057） | docs/design/permission-center/query-engine-unification.md（终态设计，T-PERM-057 落地并入 implementation §3）、docs/archive/2026-09-12/tasks/T-PERM-057.md、docs/archive/2026-09-12/permission-query-unification-plan.md | 报了先核出处 |
| 2026-09-09 | 判定面继承默认值矩阵 + 评估口径定案（grill Q12/Q13/Q5）：管理面写门禁（code/entityId 两轨）与读过滤面（菜单可见/组织可见/日志过滤）默认**开**（父授权覆盖子实例）；/auth/check 关+可选参数显式开（inheritMode 从「对单点判定结论无效」接通为目标闭包真实语义）；网关天然关；清单/视图面判定面继承**不适用**（无目标集，树扩展归展示面展开轨道、默认关+参数显式开）。管理面门禁条件评估**拉平为评估**（消除单点不评估/批量评估分叉）；冲突过滤**入参化**（默认运行时面开、配置面关，取代此前引擎常开的设计倾向）。行为差异（读面可见集变大/写门禁变严）随 T-PERM-057 回写 runbook FAQ | docs/design/permission-center/query-engine-unification.md §6/§7/§10、docs/archive/2026-09-12/tasks/T-PERM-057.md | 报了先核出处 |
| 2026-09-09 | T-API-002 check 族裁剪推翻（grill Q10+D1）：/auth/check、/auth/batch-check、/auth/check-interface 三端点**同口径全量回传**结果记录（matchedRoleIds/matchedPermissionIds 与 matchedResources[].resourceId 回线格式）——统一引擎消费方模型「调用方根据结果记录判定」需要记录在场；**Query\* 响应族六字段裁剪维持不变**（推翻范围仅 check 族）；双副本 DTO（access-service+perm-common）同批改、CheckFamilyWireShapeTest 防回潮负向锁按新口径改写，实施 T-API-003。评审勿再报「check 响应泄漏内部 id」——该口径已按本定案推翻 | docs/archive/2026-09-12/tasks/T-API-003.md、docs/design/permission-center/query-engine-unification.md §9、api-contract.md §6.1/§6.2/§6.6（随 T-API-003 回写） | 再报直接撤回 |
| 2026-09-09 | 关联权限自动授权方向定案（grill Q8/Q11，T-PERM-054 暂缓原因更新）：**API 不单独授权、接口权限由操作权限关联派生**（授操作权限即有对应接口权限；网关按 {资源类型:操作权限} 关联映射 API 鉴定）为期望终态；方案与实施**后置**（现状 ACCESS-on-实体 + resource_api_mapping 绑定维持不动，单独任务 T-PERM-054 承载）；登录权限串维持 TYPE:OP+实例折算，与网关的一致性终态等本方向落地后在 {类型:操作} 基座对齐（粒度差=串类型粒度/门禁实例粒度为已知事实，不借道 T-PERM-054 挂起） | docs/tasks/T-PERM-054.md、docs/design/permission-center/query-engine-unification.md §9 | 报了先核出处 |
| 2026-09-09 | 权限视图/排查删除重设计方向（grill Q14+D2）：permission-view 七端点（effective-permissions/resource-users/role-permissions/effective-roles/resource-tree/explain/recent-changes）+ 前端排查页 + /query-permission-tree（零外部消费端点，D2 并入评估）拟删除重设计、具体范围待定（T-PERM-059）；**effective-permission-codes（登录串端点）不在删除面**；连动面（Gateway bootstrap 固定图两行、T-FE-043 absorb/cancel、architecture §14/runbook）随 T-PERM-059 处置 | docs/archive/2026-09-12/tasks/T-PERM-059.md、docs/design/permission-center/query-engine-unification.md §9 | 报了先核出处 |
| 2026-09-09 | T-PERM-057 实施定案一：两级互斥归属——**角色互斥（ROLE_MUTEX）不归引擎**（管线不含角色互斥阶段），条目互斥（PERM_MUTEX）入参开关按需开启（默认按入口：运行时面开、配置面关）。现状核实：授权写路径（assignRole/applyGrantPlan）无任何角色互斥校验、detectConflictRule 端点只检测 PERM_MUTEX 操作对、角色互斥唯一运行时拦截点=快照构建与权限树的手动 filterRoleMutex；该两处调用点**保留为调用方自理**（网关对存量互斥角色对继续拒绝，无安全回退）。「角色互斥授权时校验」为待补能力，**另行立项**（不扩入 T-PERM-057） | implementation.md §3.5、PermissionQueryAppServiceImpl（interfaceSnapshot/prepareTreeContext 调用方自理注释）、PermQueryEngine（管线无角色互斥阶段） | 报了先核出处 |
| 2026-09-09 | T-PERM-057 实施定案二：条件评估上下文为**专门多层对象** `PermEvalContext`（用户环境 clientIp + 服务器环境 evaluatedAt + 调用方上下文 attributes）——四便捷入口自动从当前请求装配 clientIp（explain T-PERM-033 先例 HttpRequestUtils.getClientIp(currentRequest())），无请求上下文的内部调用 IP 类条件按 ConditionEvalUtils 既有 fail-closed 拒绝；evaluatedAt 为服务器环境键（ConditionEvalUtils 时间类条件优先消费、缺省回退本机时钟——Gateway 快照重评等无服务器上下文调用方维持既有行为）；SDK 契约 context Map 经 fromCallerMap 转换（clientIp 键提取、其余归 attributes） | implementation.md §3.5、PermEvalContext.java、ConditionEvalUtils（CONTEXT_KEY_EVALUATED_AT） | 报了先核出处 |
| 2026-09-10 | codex luna max 第四轮外评 T-PERM-057（commit 2cb4646ef，read-only，session 01a088c4）：P1×1+P2×4+P3×1，四修一登记一改进项——①P1 canGrant 收编后消费 ROLE_PERM_SNAPSHOT（10s TTL 陈旧+旧读回填竞态，撤权后窗口内可放行已撤销转授权限）：修复=PermQuery 新增 bypassPermSnapshot（LIST 直查不读不回填），canGrant 启用（恢复收编前新鲜度；读面维持缓存=T-ACCESS-008 定案）；②P2 INSTANCE 目标解析空丢 scopeAll 评估拒绝原因：修复=scopeAllEvaluatedEmpty 参与；③P2 deleteRoles/createRole 不持 ABSTRACT_ROLE 树写锁（并发挂入被删子树成存活孤儿）：修复=同 updateRole/moveRole 同款锁先例；④P2 批量入口无上限（闭包 CTE 按 目标×深度 放大）：修复=ResourceKeysReq/IdsReq @Size(max=1000)（project-rules 分批约束合规）；⑤P3 单次查询多阶段 evaluatedAt 各取 now()：修复=query() 入口一次钉住；⑥P2 explain USER INSTANCE 候选明细缺实例授权行→登记 T-PERM-059（该端点在删除重设计面，非安全缺陷不修待删端点）；⑦引擎内部闭包/展开 500-1000 分片→改进项登记（入口限 1000 后祖先链放大 ≤1000×深度，PG 可承受，分片待真实负载证据再立项）。均配真锁（bypass/拒绝原因两例）。评审勿再报①-⑤——已修 | implementation.md §3、PermQuery（bypassPermSnapshot）、PermQueryEngine（入口钉时钟/拒绝原因）、RoleManageAppServiceImpl（createRole/deleteRoles 树锁）、ResourceKeysReq/IdsReq（@Size）、T-PERM-059（explain 候选遗留） | 再报直接撤回 |
| 2026-09-10 | X-Forwarded-For 信任面收口定案（codex 外评 P1，用户三选一拍板「Gateway 清洗+重建」）：Gateway 清洗列表加入 X-Forwarded-For/X-Real-IP（外部传入一律删）并以自身观测的 remoteAddr 重建 XFF 写回下游——access-service 门禁/日志与网关快照条件重评（T-PERM-017 存量同款信任面）统一消费重建值，一并修复；多层 LB 部署在最外层可信设施终结（Gateway 观测 LB IP，白名单粒度变粗或 LB 侧透传，部署前提随任务回写 security-standards/runbook）。弃可信代理链配置（多层精确但需手写解析+部署方配置成本，与中小企业单层 Gateway 主拓扑定位不匹配，NTP/简单拓扑先例）；弃登记不修（拉平后信任面已及管理面写门禁）。修复独立任务 T-GW-008 承载（跨组件不塞入 T-PERM-057） | docs/archive/2026-09-12/tasks/T-GW-008.md、gateway HeaderCleanFilter、PermissionFilter（resolveClientIp） | 报了先核出处 |
| 2026-09-10 | codex luna max 外评 T-PERM-057（commit 57cdc5500，read-only，session 01a088a2）：P1×1 + P2×3。P2×3 核实属实已修复——①TYPE_LEVEL/INSTANCE 评估清空误报 NO_PERMISSION（基线为 CONDITION_NOT_MET_OR_CONFLICT，拒绝原因进 check/explain 响应）：修复=评估前后集合对照区分拒绝原因；②ROLE explain scopeMode=INSTANCE 经引擎 INSTANCE 管线错误回退 scopeAll 类型级放行（契约 §explain 要求精确匹配）：修复=PermQuery 新增 exactInstanceOnly 开关（INSTANCE 跳过 scopeAll 查询，对齐旧 queryScopeAll=false 语义），explain ROLE INSTANCE 分支启用；③PermEvalContext Map.copyOf 禁 null——SDK context 含 null 值 JSON 反序列化后在判定前 NPE 变 500：修复=构造器防御过滤 null 键值。三条均配真锁。P1（X-Forwarded-For 可伪造）为跨组件安全设计取舍待用户拍板（Gateway 清洗/重建策略有部署模型差异），另行处置。评审勿再报上述三条——已修 | implementation.md §3.3、PermQuery（exactInstanceOnly）、PermEvalContext（filterValid）、PermQueryEngine（deny reason 区分）、PermissionViewAppServiceImpl（explain 精确实例） | 再报直接撤回 |
| 2026-09-10 | grok-4.6 xhigh 外评 T-PERM-057（commit 8f5905ccb，read-only）：P1×1 + P3×1 均核实属实并已修复——①P1 query-scopes EMPTY 误判 DENIED（引擎 LIST 操作定义装载源用评估后条目致条件摘光类型缺操作定义 + deny 路径不装 ancillary + AppService 把评估清空一律整表拒绝）：修复=装载源改 rawEntries 超集/deny 路径仍装载/整表拒绝收窄至父判定失败与无角色，配 AppService 分态真锁 + 引擎 deny 装载真锁；②P3 explain 判定与明细双时钟（evaluatedAt 未钉住，展平 Map 经 fromCallerMap 往返丢字段）：修复=入口一次 `new PermEvalContext(ip, now(), …)` 直传。存量观察三条不处置（per-type 循环为 rule §6 明示合法/scopeCodeType 收编前后一致/子孙扩展跨类型已有改进项登记）。评审勿再报「query-scopes 评估清空整表拒绝」与「explain 双时钟」——已修 | implementation.md §3.6/§3.8、PermQueryEngine（loadAncillaryForView 装载源）、PermissionQueryAppServiceImpl（queryScopes 整表边界）、PermissionViewAppServiceImpl（explain 钉时钟） | 再报直接撤回 |
| 2026-09-09 | T-PERM-057 实施定案三：判定面闭包**止步同类型**（递归 CTE 上溯带 resource_type 过滤，跨类型祖先不参与闭包）——依据：sync 通道允许跨类型父子边（T-PERM-052 注释实证：MANAGED 根子树可含 SYNC 类型后代）而操作位空间按类型隔离（跨类型位命中是巧合不是语义）、deleteRoles 等价性证明依赖同类型闭包、授权页按类型展示矩阵（授权行为本就是类型内的）；软删祖先截断（CTE delete_flag=0）顺带定界无争议。**改进项登记（用户指示）**：后续应禁止资源节点树跨类型（sync 通道跨类型边治理，另行处理，不阻塞本定案） | implementation.md §3.4、ResourceEntityMapper.selectSelfAndAncestorClosureBatch（CTE resource_type 过滤）、TargetModeClosurePgIT（closureStopsAtTypeBoundary 锁） | 报了先核出处 |
| 2026-09-10 | codex luna max 第五轮外评 T-PERM-057（commit 1349b919a，read-only，session 01a08987）：P2×2+P3×1 全核实属实并已修——①P2 引擎批量入参上限不完整（四轮只封 getDenied\* 直连两 DTO）：UserAssignRoleReq/UserRoleBatchRevokeReq.items（角色集经 getDeniedResourceCodes 进闭包 CTE）+ BatchAuthCheckReq.items 双副本（逐项全管线）+ QueryScopesReq 三列表（内存网格）补 @Size(max=1000)，配 Bean Validation 层锁（含四轮两 DTO 补锁）；死 DTO PermissionCheckBatchReq（零消费方）删除；②P2 组织写链无树锁（核实比报告宽：createOrg/deleteOrg 连 SYS_ORG 锁也无、updateOrg 仅 SYS_ORG、投影同事务写 resource_entity 亦无锁）用户三选一拍板**全量修**：createOrg/updateOrg/deleteOrg 各持 SYS_ORG+ABSTRACT_ROLE+RESOURCE_ENTITY（锁序固定三段，本类为全仓唯一多树持锁方、无反向持锁无死锁面），InOrder 真锁三例；③P3 query() 空上下文分支未钉 evaluatedAt（四轮修复不完整——null 分支留 null 致 toEvalMap 每阶段各自 now()）：null 分支入口一次 now() 钉住，配非空真锁。存量观察（同步写路径未 markRoles/markUsers，EFFECTIVE_ROLES 10s 陈旧）为 T-ACCESS-019 已登记遗留不处置。评审勿再报①-③——已修 | OrgWriteAppServiceImpl（三树锁）、PermQueryEngine（入口钉时钟）、UserAssignRoleReq/UserRoleBatchRevokeReq/BatchAuthCheckReq×2/QueryScopesReq（@Size）、BatchEntrySizeValidationTest、OrgWriteAppServiceTest、PermQueryEngineTest（nullEvalContextMustBePinnedWithEvaluatedAtOnEntry） | 再报直接撤回 |
| 2026-09-10 | T-GW-008 实施定案与存量缺陷修正：①PermissionFilter.resolveClientIp **直用 socket remoteAddr**（不从重建 XFF 读回）——nginx/CDN 拓扑下两方案观测值相同（均为代理 IP，粒度损失属 XFF 收口定案已知接受项、由部署文档兜底），直用消除「-90 已重建」顺序耦合，yml 误删清洗项时网关自评不复活伪造面（用户二选一拍板）；②回归锁钉 Gateway 单测不进 e2e——「下游可见」验证需 example-service 加回显端点，超出任务范围边界（用户拍板）；③事实修正一：clean 匹配 `contains(key)` 大小写敏感，小写变体（x-forwarded-for/x-internal-secret）可绕过**整个**清洗列表（RFC 7230 头名大小写不敏感）——改走 HttpHeaders 底层大小写不敏感 remove；④事实修正二：原「构建干净头副本再 putAll」为**叠加语义**（`ServerHttpRequest.Builder#headers` consumer 收到原请求头可写视图，putAll 不删除）——清洗列表项归并起从未被真正删除（仅靠下游注入 filter 的 set 覆盖兜底），改显式 `remove` + `set`（set=替换，配置误删时外部任意值/多值也被覆盖为单值观测值）；⑤clean 列表扩列 `X-Forwarded-Host/Port/Proto/Prefix` 与 `Forwarded`（用户拍板一并清洗——同属可伪造转发声明，当前三服务零消费，防未来 `forward-headers-strategy` 开启时伪造面复活）；⑥access-service 注释级触达（用户拍板）：PermEvalContext/HttpRequestUtils javadoc 与 architecture §8.2 两句口径同步（透传→重建），零行为改动；⑦SCG 内置 XForwardedHeadersFilter `for-append` 置 false（用户拍板——默认 true 在代理出口追加一段同值致下游 XFF 双段冗余，三服务消费链取首段本无安全差异，关闭为形态统一：文档/单测/生产三者一致单值）。评审勿再报③④——已修 | docs/archive/2026-09-12/tasks/T-GW-008.md、gateway.md §请求头清洗与客户端 IP 重建、security-standards §7、rebuild-runbook §3、gateway application.yml（x-forwarded.for-append）、HeaderCleanFilter、HeaderCleanFilterTest | 再报直接撤回 |
| 2026-09-10 | T-API-003 check 族结果记录回传实施定案：三端点（check/batch-check/check-interface）按 2026-09-09 推翻定案恢复 T-API-002 裁剪前线格式——AuthCheckResp 于 reason 后回位 matchedRoleIds/matchedPermissionIds、AuthCheckItemResult 于 reason 后回位两字段、MatchedResource 首位回位 resourceId+尾部两 matched 字段（组件位置=裁剪前原样；deny/USER_NOT_FOUND 分支空列表非 null）；组装层恢复 PermResultUtils 双方法与 batchCheck 逐项填充（PermResult 数据面收编后仍持有，无引擎改动）；回归锁分族改写——CheckFamilyWireShapeTest 负向锁收窄至 Query\* 五 record（check 族改正向快照钉新线格式）+ Feign 契约快照 + AppServiceImplTest matched 填充真锁三处（裁剪态下失败）；Query\* 响应族六字段裁剪维持（负向锁原名单五字段对 Query\* 不变）；MatchedResource.resourceTypeCode 恒 null 存量漂移（裁剪前已存在、示例展示 "API"，组装层从不填充）用户拍板**维持现状**随 T-PERM-059 一并处置（Gateway 专用端点现役无人读该字段，填充/改示例均超恢复边界）。评审勿再报「check 响应泄漏内部 id」——2026-09-09 已按定案推翻；勿再报「Query\* 响应缺 matched 字段」——维持裁剪终态；勿再报「matchedResources[].resourceTypeCode 示例与实现漂移」——已拍板随 T-PERM-059 处置 | api-contract §5.7/§6.1/§6.2、core-flows §10/§15、CheckFamilyWireShapeTest、PermissionFeignClientContractTest、PermissionCheckAppServiceImplTest、双副本 dto/resp 六文件、PermResultUtils、PermissionCheckAppServiceImpl | 报了先核出处 |
| 2026-09-10 | codex luna max 外评 T-GW-008+T-API-003 联评（commit 273f89891+0f2be836e，read-only，session 01a08ae4-75a4）：P1×1 其余零——①P1 `server.forward-headers-strategy=framework` 场景清洗失效+remoteAddr 污染（ForwardedHeaderTransformer 在 HttpWebHandlerAdapter.handle 首指令、全部 WebFilter 之前把外部 X-Forwarded-For 解析进 getRemoteAddress() 并删除转发头；ReactiveWebServerFactoryAutoConfiguration @ConditionalOnProperty(havingValue="framework") 注册；spring-web 6.1.5+spring-boot 3.2.4 本地 jar 字节码复核属实）：用户三选一拍板**启动护栏**——新增 ForwardHeadersStrategyGuard（framework/native 拒启、缺省/none 放行，先例对齐 GatewayCorsConfigValidator），gateway.md 承诺句修正（「清洗防未来开启时伪造面复活」对 framework 不成立——清洗对 transformer 场景来不及参与）+ yml 注释 + security-standards §7 护栏条目 + runbook §3 拒启排障行；回归锁五分支（缺省/none/NONE 放行、framework/FRAMEWORK/native 拒启）。弃仅修文档（护栏弱）；弃 removeOnly（与不采信代理链定案相悖、引入新行为面）。评审勿再报「forward-headers-strategy 开启可绕过 XFF 收口」——已护栏 | gateway ForwardHeadersStrategyGuard(+Test)、gateway.md §请求头清洗与客户端 IP 重建、gateway application.yml clean 注释、security-standards §7、rebuild-runbook §3 | 再报直接撤回 |
| 2026-09-10 | T-PERM-058 depend_on 单点门禁闭合四项定案（任务内逐项拍板）：①单点面 fail-closed——/auth/check、batch-check 增可选主资源上下文四字段（parentResourceTypeCode/parentResourceCode/parentCodeType/parentOperationCodes，与 query-scopes 同名对齐、type/code 成对提供半传 400、batch-check 为请求级），不传时 depend_on 行不参与判定（拒绝原因 DEPENDENT_NOT_IN_PARENT_CONTEXT 与 NO_PERMISSION 区分）；②TYPE_LEVEL 仅读侧排除（二次确认维持「仅 SQL 排除」名下的读侧语义——曾议「父权限禁类型级」经核实无效（缝隙在子行自身 scope_all 标志与父形态无关）且误伤父=类型级+子=补充实例合法形态；scopeAll 子行形态保留、生效面为 LIST 父上下文与带主资源上下文的 INSTANCE，写侧不禁止）；③query-resources 清单与 Gateway 快照两处排除（组装层排除而非引擎 LIST 层——canGrant 零行为差（子行 canGrant 恒 false）、permission-view/登录串维持现状）；④设计定案与引擎实施一并收口；⑤双轨评审 P1 处置（用户拍板）：给出父上下文时 parentOperationCodes 一并必填且非空（@AssertTrue + 400）——「缺省=父任意授权命中即可」为实现不支持的错误承诺（引擎对空操作集不发父判定查询、父判定必不命中），按 §6.7 同名 @NotEmpty 必填先例收敛，弃引擎展开父类型全操作位的「任意命中」实现（语义模糊且与 query-scopes 口径分叉）。实现全部内存过滤（SQL 零改动）：INSTANCE 两阶段 filterDependentEntries（LazyParentCheck 惰性父判定、单次 query 两阶段共享一次查询、父判定递归本引擎自身无父上下文=单层语义）+ TYPE_LEVEL/getDenied* 管线排除 + 组装层两处。评审勿再报「单点/类型级门禁被 depend_on 子权限行放行」与「query-resources/快照含子行独立条目」——已修；claude 外评（默认模型）P2×2 处置：parentOperationCodes null 元素过滤（filterValid 先例同款）+ @Size(max=1000) 对齐；处置后 codex luna max 复评（f1e1165e3，read-only）零缺陷收口 | docs/archive/2026-09-12/tasks/T-PERM-058.md、implementation.md §3.1/§3.3、api-contract §6.1/§6.6、PermQueryEngine（filterDependentEntries/LazyParentCheck）、PermissionCheckAppServiceImpl（透传）、SnapshotAssembler、PermissionQueryAppServiceImpl、双副本 AuthCheckReq/BatchAuthCheckReq、permission-query-pipeline skill 5.1.0 双副本 | 再报直接撤回 |
| 2026-09-11 | T-PERM-048 条件实例投影与双轨制五项定案（任务内逐项拍板）：①内联条件**随 apply-grant-plan 同事务创建/回收**（弹窗内联填写规则、保存时后端同事务建 permission_condition(source=INLINE, code 自动生成 inline- 前缀) + 授权行；取消弹窗零残留；1:1 属于授权记录不进选择器不可共享，授权行删除/换绑时引用归零同事务回收条件行；弃弹窗内即时 create 方案——取消授权会留孤儿 INLINE 行）；②内联条件创建/编辑门禁**随 ROLE:MANAGE 携带**（语义=内联条件是授权记录属性；操作者本可选任意管理页条件挂授权，自造一条边际风险≈0；弃仍查 CONDITION 写权限——「只能在授权页更改」对无权限者名存实亡）；③管理页条件删除**引用守卫拒绝**（新码 20059：role_resource_permission.condition_id 挂靠或投影行下实例授权两类引用任一命中整批拒绝，零引用才放行，T-PERM-056 先例；弃 TYPE_DEFINITION 级联软删——授权资产被删条件连带静默消失比要求显式解绑更危险）；④CONDITION update/delete 门禁**升级实例级** CONDITION:UPDATE/DELETE@{code}（USER:MANAGE 同款，create 维持类型级；bootstrap 固定图与存量授权全是 scope_all 天然覆盖全部实例，零破坏）；⑤**只投影 MANAGED 条件**（INLINE 不建 resource_entity 行——无消费者：不进授权资源树、不走管理面门禁、评估链路直查条件表；弃统一投影——需在树/列表查询加 join 过滤排除否则授权页树出现不可选内联条件节点噪音）。事实性修正（先例唯一）：source 列 CHECK('MANAGED','INLINE') 存量默认 MANAGED；CONDITION 入 SYNC 族 IN 清单第 7 项；管理面 list 默认只回 MANAGED、授权页 includeInline=true 回显 | docs/archive/2026-09-12/tasks/T-PERM-048.md、schema access-service.sql（permission_condition.source + SYNC IN 清单）、api-contract §5.6/§6.5.1、ConditionAppServiceImpl、PermissionGrantPlanDomainServiceImpl、LocalProjectionDomainService（upsertConditionResource/backfillConditionProjections） | 报了先核出处 |
| 2026-09-11 | T-PERM-048 双轨评审遗留竞态处置（用户二选一拍板）：条件删除守卫与 apply-grant-plan 的 check-then-act 毫秒级交错窗口**接受不闭合并登记**——交错序「删方守卫查引用=0 → 授权方 prevalidate 读到条件存在 → 删方提交软删 → 授权方插授权行挂软删条件」，后果=新授权评估 fail-close 拒绝（拒绝方向不越权不泄权，守卫查询时点引用=0 是真实时序）；弃 FOR UPDATE 行锁（T-PERM-046 先例）——apply-grant-plan 是最高频写路径，为极窄窗口每次多锁 N 行条件行成本不成比例。评审勿再报「删除守卫与授权计划竞态致授权悬挂」——已定案接受 | ConditionAppServiceImpl.deleteConditionsByCodes（守卫 check）、PermissionGrantPlanDomainServiceImpl.prevalidate（selectValidByCodes） | 再报直接撤回 |
| 2026-09-11 | T-PERM-048 claude CLI 外评处置（deepseek-flash 默认模型，read-only，12 commit 范围）：零 P0/P1、P2×2+P3×3 逐条代码级核实**全部属实**并已修复——①P2 保存后 conditions 不刷新（depsLoaded 闩锁单次拉取，新内联 code 缺失致回显「无条件」/复制误判/误点清除并回收该内联）：修复=refreshConditions 独立于闩锁 + handleSaveAll 成功后刷新；②P2 内联确认前校验只看聚焦记录（切走后半成品绕过校验：空名 400 整批失败、未编辑 "{}" 创建成功但引擎 items 缺失恒拒绝=静默失效）：修复=validateInlineDefs 纯函数全草稿扫描（空名/空 items 拒绝，含 "{}" 初值面）；③P3 级联孤儿（资源删除/类型删除级联删授权行不回收内联条件）：修复=两处级联前 selectConditionIdsByPermIds 收集、删后 recycleOrphanInlineConditions（新 mapper 方法，ResourceDeletePermChangeRegistrationTest 补锁）；④P3 内联展示缺位（变更清单/详情层/来源链显示「无条件」）：修复=ChangeSummary.inlineName + 各展示面条件标签统一；⑤P3 六→七类型残留 7 处（permission-condition.md API 表/api-contract remove 幂等段/三代码注释）；另存量文案「关联的授权关系将失效」随 20059 失真顺手修正。评审勿再报上述五项——已修 | frontend/src/views/perm/grant/utils/hook.ts（refreshConditions）、grant-plan.ts（validateInlineDefs/ChangeSummary.inlineName）、GrantDialog/ChangeListPanel/PermissionDetailDrawer/MatrixCell、ResourceManageAppServiceImpl+TypeDefinitionAppServiceImpl（级联回收）、RoleResourcePermissionMapper.selectConditionIdsByPermIds、docs/design/frontend/permission-condition.md、LocalProjectionGuard/ResourceEntitySyncAppServiceImpl 注释 | 再报直接撤回 |
| 2026-09-11 | T-PERM-048 codex P2-1 同族竞态扩展登记（用户拍板接受）：资源/类型删除级联 vs 授权计划并发的悬挂授权窗口——交错序「授权 prevalidate 解析资源存在+建内联条件 → 删除事务删资源级联授权行并提交 → 授权 apply 插入引用软删资源的授权行」，后果=悬挂授权（资源解析 fail-close 拒绝，非越权）+伴生内联条件孤儿（删除事务快照不可见）；系 T-PERM-034 资源引用 check-then-insert **存量窗口**（非 T-PERM-048 引入），与同日「条件删除守卫竞态接受」定案同族同取舍（fail-close 拒绝方向、亚秒级交错、弃高频授权写路径加锁）；孤儿面已随资源/类型级联回收修复（5b75a518b）自然减小，仅竞态窗内新建孤儿无回收（runbook 清理语句兜底可复用）。评审勿再报「资源/类型删除与授权计划并发致悬挂授权/孤儿内联」——已定案接受 | ResourceManageAppServiceImpl.deleteResources、TypeDefinitionAppServiceImpl.deleteTypesByIds、PermissionGrantPlanDomainServiceImpl.prevalidate/apply | 再报直接撤回 |
| 2026-09-11 | T-PERM-048 codex luna max 外评处置（read-only，12 commit 范围）：零 P0/P1、P2×5——四修一登记：①P2 backfill 已有投影判定未限 code_type=default（同 code 非 default 行误跳补种，TYPE_DEFINITION 先例已带过滤）：改 selectByTypeAndCodesAndCodeTypes 查询 + 单测锁；②P2 validateInlineDefs 只查 items 数量不查参数完整（空 DATE_RANGE 经非聚焦草稿提交落库后引擎恒拒绝=静默失效）：完整性口径收敛 condition-rules.validateRulesComplete（editor itemsValid 同源切换）+ 三参扩展 + spec 锁；③P2 resumeSlot 差异判断漏 inlineCondition（编辑内联→取消→恢复静默丢弃）：after.inlineCondition 非空恒有差异（后端 editInline 同 id 幂等重写，误发无害）+ spec 锁；④P2 refreshConditions 无代际守卫（连续保存乱序回写过期列表）：单调序号守卫（主从页 seq 先例同款）+ 乱序用例；⑤P2-1 竞态→上行扩展登记（用户拍板接受）。评审勿再报①-④——已修 | LocalProjectionDomainServiceImpl.backfillConditionProjections、condition-rules.ts（validateRulesComplete）、grant-plan.ts（validateInlineDefs/resumeSlot）、hook.ts（conditionsSeq）、ReConditionEditor（itemsValid 同源） | 再报直接撤回 |
| 2026-09-11 | T-PERM-061 设计定稿 v2（claude+codex 双外评 P1×3+P2×6+P3×2 逐条核实全成立后修订）+ 两项实施定案（任务内拍板）：①**evaluatedAt 请求级单一时刻**（a2）——batchCheck 批量化后整批共享一个评估时刻，批内时间窗条件判定一致化；可观察行为变化（现状逐 item 各自取时，批跨时间窗边界可 item1 允 item2 拒）按定案接受，实施时回写 api-contract §6.1 批量口径注记并补时间窗边界测试；②**notifyPermConflict 组级去重**（b2）——互斥冲突审计通知粒度从逐 item 改为 (组, 冲突规则) 一条、detail 携带命中 item 数，审计计数 N→1 随实施登记口径并补通知次数锁。设计核心不变量：A+ 形态挂真实 SQL 路径（scopeAll/实例行/闭包/互斥静态数据 DB 新鲜读，**不引入 ROLE_PERM_SNAPSHOT 陈旧语义**）+ 评估粒度 = scopeAll 段组内一次（子集与 item 无关）/ 实例段逐 item（PERM_MUTEX 集合语义，子集按闭包集切）。随批存量修复：BatchAuthCheckReq.items 双副本补 @Valid 嵌套级联（commit 1cebf1ad9，空白嵌套字段 deny→400） | docs/archive/2026-09-12/tasks/T-PERM-061.md（设计定稿节，v2 修订已并入现行 v4）、PermissionCheckAppServiceImpl.batchCheck（实施对象）、BatchAuthCheckReq 双副本、BatchEntrySizeValidationTest | 报了先核出处 |
| 2026-09-11 | T-PERM-061 设计 v3 复审处置（claude+codex+grok 三轨并行外评，均只读）：v2 骨架三轨一致确认成立（真实 SQL 挂载/评估粒度/反例闭合/1cebf1ad9 零缺陷），处置=v3 修订——①claude/grok 共同 P1「共享闭包映射未按 inheritClosure 分档」→ **投影谓词不变量表**（resourceType==组类型 + 组 coveringMask + inheritClosure 分档闭包（false 档恒 {target}）+ targetMode 分轨 depend 过滤（TYPE_LEVEL 无条件丢子行））+ 默认模式否定锁（不传 inheritMode 授父查子必 deny——预置操作 CROSS JOIN 各类型同四位，缺类型谓词即跨组泄漏）；②空目标集守卫（三轨收敛）：可解析 entityId 并空 → 禁调闭包 CTE（空 foreach `IN ()`=500）与实例 SQL（`<if>` 空集丢实体过滤=无界装载），纯 TYPE_LEVEL 批/全幽灵 code 批 200 全 deny；③reason 树**双轨规格**（INSTANCE 目标空/评估空两段各三支补 DEPENDENT 中间支；TYPE_LEVEL 二值轨无 DEPENDENT）；④a2 强化：唯一非空 PermEvalContext 强制注入全链（item/父递归/条件评估），禁各 item 重钉禁 now() 回退；⑤b2 聚合机制：计算通知解耦 + (组,ruleId)→命中 index ledger + scopeAll 短路 return 前 flush + detail 按实际命中规则集（AND）构造携 hitItemCount；⑥装载分段化（scopeAll 未放行组才装实例行）+ 批量路径不调 loadAncillary + 新增批量条件快照接口；⑦codex「DB 一致性时点」三轨交叉裁决降级为登记句（READ COMMITTED 批内同源与 a2 同向）；⑧闭包规模上限为 2026-09-10 已登记改进项，codex 重报撤回。评审勿再报①-⑥——已并入 v3 设计 | docs/archive/2026-09-12/tasks/T-PERM-061.md（设计定稿节，v3 修订已并入现行 v4）、ResourceEntityMapper.xml（CTE foreach）、RoleResourcePermissionMapper.xml（实例 SQL if 守卫）、queryTypeLevel（dependOn 无条件过滤）、BatchEntrySizeValidationTest（SDK 副本补强） | 报了先核出处 |
| 2026-09-11 | T-PERM-061 设计 v4 第三轮处置（codex sol xhigh，read-only，session 01a090b0；判定「v3 需修订暂不可定稿」→ v4 修订，设计稿随后已被同日定稿行确认）：①P1 批量条件快照未保留 LoadedRules 四态 fail-close 语义（selectValidByIds 只滤租户+软删不滤 enabled——朴素批量把禁用条件解析为有效规则入正缓存，规则体评估为真时放行本应拒绝的权限=绕过方向）→ 快照四态建模（OK/NOT_FOUND/DISABLED/INVALID）、仅 OK 入 CONDITION_RULES 正缓存、失败态请求级记忆评估 fail-close、两轨禁用条件回归锁；②P2 分段装载与「条件预取 ×1」数据依赖环（scopeAll 放行判定需条件评估、实例条件 ID 需实例装载后才知、父条件 ID 需父查询后才知）→ **请求级增量条件快照**口径（每阶段至多一批次、同 conditionId 请求内至多回源一次）+ 次数锁，弃严格 ×1；③P2 评估顺序不变量未钉（evaluateIfNeeded 现状序 = depend_on 过滤→条件→互斥；重排致条件摘掉互斥一端前两端同场全丢=false deny+虚假审计）→ 顺序不变量入设计 + 条件-互斥组合锁；④P3 父判定递归绕过 (组,ruleId) ledger → **父判定审计桶登记口径**（每请求 ≤1 次无去重需求、维持现有通知形态不入 ledger；纳入需穿透递归 query 与「父判定共享只经既有注入面」决策冲突）；sol 同时确认投影谓词/分档/空集守卫/reason 双轨/a2 传递四件 v3 核心物无新缺陷。存量观察登记：batchResolveDomainIds 结果计算后未参与过滤（批量路径不校验 domainCode 存在性）。评审勿再报①-④——已并入 v4 | docs/archive/2026-09-12/tasks/T-PERM-061.md（设计定稿节）、PermissionConditionDomainServiceImpl.loadRules（四态基准）、PermissionConditionMapper.selectValidByIds、PermQueryEngine.evaluateIfNeeded（条件先于互斥序） | 报了先核出处 |
| 2026-09-11 | T-PERM-061 设计**定稿**（用户确认 v4）：A+ 形态（分组 + 请求级共享装载）经多轨外部评审（claude / codex-luna / grok / codex-sol）全部 P1/P2 级发现逐条核实处置后定稿；设计正文回写 implementation.md **§3.10**（共享装载分段化/条件增量四态快照/分组键/投影谓词不变量表/评估粒度与顺序不变量/reason 双轨/b2 ledger 与父判定审计桶/回归锁清单），§3.8 对外接口表 batch-check 行指向目标形态；api-contract §6.1 的 a2 批量口径注记（请求级单一评估时刻）随实施回写。三项语义定案 a2/b2/c1 维持前登记行不变。实施未开始（任务 proposed，回归要求以卡内 v4 验收条为准） | docs/design/permission-center/implementation.md §3.8/§3.10、docs/archive/2026-09-12/tasks/T-PERM-061.md（设计定稿节） | 再报直接撤回 |
| 2026-09-11 | T-PERM-061 定稿文档批次五轨评审处置（内部双轨子代理 + claude + codex-luna + grok 外评，对象=c67549639..a3ce513f5 的 docs 变更）：五轨完全收敛零矛盾，全部为文档层事实性修正，已随批落地——①任务卡 frontmatter acceptance 由滞留 v3 更新为 v4 终态（补条件四态/评估顺序/父判定审计桶不变量与 ⑥⑧⑨⑩⑪ 回归锁——含禁用条件 fail-close 安全锁，防实施按过期验收合同 done）；②看板 EXT-7 分区注记「异构项分组设计待定」残留同步为定稿口径（与同文件任务行矛盾）；③任务卡「设计评审记录」过程流水节删除（轮次词/评审日记违反 project-rules §文档治理活跃文档禁令，处置详情以本表四行为准），随批修复与 batchResolveDomainIds 存量观察改入终态节；④implementation §3.10 补漏三句（需新增批量条件快照接口归属 §2.5 面/READ COMMITTED 批内同源登记句/引擎 mock 测试改 stub 注记）+「sol r3」代号清理 + 评估顺序锚点修正（depend_on 过滤在调用方非 evaluateIfNeeded 体内）+ 回归摘要补强（1000 项形态/DEPENDENT 场景）；⑤implementation §5.1 快照链路四缓存行既有债修正（L1_L2/30min → L2_ONLY/10s 对齐 PermCacheCatalog，ROLE_PERM_SNAPSHOT 注明仅 LIST 消费）；⑥本表 v2/v3/v4 行节名锚点修正（三轮原地改写同一节致 v2/v3 节名失效→改为「设计定稿节（该轮修订已并入现行 v4）」）+ v4 行「待用户确认」补确认状态 + 定稿行计数与轮次枚举改为多轨口径（「四轮/共 12 条」与逐轮合计不相容）；⑦改动面补 permission-query-pipeline skill 双副本（逐 item 示例随实施同步）。评审勿再报①-⑦——已修 | docs/archive/2026-09-12/tasks/T-PERM-061.md（frontmatter/评审记录节终态化）、docs/tasks/README.md（任务行/EXT-7 注记）、docs/design/permission-center/implementation.md（§3.10/§5.1/last_reviewed）、本表 v2/v3/v4/定稿行 | 再报直接撤回 |
| 2026-09-11 | T-PERM-061 实施收口（commit a03d6117f 实施主体 + 双轨本地评审修复批次；容器轨 BatchAuthCheckPgIT 回归锁①-⑪与 evaluator 单测轨全绿）：A+ 形态落地——引擎 `queryBatch`（六元分组键/请求级共享装载/分段化/闭包分档 false 档恒 {自身}/投影谓词三分量切分/评估粒度与顺序不变量/reason 双轨）+ `BatchConditionEvaluator` 四态增量快照（仅 OK 入正缓存 fail-close）+ `BatchPermMutexEvaluator` 计算通知解耦 + (组,ruleId) ledger 聚合 + batchCheck 编排重写（唯一非空 PermEvalContext 钉住）+ queryInstance 空目标集守卫；api-contract §6.1 批量口径注记/implementation §3.8+§3.10 实施落点/coding-standards §2（6.1.0）/skill 双副本（5.2.0）同步。**用户定案（2026-09-11）：父判定段条件装载维持既有递归单条形态**——§一.7「父判定共享只经既有注入面（不穿透递归）」定案优先（父判定每请求 ≤1 次、正确性共享 roleIds+evalContext 已闭合、同 conditionId 请求内至多回源一次经缓存成立；§一.9「三段批量预载」的父段一批次字面口径不达成，与父判定递归内操作解析同类登记任务卡遗留——最坏每请求多 ≤父私有冷条件数次单查，正常路径零差）。双轨评审处置（均为事实性最小修正）：core-flows §7 旧口径句/implementation §2.4+§2.5 接口摘录补两 evaluator 方法/§3.10 时态句/看板回写列四处文档修正；ResolveContext 缓存直取消幽灵码逐组单条探测 + PermQuery.inheritClosureOf 唯一口径两处代码修正；ledger 组键 `"-"` 占位碰撞（冷僻审计归并面）登记维持。评审勿再报——实施面与父段定案均以本行为准 | docs/archive/2026-09-12/tasks/T-PERM-061.md（实施记录/遗留节）、implementation §3.10 实施落点段、api-contract §6.1、BatchAuthCheckPgIT、ResolveContext（getCachedTypeValue/getCachedOperationId） | |
| 2026-09-12 | T-PERM-061 实施批次三轨外评处置（claude plan 只读 + codex luna max 只读 + grok 三次重跑均中断未出报告——会话存档实证外部取消 run_terminal_command，该轨道本次不可用，按两轨收敛处置）：claude/codex 双轨结论一致——v4 设计逐项对照实现、queryBatch 与单条 query() 等价性（reason 双轨/matched 派生/scopeAll 短路）、安全面（条件四态 fail-close 仅 OK 入正缓存/类型谓词双保险/false 档闭包恒 {自身}）、a2 时刻唯一性全链、evaluator per-request 状态隔离、既有面（resolveBitMasks 重构等价/queryInstance 守卫无回归）、文档六处口径与 skill 双副本零漂移——全部通过；codex P0-P3 全零。claude P3×1（验证缺口，非功能缺陷，手工走查实现无缺陷）：批量路径「请求级父上下文 + 实例 depend_on 子行」分支零测试覆盖（唯一 setParentResource 用例挂 TYPE_LEVEL item 上不触父判定，①的 depend_on 子行无父上下文）→ 已核实属实并补 `BatchAuthCheckPgIT.batchParentContextMustResolveThroughSharedCarrier`（父命中放行锁 carrier 装配三线 + 父操作不覆盖 DEPENDENT 两变体），容器轨 11/11 全绿。存量观察随批登记任务卡遗留节（实例 SQL XML `<if>` 空集丢实体过滤陷阱面——引擎守卫已封现有调用方；单条 computeMutexContext 规则表无缓存维持现状）。评审勿再报——已修/已登记 | docs/archive/2026-09-12/tasks/T-PERM-061.md（遗留节）、access-service/src/test/.../BatchAuthCheckPgIT.java（父判定装配用例） | 再报直接撤回 |
| 2026-09-12 | 任务/计划归档制定案（用户指令当轮落地）：附属于**活跃计划**的单卡 done/cancelled 不单独归档，随计划归档一并迁入；**无所属计划或计划已归档**的终态单卡即行单卡归档；归档统一落点 `docs/archive/YYYY-MM-DD/`（归档夹内部按日期目录区分，任务卡住 `tasks/` 子目录、证据随卡迁 `tasks/evidence/`）；`archived` 由文件位置表达——卡 frontmatter 与看板行保留终态状态标记不回改；cancelled 同为终态触发归档；禁止 `plans/archive/` 侧挂归档目录（存量 2026-06/2026-07 两批已并入统一归档）。存量补迁同批执行：94 张终态卡 + 4 计划/侧挂文件（批次记录见 docs/archive/2026-09-12/README.md） | design-plan-task-lifecycle skill §1.1/§3.3/§6.5（双副本）、docs/README.md 归档规范节、docs/tasks/README.md 序言归档约定 | 报了先核出处。claude CLI 外评处置（2026-09-12，deepseek-flash[1M] 默认模型，read-only plan 模式，范围 b8d3de523..42d424e7e）：P0-P2=0、P3×4 全修——①2026-07-12 批次 README「当前权威入口」活指针仍指 docs/tasks/（改指同目录 tasks/）；②09-12 自检「清零」口径与收窄判据分叉（改「无活引用」口径）；③归档记录表缺 07-26/08-15/09-10 三行 + 目录树滞后 4 目录（补全对齐 19 目录）；④skill §8 例外括号与 §6.5 不对称（补「历史描述文本」）。随批修复 registry 表体截断空白行（GFM 渲染）与 skill §2.3 枚举 archived 注。评审勿再报①-④——已修 |
| 2026-09-12 | T-FE-023 扩展指南定位与验证形态定案（Phase 4 批次内拍板）：原「SPI 策略扩展验证」经盘点证实无代码插槽（条件操作符封闭 4 类、权限源概念随 T-PERM-057 统一引擎收编——扩展模型为数据声明式，勿建议为本任务补 SPI 插槽）；指南定位=**跨端全景**落 `docs/design/extension-guide.md`（design 根，取代原 design_refs `frontend/extension-guide.md` 纯前端口径）；验证形态=补自有资源类型端到端容器用例 `CustomResourceTypeSlicePgIT`（声明→操作→服务身份同步→部署方种子→授权→判定 + 20040 必要性锁/NO_ROLE/未同步资源负向组），另两场景文档级走查。指南 status 待用户确认后转 adopted | docs/design/extension-guide.md、docs/tasks/T-FE-023.md、access-service/src/test/.../extension/CustomResourceTypeSlicePgIT.java | 报了先核出处。claude 外评处置（2026-09-12，默认模型只读 plan 模式，45ce95912）：代码实体零缺陷（负向锁三条独立核实无伪绿）；指南 §3.1/§3.3 补 20056 is_system 钉死口径 + §6 补异常告警不做行；任务卡轮次词清理 |
| 2026-09-12 | T-ADMIN-020 admin 域清理范围定案（Phase 4 批次内拍板）：不按 2026-06 原范围做全量风格清理，**重新排查核对范围后按现行规范清单清扫**——重排查结论零可清扫描项，零代码变更收口（原痛点「185 文件零测试」口径失真，现状 197 源/37 测试；已知边界：机械清单扫描+抽样核实，非逐行风格 diff，后者性价比不成立勿再建议）；admin 域裸 IllegalArgument/State 命中均为规则豁免面（bootstrap fail-fast、JobInvoke 框架适配层） | docs/tasks/T-ADMIN-020.md（排查定性表） | 报了先核出处。claude 外评处置（2026-09-12，45ce95912）：排查表 DTO 校验行系扫描 pattern 漏 @Min/@Max 的误判，按实测改写为 38/38 全覆盖（分页注解 T-ADMIN-026 已落地）+ 裸异常豁免面补全第三类（@PostConstruct 启动校验）；「registry 复制错误口径」子项经核实撤回（引用错行）；存量观察：OrgQuery/UserQuery 零注解 DTO（非 Req 命名面未触达） |
| 2026-09-12 | 新类型首笔授权引导缺口处置定案（T-FE-023 产品发现→T-PERM-062 立项；claude/grok-4.6/codex gpt-5.6-sol xhigh 三路独立方案咨询收敛后用户四项拍板）：**类型生命周期种子**——createType（CRUD 四条）+ createOperation（每追加操作一条）同事务向「类型所有者角色」落单 bit `scope_all+can_grant` 首授行；所有者=TypeCreateReq 可选 ownerRoleTypeCode/ExternalId 持久化于 type_definition.extra.grantOriginRole（缺省 BASIC_ROLE/bootstrap-admin，解析失败整单回滚）；**GrantSource 新增 AUTHORITY_ROOT + DDL CHECK 焊死形状 + apply-grant-plan 不可改删**（20034 先例；类型/操作删除级联清理是唯一回收路径）——结构防护下不采 reseed API；20040 细分 TYPE_GRANT_ORIGIN_MISSING reason+文案；实施首步出授权根活性报告（内置类型 canGrant=false 维持不动勿顺手升级）。写入通道复用 bootstrap 直写通道下沉共用方法，**checkCanGrant 与通用授权链零改动**（§14.1 红线维持）。否决项（三路一致，勿再提议）：运行时豁免/超管旁路、GRANT_ORIGIN 能力位/赋权权拆分、纯文档方案 | docs/tasks/T-PERM-062.md（设计定案节）、CustomResourceTypeSlicePgIT（5a/5b 现状锁）、extension-guide.md §3.5 | 报了先核出处 |
| 2026-09-12 | T-PERM-062 实施期四项补充定案（活性报告后用户逐项拍板）：①20040 reason 细分**仅自定义类型**（is_system=false）——内置类型零可转授行是 T-PERM-027 转授链收窄的设计状态，reason 维持 NO_PERMISSION/NO_GRANT_RIGHT（引导文案不失真）；②**无历史数据负担**（项目未正式部署，存量自定义类型回填面不存在）；③所有者指针 extra.grantOriginRole **允许变更并补齐种子**（取代立项时「指针不可变 + runbook 兜底」口径），且变更=**同事务先清后种迁移**（软删该类型全部 AUTHORITY_ROOT 行——含已删角色/误配旧 owner 残留——再向新所有者补齐全部有效操作位；只补不迁则旧 owner 持有不可移除的永久扩权）；④owner 角色被删除**不加删除守卫**，恢复路径=经 type-definition/update 重指所有者（迁移语义自动清理残留行）——恢复面收敛进 update 单入口，「不采 reseed API」的定案精神以此形态维持（无独立 reseed 端点）。附带实现口径：种子直写通道下沉落 PermissionGrantPlanDomainService.seedGrants（plan→grant 域既有单向依赖，反向下沉会构造循环）；指针为服务端管理键（create 请求 extra 自带拒绝 20044）、无清除语义（update 未携带键=保留现值） | docs/tasks/T-PERM-062.md（设计定案节更新）、api-contract.md §5.1/§6.5.1、access-service/.../GrantOriginDomainServiceImpl.java | 报了先核出处。claude CLI 外评处置（2026-09-12，默认模型 stdin 注入 read-only plan 模式，范围 025936e97..579ee12b0）：P0/P1=0；P2×1（createOperation 陈旧所有者读×所有者迁移交错遗留不可回收种子行）经核实**撤回**——TreeWriteLockSupport 锁经 afterCompletion 释放覆盖整个事务、selectByTypeKeyAndCode 为无缓存直查且 typeDef 读在锁内，两事务在锁上严格串行、后进锁者必见前者全部提交结果（触发场景混淆前端展示层 stale 与服务端锁内 fresh 读）；其附带缺陷成立已修（typeDef==null 时静默跳过种子并插孤儿操作行 → 锁内 fail-closed 20021）；P3×2 已修——①PermissionDetailDrawer 补 AUTHORITY_ROOT 三态（class/tag/只读提示/子权限空态文案，与 MatrixCell 同源口径）；②ownerRoleTypeCode 值域经用户拍板收紧仅 BASIC_ROLE（容器角色 ORG/POSITION/GROUP_ROLE 携带 20044——授权根是类型级可转授行，挂容器角色=全体容器成员获得转授权且种子 20061 只读无逐行移除通道；回归锁 + api-contract/schema 同步）。存量观察：RolePermissionItemResp grantSource Javadoc 扩列已修；refine 候选行不筛角色停用命中任务卡已知边界撤回。评审勿再报——已修/已撤回 |
| 2026-09-12 | T-PERM-062 grok 外评处置与操作生命周期联动定案：①createOperation 锁内未重绑 typeValue（「删类型→同码重建」交错下操作行+授权根种子落已级联清理的死号、活类型缺该操作死锁复发）——已修（锁内以 typeDef.getTypeValue() 重绑全链：插入/补种/缓存失效），补「锁内空 20021 不落库」「锁前后值不一致落锁内值」两类回归锁；②操作位变更/删除不维护授权根种子（grok 存量升级，用户拍板「补联动」取代「登记边界」）——updateOperation 位有效变更同事务迁移种子（软删旧位+补种新位）、deleteOperations 同事务级联清理被删自定义类型操作种子行（is_system 不联动），两入口共持 RESOURCE_ENTITY 树写锁并锁内重读，对齐 T-PERM-028/050 类型级联对称先例；③claude 外评处置被独立复核确认（P2 撤回的锁语义论证、值域收紧入口唯一性、20021 错误码一致性）。grok 专项七项全过、P0/P1/P3=0 | api-contract.md §5.3 操作生命周期联动段、access-service/.../GrantOriginDomainServiceImpl.java（migrate/removeOperationAuthorityRootGrants）、OperationAppServiceImpl.java | 报了先核出处。评审勿再报①②③——已修 |
| 2026-09-12 | 外部评审整合与子代理禁令（用户指令当轮落地）：外部 AI 评审统一三通道 claude/grok/codex（codex 含 luna+max 默认与 sol+xhigh 点名两形态），执行规范收编单一 `external-review` skill（原 codex-external-review 更名，claude/grok 调用形态自记忆收编入仓）；**外部评审全程禁止子代理**——codex `--disable multi_agent`、claude `--disallowedTools Task`、grok `--no-subagents` 开关硬关 + 提示词禁令双保险，原「codex 实跑测试 spawn_agent 委派子代理」手法退役；本地双轨评审的并行子代理不受本禁令影响 | external-review skill（双副本）、AGENTS.md 技能表 | 报了先核出处。三路评审处置（2026-09-12，内部双轨 + claude deepseek-flash[1M] + grok-4.6 xhigh，对象 589a7ac4e）：claude 硬关扩面 `--disallowedTools Task Workflow`（Workflow 多代理编排实测仍在工具面、plan 模式系统指示主动要求并行 Explore，claude/grok 双路独立发现）；resume 段「不认 --disable」失实修正（exec resume help 实测支持）；grok 节调用形态重写——headless 后台 plan/dontAsk 工具全「User cancelled」（会话存档实证），改 `--always-approve --sandbox read-only` + 显式 `--reasoning-effort`；AGENTS.md:91 与本表 :17 消费协议单通道枚举两处对齐；框架/模板槽位映射对齐 + 模板只读句补 CLI --help 查询——均已修（skill v2.1.0 双副本）。存量观察：.claude/settings.local.json 两条 permissions.allow 指向已删除的 resource-permission-validator skill（用户域文件待定夺）。评审勿再报——已修 |
| 2026-09-12 | 外评提示词构建标准 v2（用户三项指令）：①registry 整段注入废止（77KB 文件是 ~90KB 提示词主因）——三通道均有仓库读权限，改模板「硬性纪律」首步指令自读 registry 全文 + 槽位只挑任务相关行（日期+口径一句）+ 报告首行声明已读；②模板精简 ~10KB→~3KB（16 条评审原则/逐类检查/不要只看 Diff 四节等压缩为方法论要点；P0-P3 定义/输出格式/两轮复查/存量观察/未发现缺陷声明保留）；子代理禁令前置到模板「硬性纪律」节开头（原埋末尾「重要执行要求」易被长上下文稀释）；③复评轮槽位固化：增量范围 + 专项清单含「复核上一轮外部评审处置」附上轮 P0~P3 结论与处置状态（已修/已撤回/待定）；目标体量 ~5-8KB。随批清理 .claude/settings.local.json 两条指向已删 resource-permission-validator skill 的 permissions.allow | external-review skill v2.2.0（双副本）「提示词构建标准」节、本表消费协议 | 报了先核出处 |
| 2026-09-12 | 扩展指南定稿（用户确认）：`docs/design/extension-guide.md` status 待确认评审稿 → **adopted**——经 claude+grok 双通道两轮外评处置收口（首轮 13 项 P1×2+P2×9+P3×2、复评 5 项 P2×4+P3×1，全部逐条代码级核实成立后修复，提交 46257d49d/23f882085；复评 13 项修复 9~11 项通过、P1 清零收敛）。两轮一致存量观察**同日已处置**（契约勘误随批）：api-contract §6.2.2.2 同步失败信封码描述改与实现对齐（恒 code=200、失败以 accepted=false+retryClass 判定、STALE_VERSION=accepted:true/applied:false/stale:true）+ §顶部协议定义补授权写入口 scopeMode 值域口径（DTO 共享四态枚举、DENIED/EMPTY 拒绝 20027 VALIDATION_FAILED——实现本有校验系契约漏写；EMPTY 分支补单测锁 ScopeModeSupportTest） | docs/design/extension-guide.md（frontmatter status）、本表同日 T-FE-023 行「待用户确认后转 adopted」口径闭环 | 报了先核出处 |
| 2026-09-12 | 角色互斥授权时校验三项拍板（T-PERM-063 立项当轮）：①冲突处置=**整批原子拒绝**——`user-role/assign`、`batch-assign` 命中互斥对整批拒绝（对齐入口既有 errors 整批抛风格，无部分成功）；②存量违规=**有持有对拒绝立规**——conflict-rule create/update 的 ROLE_MUTEX 分支存量双持非空即拒绝（先解绑再立规，运行时双删不再是常态兜底）；③附加范围=**双删补 CONFLICT_DETECTED 日志**（Caffeine 有界去重，每租户×用户×规则对每 JVM 1h 一条）**+ detect 端点支持 ROLE_MUTEX 角色对预检**。事实基线（代码级核实）：用户持有角色唯一通道=user_role 直授（组织/岗位拿权限行不持角色，无绕过通道）；applyGrantPlan 不涉用户-角色持有、不适用；写路径规则读取 DB 直查不走 10s 缓存（新规则即刻生效）；并发双开窄竞态接受（运行时双删兜底） | docs/tasks/T-PERM-063.md 当前口径、api-contract §5.5/§5.6 要点、implementation §2.4/§3、core-flows 评估口径行 | 报了先核出处 |
| 2026-09-12 | 互斥守卫通道补全拍板（claude 外评 T-PERM-063 P2-1 核实成立后）：user-role/sync、full-sync 接受 BASIC_ROLE/PERSONAL/自定义角色类型且 BIND 无守卫=管理面之外真绕过通道 → **守卫补全**——①sync BIND 逐条守卫（冲突 item NON_RETRYABLE + reason=ROLE_MUTEX_CONFLICT，走通道错误信封而非管理面 20062 整批语义）；②conflict-rule create/update 拒 ORG/POSITION 角色对（本地投影通道只写结构角色行，规则面拒绝后该通道结构性造不出违规；UI 本就不提供结构角色对）。弃「登记不适用」（豁口与治理目标矛盾）与「只堵 sync」（ORG 对规则经直调 API 仍可造双持） | docs/tasks/T-PERM-064.md 当前口径、api-contract §5.6/§6.2.2.2 | 报了先核出处 |
| 2026-09-12 | T-PERM-021 工作单 F 执行门禁三项拍板（当轮逐项）：①F1.c（范围先经事实更正收窄——`resource_entity.sync_key` 已随 T-PERM-052 物理删除，schema 仅余 sync_metadata.sync_key）：`owner_service_code`/`maintain_source` **两列保留**，F1.c 范围=文档收敛（「判定靠 type_definition.extra 类型声明、记录靠两列、外部同步 ownership 以 sync_metadata 为单源」口径写死），零 schema 改动——弃评审原文删列形态（撞码归属判定/通道清理边界/投影防线三消费点在用，删除需按服务拆类型重构另议）；②F1.d：requestId/traceId 语义收敛（全链路单 ID，traceId 仅为 MDC 键/响应字段别名）**+ operation_log/permission_change_log 两列收紧 NOT NULL**——RequestContext 无头直连兜底生成 UUID、bootstrap/异步等无请求上下文写入方合成 ID，存量 dev 库重建；③F1.f：`PermQuery.operationPermissionIds` 死分支（全仓零写入方、引擎分支永不可达）**删除**（字段+getter+引擎两处分支），不保留待激活。F1.b（保留 DTO/Command 分层边界）/F1.e（runbook 落 docs/ops/runbook-full-sync.md）按卡面默认执行 | docs/archive/2026-09-12/tasks/T-PERM-021.md（定案节）、docs/design/schema/access-service.sql、docs/design/permission-center/api-contract.md | 报了先核出处。claude+grok 双通道外评（2026-09-12 收口后首轮）：P0/P1/P2=0、P3×2 双通道同发现（Gateway 两过滤器拒绝路径缺 traceId 同值/RResponseAdvice 头链未 trim+截断 64）已修，处置记录见任务卡外评处置节；评审勿再报 |
| 2026-09-12 | T-PERM-065 Req DTO 双轨收敛三项拍板（任务内逐项）：①**admin 域不换绑**——admin `IdsReq`（26 文件消费的批删端点族）不进引擎闭包、历来无上限契约，不随 14 对收敛；admin `UserRoleListReq`（`userId` 内部 ID）与 perm-common（subjectTypeCode+subjectExternalId）同名异义，结构上不可换绑；②**ResourceBatchCreateReq.items 只补 @Size(max=1000) 不补元素级 @Valid**——仅封规模，嵌套畸形项走宽容收集逐条跳过、部分成功语义为有意设计（消费方=管理面前端+菜单同步，规模受控）；③**project-rules §7.1 登记单源纪律**——「permission 域对外 Req 复用 perm-common 单源、不建域内副本」（对齐 T-ADMIN-027 分页信封先例）。实施事实：14 对同名 Req 服务端 Controller/AppService 与 SDK 统一消费 perm-common 类型、A 侧副本删除；SDK `ResourceUpdateReq` 由 id 定位旧形态修复为业务键形态（T-PERM-028 漏改，PermissionFeignClient.updateResource 活引用原必 400）；守卫 `PermCommonReqContractTest` 重写为注解签名快照 + 组件序与类型快照双锁单源守卫（含 element:: 容器元素位置——修复原守卫抓不到长度类分叉缺陷） | docs/tasks/T-PERM-065.md（完成记录）、api-contract.md §3.5、project-rules.md §7.1、perm-sdk/perm-common dto/req、PermCommonReqContractTest | 报了先核出处。双轨本地评审（2026-09-12，commit fd9ad94a8）：代码轨零 P0-P2、P3×3（快照注释举例过时/双副本断言重复/任务叙述 P 侧更严格失实——A 侧原本即元素级同形，换绑零行为变化）全处置；文档轨 P2×2（registry 缺行/任务卡状态行分裂）+P3×4（javadoc 残留/计数 18→17、198→179/last_reviewed 注记）全处置。claude+grok 外评（2026-09-12 收口后，read-only）：claude P3×4 全处置——①batch-create items null 元素穿透 NPE 500（与拍板句「宽容收集」冲突）：items 补元素级 @NotNull（只拦 null 不级联，不触碰宽容语义）+ null 元素 400 行为锁（同族 UserAssignRoleReq/UserRoleBatchRevokeReq 一并补）；②注解快照不含组件类型与顺序：补组件序与类型快照锁（containsExactly 有序）；③perm-common 三处 javadoc「双副本同形」现在时残留：改单源口径；④计数订正不彻底（任务卡范围节 18 残留/registry 90→91 失实）：订正去计数。grok 全零缺陷（专项 6 项全过）。双通道同发现存量：SDK `getRole(IdReq)` 与 `deleteResources(IdsReq)` 为 T-PERM-028 同批第三、四处漏改（调用必 400）——用户拍板「顺手修」：perm-common 新增 `RoleDetailReq`/`ResourceKeysReq`/`ResourceKeyReq` 三共享类型 + Feign 两签名对齐 + 服务端同步换绑删副本（收敛面 14→17 对，遵守 §7.1 不造新双轨对）；评审勿再报 |
| 2026-09-13 | access-service 两域融合定案（grill 五轮收敛逐项拍板，全文见 capability-structure）：①**结构**——能力包终态（12 能力包 + sync 通道包[Controller 通道层独立、执行 AppService 随实体、sync_metadata 记账归 sync 包、接受包级双向边] + 引擎独立子系统 + infrastructure 底座；application 包解散归位）；admin 14 对 Service/Impl 收敛 AppService（project-rules §8 既有规范的事实性修正）；节奏=先机械迁移（T-ACCESS-001 剧本）后逐项收敛；验收五条见设计 §1。②**概念**——六对双轨表全部维持（引擎零直读 sys_* 不变量保持）；字段消减六项（零读取方实证，resource_entity.sort_order 因涉对外契约单列任务）；操作码=常量面合一（两套并一 + 补录 API:ACCESS[消除四处裸字符串]与 ROLE:ASSIGN/ROLE:REVOKE[注册表镜像收录、口径关系随 T-ACCESS-034 登记]）+ **USER 轨细粒度化**（UserManageAppServiceImpl 换绑：updateUser 字段分档 name/extra→USER:UPDATE、enabled≠null→USER:ENABLE，deleteUsers→USER:DELETE；USER:MANAGE 退役；固定图零变更、空库 perm 轨由拒转放行为预期行为变化；粗细统一仅 USER，ROLE/RESOURCE 的 MANAGE 惯例不动；SDK DefaultOpCode 不动、EDIT 无服务端预置登记差异）——唯一权限语义变更；system_config 单入口化（admin /config 退役，perm 入口保留）。③**表面**——URL 两风格维持仅登记后续单独改；错误码合类不合号（1xxxx/2xxxx 编号段原值保留）；前端信息架构维持不纳入。④**文档**——契约深合一（两份并一册、permission-center/ 目录解散、设计内档归引擎位）；任务 ID 前缀不动。⑤定案修订对照见设计 §9（§1.2「不扁平化」由本定案承接启用） | docs/design/access-service-capability-structure.md、docs/plans/access-capability-fusion-plan.md、T-ACCESS-032~041 | 报了先核出处。claude+grok 双通道外评处置（2026-09-13，均 read-only + 禁子代理，对象=本行与 fusion 计划/任务卡未提交稿）：claude P2×6+P3×4、grok P1×1+P2×4+P3×2，逐条代码级核实后全处置——①grok P1（036 删字段后旧载荷 400）——首轮主代理裁决曾误降级为 P2（误判 Spring Boot 默认宽容反序列化），codex sol 复评推翻后**恢复 P1** 并亲核实证：全局唯一 ObjectMapper 为 CacheAutoConfiguration.cacheObjectMapper 裸实例（AccessServiceApplicationTest 断言唯一 bean 且 Boot jacksonObjectMapper 回退）、FAIL_ON_UNKNOWN_PROPERTIES 保持 Jackson 默认开启、T-PERM-053 的 sync_withLegacyOperationCode_rejected400 行为锁在册（严格 mapper 为有意设计）；036 定稿为 400 负向锁 + 前后端/SDK 同批锁步 + sync 载荷与前端表单链路入范围。②034 换绑改字段分档（enabled≠null→USER:ENABLE，防 UPDATE 绕过启停分权——双通道矛盾处采 grok，忠实于本行拍板原文的 ENABLE 枚举）+固定图「语义等价」改「由拒转放行」（固定图无 MANAGE 位、空库 perm 轨死锁实证）+调用点计数删数字+补录理据修正（ROLE:ASSIGN/REVOKE 非 bootstrap 裸字符串）。③039 补依赖 034+验收前提中性化+PermCacheBoundaryValidator 判据重绑（全目录纳入会启动失败——T-ACCESS-008 快照边界语义实证）+key 形态定稿口径。④033 断言面补全（type-aliases-package/mapper XML FQCN/HttpApiPathSnapshotTest 签名/AccessServiceArchitectureTest/AppServiceOperationLogCoverageTest+负向样例自证）+残留面收窄（规则文档归 040/041）+application.query 三对命名裁决。⑤040 活引用改允许清单二分（权威入口 vs 档案；引用面实测非归档 94 文件/228 处）+下游卡 design_refs 重挂新册。⑥041 依赖扩至全部收敛任务（收尾位）。⑦032 增跨域同名类三组裁决（admin/permission 双份 UserCreateReq/UserUpdateReq/UserResp 实证）+全部包名扫描断言重建设计交付。⑧验收①②可判定化（ArchUnit 负向规则+归属清单三步映射）。codex sol xhigh 复评处置（同日，read-only + 禁子代理）：P1×1+P2×5+P3×1 全处置——①036 恢复 P1（见上段，含空 patch 关联修正）；②034 补空 patch 门禁（UserUpdateReq 至少一个业务字段 + 组合字段须全过涉及操作码 + 行为锁清单）；③038 补同名异号三组碰撞裁决（USER_NOT_FOUND 10001/20015、USER_ALREADY_EXISTS 10002/20016、INVALID_PARAM 10008/20044，双侧生产调用实证，编号对与语义原样保留 + 全量映射断言）；④039 滚动发布双命名空间失效**登记不实施**（未部署无新旧并存场景；Nacos 覆盖键同批核对入验收）；⑤040 验收命令改 rg --hidden（默认扫描漏 .claude/.agents 4 个技能双副本，实测 94→98 文件）；⑥033「非 import 行零改动」改可判定语义等价判据（映射表 + 规范化 diff，允许机械替换白名单）；⑦P3 部分采纳：看板顺序块精简指向计划、计划/任务卡过程叙述清理（registry 为过程唯一载体）。第三轮复评处置（同日，claude + grok 双通道并行，均 read-only + 禁子代理）：claude P2×2+P3×3、grok P2×2+P3×2，两通道均确认第二轮处置全部正确落地且无生产行为缺陷、039 登记裁决不挑战；发现全处置——①034 补 AccessServiceSchemaH2Test 的 USER:MANAGE 位值断言与 schema「16 被 MANAGE 占用」注释同批修正；②034 自身豁免裁决=**保留**（admin 轨 UserServiceImpl 同款 `if(!userId.equals(currentUserId))` 豁免实证在册——两轨一致即「对齐」定案原文），行为锁含「自身更新且无操作位仍成功」；用户定案（2026-09-13）：自身豁免的**范围限定**（改自己可改的字段/动作边界）为已知问题登记遗留、不在本计划解决；空 patch 钉 Bean Validation→MethodArgumentNotValidException→90001 通道（禁服务层 IAE——该通道 code=400）；③032 增必裁决句（AccessServiceArchitectureTest 互不依赖族规则能力化：允许同层跨能力、保留 Mapper 边界 + bootstrap 排除项重判）+ QueryMapperXmlContractTest 随迁入列 + 同名类「保留=分包」+ UserResp 三份点名；④033 重建断言文件列入机械 diff 豁免 + XML 白名单补 resultMap@type + @MapperScan 覆盖全部 mapper 包 + Nacos 同键「有则同步无则记无」；⑤035 验收类名改以 033 映射表新名为准 + 容器行停传细化；⑥036 补 access-service 服务端册 ResourceResp（HTTP 线格式独立于 SDK）与 ResourceEntitySyncItem；⑦037 明确不删 SYSTEM_CONFIG:DELETE（CRUD 预置码）；⑧040 豁免面落显式路径清单（命中集合⊆清单）+ 本卡 design_refs 随批改挂注记；⑨041 明确任务前缀不作域叙事残留。评审勿再报——已修 |
| 2026-09-13 | T-ACCESS-032 归属清单十项裁决（用户逐项拍板，细化融合定案①③遗留争议项；⑨⑩为双轨评审后追加拍板）：①跨域同名用户 DTO——perm 轨改名 `AbstractUserCreateReq`/`AbstractUserUpdateReq`/`AbstractUserResp`（与 `/api/perm/abstract-user/*` 端点语义对齐；admin 轨保名；perm-common SDK 副本不动——T-PERM-065 单源纪律维持；仅 Java 类名变、JSON 契约零变化，签名快照 033 全量重建吸收）；②operation_permission 全链归 **type**（操作位空间按类型隔离核心语义、类型删除级联同包闭环）；③service_config 全链归 **resource**（MappingSyncHandler 直读 ResourceEntity/ApiMapping mapper，同步产物即资源行）；④任务治理设施（sys_task_execution 链 11 类：租约实体/Mapper、TaskExecution/JobInvoke DomainService、schedule 两类、TaskExecutorConfig、JobInvocable、TaskExecutionContext）归 **infrastructure.task**——job 业务链（JobController/AppService/JobLog）归 platform，租约/执行/调度设施随 §8.1 多实例协调定位归底座；⑤application.query 三对随 033 改名 `XxxQueryAppService`（§2.5 不留第二套调度层命名）；⑥bootstrap 独立**顶层包**（BootstrapSeedWriter 接口与实现随迁，`bootstrapSeedWriterIsBootstrapOnly` 断言排除集收敛单包）；⑦AdminPermissionValidator 门面（含 Impl，22 消费方）+ 操作码常量面（AdminOperationCode/OperationCodeConstants 033 两册随迁、034 合一单册）都归 **engine**（能力包→engine 查码/校验单一方向）；⑧LocalProjectionDomainService + PermConstants 独立**顶层 projection 包**（横跨五能力不可拆，§2.2 已排除 sync、engine 不宜进写路径）。终态顶层包=**17**（12 能力包+sync+engine+projection+bootstrap+infrastructure，对定案①的 +2 扩充）。capability-structure §8 填充完成并转 **adopted**；architecture §1.2/§11 启用注记、§3 能力包口径重写（033 完成前代码维持旧包结构）、§9 合类不合号衔接、§11 Q-001 指针行；project-rules §8.2 能力口径条目。盘点副产物：`permission.dto.req.PermissionCheckReq` 零消费方（僵尸 DTO），按「无消费即删」政策随 033 删除。**双轨评审处置（同日收口）**：代码轨全量核对发现 13 类漏网 + 2 接口无行（ResetPasswordResp/RoleTreeReq/AuthCheckResp 三件/OperationLogRuntimeContext/PermissionConstants/TreeBuilder/RoleType/DependencyAppService 对/两 sync AppService 接口——已按消费面补行）、`sys_task_execution` 表漏登记（已补 infrastructure 节）、SyncMetadata 存量直读一处措辞失实（已如实登记）、断言面统一 mapper 包口径（实体 import 不禁——跨能力实体引用为既有普遍形态）；**用户两项追加拍板——⑨存量跨能力 mapper 直读（时点计数 18 类 24 处——外评复扫修正为 **19 类 30 边**，见后文处置段；存量代码既有、拆包后撞断言负向样例）= 冻结白名单**（§8.4 第六条豁免：机械原样保留 + 断言锁「不得新增」；存量收敛改走对方 DomainService 不在融合计划内，登记 Q-009）；**⑩僵尸 DTO 扩面——BatchResultResp、OperationDetailReq 与 PermissionCheckReq 同批随 033 删除**（全仓零消费实测）。文档轨：看板/计划正文 draft 残留与 17 包列举缺口（已同步）、§10 门禁句历史注记（已加）、033 标题三处统一「断言面重建」（已同步）、消费方计数去计数化（已改定性）。**claude+grok 双通道外评处置（同日收口后，均 read-only + 禁子代理；claude P2×3+P3×4、grok P2×5，双通道判定「需修订」，处置明细见 T-ACCESS-032 完成记录外评轮）：双通道同发现①§8.2 漏登 AdminUserRoleController（双轨代码轨 controller 全覆盖结论误报）与 OperationLogRuntimeContext（双轨处置声称补行未落地）→ 均已补行 + §8.4 新增「类必须落在 17 顶层包内」落位兜底断言；②冻结白名单数字失实（双通道实测 29/31 边互不一致，主代理以 import+内联 FQCN 全形态权威复扫定案 **19 类 30 边**，闭合清单表落 §8.4，采集口径写死字节码级禁单 import 扫描，Q-009 同步）→ 已修；③architecture §3 白名单句「Mapper/实体」与同节 mapper 口径自相矛盾（本 commit 新写入）→ 已改 mapper 口径；④033 卡验收仍 15 包枚举、无僵尸三件删除、文件删除不在机械 diff 允许面、「四个文件」≠五测试 → acceptance/范围已按 17 包+删除项+五测试修订；⑤Mapper 落点：SyncMetadataMapper/SysTaskExecutionMapper 原落 sync.metadata/infrastructure.task 非 `*.mapper` 包会漏 @MapperScan 注册 → §8.1 新增「Mapper 接口一律 *.mapper 子包」约定、两 mapper 改落 sync.mapper/infrastructure.mapper；⑥状态同步缺口（fusion plan 正文 proposed≠frontmatter active、plans/README 状态列、design/README 与 docs/README 索引——git add 漏提交 design/README 实锤）+ 三处「八项」→「十项」计数残留 + §8.2 三处绝对化断言（PermConstants 消费面/engine.core 半句/白名单限定语）→ 全部修正并补提交。**codex sol 默认档（max）默认上下文外评处置（同日，read-only + 禁子代理；P2×4+P3×1，判「需修订」；专项确认上轮处置全部正确、白名单 19 行 30 边完整复扫一致、上轮补登两类正确落地）：①§8.1 登记启动类 AccessServiceApplication 保留根包（e2e FQCN 引用），落位兜底断言精确豁免该类并反向锁根包仅允许它；②033 卡 @MapperScan 验收锚点由 application.yml 改正为 AccessServiceApplication 启动类（注解在 Java 源码）+ XML 残留扫描改 `resources/mapper/**/*.xml` 递归（mapper-locations 已递归通配无需改）+ 启动类测试扩展断言扫描包集合；③§1 验收第 1 条可判定形态**用户拍板弱化**——「platform 零 import 其他能力包」绝对规则与归属清单保留的既有依赖冲突（6 成员 import ResourceTypeCode、Notice 调 UserDomainService，机械迁移不可消除）→ 改「platform 不 import 其他能力包的 mapper 包」（Service/枚举/DTO 允许，day-one 可过且保留数据自治语义）；④architecture §3 engine.core 句同步精确版（不依赖能力包 Service/DomainService；实体/DTO/枚举/mapper 输入面允许——与 §8.2 对齐，消除两文档互斥）；⑤残留收口：Q-009 影响与设想段 24→30 边、关联改 —（open 条目不挂任务、033 仅白名单落地载体）、registry 裁决⑨时点计数加括注标废弃、032 卡 acceptance 第 1 条 17 包括注与完成记录⑨句括注 | docs/design/access-service-capability-structure.md §2/§8（adopted）、docs/design/access-service-architecture.md §1.2/§3/§9/§11、docs/design/project-rules.md §8.2、T-ACCESS-032/033 | 报了先核出处。评审勿再报 032 双轨评审与外评处置项——已修 |
| 2026-09-13 | 待解决问题清单机制：新增 `docs/pending-problems.md`（单文件、Q-NNN 全局计数器）作为 design/plan/task 三层的**前置队列**——登记已确认但暂不足以立任务/计划的问题（含用户拍板「记录问题本次不解决」与任务卡「非目标/遗留」延期项），必含现象+证据锚点+影响；可执行即转任务（不占 Q-ID）、关联任务 done 且验收覆盖后收敛为索引行；定案结论仍唯一走本表，问题文件不复制定案正文。既有登记收编：URL 两风格→Q-001、自身豁免范围限定→Q-002（原「architecture 演进方向区登记」口径由 Q-001 承接）。机制全文见 skill §2.5/§3.4/§6.6 | docs/pending-problems.md、.claude/skills/design-plan-task-lifecycle/SKILL.md（双副本）、project-rules.md §文档治理分层职责表 | 报了先核出处 |
| 2026-09-13 | 待解决问题清单登记边界补充：**已有任务/计划载体的事项不登记**（含 done/cancelled 卡定案中「另立任务 / 新形态另立任务」的安排——看板与 registry 已是其载体）；Q-005 权限视图重做按此收敛 closed（载体=T-FE-043 随卡归档「新形态另立任务」+ T-PERM-059 定案③，重做启动时从看板计数器取号） | docs/pending-problems.md（已收敛索引）、design-plan-task-lifecycle skill §2.5 | 报了先核出处 |
| 2026-09-13 | T-ACCESS-034 外评处置拍板（claude+grok 双通道收敛事实、主代理亲核后用户决策）：admin 轨 `/user/update` 的 `status` 写入**本批补 USER:ENABLE 门禁**——UserWriteAppServiceImpl.updateUser 非自身且 req.status()!=null 时补查 checkInstanceLevel(USER, id, ENABLE)，对齐 perm 轨字段分档（§7.8）与 /user/enable 门禁，防仅持 USER:UPDATE 旁路启停（停用清空目标有效角色/复活被停用账号；前端 update 不发 status 已核，纯 API 面可达）。弃「登记遗留另立任务」与「移除 status 字段」两案（后者对外契约破坏面大） | UserWriteAppServiceImpl、UserWriteAppServiceUpdateGateTest、契约总册 §7.4 | 报了先核出处 |
| 2026-09-13 | T-ACCESS-034 操作码合一两项命名拍板（实施期 AskUserQuestion）：①合一后单一操作码常量类命名=`OperationCode`（落 `engine.constant`，对齐 ResourceTypeCode 合一先例保留 perm 侧简名；AdminOperationCode/OperationCodeConstants 两旧类均消亡，满足验收「旧类消亡」）；②类内类型专属码常量命名=值名一致+分节（ACCESS/ASSIGN/REVOKE 直接作常量名，靠「按资源类型分节」注释区分归属——共享码 MANAGE 等本就只能按值命名，风格统一；弃类型前缀 API_ACCESS 形态） | access-service engine/constant/OperationCode.java、T-ACCESS-034 | 报了先核出处 |
| 2026-09-13 | T-ACCESS-040 契约深合一三项拍板：①契约总册路径=`docs/design/access-service-api-contract.md`（design 根，与 architecture/capability-structure 并列权威三件套；services/ 目录维持跨消费方服务视角）；②引擎子系统文档位=新建 `docs/design/engine/` 保裸文件名（目录名对齐代码 engine 包；overview/core-flows/implementation 内容原样迁移锚点不变；superseded 残件按既定口径留 permission-center 原位待物理归档）；③已终态任务卡【正文完成记录】旧路径引用=纳入显式允许清单（历史记录不改写，清单随 T-ACCESS-040 卡存档可审计） | docs/tasks/T-ACCESS-040.md（含 59 文件允许清单）、docs/design/access-service-api-contract.md | 报了先核出处 |
| 2026-09-13 | T-ACCESS-038 形态与命名拍板（实施期 AskUserQuestion 两项）：①合一形态=**单枚举平铺+段前缀**（AccessErrorCode 单册承载两段，两旧枚举 118 项常量合一=112 项原名平移 + 六碰撞常量 ADMIN_USER_NOT_FOUND/ADMIN_USER_ALREADY_EXISTS/ADMIN_INVALID_PARAM（10001/10002/10008）与 PERM_ 三对（20015/20016/20044）段前缀消解；弃嵌套双枚举分组——全部引用点需加组前缀且「合并后单枚举内自证」验收字面不满足、弃接口统一面——import 面不收敛任务实质空转）；②新册名=`AccessErrorCode`（与 common GlobalErrorCode 命名家族一致：Global=9xxxx 公共段/Access=业务两段；弃 ErrorCode/BizErrorCode）。编号对与各自调用方语义原样保留为既有定案（融合行处置③）；碰撞前缀词不能按能力命名——两侧调用方同在 user 能力包（管理轨/同步轨），只有编号段前缀一一对应 | infrastructure.enums.AccessErrorCode、ErrorCodeContractTest（COLLISION_PAIR_LOCK）、architecture §9 | 报了先核出处 |
| 2026-09-13 | T-ACCESS-039 目录合一两项命名拍板（实施期 AskUserQuestion 两项）：①统一目录册类名=`AccessCacheCatalog`（落 infrastructure.cache，AdminCacheCatalog/PermCacheCatalog 两旧册消亡；与 AccessErrorCode 命名家族一致 Access=服务级单册；弃保持 PermCacheCatalog 类名——「Perm 册装 admin 条目」即本卡消除的越域形态在类名层面复刻、弃 AccessServiceCacheCatalog——与既有单册命名家族不一致）；②ORG_VISIBILITY 越域 code 归位=`access:org-visibility`（org 能力读写 + 权限变更触发失效的跨能力条目取服务级前缀，与 038「能力无专属段」同构；弃 perm:org-visibility——越域仅换向前缀、归位落空，弃 org:org-visibility——为一个条目立新前缀族无先例且成本不成比例）。全部条目 mode/TTL 与快照链路 6 条安全边界判据零改动为既有定案（T-ACCESS-008 维持）；PermCacheBoundaryValidator 名称与判据维持（语义=授权快照安全边界，非目录合法性） | infrastructure.cache.AccessCacheCatalog、AccessCacheCatalogBoundaryTest、capability-structure §3/§8.2 | 报了先核出处 |
| 2026-09-14 | T-ACCESS-041 规则与域叙事口径拍板（实施期 AskUserQuestion 两项）：①规则文件改名=`permission-coding-standards.md`（去退役独立服务名 permission-center、保留权限主题；定位=access-service 权限面（engine 引擎子系统 + role/grant/resource/type/domain/rule/sync 权限事实能力包 + user/org 主体与投影轨 + projection 门面）代码改动必读，与通用分层规范（project-rules §8 / accessmesh-patterns）零重叠；弃 access-service-coding-standards——与 project-rules §8 大面积重叠两处维护必漂移、弃保名——文件名继续携带退役服务名与 038/039 已清除的命名残留同性质）；②域叙事替换词汇=**管理面 / 权限面**（表级「管理事实表/权限事实表」沿 capability-structure §4.1 既有、精确处直呼能力包名；「管理面」为 engine 文档既有用词零新造词；弃全部直呼能力包清单——user 包横跨 sys_user 管理轨与 abstract_user 主体轨无法归单侧、句子膨胀；弃保留「域」字+术语注记重定义——验收第 4 条机械二分判定无法闭合）。带日期历史句与「原 admin 域」出处注记保留不改为既有口径（任务卡 acceptance 4） | .claude/rules/permission-coding-standards.md、AGENTS.md 指针表、project-rules §17.2 检查清单 | 报了先核出处 |
| 2026-09-14 | T-FE-044（资源依赖页补遗联调）实施定案与外评处置：①api 层 URL 回归锁拍板补齐（`src/api/resource-dependency.spec.ts` 6 用例，mock `@/utils/http` 断言六端点 `/perm` 前缀——grant-store.spec 同形态先例；旧缺前缀形态必红）；②claude+grok 双通道外评（范围 ab1fe28e9..f6436c6f7）零 P0-P2，claude P3×3 + grok 全零（存量观察 5 条）、两轨交叉命中 2 条，六项逐条核实属实全修（README 当前限制句矛盾/runbook 现值 86/85/tsconfig 空 glob/resource-operation mock 终态句/hook checkCycle 死导出删除/README 列举补类型定义）。评审勿再报六项——已修 | docs/archive/2026-09-14/tasks/T-FE-044.md（完成记录外评处置节）、frontend/src/api/resource-dependency.spec.ts | 报了先核出处 |
| 2026-09-14 | T-PERM-066（Q-003 转出）operationCodeKey 族大小写口径拍板（AskUserQuestion 单问）：**raw 严格化**——①入站 DTO `operationCode` 族字段（operationCode/operationCodes/parentOperationCodes/sourceOperationCodes/requiredOperationCodes/scopeOperationCodes 等）与同链路的 `resourceTypeCode` 族字段（含复数形）统一 `@Pattern("^[A-Z][A-Z0-9_]*$")` 前置拒绝（400/90001，BizDomainCreateReq 同款正则先例），覆盖 SDK 单源 5 Req（AuthCheckReq/BatchAuthCheckReq+AuthCheckItem/QueryScopesReq/QueryResourcesReq/UserEffectivePermissionCodesReq）与 access-service 4 Req（ApplyGrantPlanReq.GrantRecordKey/ResourceDependencyCreate+UpdateReq/DependencyBatchSyncReq.DependencySyncItem）；②定义侧锁死：`OperationCreateReq/UpdateReq/KeyReq(s)` 的 code+resourceTypeCode 与 `TypeCreateReq.typeCode` 同款 @Pattern（typeKey 小写惯例不动）——小写操作码/类型码定义自本批不可再建（未部署零存量迁移）；③授权域 `PermissionGrantDomainServiceImpl`/`PermissionGrantPlanDomainServiceImpl` 的 toUpperCase/trim 归一全部退役（DTO 保证大写后归一成死代码），两域统一 raw 裸拼；小写入参从「授权面成功、查询面 20005」双语义收敛为全链 400/90001 一致拒绝。弃归一宽松化（store-normalized）——读侧归一触点分散漏一面分叉依旧、typo 远离犯错点暴露、DB uk raw 敏感须先归一写侧防 VIEW/view 碰撞。roleTypeCode/subjectTypeCode/domainCode/typeKey 扩面不做（两域一致 raw，非本问题不对称面；A+ 全码族锁选项用户未选）。OrgQuery.operationCode VIEW/CREATE 白名单 10008、check-interface 固定 ACCESS、DomainClassifyServiceImpl typeCode 归一维持既有口径不动（classify 匹配语义非键构造）。2026-08-30「裸 toUpperCase 不修 Locale」行不受影响——本批为归一站点整点退役，非 Locale 化改写 | docs/tasks/T-PERM-066.md、api-contract §2.4/§2.5（入参大写集中注记）、engine/implementation §8.2 | 报了先核出处 |
| 2026-09-14 | T-PERM-067（Q-002 转出）USER 写入口自身豁免收窄拍板（AskUserQuestion 两问）：**收窄为档案字段**——①档案字段自身豁免保留（admin 轨 name/phone/email、perm 轨 name/extra），自助资料编辑为基线能力不要求配权；②启停不豁免：admin 轨 `/user/update` 自身 `status=0` 对齐 `/user/enable` 硬拒 `CANNOT_DISABLE_SELF`（此前自身全免旁路该硬禁、同规则两入口相反结果）、自身 `status=1` 与非自身一致须持 `USER:ENABLE`；perm 轨 `abstract-user/update` 自身 `enabled` 变更须持 `USER:ENABLE`（生产面操作者==目标必被 LOCAL_USER 投影守卫先拒，为死分支语义统一）；③删除不豁免：perm 轨 `abstract-user/remove` 删除 nonSelfUserIds 静默剔除特例、门禁查全量 ids（对齐 admin 轨 CANNOT_DELETE_SELF 硬禁口径）；④`/user/reset-password` 自身豁免**保留并定位自助改密通道**（全系统唯一事实自助改密入口，旧句「系统无自助改密通道/走另外的修改密码接口」证伪删除；旧密码验证另立任务不扩本批）。弃取消豁免统一查码——普通用户将完全失去自助能力（资料/改密全经管理员），与行业基线相悖；操作位类型级授权虽可覆盖自助，缺省配权下仅管理员持码。盘点修正：可达暴露面全在 admin 轨（perm 轨两处豁免被投影守卫挡死为不可达死分支），登记时「SDK deleteUsers 自删生效」的可达性叙述不成立（守卫先拒），暴露定级以 admin 轨两处为准 | docs/archive/2026-09-14/tasks/T-PERM-067.md（完成记录）、api-contract §4 表/§7.4/§7.7/§7.8/§21.2/§22.2 决策 13 | 报了先核出处 |
| 2026-09-14 | T-PERM-066/067 双通道外评处置（claude P3×1 + grok P2×1，结论逐条代码级核实全成立、全采纳修复）：①grok P2——`TypeCreateReq.typeCode` 的 `@Pattern` 对空串 `""` 参与匹配即 400（Jakarta 语义 @Pattern 只跳过 null），打穿契约「留空按规则生成」——正则改 `^$|^[A-Z][A-Z0-9_]*$`（空串放行回服务层 isBlank 生成分支；纯空白串仍拒=收紧），同批 TypeForm.vue/type-definition.md 字符集对齐大写族（OperationForm 已对齐、TypeForm 漏改）、契约 §2.5 注记修正、行为锁补空串放行+中划线拒绝两断言；②claude P3 拍板（AskUserQuestion 三选一，用户选「自身置 false」）——`/user/reset-password` 的 `force_reset_pwd` 置位改按调用方：自身自助改密置 `false`（用户亲手完成改密、密码经响应当即已知，归档设计「修改个人密码成功后置 false」口径回归——此前一律置 true 且全仓无清除通道，自助改密后「初始密码」提示永久失真）、非自身重置维持 `true`（密码经管理员之手须改密），契约 §7.7 同步 + 行为锁补双向断言；弃「凡自选 newPassword 均置 false」（管理员分发自选密码也清标记=弱化）与「仅修文档」（失真问题留存）。存量观察不处理仅登记：`/user/update` 无默认树边界二次校验（契约已如实排除的既有文档化状态）、admin `UserUpdateReq` 无至少一业务字段 @AssertTrue（perm 轨有）、@Pattern 仅 HTTP 边界生效、BusinessKeyUtil 两处过时 javadoc 已顺手修（【已统一】注记） | api-contract §2.5/§7.7、archive/2026-09-14/tasks/T-PERM-066.md、T-PERM-067.md（外评处置见本行）、TypeCreateReq/UserAppServiceImpl/TypeForm.vue | 报了先核出处 |
| 2026-09-15 | Q-009 转出跨能力 mapper 直读收敛定案（计划经 claude+grok 双通道外评修订后用户确认）：①**全 30 边一次收敛、四批串行**（T-ACCESS-043 直换+死边 9 边 → 044 grant 授权事实服务+rule 条件读 6 边 → 045 type/resource/domain 供给读服务 11 边 → 046 user-role 原始行/投影+白名单退役 4 边；计划 capability-mapper-convergence-plan，边清单以「消费类:行号 → mapper#方法」定位——外评证实代理盘点 # 编号与白名单行序两套方案互不自洽，废弃编号）；②**宿主原则**——role/user 主体事实与主体 id 解析 4 边（UserRoleProjectionWriter→AbstractUserMapper、UserManage/BatchAdminUserProjectionWriter→UserRoleMapper、UserMenuQuery→UserRoleQueryMapper）收敛到 engine.core 既有 SubjectDomainService/TypeResolutionService（主体域服务既有宿主，属 §8.4 豁免 1 面，**不建能力包内 DomainService**——防后续按「未收敛」重报）；其余边落被读方能力包内**实体命名 DomainService**（RoleResourcePermissionDomainService/OperationPermissionDomainService/TypeDefinitionDomainService/DomainConfigDomainService/ResourceApiMappingDomainService/ServiceConfigDomainService）；③**新服务硬契约**：只依赖本包 mapper、不注入他能力包 bean（Spring 无环）；无缓存直读直写（同事务可见性语义保持，禁复用 loadRules 类缓存路径）；不声明独立事务（REQUIRED 跟随调用方）；④**包级环以 bean 级无环为准**（grant↔type、grant↔rule、domain→type→grant→domain 服务边成包环，bean 级全部开放；type↔resource 双向为现行既有先例；断言面=mapper 包不变）；⑤外评必改项三条内嵌为批次验收——操作定义读拆**全租户口径 selectAllOperationsByTenant**（保 knownOperationCodes 的 20008/20005 契约区分，弃一刀切 IN——会把「码存在于他类型」从 20008 退化 20005）与 **IN 形态空集短路**（防 IN () 500）；**测试装配适配清单入计划**（claude 实测 26 测试文件位置定参构造，InOrder 级联 verify 改挂新服务不删锁、非 bean ProjectionWriter 经 LocalProjectionDomainServiceImpl 装配改线）；**负向自证终态=测试源集 architecture/fixture 违规样例包 + 专用 ClassFileImporter 导入**（主断言 DO_NOT_INCLUDE_TESTS 不含测试类，白名单清空后无真实边可裁剪）；治理回写锚点=project-rules **§8.2** 白名单指针句 + access-service-architecture「存量 19 类 30 边」句 + capability-structure §8.4/裁决表 row9 + permission-coding-standards §8 | docs/archive/2026-09-15/capability-mapper-convergence-plan.md、docs/archive/2026-09-15/tasks/T-ACCESS-043~046.md（已归档）、QueryBoundaryArchitectureTest | 报了先核出处 |
| 2026-09-15 | Q-009 收敛完成补记（T-ACCESS-043~046 四批全 done）：冻结白名单 19 类 30 边全量收敛至零——批次① 9 边（既有服务直换+死边）、批次② 6 边（RoleResourcePermissionDomainService + PermissionConditionDomainService 三读）、批次③ 11 边（OperationPermissionDomainService 全租户/IN 双口径等五新服务）、批次④ 4 边（SubjectDomainService user_role 原始行层 + ResourceEntityDomainService 投影轨方法）。终态：FROZEN_WHITELIST 清空退役，`checkCapabilityMapperBoundary` 零容忍绝对禁断，负向自证改测试源集夹具（`menu.fixture.BoundaryViolationFixture` + 专用 ClassFileImporter，DO_NOT_INCLUDE_TESTS 双重不干扰）；治理回写 capability-structure §4 row9/§8.4、project-rules §8.2、access-service-architecture、permission-coding-standards §8、design/README 归档行注记同批完成。四批回归证据见各任务卡完成记录，收口全量 `mvn test -T 1C` 1698/0F/0E/0S 含 E2E BUILD SUCCESS（2026-09-15，t046_reactor3.log；首跑失败一次为 t046_reactor2.log，随机端口竞态抖动已隔离定性） | docs/archive/2026-09-15/tasks/T-ACCESS-043~046.md（完成记录，已归档）、QueryBoundaryArchitectureTest、capability-structure §8.4 | 报了先核出处 |
| 2026-09-15 | T-ACCESS-042（Q-001 转出）URL 单命名空间定案（AskUserQuestion 两问，用户自由组合）：①目标形态=**全链路单形态 /api/access/<资源>/**（「1+3，不要有 admin 和 perm，统一为 access」）——前端、控制器、契约册、SDK、path_pattern 注册五处同形，外部路径=服务路径，Gateway /admin、/perm 前缀与 StripPrefix 退役、auth-routes 并入统一路由；example 同规则 /api/example/**，全局规则=/api/<服务命名空间>/** 按第二段路由（弃 A 家族段 /api/admin+/api/perm——家族段保留则心智分裂实质存续；弃 B 仅服务层统一——外部 vs 内部两形态换位存续且需 RewritePath；弃 C 无服务段——SDK 17 端点全量改且失去命名空间归属）；②登录族一并迁入 /api 根：/auth/**→/api/access/auth/**、/oauth2/client→/api/access/oauth2/client（弃维持裸形态——留洞需文档解释且 vite proxy 双前缀并存）。任务内裁决（含实施期收口）：user-role/list 双轨碰撞——管理轨全类型视图改名 /api/access/user-role/view（门禁 USER:VIEW 语义），权限轨持有角色 list 保留（与 assign/revoke/sync 同族读写、SDK 消费）；拦截器边界语义化——InternalApiSecret/HeaderSignature 扩 /api/access/**，**管理面**收紧为仅经 Gateway 或持密服务可达（实现 T-ACCESS-004「所有受控路径」原意），白名单 /auth/**→/api/access/auth/ 会话入口族**精确清单**（captcha/login/login:sms/logout/userinfo/user-menu/oauth2/**——外评 P2 处置：整族 /api/access/auth/** 会罩住运行时鉴权六端点，旧形态它们不匹配 /auth/** 非等价替换，收窄后语义等价且不依赖租户头清洗的偶然防线）；密钥拦截器对会话入口族**精确豁免**（captcha/login/login:sms/logout/userinfo/user-menu/oauth2/**——保留公开/Sa-Token 会话/JWT 自有信任模型，运行时鉴权六端点 check/batch-check/check-interface/query-resources/query-scopes/interface-snapshot 维持密钥覆盖，与迁移前 /api/perm/auth/* 覆盖关系逐端点对应；全豁免会使签名拦截器按 tenant-only 拒绝服务凭证调用，CustomResourceTypeSlicePgIT 实证后收窄）；OAuth2ResourcePathProperties「内部凭证路径重叠」启动防护退役（单命名空间后开放路径必然位于 /api/access/**，合法流量恒经 Gateway 携带密钥、JWT 验证在密钥拦截器后独立执行，双凭证并存不构成机制冲突；会话端点覆盖防护保留）；AdminPermissionValidatorImpl 无用户操作者（SERVICE 上下文/未登录直连）显式 403（原落 StpUtil 环境性异常 500）。外部评审轮（2026-09-15 claude+grok 双通道）：claude P0×1+P2×1+P3×3、grok P0×1（与 claude P0 交叉命中）——P0=本地清扫批子串替换致前端登录三端点双前缀（15 处全修+auth.spec 契约锁）、P2=白名单收窄（本行上文）、P3=快照负向锁收紧+OAuth2 开放路径遮蔽启动告警+存量注释扫尾，全处置；grok 对白名单项判不报（fail-closed 论证成立）与 claude 分歧，主代理亲核裁决采纳收窄。一次性切换不留兼容（未部署零存量；API 资源业务键 METHOD:路径 随路径变=资源身份变，dev 重建库+bootstrap 重种） | docs/archive/2026-09-16/tasks/T-ACCESS-042.md、gateway/src/main/resources/application.yml、BootstrapGraphDefinition、契约总册 §2 | 报了先核出处 |
| 2026-09-16 | release-preview 双轨评审处置（代码轨 P3×6 + 文档轨 P1×2/P2×2/P3×6，逐条代码级核实全成立；两项用户拍板）：**①JWT_SECRET_KEY 短密钥 fail-fast**——OAuth2JwtSupport.validateHmacSecretStrength（≥32 字符=HS256 强度对齐；sa-token-jwt 长度零校验、短密钥静默弱签名）挂 OAuth2AppServiceImpl @PostConstruct，四个短测试密钥同批加长（batch-sql/login-lock/menu-write/tree-cycle），边界锁 31/32 精确两侧（弃只改文档口径——IAM 静默弱签名通道不可留）；**②OAuth2 能力如实口径**——CHANGELOG/README 删「客户端凭证」（全仓零实现、契约 §287 仅 authorization_code）与「密码」（无 token 端点发放，唯一依据是登录链客户端校验含 password 字样），改「授权码 + PKCE / 刷新令牌」（refreshToken 端点实证存在）。直接修复（修法唯一最小）：compose 补 GATEWAY_CORS_ALLOWED_ORIGINS 透传（带 yml 同款默认值——空默认会把未配置误变为禁用 CORS）与 DB_USERNAME/DB_PASSWORD 透传、.dockerignore ×3 补 target/archive-tmp、nginx 反代补 300s 读写超时（100MB 上传超默认 60s 必 504）、CHANGELOG 删 explain（T-PERM-059 已删端点）与「v3」内部版本叙事、quickstart 授权闭环入口修正（权限授予页不进侧栏=角色管理页按钮、菜单名「服务与接口」）、architecture §14.2/capability-structure 两处「仅基建/Q-006 登记不实施」旧口径随批更新。不修（含理由）：evict-only 别名不加运行时防呆（短命过渡物、形态锁+javadoc+skill 三层在案，第二个改名别名出现时再泛化）、FakeRedis/回归锁分布维持（AspectTest 行为锁 + DualInstance store 层能力锁互补非重复） | docs/archive/2026-09-16/tasks/T-ACCESS-047~050.md（完成记录）、OAuth2JwtSupport、docker-compose.yml、CHANGELOG.md | 报了先核出处 |
| 2026-09-16 | release-preview 收口（T-ACCESS-047~050 全 done，计划归档 archive/2026-09-16/）：空环境全栈冒烟 22/22（`down -v` 重建 → bootstrap 空库播种 tenant=1/adminSubjectId=1/apiResources=86/mappings=85/grants=135/menus=14 → 登录 → user-menu → 建角色/用户/绑定 → example 接入五步 service-config/save+sync FULL → 403 → apply-grant-plan API:ACCESS → 0.2s 转 200 含身份回显）；开发模式走查（quickstart 路径 B：手工 mvn spring-boot:run + VITE_PORT=8890 pnpm dev，bootstrap no-op 幂等实证）；最终全量 `mvn test -T 1C` 十模块含 E2E 全绿（八结果段 1706 项 0 失败，t050_full_regress4.log）。**实施期加固两项**：①TaskExecutionLeaseConcurrencyTest.claimBlockedOverMaxAttempts 的 `sleep(1200)` 固定余量改 5s 有界轮询——T-ACCESS-030 已在同类 takeover 用例实证该形态不可靠（同文件 L290 注释点名）、本方法漏改，本批 `-T 1C` 2/3 失败隔离恒绿三实证；②FakeRedis varargs 桩缺陷随 Q-006 用例暴露顺带修复（见 455b2c29e）。版本打点：v0.1.0 定格 commit + 本地 tag（不 push）→ 主线 0.2.0-SNAPSHOT。T-ACCESS-042 终态卡随批次单卡归档 | docs/archive/2026-09-16/（计划+五卡+批次 README）、docs/quickstart.md、docs/ops/deployment.md、CHANGELOG.md | 报了先核出处 |
| 2026-09-16 | release-preview-plan 立项三定调（AskUserQuestion 三问，任务板空档选向后用户拍板）：**首个发布预览批次 = 全栈 compose profile + v0.1.0 + 文档全套**——①部署形态=compose 新增 `--profile app`（gateway/access-service/example-service/frontend 四服务镜像化，默认 `docker compose up` 仍只起基建，T-ACCESS-020「仅承诺基建」口径对默认行为不变；弃仅基建+文档化手工启动——「一键全栈」目标不达成）；②版本化=**v0.1.0 预览版**（tag 定格 pom 0.1.0、主线回 0.2.0-SNAPSHOT；弃 v1.0.0——未交付清单挂账+DDL 无迁移路径下 GA 语义失真；弃暂不打 tag——外部使用者无版本锚点）；③文档面=全套（quickstart/deployment 基线/CHANGELOG/frontend README/根 README 分层），**根 LICENSE 重写为本项目名义**（用户口径：项目已与 RuoYi 无关；frontend/ 的 pure-admin-thin 上游署名保留处理在 T-ACCESS-049 任务卡单列执行期复核）。顺带项：Q-006 双命名空间失效转出实施（T-ACCESS-048）、T-ACCESS-042 终态卡归档（T-ACCESS-050）。非目标：不建发布流水线、不引入 jib/flatten、不做 DDL 迁移框架、暂缓卡不动 | docs/archive/2026-09-16/release-preview-plan.md（已归档）、docs/archive/2026-09-16/tasks/T-ACCESS-047~050.md、docker-compose.yml | 报了先核出处 |
| 2026-09-17 | Q-007 资源域跨类型父子边收紧三定案（T-PERM-068，AskUserQuestion 三问用户拍板；角色域 ORG/POSITION 容器树为结构性跨类型、不在范围）：**①sync/full-sync 父边收紧同类型**——parentResourceTypeCode 显式异类型 → item 级 NON_RETRYABLE/PARENT_TYPE_MISMATCH（先于父解析与版本写入，码比对足够：code↔value 双射）；弃维持合法+文档化（无已知合法用例，类型树查询下跨类型子节点不可见，真有容器树需求再放开比事后收紧容易）。**②管理面 create/batch-create 对齐 move（20053）**——业务键父轨跨类型拒绝、单条 create 裸 parentId 补存在性（20004）+类型校验（此前零校验可落悬挂引用）；batch-create 跨类型项走既有宽容收集跳过。**③sync 父字段缺省=同类型回填**——parentResourceTypeCode 缺省按 item/scope 类型解析（半传 parentResourceCode 也挂父，兑现契约 §19.2 原意与角色域 full-sync effectiveParentTypeCode 先例；旧实现半传静默解挂是契约-实现漂移）；弃成对严格（同步同类型树每条 item 须全传四字段）；管理面 create 父业务键半传维持「无父造根」（与 sync 有意不同：交互式 API 半传视为前端缺陷）。判定面闭包止步同类型（T-PERM-057）与 remove 跨类型级联守卫维持，作 DB 直写脏数据防线 | docs/archive/2026-09-17/tasks/T-PERM-068.md、契约总册 §19.1/§19.2/§12.1、engine/implementation.md §3 遗留节、extension-guide §3.4 | 报了先核出处 |
| 2026-09-18 | T-PERM-068 外评处置（claude/grok 双通道同提示词，各 P2×1+P3×1，逐条代码级核实全成立；claude P2×1 修法二选一经用户拍板、grok P2×1 修法唯一直接修、共同 P3 runbook 直接修）：**①父字段组仅 UPSERT 生效（用户拍板，claude P2 处置）**——DISABLE/DELETE 忽略父字段（删/停不被父资源存否绑架；「只传 code」形态的删/停改前可执行、定案③后曾被父缺失阻塞为新失败面；对齐角色域 OP_UPSERT 守卫先例）；弃全 operation 校验+文档化（删/停可被无关父状态永久阻塞，调度器重发修不好）。**②DELETE 有子拒绝（用户拍板，外评追问引出）**——单条 DELETE 存在有效后代（含脏数据跨类型子，保守阻塞）→ DEPENDENCY_MISSING/CHILDREN_EXIST 先于版本写入（乱序删除窗口短退避自愈：先删子后重发父即通过，子永不悬挂）；弃级联删（单条=单行事实原则，连带删需补跨来源守卫复杂度高）；弃维持单行删不查子（幽灵子节点+授权残留等 full-sync 自愈）；full-sync 差异校准维持 scope 逐行口径不查子。**③解挂落列修复（grok P2，直接修）**——全不传父字段 UPSERT 更新已存在行改 UpdateEntity 显式清 parent 列（flex update(entity) 忽略 null 列——applied=true 且版本已推进、旧父边残留 fail-open、同版本重发被 STALE 挡；moveResource/投影 upsertResource 同款先例）。**④runbook-full-sync 同步（共同 P3，直接修）**——§3 步骤与 §4 重试表对齐新语义（原指导「异类型父显式传 parentResourceTypeCode」自收紧起恒拒）+ CHANGELOG [Unreleased] 补收紧型破坏变更条目。回归锁 +4（DISABLE 忽略父字段/DELETE 有子拒绝不推进版本/DELETE 无子照常删/解挂 updates map 含 parentId=null——UpdateWrapper 先例写法） | 契约总册 §19.1（last_reviewed 2026-09-18）、docs/ops/runbook-full-sync.md §3/§4、CHANGELOG.md [Unreleased]、ResourceEntitySyncAppServiceTest 外评处置节 | 报了先核出处 |
| 2026-09-18 | Q-008 收敛定案（T-PERM-069，AskUserQuestion 用户拍板「仅 API 收紧」，SERVICE/API 分治）：**①API 类型种子声明 SYNC+access-service**——API 资源行唯一事实入口=service-config/sync 接口声明通道（契约 §19.8，领域直写不经管理面门禁）+bootstrap 固定图种子，管理面资源 CRUD 一律 20055（enforcement 堵手工 MANUAL 行口径外通道——FULL diff 清理与服务删除级联均只覆盖 SERVICE_SYNC 行，MANUAL 行成永久孤儿）；保存边界 API 硬拒保留（理据改「种子钉死」）、20055 内部来源文案补「服务接口同步 service-config/sync」入口；回归锁=真实 DDL 驱动 PgIT 双向锁（旧种子下实证失败）+SERVICE 放行对照。**②SERVICE 维持 MANAGED**——代码级盘点实证新 SERVICE 行唯一通道=管理面手工建行（bootstrap 只种 access-service 一行、service-config save/sync 均不产 SERVICE 资源行），该行是按服务实例级授权（如 SERVICE:SYNC_INTERFACE@某服务）的目标对象，收紧即零 writer 死局；未来重启评估须以「service-config 联动 upsert SERVICE 行」配套事实链为前置。存量核查：dev 库 86 行 MANUAL 全为 bootstrap 固定图清单（零口径外野行，目标接口 my-info 无映射为设计预期）；存量库订正 UPDATE 与野行检测语句登记 rebuild-runbook。双轨本地评审 P1×3/P2×5/P3×5 全处置（extension-guide §2 残留句、§6.3 死锚点簇 6 处本批新增+5 处历史、architecture L144 增补句、runbook 登记）；全量 `mvn test -T 1C` 含 E2E 1721 项 0 失败 | docs/archive/2026-09-18/tasks/T-PERM-069.md、契约总册 §5/§13/§19.1/§19.8、schema（种子+注释）、ResourceTypeOwnershipGuard、ServiceConfigCascadePgIT、AGENTS.md 权限中心提醒 | 报了先核出处 |
| 2026-09-18 | T-PERM-069 外评处置（claude/grok 双通道同提示词，均 P0-P2=0、P3×2，逐条代码级核实全成立——去重后 3 条独立发现全采纳）：**①API 种子 extra 等值回写误报 20044（grok P3，代码修）**——validateExtraDeclaration 的 API 硬拒原置于内部来源豁免之前且无条件，种子落地后 get-then-update 原样回传 extra 必 20044（主 UI 对 is_system 行不渲染编辑故 P3；直调 HTTP/脚本会打中），与 USER 系统类型「同声明重复提交放行」先例（shouldPinSystemTypeDeclarationButAllowUnchangedResubmissionAndFieldEdits）分叉且错误码语义错位（20044 参数错 vs 20056 声明变更钉死）——修法=硬拒收窄为「SYNC 且来源非 access-service」（种子等值回写落内部来源豁免，有效变更仍由 is_system 20056 钉死；create 形态由「内部来源仅系统预置类型可声明」拒绝），回归锁 +3（GuardTest 等值回写放行/create 形态拒绝 + TypeDefinitionAppServiceImplTest API 回写全链放行）；**②契约 §12.1 20055 message 文案分叉（claude P3，文档修）**——该行只记载外部来源默认文案，内部来源分支文案（T-PERM-052 起七类型、T-PERM-069 起 API 在用）未入契约且 API 未入行；本批任务卡曾声称「§12 已改」实未改（本地双轨评审「天然覆盖」结论漏文案面）——修法=§12.1 行改写为双分支 message 并登记 API 入列；**③runbook 野行检测 SQL 结构性漏检（claude+grok 同发现，文档修）**——`owner_service_code='access-service'` 谓词检不到管理面手工行（create/batch-create 只写 maintain_source 不写 owner，列可空），实际只命中 bootstrap 无映射行（恰为自注的预期命中）——修法=改按固定图行名恒 `bootstrap:` 前缀排除（86 行实测全覆盖、手工行无论 owner/映射形态全命中、改名历史行假阳性宁可多报），dev 库实测零命中。存量观察顺手处置 4 处注释（ResourceEntitySyncAppServiceImpl/ResourceManageAppServiceImpl 事实链路清单补 API、ResourceManageAppServiceImplTest「API 为非保留类型」现在时残留改写、ResourceSyncHandlerImpl §6.3 死锚→§19.8）；PgIT 锁补 errorCode==20055 断言。存量不处置：前端设计册死锚点簇（service-interface-mapping §6.10.4/permission-grant §6.3·§6.10.3——旧册编号考据无把握宁缺毋错；admin-service-api-contract §6.2.2.4 系 superseded 册豁免）。定向回归：单测 107 + 容器 12 项 0 失败 | 契约总册 §12.1、ResourceTypeOwnershipGuard（API 硬拒收窄）、rebuild-runbook（野行检测订正）、ResourceTypeOwnershipGuardTest/TypeDefinitionAppServiceImplTest/ServiceConfigCascadePgIT | 报了先核出处 |
| 2026-09-18 | T-ACCESS-051 外评处置（claude/grok 双通道同提示词，claude P0-P2=0、P3×2，grok 全零+存量观察 4 条；去重后 3 条独立发现逐条代码级核实全成立、全采纳直接修）：**①循环注释谓词失实（claude P3）**——takeoverReexecutesWithSameIdempotencyKey 注释「selectRetryable 仅选 lease_until < now()」漏写 `OR status='FAILED'` 分支（未超限 FAILED 行被重复扫描会按至少一次语义重跑——本用例 bean 恒成功不触发，但注释作为「重复扫描恒无副作用」论据会误导排查方向）——补全双分支谓词；**②Q-014 证据数据失实（claude+grok 交叉命中）**——登记行「2s idle 窗口/余量 800ms/sleep×4」与代码不符：sa-token 整秒除法口径下 idle 翻转阈值实测 ≈4s（剩余 <= -2 才冻结）、网关冻结阈值按 3.5s 设计、AuthTokenFilterTest 为 4 次调用 3 次 sleep——改写真实阈值；提问时余量数据低估约一倍，但 AbsoluteTimeout 用例累计拉伸预算 ≈400ms（与 Q-013 同量级）支撑登记必要性，登记拍板维持；**③docs/README.md 归档记录表缺 2026-09-17/09-18 两行（grok 发现，存量+本批各一）**——该页 L152 自载「归档后需更新本页归档记录表」，T-PERM-068/069 归档时漏更、本批次二亦未补——两行同批补齐。claude 存量观察两条自判不计发现（Q 登记拍板不入 registry 属既有惯例——队列动作非定案；看板图例 — 档缺项为十余行存量先例一致）。处置后 git status 干净（双通道均 read-only） | access-service/.../TaskExecutionLeaseConcurrencyTest.java（注释）、docs/pending-problems.md（Q-014）、docs/README.md（归档记录表） | 报了先核出处 |
| 2026-09-19 | 前端会话/交互外评处置立项（基线 commit 5495d2ffd、距今 6 提交前端零改动；14 项结论逐条代码级核实：**成立 12、降级 3**——守卫双 next 当前触发面为零（全仓路由 meta 零 roles + VITE_HIDE_HOME=false，biz-domain.md:223 佐证）、token 过期放行系 T-FE-041 注释明文口径、路由可达系 T-PERM-037 定案口径；**评审半误报 1**：「拖拽改权限继承」运行时方向错误（T-PERM-057 auth-check 判定面继承默认关，运行时无角色继承）但管理面影响真实——拖拽经 ROLE 资源投影父链镜像（LocalProjectionDomainServiceImpl.upsertRoleResource 369-376）改变实例级管理覆盖（RoleManageAppServiceImpl:216/272 实例级 MANAGE，forValidate 闭包默认开）与 remove 级联范围；**评审遗漏 1**：T-PERM-067/066 已备自助改密闭环前提（/user/reset-password 自身通道+自身改密置 false））。四定案（AskUserQuestion 四问，第四问经两轮引擎事实修正；codex sol 复评后第四项改判）：**①强制改密=自助改密页+阻断**——forceResetPwd=true 只放行改密页、改密成功（后端自身置 false）后进系统；弃非阻断（「force」名不副实、初始密码弱凭证长期存续）与仅修文案（缺口留存）；T-ADMIN-022 非阻断口径退役。**②会话权限热刷新=403 触发自动刷新一次+顶栏手动入口**——统一「会话能力刷新入口」（权限串+侧栏+路由门禁 path 集原子更新，四调用方共用）；不自动重放请求防循环、授予页重试先刷权限串；弃轮询（延迟 N 分钟+常驻请求量）与服务端推送（需后端新事件机制，长期项）；机制落被授权人会话侧（管理员浏览器内刷新帮不了被授权人——外评修法盲点）。**③路由级 UX 门禁=后端 menus 派生+显式动作路由映射**——门禁集合=menus 树 path ∪ 非菜单业务路由显式映射（/perm/grant showLink:false 不进菜单，纯 menus 白名单会封死授权页——codex sol P1 采纳）；守卫状态机 uninitialized/loading/loaded/failed，首导航等待初始化（防冷启动深链绕过——现状 initRouter 后仅 to.name 空才重导航）、仅明确 failed 放行（fail-open 不锁死）；T-PERM-037「路由可达、后端 403 兜底」口径演进（当时弃 meta.auths 的「前端静态声明可绕过」理由不适用于后端派生 menus）。**④高影响操作确认=跨层级拖拽+角色禁用都加确认**（初判「仅禁用加确认」、拖拽维持直接生效；codex sol 复评证伪其依据「实例级角色授权当前无人用」——该判断混淆数据状态与机制约束：授权页类型候选=全部资源类型含 ROLE、GrantDialog 支持 INSTANCE 多选、update/move 实例级 MANAGE 闭包默认开，实例级委托**现役可配无约束禁止**（dev 库零存量行仅是当前数据状态）；给委托管理员授父角色实例 ROLE:MANAGE 后，拖入/拖出子角色立即改变其可管理集合与级联删除范围——用户改判）；仅 parentId 变化的拖拽（角色树+组织树 inner/跨父）加确认，同级排序直接生效；禁用确认文案含「全部持有者立即失去该角色权限」。codex sol 复评其余全采纳：强制改密标记跨标签共享持久化（sessionStorage 单标签漏失即绕过、userinfo 不含 forceResetPwd、DataInfo 无 userId——补登录主体绑定持久化）、列表顺扫补 permission-condition 页、PositionTab 系正确实现非同款 bug（主代理自误报撤回——其递归返回 null）、menus 空态用例改标安全/特征锁（非 old-fail）、T-FE-051/056 design_refs 补全；部分不采纳：任务卡/registry 引「外评 P1/用户拍板」系定案登记与背景溯源形态（registry 写入协议与全表先例一致，project-rules 禁的是轮次标记词），看板行超「标题简短」边界按规则压缩。**⑤Gateway 白名单纳入 /user/reset-password（claude 外评 P1 触发，AskUserQuestion 用户拍板）**——该端点不在 Gateway 会话入口族白名单（application.yml 精确清单），PermissionFilter 快照条目仅由 API 类型 ACCESS 位派生、实例授权只种子给管理用功能角色（绑首管理员），普通用户（forceResetPwd 阻断目标人群）经 Gateway 必 403=阻断落地即锁死；定案=白名单纳入（与 logout/userinfo/user-menu 同族——服务层门禁不变：Sa-Token 登录校验+自身路径豁免/非自身须 USER:RESET_PASSWORD，T-PERM-067 语义零改动；弃创建用户时联动授 API 实例 ACCESS——每用户一行授权膨胀且漏配仍锁死；弃先验证再定——多一轮往返），T-GW-009 立项为 T-FE-046 前置，契约与 T-ACCESS-042 白名单清单同步。claude 外评其余处置：T-FE-056 公共白名单改 remaining.ts 全量自动纳入（漏 /redirect/:path 会拦死标签刷新）+门禁状态迁移表钉死（failed 仅首载产生、刷新失败维持 loaded——消除与 T-FE-048 语义互斥）+design_refs 补 system-config 与各页权限表效应格清扫；T-FE-045 注销请求显式携 token（过期分支免头路径注销空转）+防补「进行中短路/完成后零请求」锁；T-FE-051 现状勘误（十页均已有 catch+message、代际已有三处先例——真正缺口=用户页无 catch+八页无代际）+非 2xx 错误文案统一消费后端 body；任务卡过程叙事清扫（registry 2026-09-13「registry 为过程唯一载体」定案对齐——删「外评修正已采纳」段改链本行）；外评原文计数勘误 15→14 项 | docs/plans/frontend-session-consistency-plan.md、docs/tasks/T-FE-045.md、docs/tasks/T-FE-046.md、docs/tasks/T-FE-048.md、docs/tasks/T-FE-051.md、docs/tasks/T-FE-056.md | 报了先核出处 |
| 2026-09-19 | T-GW-009 claude 外评处置（模型 astron-code-latest；P0-P2=0、P3×2，逐条代码级核实全成立、全采纳）：**①P3-1 密钥豁免族第六载体漏改**——`SecurityWebMvcConfig` 类级 javadoc 枚举未随批更新（同文件内联注释已更、两处互斥）——去枚举化改指 excludePathPatterns 精确清单（正是本批 P0 的「清单漏同步」成因形态，防再漏）；**②P3-2 白名单化端点固定图死授权面**（`BootstrapGraphDefinition` reset-password API 行 withMapping=true——ACCESS 位对 Gateway skipAuth 路径不再生效、授权页对它授予/收回不影响可达性、双向误导）——用户拍板「**保行+标注失效**」（弃移除：保留白名单回滚面（去白名单条目即恢复 Gateway 快照强制）、不动固定图与 runbook 种子计数（API 资源 86/映射 85））：行内注释+gateway.md 白名单行标注「端点边界=服务层门禁」；**锁补强×2**（评审 d/e 节建议采纳）：GatewayApplicationConfigTest 补 `new GatewayProperties()` 默认清单断言（yml/默认值/密钥豁免三载体漂移回归锁）+ SecurityMatrixIT 补「非豁免 user 端点无密钥 403」反向锁（豁免不被加宽为 /api/access/user/**）；**Q-015 清扫面扩两处**（用户拍板并入）：access-service-architecture.md:212「豁免 oauth2/**」半句 + SignatureEnrichFilter.java:31/192 注释与执行序不符（order=-35 晚于 InternalSecret(-40)，无运行时影响）；评审「验证留痕不可复核」观察半数勘误——gateway 汇总 89 项系 surefire @Nested 分桶外层 0 计报告误读（TEST-*.xml 实为 106）、SecurityMatrixIT FAILURE 报告系红跑验证留痕（处置后复跑 14/14 绿覆盖）。定向复跑：SecurityMatrixIT 14/14 + GatewayApplicationConfigTest 7/7 绿 | SecurityWebMvcConfig、BootstrapGraphDefinition、GatewayApplicationConfigTest、SecurityMatrixIT、gateway.md、docs/pending-problems.md（Q-015） | 报了先核出处 |
| 2026-09-19 | T-FE-045 claude 外评处置（模型 astron-code-latest；P0-P1=0、P2×1、P3×1 + 存量观察×5，主代理逐条代码级核实全成立）：**①P2「本地过期路径服务端注销不可达」成立**（全链亲核：`getToken()` cookie 缺失兜底 localStorage，而 `setUserKey` 七字段不含 accessToken；cookie Expires≈本地 expires 同刻，拦截器过期分支触发时 cookie 已被浏览器清除 → `token?.accessToken` 恒假、注销静默跳过；服务端 `RequestContextInterceptor` logout 匿名放行但无 token 无法定位会话）——**用户拍板维持现状不修**：修法（setUserKey 增写 accessToken）会把令牌存续从 cookie 生命周期（随 expires 自毁）延长到 localStorage 生命周期（躺到 removeToken/清 storage），扩大凭证存续面反而增加风险；正常路径 cookie 与会话同生命周期（点退出时 cookie 在、token 在，主路径真注销已通），出事仅 cookie 被清/本地过期小众路径且残留会话有 Sa-Token 2h 绝对 TTL 兜底；任务卡验收括注与 login.md 表述按现状保留、偏差已知悉接受。同根因存量观察两条（getToken 类型注解可返 null 与实现不符、cookie 缺失路径发 `Bearer undefined`）随本拍板一并维持现状。**②P3「本地清理被 await 注销请求推迟」成立**（后端黑洞时最长挂 10s 无反馈+连点被防抖短路；窗口内新登录会被旧清理链清掉凭据，可重登恢复；服务端健康路径几百毫秒无感）——**用户拍板登记 Q-016 不修**（候选方向：注销改 fire-and-forget 或令牌代际守卫，可并入 T-FE-054 相邻面）。③其余存量观察×3 维持现状不处置（logOut 清理未含 avatar/nickname 再登录即重置、路由守卫 removeToken 不清 Pinia 属既有形态、mock/login.ts 缺 /logout 为 VITE_MOCK_LOGIN 默认 false 的 dev-only 面）。④专项清单七条执行结果抽查无失实（logout 匿名放行 L434、白名单唯一消费点、api/auth 未新增循环边、样式提交去空白 token 全等核对零语义）；主路径真注销链路评审确认连通（显式 Bearer → Gateway 白名单原样透传 → 匿名放行 → StpUtil.logout 立即失效）。⑤样式提交信息勘误 amend（87b87ed40→02356e7e7：config/index.vue 内容 diff 为空未入提交，用户拍板 amend） | frontend/src/store/modules/user.ts、frontend/src/utils/auth.ts、docs/pending-problems.md（Q-016）、87b87ed40（amend） | 报了先核出处 |
| 2026-09-19 | T-FE-047 claude 外评处置（模型 deepseek-flash[1m]——DeepSeek anthropic 代理通道，settings 漂移自上轮 astron-code-latest；P0-P2=0、P3×1 + 存量观察×2，主代理逐条代码级核实全成立；变更主体确认弹窗+失败反馈+三 old-fail 锁两轮核查**未发现生产级缺陷**）：**①P3 新增 spec 的 mock 跨用例泄漏**——`vi.clearAllMocks()` 只清调用记录不清 implementation（@vitest/spy 源码 mockClear vs mockReset 实证；vitest.config 未配 clearMocks/resetMocks），用例 3 的 `mockDeleteUser.mockRejectedValue` 会泄漏给其后追加的用例（T-FE-051 acceptance 明确将扩写本 hook.spec——届时删除成功用例吃到泄漏 rejection 进 catch 分支、红灯指向错误方向）；当前用例 3 恰为文件末位故无影响——**用户拍板直接修**：beforeEach 逐 mock 重设默认实现（补 mockDeleteUser.mockResolvedValue(undefined)，附 mockClear/mockReset 语义注释防回退）。**②存量观察① index.vue 死解构+组织点击双请求 → 登记Q-017**（useUserManage 双实例：index.vue 解构 12 项只用 3 项、每次点组织两边各发一次 /user/page 其中一次响应无人消费；关联 T-FE-050/051 同文件串行顺带收敛）。**③存量观察② 启停失败不透后端 message → 写入 T-FE-051 卡**（acceptance #2 顺带收口清单扩入 handleToggleStatus——MemberTab 启停 catch 只显通用文案，与删除路径新形态同页双标准）。④专项清单五条执行结果抽查无失实：handleDelete 调用方全集=MemberTab 两入口（index.vue 解构为死引用）、成功分支三句与旧实现逐字等价、mock 同形性对 element-plus message-box 源码级取证（取消 reject("cancel")；name 经模板字符串进 confirm 无 XSS 面——dangerouslyUseHTMLString 默认 false 走 textContent）、红跑证据链三用例旧实现下首断言即红（且反向约束「先删后弹确认」错序实现）、越界检查 diff 仅 hook.ts+spec+3 文档。复跑：hook.spec 3/3 绿 | frontend/src/views/system/user/utils/hook.spec.ts、docs/pending-problems.md（Q-017）、docs/tasks/T-FE-051.md | 报了先核出处 |

## 已推翻（superseded）

| 原口径 | 出处 | 被取代 |
|---|---|---|
| T-PERM-040「includeGlobalFallback 合并参数 + 专属优先/全局回退」合并语义 | docs/tasks/T-PERM-040.md 退役注记 | 2026-08-30 全局操作概念退役（T-PERM-049） |
| T-PERM-037 投影轨条目（随全局操作键轨） | docs/tasks/T-PERM-037.md 失效注记 | 同上 |
| T-ACCESS-018「取消类型级保留」行级口径 | docs/design/access-service-architecture.md（T-PERM-052 取代注记） | 2026-09-05 资源类型级所有权（T-PERM-052） |
| 2026-09-06「SDK 直连端点内部 id 全线裁剪（T-API-002）」的 **check 族追加部分**（check/batch-check/check-interface 响应的 matchedRoleIds/matchedPermissionIds 与 matchedResources[].resourceId 裁剪）——**部分推翻**：原定案 Query\* 六字段裁剪仍有效，check 族三端点恢复全量回传 | docs/tasks/T-API-002.md（历史）、api-contract.md §5.7 注记 + §6.1/§6.2/§6.6（随 T-API-003 回写） | 2026-09-09 check 族三端点全量回传定案（T-API-003，见同日登记行） |
