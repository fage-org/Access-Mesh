---
doc_type: task
id: T-FE-001
title: 跨页组件抽象池（清单维护 + 派生子任务）
status: done
plan: docs/archive/2026-07-12/frontend-phase1-plan.md
domain: frontend
design_refs:
  - docs/design/frontend/README.md
depends_on: []
blocks: []
acceptance:
  - "维护 frontend-phase1-plan.md §跨页组件抽象池 清单的确认状态"
  - "每个候选组件在 2+ 页面推进时确认是否抽取；确认抽取后派生独立 T-FE 子任务（权限条件选择器/资源树选择器/角色选择器/Diff 对比面板）"
  - "抽取的组件使用 Re 前缀命名（如 RePermCondition、ReResourceTreePicker）"
  - "组件抽取遵守 improvement-plan §4.1 组件抽象原则：先确认再抽象、2+ 页面才抽取、不过度抽象"
design_writeback:
  required: false
  status: done
last_updated: 2026-07-12
---

# T-FE-001 跨页组件抽象池

> 状态：done
> 作用：维护 Phase 1 跨页组件抽象池清单，在页面推进到 2+ 复用时派生子任务。不直接实现组件。

## 背景

improvement-plan §4.1 Step 1.5 要求可复用组件抽取必须先与用户确认。本任务作为清单维护中枢，避免每个页面任务内嵌组件决策。ReOrgTreePanel 已在 2.1 确认并实现，不纳入。

## 候选清单

见 `docs/plans/frontend-phase1-plan.md` §跨页组件抽象池。

## 工作方式

1. 各页面任务（T-FE-002~014）在 Step 1.5 识别可复用组件时，回写本任务的清单确认状态
2. 当某候选在 2+ 页面确认复用时，本任务派生独立 T-FE 子任务实现该组件
3. 派生的组件任务被相关页面任务 depends_on

## 验收标准

- 清单确认状态与各页面任务实际推进一致
- 派生的组件任务命名遵循 Re 前缀
- 不在未达 2+ 页面时提前抽取

## 完成记录

### 2026-07-12 汇总确认（Phase 1 十三页完成后）

13 页前端任务（T-FE-002~014）全部 done，各页设计回写已登记组件识别结论。本任务汇总确认如下：

**✅ 确认抽取（派生子任务）：**

| 候选 | 使用页 | 派生子任务 |
|---|---|---|
| 权限条件选择器 + 规则编辑器 | T-FE-009 条件管理 / T-FE-014 附加设置 | T-FE-024（ReConditionPicker + ReConditionEditor，归 frontend-phase4） |

**📋 登记数据适配层，不抽整块 UI（页面语义差异大）：**

| 候选 | 使用页 | 结论 |
|---|---|---|
| 角色树 | T-FE-002 父角色单选 / T-FE-014 五类型+虚拟根+搜索+懒加载 | 语义差异大，只登记数据适配层 |
| 资源树 | T-FE-008 父资源选择 / T-FE-013/014 权限树表（融合动态操作列） | 同上 |

**📋 待 Phase 4 评估（达 2+ 页但各页已稳定实现，抽取是重构）：**

| 候选 | 使用页 | 说明 |
|---|---|---|
| 分页表格 hook | T-FE-003/004/005/012（4 页） | 同范式，可抽 ReTableHook，但 4 页已稳定，留 Phase 4 评估价值 |
| JSON 编辑校验 | T-FE-004/006/009（3 页 JSONB 字段） | 可抽 ReJsonEditor，组件简单低优先级 |

**❌ 不抽取：**

| 候选 | 原因 |
|---|---|
| DiffSnapshotPanel | 仅 T-FE-012 内联，T-FE-005 未扩展 diffSnapshot；待 T-FE-005 扩展后达 2 页再抽 ReDiffViewer |
| 详情抽屉 | T-FE-005/012/013 模式差异（全字段 vs diff vs 拒绝原因） |
| 资源键输入 / SubjectInputBar | T-FE-013 单页 Tab 内复用，未跨页 |
| typeKey 下拉 / 依赖图 / 服务选择器 / 主从 hook | 单页或模式独特 |

**派生决策**：经与用户确认（2026-07-12），只派生 T-FE-024（ReConditionPicker + ReConditionEditor，唯一被 T-FE-014 §15.4 明确确认）；ReTableHook/ReJsonEditor 登记「待 Phase 4 评估」不立即建档，避免 Phase 1 收尾膨胀（遵循「不过度抽象」原则）。

**Phase 1 归档条件达成**：13 页 T-FE done + API 核对清单产出 + 组件池确认状态更新。关联 [[frontend-phase-tasks-decomposition]]。
