# T-ACCESS-064 验证记录

执行日期：2026-09-26。范围为文档治理、规范归位、历史引用与一处测试 Javadoc 引用；无业务或测试行为改动。

## 基线与增量

方案基线 `c0604ad417964fff9577efd3884568f936f7a7ef`，实施/最终核对基线 `f8dee10f9408ee1f78cebfed5a00fc1be8b3582d`。切换前已逐行核对旧入口：相对实施基线唯一新增为本次用户采纳记录，原条目无改删；历史册无新增/改删。该授权已归本任务与 project-rules，原文见 decision-approval.md。

方案基线之后四个提交已核：T-PERM-095 当前行为、基线 tag、083+095 全量完成均已归位；200字计数单位仅作为被替代旧协议留原文。最终工作树修改为本批归位及评审事实修正，源快照哈希始终不变；没有未经核对的新决定。来源映射逐条定位压缩前原文，完整历史行逐字匹配，压缩留存顺序与日期匹配；结构检查不替代人工语义审查。

## 文档检查

最终执行结果（2026-09-26）：

- 原文 SHA-256、字节数及 `git show <commit>:<path>` 三重核验：3份快照全部一致；用户方案与附件原文逐字节一致。
- 一次性来源定位：218个两册数据/已推翻条目均有来源及语义对照，未核对项为零；条目数只证明结构覆盖，语义另由两轨逐项审查。
- 技能双副本：4组逐字节相同。
- 临时 Python 本地链接/章节检查：351处，失败0；检查新增/修改行及新证据文件，冻结原文不改写。
- `git diff --check`：通过；最终归档后再次核验链接通过。
- 活跃旧消费/写入指令专项检索：零残留；另按“回写/登记+历史快照”复核未来写入语义，不能仅以registry字面零命中代替。
- Java 精确差分仅一处 Javadoc 来源；SQL 去行注释内容相同。定案入口 3660 bytes，只含使用说明/主题路由/历史追溯。

一次性校验脚本运行于临时目录，不安装常驻工具或CI任务。变更文件最终哈希见 [工作树清单](decision-output-manifest.json)；该清单用于此次增量审计，不作为后续维护服务。

## 文本场景验收

| 场景 | 核对结果 |
|---|---|
| 重提全局操作回退/源码扫描守卫 | 类型隔离和业务键当前规则可定位；相同前提不重开 |
| 历史已修问题再次发生 | 新评审协议明确按当前证据评估，不以已修撤回 |
| 单条审计摘要例外扩到批量 | R2 §6.1明确排除批量及已经迁移的getDenied |
| INLINE绝对1:1被部分取代 | 契约/实现/前端表达一个MANUAL属主、系统派生可引用；用户仍不能显式共享 |
| ROLE_MUTEX旧引擎归属/算法演进 | 当前判定统一入口及R2 S/H/D分开，旧不归引擎口径不复活 |
| 骨架已采纳但未接入生产 | R2 §4.1保留零bean/零消费者/UOE及后续验收范围 |
| 关联任务done但窗口未证实关闭 | 条件删除/类型投影剩余窗口按真实路径保留，已共锁资源路径解除 |
| 083全量让渡095 | 095证据确认已兑现，不再作为未完成例外 |
| 会话记录中独有决定 | Locale、树锁等待等有当前章节和冻结来源，未补造原理由 |
| 历史任务归档 | 063原完成事实保留；当前规则仍可定位，064证据同批归档 |
| 切换期间有新决定 | 基线差分与未核对增量清零为硬门槛，变化须重核；回退保留新决定 |
| 新增无关历史 | 入口按稳定主题路由，不增逐案摘要或默认必读历史 |
| 两轨独立获取当前范围 | 共用f8dee10f9到工作树对象，各自扩展读取当前规范及必要原文 |

## 双轨本地评审

两轨均完成只读复核，遗留 P0/P1/P2/P3 均为零，存疑决策与过度设计可裁剪项均为零。已核实的最小事实修正直接完成：未来写入指令从冻结快照改到当前规范；新增有效边界从 draft IAM 稿迁至 adopted 文档；MockMvc夹具恢复白名单自服务限定；mock注册表名、会话终结方法职责和任务号校准。无需新增设计选择，未请求重复批准。

## 验证边界

原始历史快照及用户方案中的旧协议、旧路径、措辞和原始空白有意冻结，不作为活指令，也不因现代链接/文风规则改写。只验证本批引入或修改的当前链接，不承诺修复全仓历史链接。没有运行 Maven/前端全量业务测试；Java仅Javadoc引用、SQL仅来源注释，去注释内容相等检查验证其行为面未改动。
