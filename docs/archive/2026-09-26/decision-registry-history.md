# 定案登记表——历史册（append-only）

> T-ACCESS-063（2026-09-26）双册分层产物：本册只增不改、不整读——评审存疑结论用 `rg <关键词>` 检索本册再定性。迁入行**原文不改写**（沿「历史行不改写」纪律）；完整历史同时以 git 历史为备份。
>
> 迁入判据：载体已全部归档（任务卡/计划已迁 docs/archive/**）或内容已被现行设计文档吸收；常青口径行永留现行册，不入本册。首迁 2026-09-26（30 行）。

| 日期 | 定案口径 | 出处锚点 | 评审处置 |
|---|---|---|---|
| 2026-09-10 | T-PERM-059 权限视图/排查删除重设计三项定案（任务内逐项拍板）：①删除边界=全删 8 端点（permission-view 七端点 effective-permissions/resource-users/role-permissions/effective-roles/resource-tree/explain/recent-changes + /auth/query-permission-tree），explain 不保留（codex 四轮外评登记的「USER INSTANCE 候选明细完整性」遗留随之消亡不修），登录串 effective-permission-codes 与运行时 query-scopes 不在删除面；②前端面=页面目录+api/permission-query.ts+api/perm-scope.ts 一并删除，T-FE-043 cancel（重做考虑事项随卡归档、新形态另立任务）；③分期收口=本卡只做删除（新设计方向基于统一引擎结果模型的产出另立任务，需 grill 级产品讨论）。连动面：bootstrap 固定图删 effective-permissions/explain 两行（query-scopes 行保留维持空库 API 映射种子）+「权限排查」菜单种子删；perm-common SDK 三 DTO 副本（UserPermissionViewReq/UserPermissionViewResp/PermissionEffectivePermissionsResp）与 PermissionFeignClient.getEffectivePermissions 同批删除；PermissionConditionDomainService.evaluateDetailed/PermissionConflictDomainService.filterPermMutexWithDrops（explain 专属）删除，STATUS 常量与 ItemDetail 迁入实现类私有载体；④收尾两项（用户拍板）：check-interface `matchedResources[].resourceTypeCode` 契约示例改 null（实现恒 null——T-API-003 行挂靠本卡的示例漂移收口，Gateway 专用端点无人读该字段）；permission-query-unification 计划随本卡归档 + query-engine-unification.md 转 superseded（四任务全 done） | docs/archive/2026-09-12/tasks/T-PERM-059.md、api-contract §5.8/§6.1/§6.7、implementation.md、access-service-architecture §14.4、rebuild-runbook FAQ、BootstrapGraphDefinition | 评审勿再报「permission-view 端点 404/排查页缺失」——预期行为，重做另立任务。claude 外评处置（2026-09-10，6e665334b 后）：P2×4（§6.8 活引用 23 处改指 §5.8+编号锚点/契约表格空行/impactLevel 与排查页暂停注记两处自相矛盾/admin 契约菜单种子行）+P3×7 组（PermMutexContext 三死方法与 fromSnapshot 死方法删除/注释现在时 6 处/mermaid 与遗留清单等文档漏改/归档索引四处/Bootstrap Javadoc「原」字/mock 注释）全处置。codex luna max 复评（2026-09-11，93daea457 read-only，813k tokens）：P0=0/P1=0，P2×2+P3×4 全处置——①归档状态同步（design/README 索引 superseded+query-engine-unification 补 superseded_by+archive/2026-09-10 补批次 README）；②runbook 固定图历史叙述补现值注记+Initializer 菜单注释 15→14；③claude 处置轮自我引入的「（原 §5.8）」双重替换错引修正为「（原 §6.8）」+行号引用清理；④任务卡「整删」表述修正+ScopeModeSupportTest 空方法删；⑤AbstractRoleMapper.selectFilteredByIds 死查询（唯一消费方 filterRoleIds 已删）连 XML 删除；⑥operation-log/change-log 两处前端设计现在时句加删除注记 |
| 2026-09-06 | 权限排查页（permission-query）暂停待重做（T-FE-043 登记）：页面问题与功能记录后整体重做，后续任务对该页只做编译一致最小改动，不投入页面改造；评审勿报该页 UI/交互缺陷，改登记 T-FE-043（**已被 2026-09-10 T-PERM-059 行取代**：页面已删、卡已 cancel，本条操作指引失效） | docs/archive/2026-09-12/tasks/T-FE-043.md、api-contract.md §6.7 排查页暂停注记 | 再报直接撤回 |
| 2026-09-07 | operationCodeKey 族不做大小写归一：授权域（先 toUpperCase 再拼）与查询/解析域（裸拼）语义不一致为已登记事实，保持现状待统一；统一方向（raw 严格化/归一宽松化+DTO Pattern）属行为变更需单独立项【已统一：2026-09-14 T-PERM-066 定案 raw 严格化，见同日行】 | docs/design/permission-center/implementation.md §8.2 | 报了先核出处 |
| 2026-09-10 | codex luna max 第四轮外评 T-PERM-057（commit 2cb4646ef，read-only，session 01a088c4）：P1×1+P2×4+P3×1，四修一登记一改进项——①P1 canGrant 收编后消费 ROLE_PERM_SNAPSHOT（10s TTL 陈旧+旧读回填竞态，撤权后窗口内可放行已撤销转授权限）：修复=PermQuery 新增 bypassPermSnapshot（LIST 直查不读不回填），canGrant 启用（恢复收编前新鲜度；读面维持缓存=T-ACCESS-008 定案）；②P2 INSTANCE 目标解析空丢 scopeAll 评估拒绝原因：修复=scopeAllEvaluatedEmpty 参与；③P2 deleteRoles/createRole 不持 ABSTRACT_ROLE 树写锁（并发挂入被删子树成存活孤儿）：修复=同 updateRole/moveRole 同款锁先例；④P2 批量入口无上限（闭包 CTE 按 目标×深度 放大）：修复=ResourceKeysReq/IdsReq @Size(max=1000)（project-rules 分批约束合规）；⑤P3 单次查询多阶段 evaluatedAt 各取 now()：修复=query() 入口一次钉住；⑥P2 explain USER INSTANCE 候选明细缺实例授权行→登记 T-PERM-059（该端点在删除重设计面，非安全缺陷不修待删端点）；⑦引擎内部闭包/展开 500-1000 分片→改进项登记（入口限 1000 后祖先链放大 ≤1000×深度，PG 可承受，分片待真实负载证据再立项）。均配真锁（bypass/拒绝原因两例）。评审勿再报①-⑤——已修 | implementation.md §3、PermQuery（bypassPermSnapshot）、PermQueryEngine（入口钉时钟/拒绝原因）、RoleManageAppServiceImpl（createRole/deleteRoles 树锁）、ResourceKeysReq/IdsReq（@Size）、T-PERM-059（explain 候选遗留） | 再报直接撤回 |
| 2026-09-11 | T-PERM-061 设计定稿 v2（claude+codex 双外评 P1×3+P2×6+P3×2 逐条核实全成立后修订）+ 两项实施定案（任务内拍板）：①**evaluatedAt 请求级单一时刻**（a2）——batchCheck 批量化后整批共享一个评估时刻，批内时间窗条件判定一致化；可观察行为变化（现状逐 item 各自取时，批跨时间窗边界可 item1 允 item2 拒）按定案接受，实施时回写 api-contract §6.1 批量口径注记并补时间窗边界测试；②**notifyPermConflict 组级去重**（b2）——互斥冲突审计通知粒度从逐 item 改为 (组, 冲突规则) 一条、detail 携带命中 item 数，审计计数 N→1 随实施登记口径并补通知次数锁。设计核心不变量：A+ 形态挂真实 SQL 路径（scopeAll/实例行/闭包/互斥静态数据 DB 新鲜读，**不引入 ROLE_PERM_SNAPSHOT 陈旧语义**）+ 评估粒度 = scopeAll 段组内一次（子集与 item 无关）/ 实例段逐 item（PERM_MUTEX 集合语义，子集按闭包集切）。随批存量修复：BatchAuthCheckReq.items 双副本补 @Valid 嵌套级联（commit 1cebf1ad9，空白嵌套字段 deny→400） | docs/archive/2026-09-12/tasks/T-PERM-061.md（设计定稿节，v2 修订已并入现行 v4）、PermissionCheckAppServiceImpl.batchCheck（实施对象）、BatchAuthCheckReq 双副本、BatchEntrySizeValidationTest | 报了先核出处 |
| 2026-09-11 | T-PERM-061 设计 v3 复审处置（claude+codex+grok 三轨并行外评，均只读）：v2 骨架三轨一致确认成立（真实 SQL 挂载/评估粒度/反例闭合/1cebf1ad9 零缺陷），处置=v3 修订——①claude/grok 共同 P1「共享闭包映射未按 inheritClosure 分档」→ **投影谓词不变量表**（resourceType==组类型 + 组 coveringMask + inheritClosure 分档闭包（false 档恒 {target}）+ targetMode 分轨 depend 过滤（TYPE_LEVEL 无条件丢子行））+ 默认模式否定锁（不传 inheritMode 授父查子必 deny——预置操作 CROSS JOIN 各类型同四位，缺类型谓词即跨组泄漏）；②空目标集守卫（三轨收敛）：可解析 entityId 并空 → 禁调闭包 CTE（空 foreach `IN ()`=500）与实例 SQL（`<if>` 空集丢实体过滤=无界装载），纯 TYPE_LEVEL 批/全幽灵 code 批 200 全 deny；③reason 树**双轨规格**（INSTANCE 目标空/评估空两段各三支补 DEPENDENT 中间支；TYPE_LEVEL 二值轨无 DEPENDENT）；④a2 强化：唯一非空 PermEvalContext 强制注入全链（item/父递归/条件评估），禁各 item 重钉禁 now() 回退；⑤b2 聚合机制：计算通知解耦 + (组,ruleId)→命中 index ledger + scopeAll 短路 return 前 flush + detail 按实际命中规则集（AND）构造携 hitItemCount；⑥装载分段化（scopeAll 未放行组才装实例行）+ 批量路径不调 loadAncillary + 新增批量条件快照接口；⑦codex「DB 一致性时点」三轨交叉裁决降级为登记句（READ COMMITTED 批内同源与 a2 同向）；⑧闭包规模上限为 2026-09-10 已登记改进项，codex 重报撤回。评审勿再报①-⑥——已并入 v3 设计 | docs/archive/2026-09-12/tasks/T-PERM-061.md（设计定稿节，v3 修订已并入现行 v4）、ResourceEntityMapper.xml（CTE foreach）、RoleResourcePermissionMapper.xml（实例 SQL if 守卫）、queryTypeLevel（dependOn 无条件过滤）、BatchEntrySizeValidationTest（SDK 副本补强） | 报了先核出处 |
| 2026-09-11 | T-PERM-061 设计 v4 第三轮处置（codex sol xhigh，read-only，session 01a090b0；判定「v3 需修订暂不可定稿」→ v4 修订，设计稿随后已被同日定稿行确认）：①P1 批量条件快照未保留 LoadedRules 四态 fail-close 语义（selectValidByIds 只滤租户+软删不滤 enabled——朴素批量把禁用条件解析为有效规则入正缓存，规则体评估为真时放行本应拒绝的权限=绕过方向）→ 快照四态建模（OK/NOT_FOUND/DISABLED/INVALID）、仅 OK 入 CONDITION_RULES 正缓存、失败态请求级记忆评估 fail-close、两轨禁用条件回归锁；②P2 分段装载与「条件预取 ×1」数据依赖环（scopeAll 放行判定需条件评估、实例条件 ID 需实例装载后才知、父条件 ID 需父查询后才知）→ **请求级增量条件快照**口径（每阶段至多一批次、同 conditionId 请求内至多回源一次）+ 次数锁，弃严格 ×1；③P2 评估顺序不变量未钉（evaluateIfNeeded 现状序 = depend_on 过滤→条件→互斥；重排致条件摘掉互斥一端前两端同场全丢=false deny+虚假审计）→ 顺序不变量入设计 + 条件-互斥组合锁；④P3 父判定递归绕过 (组,ruleId) ledger → **父判定审计桶登记口径**（每请求 ≤1 次无去重需求、维持现有通知形态不入 ledger；纳入需穿透递归 query 与「父判定共享只经既有注入面」决策冲突）；sol 同时确认投影谓词/分档/空集守卫/reason 双轨/a2 传递四件 v3 核心物无新缺陷。存量观察登记：batchResolveDomainIds 结果计算后未参与过滤（批量路径不校验 domainCode 存在性）。评审勿再报①-④——已并入 v4 | docs/archive/2026-09-12/tasks/T-PERM-061.md（设计定稿节）、PermissionConditionDomainServiceImpl.loadRules（四态基准）、PermissionConditionMapper.selectValidByIds、PermQueryEngine.evaluateIfNeeded（条件先于互斥序） | 报了先核出处 |
| 2026-09-11 | T-PERM-061 实施收口（commit a03d6117f 实施主体 + 双轨本地评审修复批次；容器轨 BatchAuthCheckPgIT 回归锁①-⑪与 evaluator 单测轨全绿）：A+ 形态落地——引擎 `queryBatch`（六元分组键/请求级共享装载/分段化/闭包分档 false 档恒 {自身}/投影谓词三分量切分/评估粒度与顺序不变量/reason 双轨）+ `BatchConditionEvaluator` 四态增量快照（仅 OK 入正缓存 fail-close）+ `BatchPermMutexEvaluator` 计算通知解耦 + (组,ruleId) ledger 聚合 + batchCheck 编排重写（唯一非空 PermEvalContext 钉住）+ queryInstance 空目标集守卫；api-contract §6.1 批量口径注记/implementation §3.8+§3.10 实施落点/coding-standards §2（6.1.0）/skill 双副本（5.2.0）同步。**用户定案（2026-09-11）：父判定段条件装载维持既有递归单条形态**——§一.7「父判定共享只经既有注入面（不穿透递归）」定案优先（父判定每请求 ≤1 次、正确性共享 roleIds+evalContext 已闭合、同 conditionId 请求内至多回源一次经缓存成立；§一.9「三段批量预载」的父段一批次字面口径不达成，与父判定递归内操作解析同类登记任务卡遗留——最坏每请求多 ≤父私有冷条件数次单查，正常路径零差）。双轨评审处置（均为事实性最小修正）：core-flows §7 旧口径句/implementation §2.4+§2.5 接口摘录补两 evaluator 方法/§3.10 时态句/看板回写列四处文档修正；ResolveContext 缓存直取消幽灵码逐组单条探测 + PermQuery.inheritClosureOf 唯一口径两处代码修正；ledger 组键 `"-"` 占位碰撞（冷僻审计归并面）登记维持。评审勿再报——实施面与父段定案均以本行为准 | docs/archive/2026-09-12/tasks/T-PERM-061.md（实施记录/遗留节）、implementation §3.10 实施落点段、api-contract §6.1、BatchAuthCheckPgIT、ResolveContext（getCachedTypeValue/getCachedOperationId） | |
| 2026-09-12 | T-PERM-061 实施批次三轨外评处置（claude plan 只读 + codex luna max 只读 + grok 三次重跑均中断未出报告——会话存档实证外部取消 run_terminal_command，该轨道本次不可用，按两轨收敛处置）：claude/codex 双轨结论一致——v4 设计逐项对照实现、queryBatch 与单条 query() 等价性（reason 双轨/matched 派生/scopeAll 短路）、安全面（条件四态 fail-close 仅 OK 入正缓存/类型谓词双保险/false 档闭包恒 {自身}）、a2 时刻唯一性全链、evaluator per-request 状态隔离、既有面（resolveBitMasks 重构等价/queryInstance 守卫无回归）、文档六处口径与 skill 双副本零漂移——全部通过；codex P0-P3 全零。claude P3×1（验证缺口，非功能缺陷，手工走查实现无缺陷）：批量路径「请求级父上下文 + 实例 depend_on 子行」分支零测试覆盖（唯一 setParentResource 用例挂 TYPE_LEVEL item 上不触父判定，①的 depend_on 子行无父上下文）→ 已核实属实并补 `BatchAuthCheckPgIT.batchParentContextMustResolveThroughSharedCarrier`（父命中放行锁 carrier 装配三线 + 父操作不覆盖 DEPENDENT 两变体），容器轨 11/11 全绿。存量观察随批登记任务卡遗留节（实例 SQL XML `<if>` 空集丢实体过滤陷阱面——引擎守卫已封现有调用方；单条 computeMutexContext 规则表无缓存维持现状）。评审勿再报——已修/已登记 | docs/archive/2026-09-12/tasks/T-PERM-061.md（遗留节）、access-service/src/test/.../BatchAuthCheckPgIT.java（父判定装配用例） | 再报直接撤回 |
| 2026-09-12 | T-ADMIN-020 admin 域清理范围定案（Phase 4 批次内拍板）：不按 2026-06 原范围做全量风格清理，**重新排查核对范围后按现行规范清单清扫**——重排查结论零可清扫描项，零代码变更收口（原痛点「185 文件零测试」口径失真，现状 197 源/37 测试；已知边界：机械清单扫描+抽样核实，非逐行风格 diff，后者性价比不成立勿再建议）；admin 域裸 IllegalArgument/State 命中均为规则豁免面（bootstrap fail-fast、JobInvoke 框架适配层） | docs/tasks/T-ADMIN-020.md（排查定性表） | 报了先核出处。claude 外评处置（2026-09-12，45ce95912）：排查表 DTO 校验行系扫描 pattern 漏 @Min/@Max 的误判，按实测改写为 38/38 全覆盖（分页注解 T-ADMIN-026 已落地）+ 裸异常豁免面补全第三类（@PostConstruct 启动校验）；「registry 复制错误口径」子项经核实撤回（引用错行）；存量观察：OrgQuery/UserQuery 零注解 DTO（非 Req 命名面未触达） |
| 2026-09-12 | 新类型首笔授权引导缺口处置定案（T-FE-023 产品发现→T-PERM-062 立项；claude/grok-4.6/codex gpt-5.6-sol xhigh 三路独立方案咨询收敛后用户四项拍板）：**类型生命周期种子**——createType（CRUD 四条）+ createOperation（每追加操作一条）同事务向「类型所有者角色」落单 bit `scope_all+can_grant` 首授行；所有者=TypeCreateReq 可选 ownerRoleTypeCode/ExternalId 持久化于 type_definition.extra.grantOriginRole（缺省 BASIC_ROLE/bootstrap-admin，解析失败整单回滚）；**GrantSource 新增 AUTHORITY_ROOT + DDL CHECK 焊死形状 + apply-grant-plan 不可改删**（20034 先例；类型/操作删除级联清理是唯一回收路径）——结构防护下不采 reseed API；20040 细分 TYPE_GRANT_ORIGIN_MISSING reason+文案；实施首步出授权根活性报告（内置类型 canGrant=false 维持不动勿顺手升级）。写入通道复用 bootstrap 直写通道下沉共用方法，**checkCanGrant 与通用授权链零改动**（§14.1 红线维持）。否决项（三路一致，勿再提议）：运行时豁免/超管旁路、GRANT_ORIGIN 能力位/赋权权拆分、纯文档方案 | docs/tasks/T-PERM-062.md（设计定案节）、CustomResourceTypeSlicePgIT（5a/5b 现状锁）、extension-guide.md §3.5 | 报了先核出处 |
| 2026-09-12 | T-PERM-062 实施期四项补充定案（活性报告后用户逐项拍板）：①20040 reason 细分**仅自定义类型**（is_system=false）——内置类型零可转授行是 T-PERM-027 转授链收窄的设计状态，reason 维持 NO_PERMISSION/NO_GRANT_RIGHT（引导文案不失真）；②**无历史数据负担**（项目未正式部署，存量自定义类型回填面不存在）；③所有者指针 extra.grantOriginRole **允许变更并补齐种子**（取代立项时「指针不可变 + runbook 兜底」口径），且变更=**同事务先清后种迁移**（软删该类型全部 AUTHORITY_ROOT 行——含已删角色/误配旧 owner 残留——再向新所有者补齐全部有效操作位；只补不迁则旧 owner 持有不可移除的永久扩权）；④owner 角色被删除**不加删除守卫**，恢复路径=经 type-definition/update 重指所有者（迁移语义自动清理残留行）——恢复面收敛进 update 单入口，「不采 reseed API」的定案精神以此形态维持（无独立 reseed 端点）。附带实现口径：种子直写通道下沉落 PermissionGrantPlanDomainService.seedGrants（plan→grant 域既有单向依赖，反向下沉会构造循环）；指针为服务端管理键（create 请求 extra 自带拒绝 20044）、无清除语义（update 未携带键=保留现值） | docs/tasks/T-PERM-062.md（设计定案节更新）、api-contract.md §5.1/§6.5.1、access-service/.../GrantOriginDomainServiceImpl.java | 报了先核出处。claude CLI 外评处置（2026-09-12，默认模型 stdin 注入 read-only plan 模式，范围 025936e97..579ee12b0）：P0/P1=0；P2×1（createOperation 陈旧所有者读×所有者迁移交错遗留不可回收种子行）经核实**撤回**——TreeWriteLockSupport 锁经 afterCompletion 释放覆盖整个事务、selectByTypeKeyAndCode 为无缓存直查且 typeDef 读在锁内，两事务在锁上严格串行、后进锁者必见前者全部提交结果（触发场景混淆前端展示层 stale 与服务端锁内 fresh 读）；其附带缺陷成立已修（typeDef==null 时静默跳过种子并插孤儿操作行 → 锁内 fail-closed 20021）；P3×2 已修——①PermissionDetailDrawer 补 AUTHORITY_ROOT 三态（class/tag/只读提示/子权限空态文案，与 MatrixCell 同源口径）；②ownerRoleTypeCode 值域经用户拍板收紧仅 BASIC_ROLE（容器角色 ORG/POSITION/GROUP_ROLE 携带 20044——授权根是类型级可转授行，挂容器角色=全体容器成员获得转授权且种子 20061 只读无逐行移除通道；回归锁 + api-contract/schema 同步）。存量观察：RolePermissionItemResp grantSource Javadoc 扩列已修；refine 候选行不筛角色停用命中任务卡已知边界撤回。评审勿再报——已修/已撤回 |
| 2026-09-12 | 角色互斥授权时校验三项拍板（T-PERM-063 立项当轮）：①冲突处置=**整批原子拒绝**——`user-role/assign`、`batch-assign` 命中互斥对整批拒绝（对齐入口既有 errors 整批抛风格，无部分成功）；②存量违规=**有持有对拒绝立规**——conflict-rule create/update 的 ROLE_MUTEX 分支存量双持非空即拒绝（先解绑再立规，运行时双删不再是常态兜底）；③附加范围=**双删补 CONFLICT_DETECTED 日志**（Caffeine 有界去重，每租户×用户×规则对每 JVM 1h 一条）**+ detect 端点支持 ROLE_MUTEX 角色对预检**。事实基线（代码级核实）：用户持有角色唯一通道=user_role 直授（组织/岗位拿权限行不持角色，无绕过通道）；applyGrantPlan 不涉用户-角色持有、不适用；写路径规则读取 DB 直查不走 10s 缓存（新规则即刻生效）；并发双开窄竞态接受（运行时双删兜底） | docs/tasks/T-PERM-063.md 当前口径、api-contract §5.5/§5.6 要点、implementation §2.4/§3、core-flows 评估口径行 | 报了先核出处 |
| 2026-09-12 | 互斥守卫通道补全拍板（claude 外评 T-PERM-063 P2-1 核实成立后）：user-role/sync、full-sync 接受 BASIC_ROLE/PERSONAL/自定义角色类型且 BIND 无守卫=管理面之外真绕过通道 → **守卫补全**——①sync BIND 逐条守卫（冲突 item NON_RETRYABLE + reason=ROLE_MUTEX_CONFLICT，走通道错误信封而非管理面 20062 整批语义）；②conflict-rule create/update 拒 ORG/POSITION 角色对（本地投影通道只写结构角色行，规则面拒绝后该通道结构性造不出违规；UI 本就不提供结构角色对）。弃「登记不适用」（豁口与治理目标矛盾）与「只堵 sync」（ORG 对规则经直调 API 仍可造双持） | docs/tasks/T-PERM-064.md 当前口径、api-contract §5.6/§6.2.2.2 | 报了先核出处 |
| 2026-09-13 | T-ACCESS-034 操作码合一两项命名拍板（实施期 AskUserQuestion）：①合一后单一操作码常量类命名=`OperationCode`（落 `engine.constant`，对齐 ResourceTypeCode 合一先例保留 perm 侧简名；AdminOperationCode/OperationCodeConstants 两旧类均消亡，满足验收「旧类消亡」）；②类内类型专属码常量命名=值名一致+分节（ACCESS/ASSIGN/REVOKE 直接作常量名，靠「按资源类型分节」注释区分归属——共享码 MANAGE 等本就只能按值命名，风格统一；弃类型前缀 API_ACCESS 形态） | access-service engine/constant/OperationCode.java、T-ACCESS-034 | 报了先核出处 |
| 2026-09-14 | T-FE-044（资源依赖页补遗联调）实施定案与外评处置：①api 层 URL 回归锁拍板补齐（`src/api/resource-dependency.spec.ts` 6 用例，mock `@/utils/http` 断言六端点 `/perm` 前缀——grant-store.spec 同形态先例；旧缺前缀形态必红）；②claude+grok 双通道外评（范围 ab1fe28e9..f6436c6f7）零 P0-P2，claude P3×3 + grok 全零（存量观察 5 条）、两轨交叉命中 2 条，六项逐条核实属实全修（README 当前限制句矛盾/runbook 现值 86/85/tsconfig 空 glob/resource-operation mock 终态句/hook checkCycle 死导出删除/README 列举补类型定义）。评审勿再报六项——已修 | docs/archive/2026-09-14/tasks/T-FE-044.md（完成记录外评处置节）、frontend/src/api/resource-dependency.spec.ts | 报了先核出处 |
| 2026-09-15 | Q-009 转出跨能力 mapper 直读收敛定案（计划经 claude+grok 双通道外评修订后用户确认）：①**全 30 边一次收敛、四批串行**（T-ACCESS-043 直换+死边 9 边 → 044 grant 授权事实服务+rule 条件读 6 边 → 045 type/resource/domain 供给读服务 11 边 → 046 user-role 原始行/投影+白名单退役 4 边；计划 capability-mapper-convergence-plan，边清单以「消费类:行号 → mapper#方法」定位——外评证实代理盘点 # 编号与白名单行序两套方案互不自洽，废弃编号）；②**宿主原则**——role/user 主体事实与主体 id 解析 4 边（UserRoleProjectionWriter→AbstractUserMapper、UserManage/BatchAdminUserProjectionWriter→UserRoleMapper、UserMenuQuery→UserRoleQueryMapper）收敛到 engine.core 既有 SubjectDomainService/TypeResolutionService（主体域服务既有宿主，属 §8.4 豁免 1 面，**不建能力包内 DomainService**——防后续按「未收敛」重报）；其余边落被读方能力包内**实体命名 DomainService**（RoleResourcePermissionDomainService/OperationPermissionDomainService/TypeDefinitionDomainService/DomainConfigDomainService/ResourceApiMappingDomainService/ServiceConfigDomainService）；③**新服务硬契约**：只依赖本包 mapper、不注入他能力包 bean（Spring 无环）；无缓存直读直写（同事务可见性语义保持，禁复用 loadRules 类缓存路径）；不声明独立事务（REQUIRED 跟随调用方）；④**包级环以 bean 级无环为准**（grant↔type、grant↔rule、domain→type→grant→domain 服务边成包环，bean 级全部开放；type↔resource 双向为现行既有先例；断言面=mapper 包不变）；⑤外评必改项三条内嵌为批次验收——操作定义读拆**全租户口径 selectAllOperationsByTenant**（保 knownOperationCodes 的 20008/20005 契约区分，弃一刀切 IN——会把「码存在于他类型」从 20008 退化 20005）与 **IN 形态空集短路**（防 IN () 500）；**测试装配适配清单入计划**（claude 实测 26 测试文件位置定参构造，InOrder 级联 verify 改挂新服务不删锁、非 bean ProjectionWriter 经 LocalProjectionDomainServiceImpl 装配改线）；**负向自证终态=测试源集 architecture/fixture 违规样例包 + 专用 ClassFileImporter 导入**（主断言 DO_NOT_INCLUDE_TESTS 不含测试类，白名单清空后无真实边可裁剪）；治理回写锚点=project-rules **§8.2** 白名单指针句 + access-service-architecture「存量 19 类 30 边」句 + capability-structure §8.4/裁决表 row9 + permission-coding-standards §8 | docs/archive/2026-09-15/capability-mapper-convergence-plan.md、docs/archive/2026-09-15/tasks/T-ACCESS-043~046.md（已归档）、QueryBoundaryArchitectureTest | 报了先核出处 |
| 2026-09-15 | Q-009 收敛完成补记（T-ACCESS-043~046 四批全 done）：冻结白名单 19 类 30 边全量收敛至零——批次① 9 边（既有服务直换+死边）、批次② 6 边（RoleResourcePermissionDomainService + PermissionConditionDomainService 三读）、批次③ 11 边（OperationPermissionDomainService 全租户/IN 双口径等五新服务）、批次④ 4 边（SubjectDomainService user_role 原始行层 + ResourceEntityDomainService 投影轨方法）。终态：FROZEN_WHITELIST 清空退役，`checkCapabilityMapperBoundary` 零容忍绝对禁断，负向自证改测试源集夹具（`menu.fixture.BoundaryViolationFixture` + 专用 ClassFileImporter，DO_NOT_INCLUDE_TESTS 双重不干扰）；治理回写 capability-structure §4 row9/§8.4、project-rules §8.2、access-service-architecture、permission-coding-standards §8、design/README 归档行注记同批完成。四批回归证据见各任务卡完成记录，收口全量 `mvn test -T 1C` 1698/0F/0E/0S 含 E2E BUILD SUCCESS（2026-09-15，t046_reactor3.log；首跑失败一次为 t046_reactor2.log，随机端口竞态抖动已隔离定性） | docs/archive/2026-09-15/tasks/T-ACCESS-043~046.md（完成记录，已归档）、QueryBoundaryArchitectureTest、capability-structure §8.4 | 报了先核出处 |
| 2026-09-16 | release-preview 双轨评审处置（代码轨 P3×6 + 文档轨 P1×2/P2×2/P3×6，逐条代码级核实全成立；两项用户拍板）：**①JWT_SECRET_KEY 短密钥 fail-fast**——OAuth2JwtSupport.validateHmacSecretStrength（≥32 字符=HS256 强度对齐；sa-token-jwt 长度零校验、短密钥静默弱签名）挂 OAuth2AppServiceImpl @PostConstruct，四个短测试密钥同批加长（batch-sql/login-lock/menu-write/tree-cycle），边界锁 31/32 精确两侧（弃只改文档口径——IAM 静默弱签名通道不可留）；**②OAuth2 能力如实口径**——CHANGELOG/README 删「客户端凭证」（全仓零实现、契约 §287 仅 authorization_code）与「密码」（无 token 端点发放，唯一依据是登录链客户端校验含 password 字样），改「授权码 + PKCE / 刷新令牌」（refreshToken 端点实证存在）。直接修复（修法唯一最小）：compose 补 GATEWAY_CORS_ALLOWED_ORIGINS 透传（带 yml 同款默认值——空默认会把未配置误变为禁用 CORS）与 DB_USERNAME/DB_PASSWORD 透传、.dockerignore ×3 补 target/archive-tmp、nginx 反代补 300s 读写超时（100MB 上传超默认 60s 必 504）、CHANGELOG 删 explain（T-PERM-059 已删端点）与「v3」内部版本叙事、quickstart 授权闭环入口修正（权限授予页不进侧栏=角色管理页按钮、菜单名「服务与接口」）、architecture §14.2/capability-structure 两处「仅基建/Q-006 登记不实施」旧口径随批更新。不修（含理由）：evict-only 别名不加运行时防呆（短命过渡物、形态锁+javadoc+skill 三层在案，第二个改名别名出现时再泛化）、FakeRedis/回归锁分布维持（AspectTest 行为锁 + DualInstance store 层能力锁互补非重复） | docs/archive/2026-09-16/tasks/T-ACCESS-047~050.md（完成记录）、OAuth2JwtSupport、docker-compose.yml、CHANGELOG.md | 报了先核出处 |
| 2026-09-16 | release-preview-plan 立项三定调（AskUserQuestion 三问，任务板空档选向后用户拍板）：**首个发布预览批次 = 全栈 compose profile + v0.1.0 + 文档全套**——①部署形态=compose 新增 `--profile app`（gateway/access-service/example-service/frontend 四服务镜像化，默认 `docker compose up` 仍只起基建，T-ACCESS-020「仅承诺基建」口径对默认行为不变；弃仅基建+文档化手工启动——「一键全栈」目标不达成）；②版本化=**v0.1.0 预览版**（tag 定格 pom 0.1.0、主线回 0.2.0-SNAPSHOT；弃 v1.0.0——未交付清单挂账+DDL 无迁移路径下 GA 语义失真；弃暂不打 tag——外部使用者无版本锚点）；③文档面=全套（quickstart/deployment 基线/CHANGELOG/frontend README/根 README 分层），**根 LICENSE 重写为本项目名义**（用户口径：项目已与 RuoYi 无关；frontend/ 的 pure-admin-thin 上游署名保留处理在 T-ACCESS-049 任务卡单列执行期复核）。顺带项：Q-006 双命名空间失效转出实施（T-ACCESS-048）、T-ACCESS-042 终态卡归档（T-ACCESS-050）。非目标：不建发布流水线、不引入 jib/flatten、不做 DDL 迁移框架、暂缓卡不动 | docs/archive/2026-09-16/release-preview-plan.md（已归档）、docs/archive/2026-09-16/tasks/T-ACCESS-047~050.md、docker-compose.yml | 报了先核出处 |
| 2026-09-18 | Q-008 收敛定案（T-PERM-069，AskUserQuestion 用户拍板「仅 API 收紧」，SERVICE/API 分治）：**①API 类型种子声明 SYNC+access-service**——API 资源行唯一事实入口=service-config/sync 接口声明通道（契约 §19.8，领域直写不经管理面门禁）+bootstrap 固定图种子，管理面资源 CRUD 一律 20055（enforcement 堵手工 MANUAL 行口径外通道——FULL diff 清理与服务删除级联均只覆盖 SERVICE_SYNC 行，MANUAL 行成永久孤儿）；保存边界 API 硬拒保留（理据改「种子钉死」）、20055 内部来源文案补「服务接口同步 service-config/sync」入口；回归锁=真实 DDL 驱动 PgIT 双向锁（旧种子下实证失败）+SERVICE 放行对照。**②SERVICE 维持 MANAGED**——代码级盘点实证新 SERVICE 行唯一通道=管理面手工建行（bootstrap 只种 access-service 一行、service-config save/sync 均不产 SERVICE 资源行），该行是按服务实例级授权（如 SERVICE:SYNC_INTERFACE@某服务）的目标对象，收紧即零 writer 死局；未来重启评估须以「service-config 联动 upsert SERVICE 行」配套事实链为前置。存量核查：dev 库 86 行 MANUAL 全为 bootstrap 固定图清单（零口径外野行，目标接口 my-info 无映射为设计预期）；存量库订正 UPDATE 与野行检测语句登记 rebuild-runbook。双轨本地评审 P1×3/P2×5/P3×5 全处置（extension-guide §2 残留句、§6.3 死锚点簇 6 处本批新增+5 处历史、architecture L144 增补句、runbook 登记）；全量 `mvn test -T 1C` 含 E2E 1721 项 0 失败 | docs/archive/2026-09-18/tasks/T-PERM-069.md、契约总册 §5/§13/§19.1/§19.8、schema（种子+注释）、ResourceTypeOwnershipGuard、ServiceConfigCascadePgIT、AGENTS.md 权限中心提醒 | 报了先核出处 |
| 2026-09-19 | 前端会话/交互外评处置立项（基线 commit 5495d2ffd、距今 6 提交前端零改动；14 项结论逐条代码级核实：**成立 12、降级 3**——守卫双 next 当前触发面为零（全仓路由 meta 零 roles + VITE_HIDE_HOME=false，biz-domain.md:223 佐证）、token 过期放行系 T-FE-041 注释明文口径、路由可达系 T-PERM-037 定案口径；**评审半误报 1**：「拖拽改权限继承」运行时方向错误（T-PERM-057 auth-check 判定面继承默认关，运行时无角色继承）但管理面影响真实——拖拽经 ROLE 资源投影父链镜像（LocalProjectionDomainServiceImpl.upsertRoleResource 369-376）改变实例级管理覆盖（RoleManageAppServiceImpl:216/272 实例级 MANAGE，forValidate 闭包默认开）与 remove 级联范围；**评审遗漏 1**：T-PERM-067/066 已备自助改密闭环前提（/user/reset-password 自身通道+自身改密置 false））。四定案（AskUserQuestion 四问，第四问经两轮引擎事实修正；codex sol 复评后第四项改判）：**①强制改密=自助改密页+阻断**——forceResetPwd=true 只放行改密页、改密成功（后端自身置 false）后进系统；弃非阻断（「force」名不副实、初始密码弱凭证长期存续）与仅修文案（缺口留存）；T-ADMIN-022 非阻断口径退役。**②会话权限热刷新=403 触发自动刷新一次+顶栏手动入口**——统一「会话能力刷新入口」（权限串+侧栏+路由门禁 path 集原子更新，四调用方共用）；不自动重放请求防循环、授予页重试先刷权限串；弃轮询（延迟 N 分钟+常驻请求量）与服务端推送（需后端新事件机制，长期项）；机制落被授权人会话侧（管理员浏览器内刷新帮不了被授权人——外评修法盲点）。**③路由级 UX 门禁=后端 menus 派生+显式动作路由映射**——门禁集合=menus 树 path ∪ 非菜单业务路由显式映射（/perm/grant showLink:false 不进菜单，纯 menus 白名单会封死授权页——codex sol P1 采纳）；守卫状态机 uninitialized/loading/loaded/failed，首导航等待初始化（防冷启动深链绕过——现状 initRouter 后仅 to.name 空才重导航）、仅明确 failed 放行（fail-open 不锁死）；T-PERM-037「路由可达、后端 403 兜底」口径演进（当时弃 meta.auths 的「前端静态声明可绕过」理由不适用于后端派生 menus）。**④高影响操作确认=跨层级拖拽+角色禁用都加确认**（初判「仅禁用加确认」、拖拽维持直接生效；codex sol 复评证伪其依据「实例级角色授权当前无人用」——该判断混淆数据状态与机制约束：授权页类型候选=全部资源类型含 ROLE、GrantDialog 支持 INSTANCE 多选、update/move 实例级 MANAGE 闭包默认开，实例级委托**现役可配无约束禁止**（dev 库零存量行仅是当前数据状态）；给委托管理员授父角色实例 ROLE:MANAGE 后，拖入/拖出子角色立即改变其可管理集合与级联删除范围——用户改判）；仅 parentId 变化的拖拽（角色树+组织树 inner/跨父）加确认，同级排序直接生效；禁用确认文案含「全部持有者立即失去该角色权限」。codex sol 复评其余全采纳：强制改密标记跨标签共享持久化（sessionStorage 单标签漏失即绕过、userinfo 不含 forceResetPwd、DataInfo 无 userId——补登录主体绑定持久化）、列表顺扫补 permission-condition 页、PositionTab 系正确实现非同款 bug（主代理自误报撤回——其递归返回 null）、menus 空态用例改标安全/特征锁（非 old-fail）、T-FE-051/056 design_refs 补全；部分不采纳：任务卡/registry 引「外评 P1/用户拍板」系定案登记与背景溯源形态（registry 写入协议与全表先例一致，project-rules 禁的是轮次标记词），看板行超「标题简短」边界按规则压缩。**⑤Gateway 白名单纳入 /user/reset-password（claude 外评 P1 触发，AskUserQuestion 用户拍板）**——该端点不在 Gateway 会话入口族白名单（application.yml 精确清单），PermissionFilter 快照条目仅由 API 类型 ACCESS 位派生、实例授权只种子给管理用功能角色（绑首管理员），普通用户（forceResetPwd 阻断目标人群）经 Gateway 必 403=阻断落地即锁死；定案=白名单纳入（与 logout/userinfo/user-menu 同族——服务层门禁不变：Sa-Token 登录校验+自身路径豁免/非自身须 USER:RESET_PASSWORD，T-PERM-067 语义零改动；弃创建用户时联动授 API 实例 ACCESS——每用户一行授权膨胀且漏配仍锁死；弃先验证再定——多一轮往返），T-GW-009 立项为 T-FE-046 前置，契约与 T-ACCESS-042 白名单清单同步。claude 外评其余处置：T-FE-056 公共白名单改 remaining.ts 全量自动纳入（漏 /redirect/:path 会拦死标签刷新）+门禁状态迁移表钉死（failed 仅首载产生、刷新失败维持 loaded——消除与 T-FE-048 语义互斥）+design_refs 补 system-config 与各页权限表效应格清扫；T-FE-045 注销请求显式携 token（过期分支免头路径注销空转）+防补「进行中短路/完成后零请求」锁；T-FE-051 现状勘误（十页均已有 catch+message、代际已有三处先例——真正缺口=用户页无 catch+八页无代际）+非 2xx 错误文案统一消费后端 body；任务卡过程叙事清扫（registry 2026-09-13「registry 为过程唯一载体」定案对齐——删「外评修正已采纳」段改链本行）；外评原文计数勘误 15→14 项 | docs/plans/frontend-session-consistency-plan.md、docs/tasks/T-FE-045.md、docs/tasks/T-FE-046.md、docs/tasks/T-FE-048.md、docs/tasks/T-FE-051.md、docs/tasks/T-FE-056.md | 报了先核出处 |
| 2026-09-19 | T-FE-050 收口与 Q-017 收敛载体拍板（AskUserQuestion）：user/index.vue `findParentOrgName` 递归 bug（「未知」truthy 哨兵混入递归返回值——首个子树查找失败即被当成功值上抛，后续兄弟子树永不遍历；选中后位根子树组织时信息卡「上级部门：未知」、openOrgForm 编辑态同源）修复为 PositionTab 同款纯查找形态 `string \| null`，提取 `utils/orgTree.ts`（SFC 内函数不可测、无组件测试基建），两调用点 `?? "未知"` 保展示语义；回归锁 5 用例旧算法下 4 红 1 绿实证（旧算法逐字迁移作等价被测体）。**Q-017（index.vue 死解构+点组织双请求）收敛载体用户拍板定为 T-FE-051、T-FE-050 不顺带**——两设想方向（selectedOrgId 就地化 vs 状态上提共享单实例）正是 051「composable 化」的设计决策面，提前收敛等于替 051 定形态，且 051 反正要动同文件同批行。双轨本地评审处置（代码轨 P0-P2=0、文档轨 P0×1）：①文档轨 P0 registry 新行被空行隔断（表格断裂）直接修；②代码轨 P3 实证规范 §2 与工具链冲突——纯类型语句内联 `import { type X }` 被 `@typescript-eslint/no-import-type-side-effects` 报 error 并 autofix 回 `import type`（内联修饰符 TS 擦除后残留 side-effect import；存量 124+10 处 separate 从未按 §2 字面执行），**用户拍板直接修 rule §2 措辞**（纯类型语句 import type、混合语句内联）；③代码轨 P3 OrgUpdateReq.orgName 无非空校验（update 直连可置空名）**用户拍板登记 Q-018**；④文档轨 S-1 **T-FE-051 卡回填 Q-017 载体锚点**（先例=T-FE-047 存量②当轮扩卡）；⑤卡内红跑措辞与空名论据精确化 | docs/tasks/T-FE-050.md、docs/pending-problems.md（Q-017 关联行/Q-018）、.claude/rules/frontend-coding-standards.md §2、docs/tasks/T-FE-051.md | 报了先核出处 |
| 2026-09-19 | T-FE-053 收口（路由守卫 next() 卫生修，AskUserQuestion 两项拍板）：**①修法=删守卫 meta.roles 死分支+全部 next() 站点统一「每 next 必 return」**——触发面为零实证（全仓零路由声明 meta.roles、.env VITE_HIDE_HOME=false）+本仓路由权限=后端 menus 派生（侧栏可见性 T-FE-015+路由级 UX 门禁 T-FE-056），模板前端角色白名单永不启用；陷阱分支删除（若被误声明，登录 roles=后端角色 code 无交集=全员 403 且无人记得该机制）；`utils.ts filterNoPermissionTree` 维持现状不删（活跃链路 handleWholeMenus+cloneDeep/filterChildrenTree 结构副作用，对零声明路由恒等放行）；**②回归锁=新建 router/index.spec.ts（守卫首次可测化）**——mock createRouter 捕获守卫回调+8 模块断环（remaining 避 Layout SFC 树、@pureadmin/utils storageLocal 内存 Map 替 localStorage，node 环境零 DOM 依赖、零新增 devDep），架子 T-FE-056 状态机直接复用。红跑实证：旧实现下红锁 1 红（声明 meta.roles 不再拦 403 正常放行——旧码 next({path:"/error/403"})+贯穿二次 next，断言 got 2 times 即双 next 本体；该锁同时区分「保留 403 只补 return」替代形态）+5 特征锁绿（白名单弹回/普通放行/F5 刷新同步 next 一次 initRouter 异步不追加/未登录非白名单 removeToken+跳登录/未登录 /login 放行）；vitest 309/309（+6）+typecheck/lint/build 全 0。双轨评审：代码轨 P0-P3 全零（12 实证通过）；文档轨 P0-P2=0+P3×1（T-FE-056 卡/计划「053 先机械修 return」表述滞后，收口回写）；存疑处置——index.ts 头注补「T-FE-056 落地前路由仍全可达，后端 403 兜底」半句+spec isOneOfArray mock 分歧注释（事实性最小修正直接修）；VITE_HIDE_HOME 分支覆盖/F5 wholeMenus 非空用例维持现状（评审倾向+与拍板 spec 范围一致）。连带清扫：perms.ts 注释「beforeEach 仅按 meta.roles」改「不做角色拦截」；live 文档 6 处 filterNoPermissionTree 表述经核仍准确不动 | frontend/src/router/index.ts、frontend/src/router/index.spec.ts、frontend/src/views/perm/grant/utils/perms.ts、docs/plans/frontend-session-consistency-plan.md | 报了先核出处 |
| 2026-09-20 | T-FE-054 codex sol 外评处置（复评轮，模型 gpt-5.6-sol xhigh+read-only+禁子代理，按 2026-09-13 定规不注入上下文；P0-P1=0、P2×2+P3=0，**过度设计检查可裁剪项=0**——用户本轮特别叮嘱，8 机制逐个判定必要且最小；结论逐条代码级亲核均成立，两条均系 claude 处置轮引入的次生缝隙，全直修）：**①P2「会话终结后的成功响应仍回写旧会话状态」**——上轮「终结不拦」同时用于成功/失败两分支：失败分支正确（401 要上抛），但刷新在途+登出/401 已清令牌后 200 晚到时回写重建已清的 Pinia/userKey（`stored ?? {}` 产生残缺 userKey）——修法=成功/失败判据拆分：成功回写仅「当前仍存在且令牌即本会话」（`sessionAlive`），失败丢弃维持「仅另一活会话令牌才吞」（`sessionReplaced`）。**②P2「无凭证请求绕过单源判据」**——拦截器 `if (data)` 外壳使 getToken() 为 null 的请求走 else 无头放行（模板原始行为），并发短路首个 logOut 同步清令牌后后续请求多跑一次必然 401 的往返——修法=白名单后无条件 `isSessionTerminated(data)`（无凭证形态同短路，结构同时简化）。**sol 附带建议「isSessionTerminated 对 NaN fail-closed」不采纳**：残缺 userKey 唯一产生源即①缺陷本身，修复后正常数据流无该形态（setUserKey 恒写 expires），为不可达形态加防御属过度设计（用户本轮叮嘱）。红跑实证 2 红/30 绿（stash 修复对上轮已提交实现）；vitest 354/354+typecheck/lint/build 全 0；存量观察无新增 | docs/tasks/T-FE-054.md（外评处置节）、frontend/src/store/modules/user.ts（sessionAlive/sessionReplaced 拆分）、frontend/src/utils/http/index.ts（无条件单源判据） | 报了先核出处 |
| 2026-09-20 | T-FE-056 外评 P2 修法拍板（同日外评处置行的续行）：**删条件恢复改前行为**——守卫公共路由分支删除「阻断人群不 initRouter」跳过条件（`!userInfo.forceResetPwd`），阻断人群 F5 改密页冷启动同样后台建 wholeMenus（改密页无侧栏渲染，代价一次 user-menu 请求），改密成功跳转 getTopMenu 与登录路径同源（首个菜单/失败占位项）；弃跳常量 /welcome+getTopMenu 兜底组合（跳转语义从「首个菜单」变固定首页、零权限新用户改密后落 403 全屏）。回归锁「阻断态公共页冷启动也后台 initRouter」红跑实证（加回条件 1 红）+vitest 386/386。 | frontend/src/router/index.ts、docs/tasks/T-FE-056.md | 报了先核出处 |
| 2026-09-20 | T-FE-056 claude 复评处置（复评轮 P0-P2=0、P3×3、过度设计=0；专项清单复核上轮四项处置全部落地无次生缝隙）：**①会话终结协议三副本不等价**（menu-retry 后置判只抛不提示不清会话——判据宽于「401 型」，在途跨期+非 401 失败形态点击无反馈）**用户拍板=本卡公共层统一修**——session-expired.ts 新增 terminateLocalSession() 三件套单源（提示+logOut+抛；不 await=fire-and-forget 等价、executor 内 throw=reject 等价），http 短路/initRouter 前置/menu-retry 前后置统一替换；主锁红跑 2 红。**②上轮 P2 修复残留时序窗口**（守卫后台 initRouter 不阻塞，慢响应窗口内改密成功 getTopMenu 仍空树解引用）直修=改密成功分支 await initRouter 后取值（与登录路径同形）+catch 识别 SessionExpiredError 跳重复 toast。**③第五调用方枚举漂移**（router/utils×3+login.md×2 停在四调用方+menu-retry.vue 吞错归属失实）直修五处+归属修正。存量观察①still-empty 近乎不可达（welcome 纯展示全员可见——T-FE-049 既有设计保留）②spec mock 缺 formatToken 顺手补齐。vitest 386/386+typecheck/lint/build 全 0。 | docs/tasks/T-FE-056.md、frontend/src/utils/session-expired.ts、frontend/src/views/change-password/index.vue | 报了先核出处 |
| 2026-09-22 | T-ORG-003 claude 外评处置定案（astron-code-latest headless plan，commit cce614a42 后；P0-P1=0、P2×1+P3×3、过度设计=0，双轨处置五落点复核全过）：**P2 setPrimaryOrg 补 SYS_ORG 树锁**（T-ORG-002 挂锁枚举遗漏 set-primary——并发 deleteOrg 级联软删目标归属行后，设主清旧主 UPDATE 滤掉已删行、置新主命中 0 行，200 但主归属静默丢失；与 assign/remove 同形补锁：门禁/存在性锁前、锁先于 resolveDefaultTreeOrgIds 守卫首读，UserOrgAppServiceTest InOrder 锁序锁——摘锁复跑实证红）；P3-1 Q-025 类推收敛补全（UserAppServiceImpl/UserWriteAppServiceImpl 两处 validateOrgInDefaultTree 逐字副本换绑共享入口；空判定头部与取配置实体变体不换——前者不复制展开逻辑换绑反重、后者需新领域读入口）；P3-2 接口 javadoc 与 OperationCode.ASSIGN_POSITION_USER 注释两处旧门禁文案改与实现同文；P3-3 存在性先于判权的 10101/403 可区分收编为契约 §7.2 显性事实（不收紧——统一信封与 set-primary/remove 既有同序先例分叉）。存量观察：PositionTab org/users 失败静默渲染 0 人（T-ACCESS-055 装配注意）、mapper 大小写口径分叉不可达留观。 | access-service org/user 域（UserOrgAppServiceImpl 补锁+构造器、UserAppServiceImpl/UserWriteAppServiceImpl validateOrgInDefaultTree、UserAppService/OperationCode javadoc、UserOrgAppServiceTest 新增）、access-service-api-contract.md §7.2、docs/tasks/T-ORG-003.md | 报了先核出处；后续不得再报「setPrimaryOrg 无树锁」（已补，摘锁红跑实证）；不得再报「validateOrgInDefaultTree 双副本」（两逐字副本已换绑共享入口——空判定头部与取配置实体变体为有意保留，非手抄展开逻辑）；10101/403 可区分为契约明示口径（拍板沿既有同序先例不收紧） |
| 2026-09-23 | T-ADMIN-029 双轨评审处置定案（代码轨 P0×1+P2×2+P3×6+可裁剪×2、文档轨 P2×1+P3×1；P0 亲核全链属实、其余逐条核实成立，处置全部落地）：**P0 my-notices/read 真实 Gateway 链路恒 400 不可达**——白名单拍板只落了 yml 一载体，reset-password（T-GW-009）先例实为**四载体**（Gateway yml+GatewayProperties defaults+access-service SecurityWebMvcConfig 密钥豁免+ConfigTest 断言）；断链机理：WhitelistFilter 置 skipAuth 后 HeaderEnrichFilter 不注入 X-User-Id/X-Tenant-Id，而 InternalSecretFilter 无条件注入 X-Internal-Secret→下游密钥拦截器（未豁免）验证通过置 internalAuthenticated→RequestContextInterceptor 落「纯服务调用」分支（无 X-User-Id）→缺 X-Tenant-Id 恒 400；PgIT 旧夹具对自服务请求注入完整凭证头=非白名单路径形态，恰好绕开断链（**假阳性根源——测试形态必须按真实网关转发形态构造**）→ **四载体补齐**（SecurityWebMvcConfig 豁免+2 行：豁免后走 Sa-Token 会话分支，tenant 从会话读与 Controller StpUtil 同源；GatewayProperties defaults+2；ConfigTest wl.contains+defaults 断言+2）+ PgIT 自服务请求改真实白名单转发形态（密钥头+Bearer、无用户/租户/签名头）；**红跑实证**：移除豁免两行 my-notices 恒 400「缺少必要请求头: X-Tenant-Id」（与断链机理逐字吻合），恢复 2/2 绿。**P2×2**：publish/revoke 读-判-写竞态（严格拒绝语义并发可被绕过）→ 条件 UPDATE 原子转换（publishFrom status IN(0,2)→1 / revokeFrom status=1→2，影响行数 0 转 10402）；markNoticeAsRead 并发先查后插撞 uk_user_notice→ON CONFLICT DO UPDATE 幂等 upsert。**P3×2 直修**：门禁调用序统一（update/publish/revoke 原先查存在后判权——10401/403 差异可探测公告 id 存在性，统一先门禁）；normalizeAndValidateTarget 显式值域 else throw（非 ALL/USER 一律 10008，服务层兜底 fail-closed）。**P3×4 维持登记任务卡遗留**（update 受众缺省=全量更新语义与 title/content 必填同款契约/noticeType parseInt 无容错=既有形态/my-notices 无分页=旧形态延续/targetUserIds 无 @Size=IdsReq 同款惯例）。**可裁剪×2 采纳直修**：deserializeIds 容错返回空列表分支删除改上抛（拍板无存量=容错输入不存在，静默吞损坏会呈现「空受众 USER 行」）；targetType.isBlank() 归一分支随显式值域拒绝消解（DTO @Pattern 已拦）。文档轨：SysNotice 实体 javadoc 三处旧口径（ORG 列合法值/「逗号分隔」/两态 status）DDL 勘误同主题漏网直修；architecture:304 类名漂移（NoticeServiceImpl→NoticeAppServiceImpl）顺手修。处置后 NoticeLifecyclePgIT 2/2+单测轨道 1385+gateway 全绿。 | SecurityWebMvcConfig（豁免+2）、GatewayProperties（defaults+2）、GatewayApplicationConfigTest（断言+2）、SysNoticeMapper(+XML publishFrom/revokeFrom)、SysUserNoticeMapper(+XML upsertRead)、NoticeAppServiceImpl（原子转换/upsert/门禁序/值域兜底/反序列化上抛）、SysNotice javadoc、architecture.md、NoticeLifecyclePgIT（白名单形态夹具）、docs/tasks/T-ADMIN-029.md | 报了先核出处；①「普通用户自服务端点放行」须四载体同步（yml/defaults/密钥豁免/断言——只改 yml 一处=断链，T-GW-009 先例勿再单载体落）；②MockMvc 测白名单端点须按真实网关白名单转发形态构造（密钥头+令牌、无用户/租户头——注入完整凭证头是假阳性形态）；③publish/revoke 转换=条件 UPDATE 原子判定（勿报「读-判-写够用」——严格拒绝拍板语义依赖原子性）；④deserializeIds 损坏上抛为拍板裁剪形态（勿再报「应容错返回空」） |
| 2026-09-24 | **T-ACCESS-055 核心用户任务组合验收定案**（收官卡；AskUserQuestion 五项拍板）：①四处后端组合缺口（条件变更 S006/岗位停用权限剪枝〔T-FE-057 让渡〕/双租户同码 S011）验收载体=**全部新组合 PgIT**（真库真权限链、可红跑、进全量回归）——ConditionChangeEffectPgIT（IP 白名单→授权挂条件→命中/不命中→改规则→判定双向翻转+CONDITION_RULES 失效串联锁）、PositionDisablePruneObservationPgIT（挂岗→容器角色授权→停用→投影 status=0→引擎剪枝→恢复零授权动作自动生效）、DualTenantSameCodeIsolationPgIT（tenant 2 复制类型基建+直插同码三件；跨读 USER_NOT_FOUND/跨写同码不串/授权隔离双向）；红跑实证 3/3（破坏 markConditions/投影 status 镜像/主体解析 tenant 谓词→红→恢复→绿）。②有限管理员浏览器范围=**分配链+服务负责人双视角**——视角 A 全链通过（强制改密阻断页→菜单实例准入→左树/成员表裁剪→面板分配链〔候选裁剪+排除已绑定→挂载成功回显〕，顺路补 S009 浏览器改密链）；视角 B 判定面实证+页面缺口发现。③**Q-034 维持现状+登记**（可见性裁剪对岗位节点固定 VIEW 不按 VIEW_POSITION 精化——双向过严/过宽非越权，验收卡不夹带候选池语义变更，Q-034 继续 open 留专门任务）。④既有手工证据口径=**自动化重跑+手工引用**（PgIT/E2E 随收口全量在本基线重跑；T-ACCESS-053 compose 主线 2026-09-23 与 T-FE-057/058/059 浏览器截图引用+登记原基线日期，其后变更均不触接入主线）。⑤**视角 B 缺口修法=前端修+后端实例准入**——缺口：服务负责人（SERVICE:MANAGE@access-service 实例）服务映射页整页空白，根因两层=前端 hook fetcher `Promise.all` 并联服务目录（实例过滤 200）+映射全量（类型级 VIEW 403）单路 403 连坐清空〔违反「引用数据多路加载禁 Promise.all」纪律〕×后端 `resource-api-mapping/list` 全量列表「类型级不过即 403」（T-ACCESS-052 收口「维持原登记」遗留）；修复=后端对齐 `listServiceConfigs` 实例准入（类型级不过→持任一 SERVICE 实例 VIEW〔含继承覆盖〕进入+结果按可见服务裁剪；可见性锚点=服务目录全集，可见服务无映射返回空列表而非 403；零可见实例 403 fail-closed）+前端 `Promise.allSettled` 独立收果（映射 403→计数降级 0）；**翻 T-ACCESS-052「resource-api-mapping/list 半边维持原登记」案**；回归锁后端 DelegatedDirectoryClosurePgIT 阶段 4b（owner 全量 200+review-b 探针映射行不泄露，红跑=还原直接 throw 1 红）+前端 hook.spec +3 用例（红跑=旧 Promise.all 1 红→12/12 绿）；dev 栈复验（svc-owner2 经 Gateway 全量 200 仅职责服务；浏览器左栏职责服务可见不泄露无 403）。明确未验收项如实登记：岗位成员浏览器分配链（PositionTab→member-candidates）——VIEW_POSITION/ASSIGN_POSITION_USER 不在 T-ACCESS-052 可转授最小集四条内，经合法赋权不可构造=权限模型有意边界非缺陷（API 链 MemberCandidatesGatePgIT 已覆盖）。Q-033 随卡收敛（契约总册 §4 门禁表 org CRUD 三行+§8.4/§8.5/§8.6 正文补按目标 orgType 解析精化码——纯文档漂移对齐实现）；契约 §12.2 mapping/list 门禁句回写实例准入口径；architecture §14.4 补读端点实例准入口径注记。验证：前端四件套（vitest 450/450+vue-tsc/lint/build 0 错）+收口全量 `mvn test -T 1C`（E2E+heavy 必跑，见任务卡完成记录）。证据与场景矩阵/F 处置对照/基线环境：docs/tasks/evidence/t-access-055/。 | 新增 ConditionChangeEffectPgIT/PositionDisablePruneObservationPgIT/DualTenantSameCodeIsolationPgIT；ResourceManageAppServiceImpl（listApiMappings 实例准入+ServiceConfigMapper 注入，六处测试构造调用点同批补参）；DelegatedDirectoryClosurePgIT（阶段 4b）；frontend service-interface hook.ts（allSettled）+hook.spec.ts（+3）；契约总册 §4/§8.4~8.6/§12.2；architecture §14.4 注记；pending-problems（Q-033 终结+Q-034 拍板注记）；CHANGELOG；docs/tasks/evidence/t-access-055/；docs/tasks/T-ACCESS-055.md | ①Q-034 维持现状是有意拍板（勿报「应分发精化」——候选池语义变更留专门任务）；②岗位浏览器分配链不可达=可转授最小集边界（勿报「权限配错」——合法赋权链构造不出，扩集另行拍板）；③双租户夹具经 jdbc 直插=「现有隔离基建」拍板口径（勿报「应走租户开通产品面」——方向 2026-09-17 已停且 acceptance② 明确不宣称）；④「权限条件」菜单 resourceType=null 全员可见=菜单种子有意形态（页面内容受服务层门禁保护）；⑤T-ACCESS-052「维持原登记」半边已被本卡翻案（评审勿再按旧登记口径报 mapping/list 应类型级） |
