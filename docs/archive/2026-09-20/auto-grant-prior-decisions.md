# 自动授权原定案追溯

下列原文由当前简化设计部分取代；仅作历史追溯。未改变的服务认证、类型所有权、条件直传等边界继续有效，现行口径以[简化设计](../../design/dependency-auto-grant.md)及当前 decision-registry 为准。

| 日期 | 原口径 | 原出处 | 原处置 |
|---|---|---|---|
| 2026-09-19 | T-PERM-035 v1 设计立项定案（外部设计稿三轮讨论+现状代码级核实+过度设计重评，14 项拍板；设计稿=待确认评审稿 draft，用户确认后转 adopted 再拆卡）：主架构=实例级声明+写时物化+热路径零图计算。**①范围**——v1 仅同系统依赖（边必须连接同一 owner 服务的资源；同 owner 规则蕴含目标资格门禁，内部类型/MANAGED 目标结构性封死声明型提权通道），跨系统 export 双边协议+provenance 全链/类型级 TypeDependencyRule/条件路径级追踪/不对称熔断+重建队列/scope_all 种子/注解声明全部后置演进章节（设计完整保留不丢）；**②声明层**——新增 permission_dependency_declaration（两态 RESOLVED/REJECTED+reason_code，无 PENDING 无自动 resolver，重驱动=重新提交）/service_manifest_sync/role_permission_auto_support 三表（service_credential 随⑥凭证升格移出本稿）+ resource_dependency 改造（declaration_id/compiled_revision 溯源列+depends_on_resource_entity_id 反向索引+auto_grant 列退役+maintain_source 收敛 MANIFEST/ADMIN_UI）；resource_dependency 单写者（仅 DependencyCompiler，ADMIN_UI 也走 declaration 且完全豁免同 owner 规则）；batch-sync 端点直接删除（前端零消费方实证 resource-dependency.ts:30）；**③物化**——seed=MANUAL 实例行（depend_on IS NULL、scope_all=false、含条件行——条件直传推翻原外部稿 §29 fail-closed，多路径最宽生效+explain 放宽可见）；显式覆盖压制含条件比较；AUTO_DEP 单 canonical 操作位/行（反链物化）+canGrant=false+depend_on 恒 NULL；role_permission_auto_support 表（parent_support_id/source_grant_id/路径条件，无 origin 列）；rebuild 触发面 6 项（applyGrantPlan/编译 diff/资源删除/类型级联/角色删除/条件变更）；v1 全同步无熔断无队列；**④接入**——manifest 接口只收 dependencies+schemaVersion+revision（不含 resources[]，starter 两步编排 resource-entity/full-sync→manifest full-sync、同 revision 重试第二步）；starter=JSON 清单+Provider SPI（砍 Java 注解声明；清单格式定案 JSON——与服务端请求 DTO 同构、无缩进敏感，2026-09-19 修订）+单租户配置+多租户 TenantRegistrationProvider；**⑤并发**——两级锁 dependency-compile:{tenantId}+auto-grant-role:{tenantId}:{roleId}（锁序=编译锁→角色锁按 id 升序；授权写路径现状零锁为已核实事实）；缓存失效 afterCommit；**⑥凭证**——per-service 静态凭证（credential_id+secret、BCrypt 哈希、TLS 传输、轮换/停用；弃签名制——重放幂等 full-sync 无害、库泄露防护与公钥制等价、省验签/nonce/时钟容忍全套），manifest 与资源同步通道均接受、X-Internal-Secret 遗留形态分期退役；服务凭证升格**公共服务认证模块**（2026-09-19 修订：独立设计稿 docs/design/service-authentication.md 承载现有认证体系盘点+认证链+生命周期+Gateway/SDK 直连形态，独立前置任务卡，多通道共用平台能力——现状=全局密钥+自报 X-Service-Code/X-Tenant-Id 无签名、SDK 接入方配置即持全局密钥、OAuth2 无 client_credentials，均代码级核实） | docs/design/dependency-auto-grant.md（draft）、docs/tasks/T-PERM-035.md（待按 035A/B/C 重写）、docs/design/schema/access-service.sql（DDL 随 035A 落地） | 报了先核出处。claude 外评（2026-09-19，模型 astron-code-latest，headless plan+禁子代理，read-only）：P0/P1=0、P2×11+P3×7 逐条代码级核实全成立采纳——同键多声明**并集聚合**用户拍板（编译键单行、required 位=RESOLVED 声明并集、declaration_id 降诊断性）；机械修：触发面补⑦（操作位变更/删除/所有权声明变更——updateOperation 改 binaryBit 已有 AUTHORITY_ROOT 联动先例而 AUTO_DEP 侧原漏）、锁序改全序三段（树锁→编译锁→角色锁）+七入口取锁矩阵（树锁无 leaseTime 反向持锁=无限互等）、闭包批量装载禁 N+1、multi-bit 触发按资源聚合、清单文件删 serviceCode/操作码复数化（严格 Jackson 文件=线格式）、幂等短路绑定「全 RESOLVED」、credential_id 全局唯一（认证先于租户解析）、表清单三处对齐、client_credentials 措辞订正（schema 注释+internal-service 种子行为历史预留非零命中）、§7③ 20055 歧义澄清（指资源管理面）+declaration 写守卫（MANIFEST 行拒删改）。codex sol xhigh 复评（2026-09-19，session 01a0b957，read-only+禁子代理）：P0=0、P1×5+P2×5+P3×1 逐条代码级核实全成立采纳——INLINE 1:N 派生引用【用户拍板，T-PERM-048 ①口径部分修订（同日行注记），回收时序移 rebuild 后】；机械修：multi-bit 触发带条件证明（不同非空条件组合满足触发位 v1 fail-closed）、认证改单一仲裁器+ServicePrincipal 不可伪造 attribute（拦截器链 AND 语义并列 OR 不可实现）+禁止降级状态机、Gateway 补 M2M 放行链（AuthTokenFilter 先行 401——精确路径清单 skipAuth、禁整体白名单、管理端点不旁路）、service_manifest_sync 加 payload_hash（Provider 动态数据 revision 不变不被短路吞掉=授权撤销失效）、support uk 补 WHERE delete_flag=0（软删重插撞键整事务回滚）、存在跨 owner ADMIN_UI 边时装载租户级 compiled graph（owner 字段降诊断）、树锁族内全序 SYS_ORG→ABSTRACT_ROLE→SYS_MENU→RESOURCE_ENTITY+存量反序登记（类型混合批删）+矩阵⑦补 RESOURCE_ENTITY 锁、DDL 值域对齐（code_type 64/操作码 TEXT[]）、explain 补 DEPENDENCY:VIEW 门禁+禁裸 roleId+凭证端点集限定、support 二遍路径追踪（035C 收口）。codex sol xhigh 第三轮复评（2026-09-19，session 01a0b97d，read-only+禁子代理）：P0=0、P1×4+P2×2 逐条核实全成立采纳（上轮 11 项中 5 通过/6 部分通过——修法文字落地但语义未闭合）：①multi-bit 证明落实为算法（伪码改 buildTriggerProof 按 condition_id 分组、唯一覆盖组触发、传播条件=证明组条件非当前节点单值——遍历顺序无关；证明组=同 dependency 多条 antecedent support 行，uk 含 source_grant_id 容纳；混合组 VIEW 无条件+EXPORT 条件C→传播 C 而非 NULL）；②凭证端点白名单改**服务端强制**（authMethod=CREDENTIAL×精确路径，SDK 直连同受限防调 /auth/query-resources 等超范围端点；清单单源供 Gateway+access-service 两处消费防漂移）；③仲裁状态表补全五形态（完整凭证/半头错凭证 403 不降级/旧密钥+签名用户→USER/旧密钥无用户→旧 SERVICE/无凭证无密钥→403）+order=1/2/3 输入输出钉死（凭证路径只认 principal、用户链维持既有绑定——非"全部只消费 principal"，那会管理 API 大面积 403）；④TLS 回归定案强制（生产凭证 hop 强制 TLS、HTTP 仅显式 dev profile、生产配置检测 HTTP 拒启护栏〔ForwardHeadersStrategyGuard 先例〕，撤销此前"与全局密钥同级接受"放宽句）；⑤payload_hash 规范化契约钉死（参与字段不含 description/NULL=空数组等价/排序去重/全键字典序总排序/canonical JSON UTF-8+SHA-256+五类回归矩阵；§3.2 旧 revision-only 句清除）；⑥INLINE 回收统一模式扩至全部消灭引用入口（①③④⑤=收集候选→删除+rebuild→事务末尾一次回收；schema INLINE 注释回写列入 035B）。codex sol xhigh 第四轮终局复评（2026-09-19，session 01a0b992，read-only+禁子代理）：P0=0、P1×5+P2×3 逐条核实全成立采纳（上轮 6 项 1 通过/5 部分通过——修复语义进一步深化暴露新层）：①manifest 短路补第四条件 is_dirty（③④⑦等外部失效事件同事务置脏——防"历史 SUCCESS 掩盖当前 REJECTED/缺失"、同 R/H 重传被错误短路）；②条件模型升级**多变体**（(资源,操作,条件变体)：NULL 变体吸收非空=「最宽生效」准确形态、C1/C2 不同非空条件两行并存 OR 语义〔uk 含 condition_id 天然容纳、运行时快照按 conditionId 多变体消费同款〕；反链压缩加"操作覆盖**且条件不更窄**"约束——无条件 VIEW 不被条件 C 的 UPDATE 压缩）；③任意操作触发（sourceBits=NULL）定义证明语义（每个持位条件组各产一证明多变体、support 记实际触发 canonical 位禁 NULL 占位、伪码 buildTriggerProofs 修 NPE）；④操作定义变更⑦收紧：binaryBit 变更/操作删除存在任意授权引用（MANUAL/ROOT/AUTO_DEP）**整批拒绝**（对齐 20059 引用守卫先例、弃全量迁移；inheritMask 变更入⑦重编译+rebuild；由此⑦不消灭授权行、INLINE 回收面维持①③④⑤）；⑤TLS 按跳定责任（starter endpoint/Gateway 下游 hop〔lb 原样转发属凭证 hop，生产重加密+secure scheme 校验〕/SDK 直连三跳；dev/test 显式 allow-insecure 键+Spring profile，不依赖 Docker profile 名）；⑥payload_hash 总排序改按每条声明完整 canonical bytes（比较器末键含操作数组——同资源对不同操作组合不得比较相等）+第六类回归；⑦凭证白名单单源落 common 模块（Gateway 不依赖 perm-common）+阶段二端点扩展清单（全部 sync 族逐端点迁移）+退役判据改「仍依赖旧密钥的端点清零」；⑧035C 验收前置=租户级全量 rebuild/support 回填（support_format_version 标记）+回填完成前 explain 禁用。**过度设计重评三裁剪（第四轮后用户逐项拍板）**：①触发位 v1 限单操作（sourceOperationCodes 多值清单级预检拒绝 400——multi-bit 分组证明/多 antecedent support/跨条件 fail-closed 一整套机制随裁消失，设计保留 §14.7 作 v2 能力）；②TLS 改**信任域模型**（跨信任边界 hop 强制 TLS+拒启护栏；单机/纯内网 compose=单信任域显式 allow-insecure 声明，与现状全局密钥同界、不新增内部证书运维——弃三跳每跳强制）；③support 完整性改 **035B 一步到位**（闭包稳定后同事务二遍路径追踪，035C 的 format_version/全量回填/explain 禁用门禁三件套整体取消）。评审勿再报——已修 |
| 2026-09-19 | T-PERM-035 v1 设计定稿（用户确认定稿）：docs/design/dependency-auto-grant.md 与 service-authentication.md 双稿 `draft → adopted`（经 claude 首轮+codex sol 三轮外评 45 项发现全处置、两轮过度设计重评 6 项裁剪后用户审阅确认）；原 T-PERM-035 卡 cancelled（旧口径 resolveAutoGrants/autoGrantForInsert 随取代），拆分承接卡 **T-PERM-070（前置·服务认证）/071（声明层 035A）/072（物化 035B）/073（观测 035C）** 已建（proposed，看板计数器→074）；原 design-review §11 E4 暂缓门禁随本次定稿解除。任务拆分 claude 外评处置（同日，模型 astron-code-latest，headless plan+禁子代理）：P0=0、P1×1+P2×5+P3×4 逐条核实全成立采纳（拆分骨架正确——承接完备性矩阵全绿、反向无凭空新增）——①071 autoGrant 连带面扩为前端全调用面（api 类型+表单载荷〔hook.ts 恒携带实证〕+表格列/开关+前端设计册回写，严格 Jackson 下旧载荷即 400）+perms.ts SYNC 残留/路由/mock 收口；②070 补契约凭证章+403 错误码排号+门禁权限码登记+固定图 API 行；072 补⑦拒绝错误码与契约 §5.3 回写；③四卡 design_refs 补挂契约总册/engine 两册/schema/extension-guide/前端设计册（回写门禁可见性）；④071 校验链补 ADMIN_UI 豁免同 owner（编译期 policy，防跨卡返工）+manifest 端点免固定图判断注记；073 补 explain 固定图行+Cross-eligible 列+manifest 同步状态展示；⑤035 单卡归档（archive/2026-09-19/tasks/，看板链接改归档位）+看板四处旧暂缓口径清扫+035 卡 design_refs 改挂取代依据；⑥§9 凭证稿 status 残留改 adopted；071 空项（bootstrap 依赖种子迁移）删（实测零命中）。评审勿再报——已修 |
