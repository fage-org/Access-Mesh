<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ac.fage.accessmesh.access.permission.mapper.BizDomainMapper">

    <update id="softDeleteBatch">
        UPDATE biz_domain
        SET delete_flag = id, deleted_at = #{deletedAt}
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND delete_flag = 0 AND tenant_id = #{tenantId}
    </update>

    <select id="selectByCode" resultType="cn.ac.fage.accessmesh.access.permission.entity.BizDomain">
        SELECT * FROM biz_domain
        WHERE tenant_id = #{tenantId}
          AND code = #{domainCode}
          AND delete_flag = 0
    </select>

    <select id="selectByCodes" resultType="cn.ac.fage.accessmesh.access.permission.entity.BizDomain">
        SELECT * FROM biz_domain
        WHERE tenant_id = #{tenantId}
          AND code IN
        <foreach collection="domainCodes" item="code" open="(" separator="," close=")">
            #{code}
        </foreach>
          AND delete_flag = 0
    </select>

    <select id="selectValidByIds" resultType="cn.ac.fage.accessmesh.access.permission.entity.BizDomain">
        SELECT * FROM biz_domain
        WHERE tenant_id = #{tenantId}
          AND id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
          AND delete_flag = 0
    </select>

    <select id="selectValidById" resultType="cn.ac.fage.accessmesh.access.permission.entity.BizDomain">
        SELECT * FROM biz_domain
        WHERE id = #{domainId}
          AND tenant_id = #{tenantId}
          AND delete_flag = 0
    </select>

    <sql id="listCondition">
        WHERE tenant_id = #{tenantId}
          AND delete_flag = 0
        <if test="keyword != null and keyword != ''">
          AND (code LIKE CONCAT('%', CAST(#{keyword} AS VARCHAR), '%') OR name LIKE CONCAT('%', CAST(#{keyword} AS VARCHAR), '%')
               OR description LIKE CONCAT('%', CAST(#{keyword} AS VARCHAR), '%'))
        </if>
    </sql>

    <select id="countByCondition" resultType="long">
        SELECT COUNT(*) FROM biz_domain
        <include refid="listCondition"/>
    </select>

    <select id="selectPageByCondition" resultType="cn.ac.fage.accessmesh.access.permission.entity.BizDomain">
        SELECT * FROM biz_domain
        <include refid="listCondition"/>
        ORDER BY code, id
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="selectNonGlobalByTenant" resultType="cn.ac.fage.accessmesh.access.permission.entity.BizDomain">
        SELECT * FROM biz_domain
        WHERE tenant_id = #{tenantId}
          AND global = false
          AND delete_flag = 0
    </select>

    <select id="selectGlobalByTenant" resultType="cn.ac.fage.accessmesh.access.permission.entity.BizDomain">
        SELECT * FROM biz_domain
        WHERE tenant_id = #{tenantId}
          AND global = true
          AND delete_flag = 0
        LIMIT 1
    </select>

    <!-- T-PERM-046：域行锁（写事务内调用）——save 域解析与 remove 引用检查共用，
         串行化删除保护并发窗口（FOR UPDATE 走 mapper 同事务连接，锁持续到提交） -->
    <select id="selectByCodeForUpdate" resultType="cn.ac.fage.accessmesh.access.permission.entity.BizDomain">
        SELECT * FROM biz_domain
        WHERE tenant_id = #{tenantId}
          AND code = #{domainCode}
          AND delete_flag = 0
        FOR UPDATE
    </select>

    <select id="selectValidByIdsForUpdate" resultType="cn.ac.fage.accessmesh.access.permission.entity.BizDomain">
        SELECT * FROM biz_domain
        WHERE tenant_id = #{tenantId}
          AND id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
          AND delete_flag = 0
        FOR UPDATE
    </select>

</mapper>
