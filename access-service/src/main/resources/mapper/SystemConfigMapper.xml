<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ac.fage.accessmesh.access.infrastructure.mapper.SystemConfigMapper">

    <select id="selectByConfigKey" resultType="cn.ac.fage.accessmesh.access.infrastructure.entity.SystemConfig">
        SELECT * FROM system_config
        WHERE tenant_id = #{tenantId}
          AND config_key = #{configKey}
          AND delete_flag = 0
    </select>

    <select id="selectByTenantId" resultType="cn.ac.fage.accessmesh.access.infrastructure.entity.SystemConfig">
        SELECT * FROM system_config
        WHERE tenant_id = #{tenantId}
          AND delete_flag = 0
    </select>

    <!-- T-ACCESS-002 归并：以下语句自原 admin SysConfigMapper.xml 并入，表名统一为 system_config -->
    <update id="softDeleteBatch">
        UPDATE system_config
        SET delete_flag = id, deleted_at = #{deletedAt}
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND delete_flag = 0 AND tenant_id = #{tenantId}
    </update>

    <select id="selectPageByTenantId" resultType="cn.ac.fage.accessmesh.access.infrastructure.entity.SystemConfig">
        SELECT * FROM system_config
        WHERE tenant_id = #{tenantId} AND delete_flag = 0
        ORDER BY created_at ASC
    </select>

    <select id="selectListByIdsAndTenantId" resultType="cn.ac.fage.accessmesh.access.infrastructure.entity.SystemConfig">
        SELECT * FROM system_config
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND tenant_id = #{tenantId} AND delete_flag = 0
    </select>

    <select id="selectOneByIdAndTenantId" resultType="cn.ac.fage.accessmesh.access.infrastructure.entity.SystemConfig">
        SELECT * FROM system_config
        WHERE id = #{id} AND tenant_id = #{tenantId} AND delete_flag = 0
    </select>

</mapper>
