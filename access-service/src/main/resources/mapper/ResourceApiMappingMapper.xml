<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ac.fage.accessmesh.access.permission.mapper.ResourceApiMappingMapper">

    <update id="softDeleteBatch">
        UPDATE resource_api_mapping
        SET delete_flag = id, deleted_at = #{deletedAt}
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND delete_flag = 0 AND tenant_id = #{tenantId}
    </update>

    <select id="selectValidById" resultType="cn.ac.fage.accessmesh.access.permission.entity.ResourceApiMapping">
        SELECT * FROM resource_api_mapping
        WHERE id = #{id}
          AND tenant_id = #{tenantId}
          AND delete_flag = 0
    </select>

    <select id="selectForInterfaceCheck" resultType="cn.ac.fage.accessmesh.access.permission.entity.ResourceApiMapping">
        SELECT * FROM resource_api_mapping
        WHERE tenant_id = #{tenantId}
          AND service_code = #{serviceCode}
          AND http_method = #{httpMethod}
          AND delete_flag = 0
          AND enabled = true
    </select>

    <select id="selectForSnapshot" resultType="cn.ac.fage.accessmesh.access.permission.entity.ResourceApiMapping">
        SELECT * FROM resource_api_mapping
        WHERE tenant_id = #{tenantId}
          AND service_code = #{serviceCode}
          AND resource_entity_id IN
        <foreach collection="resourceIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
          AND delete_flag = 0
          AND enabled = true
    </select>

    <select id="selectValidList" resultType="cn.ac.fage.accessmesh.access.permission.entity.ResourceApiMapping">
        SELECT * FROM resource_api_mapping
        WHERE tenant_id = #{tenantId}
          AND delete_flag = 0
        <if test="resourceId != null">
          AND resource_entity_id = #{resourceId}
        </if>
        <if test="serviceCode != null and serviceCode != ''">
          AND service_code = #{serviceCode}
        </if>
    </select>

    <select id="selectValidByIds" resultType="cn.ac.fage.accessmesh.access.permission.entity.ResourceApiMapping">
        SELECT * FROM resource_api_mapping
        WHERE tenant_id = #{tenantId}
          AND id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
          AND delete_flag = 0
    </select>

    <select id="selectByResourceEntityIds" resultType="cn.ac.fage.accessmesh.access.permission.entity.ResourceApiMapping">
        SELECT * FROM resource_api_mapping
        WHERE tenant_id = #{tenantId}
          AND resource_entity_id IN
        <foreach collection="resourceEntityIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
          AND delete_flag = 0
    </select>

    <select id="selectByTenantAndServiceCode" resultType="cn.ac.fage.accessmesh.access.permission.entity.ResourceApiMapping">
        SELECT * FROM resource_api_mapping
        WHERE tenant_id = #{tenantId}
          AND service_code = #{serviceCode}
          AND delete_flag = 0
    </select>

    <select id="selectByUniqueKey" resultType="cn.ac.fage.accessmesh.access.permission.entity.ResourceApiMapping">
        SELECT * FROM resource_api_mapping
        WHERE tenant_id = #{tenantId}
          AND resource_entity_id = #{resourceEntityId}
          AND service_code = #{serviceCode}
          AND http_method = #{httpMethod}
          AND path_pattern = #{pathPattern}
          AND delete_flag = 0
    </select>

</mapper>