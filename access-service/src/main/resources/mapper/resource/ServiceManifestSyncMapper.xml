<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ac.fage.accessmesh.access.resource.mapper.ServiceManifestSyncMapper">
    <update id="markDirtyScopes">
        UPDATE service_manifest_sync SET is_dirty=true,updated_at=now()
        WHERE tenant_id=#{tenantId} AND delete_flag=0 AND source_service IN
        <foreach collection="services" item="service" open="(" separator="," close=")">#{service}</foreach>
    </update>
    <select id="selectScope" resultType="cn.ac.fage.accessmesh.access.resource.entity.ServiceManifestSync">
        SELECT * FROM service_manifest_sync
        WHERE tenant_id=#{tenantId} AND source_service=#{service} AND delete_flag=0
    </select>
    <update id="save">
        INSERT INTO service_manifest_sync
          (tenant_id,source_service,publication_generation,revision,payload_hash,semantic_hash,sync_status,is_dirty,last_synced_at)
        VALUES (#{state.tenantId},#{state.sourceService},#{state.publicationGeneration},#{state.revision},
                #{state.payloadHash},#{state.semanticHash},#{state.syncStatus},#{state.isDirty},#{state.lastSyncedAt})
        ON CONFLICT (tenant_id,source_service) WHERE delete_flag=0
        DO UPDATE SET publication_generation=EXCLUDED.publication_generation,revision=EXCLUDED.revision,
          payload_hash=EXCLUDED.payload_hash,semantic_hash=EXCLUDED.semantic_hash,sync_status=EXCLUDED.sync_status,
          is_dirty=EXCLUDED.is_dirty,last_synced_at=EXCLUDED.last_synced_at,updated_at=EXCLUDED.last_synced_at
        WHERE EXCLUDED.publication_generation &gt; service_manifest_sync.publication_generation
           OR (EXCLUDED.publication_generation=service_manifest_sync.publication_generation
               AND EXCLUDED.payload_hash=service_manifest_sync.payload_hash)
    </update>
</mapper>
