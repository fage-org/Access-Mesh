<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ac.fage.accessmesh.access.admin.mapper.SysNoticeMapper">

    <select id="selectByIdSafe" resultType="cn.ac.fage.accessmesh.access.admin.entity.SysNotice">
        SELECT * FROM sys_notice
        WHERE id = #{id} AND tenant_id = #{tenantId} AND delete_flag = 0
    </select>

    <select id="selectByIdsSafe" resultType="cn.ac.fage.accessmesh.access.admin.entity.SysNotice">
        SELECT * FROM sys_notice
        WHERE tenant_id = #{tenantId} AND delete_flag = 0
        AND id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <!-- XML 分页统一 offset/limit + count 双查询（T-ADMIN-026；id 作 tie-breaker 保证分页稳定） -->
    <select id="selectByTenantPaged" resultType="cn.ac.fage.accessmesh.access.admin.entity.SysNotice">
        SELECT * FROM sys_notice
        WHERE tenant_id = #{tenantId} AND delete_flag = 0
        ORDER BY created_at DESC, id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByTenant" resultType="long">
        SELECT COUNT(*) FROM sys_notice
        WHERE tenant_id = #{tenantId} AND delete_flag = 0
    </select>

    <select id="selectPublishedByTenant" resultType="cn.ac.fage.accessmesh.access.admin.entity.SysNotice">
        SELECT * FROM sys_notice
        WHERE tenant_id = #{tenantId} AND delete_flag = 0 AND status = 1
        ORDER BY created_at DESC
    </select>

    <update id="softDeleteBatch">
        UPDATE sys_notice
        SET delete_flag = id, deleted_at = #{deletedAt}
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND delete_flag = 0
        AND tenant_id = #{tenantId}
    </update>

</mapper>
