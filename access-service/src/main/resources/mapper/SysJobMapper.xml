<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ac.fage.accessmesh.access.admin.mapper.SysJobMapper">

    <select id="selectValidById" resultType="cn.ac.fage.accessmesh.access.admin.entity.SysJob">
        SELECT * FROM sys_job
        WHERE id = #{id} AND tenant_id = #{tenantId} AND delete_flag = 0
    </select>

    <!-- T-ACCESS-009：跨租户单条批量查询（启动加载/多实例对账），替代按租户循环查询（§8.4.8）；
         原按租户的 selectEnabledJobs 已随之删除，避免两套查询口径重新引入循环查询 -->
    <select id="selectAllEnabledJobs" resultType="cn.ac.fage.accessmesh.access.admin.entity.SysJob">
        SELECT * FROM sys_job
        WHERE status = 1 AND delete_flag = 0
    </select>

    <select id="selectValidByIds" resultType="cn.ac.fage.accessmesh.access.admin.entity.SysJob">
        SELECT * FROM sys_job
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND tenant_id = #{tenantId} AND delete_flag = 0
    </select>

    <!-- XML 分页统一 offset/limit + count 双查询（T-ADMIN-026；id 作 tie-breaker 保证分页稳定） -->
    <select id="selectJobsByCondition" resultType="cn.ac.fage.accessmesh.access.admin.entity.SysJob">
        SELECT * FROM sys_job
        WHERE tenant_id = #{tenantId} AND delete_flag = 0
        <if test="jobGroup != null">
            AND job_group = #{jobGroup}
        </if>
        ORDER BY created_at DESC, id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countJobsByCondition" resultType="long">
        SELECT COUNT(*) FROM sys_job
        WHERE tenant_id = #{tenantId} AND delete_flag = 0
        <if test="jobGroup != null">
            AND job_group = #{jobGroup}
        </if>
    </select>

    <update id="softDeleteBatch">
        UPDATE sys_job
        SET delete_flag = id, deleted_at = #{deletedAt}
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND delete_flag = 0
        AND tenant_id = #{tenantId}
    </update>

</mapper>
