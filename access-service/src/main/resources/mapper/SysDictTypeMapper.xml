<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ac.fage.accessmesh.access.admin.mapper.SysDictTypeMapper">

    <select id="selectByIdSafe" resultType="cn.ac.fage.accessmesh.access.admin.entity.SysDictType">
        SELECT * FROM sys_dict_type
        WHERE id = #{id} AND tenant_id = #{tenantId} AND delete_flag = 0
    </select>

    <select id="selectByIdsAndTenant" resultType="cn.ac.fage.accessmesh.access.admin.entity.SysDictType">
        SELECT * FROM sys_dict_type
        WHERE tenant_id = #{tenantId} AND delete_flag = 0
        AND id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <select id="selectByTenantId" resultType="cn.ac.fage.accessmesh.access.admin.entity.SysDictType">
        SELECT * FROM sys_dict_type
        WHERE tenant_id = #{tenantId} AND delete_flag = 0
        ORDER BY created_at ASC
    </select>

    <!-- XML 分页统一 offset/limit + count 双查询（T-ADMIN-026；id 作 tie-breaker 保证分页稳定） -->
    <select id="selectByTenantIdPaged" resultType="cn.ac.fage.accessmesh.access.admin.entity.SysDictType">
        SELECT * FROM sys_dict_type
        WHERE tenant_id = #{tenantId} AND delete_flag = 0
        ORDER BY created_at ASC, id ASC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByTenantId" resultType="long">
        SELECT COUNT(*) FROM sys_dict_type
        WHERE tenant_id = #{tenantId} AND delete_flag = 0
    </select>

    <update id="softDeleteBatch">
        UPDATE sys_dict_type
        SET delete_flag = id, deleted_at = #{deletedAt}
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND delete_flag = 0 AND tenant_id = #{tenantId}
    </update>

</mapper>
