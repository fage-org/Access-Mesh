<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ac.fage.accessmesh.access.platform.mapper.SysUserNoticeMapper">

    <select id="selectByUserAndNoticeIds" resultType="cn.ac.fage.accessmesh.access.platform.entity.SysUserNotice">
        SELECT * FROM sys_user_notice
        WHERE user_id = #{userId} AND tenant_id = #{tenantId}
        AND notice_id IN
        <foreach collection="noticeIds" item="noticeId" open="(" separator="," close=")">
            #{noticeId}
        </foreach>
    </select>

    <!-- 公告删除级联清理已读记录（T-ADMIN-029；本表无 delete_flag，物理删除即 DDL 形态） -->
    <delete id="deleteByNoticeIds">
        DELETE FROM sys_user_notice
        WHERE tenant_id = #{tenantId}
        AND notice_id IN
        <foreach collection="noticeIds" item="noticeId" open="(" separator="," close=")">
            #{noticeId}
        </foreach>
    </delete>

    <!-- 标已读幂等 upsert（T-ADMIN-029 双轨评审 P2-2：并发双 read 撞 uk_user_notice 唯一键的
         先查后插窗口消除——ON CONFLICT DO UPDATE 仓库成熟模式） -->
    <insert id="upsertRead">
        INSERT INTO sys_user_notice (tenant_id, notice_id, user_id, is_read, read_at)
        VALUES (#{tenantId}, #{noticeId}, #{userId}, true, #{readAt})
        ON CONFLICT (tenant_id, notice_id, user_id)
        DO UPDATE SET is_read = true, read_at = #{readAt}
    </insert>

</mapper>
