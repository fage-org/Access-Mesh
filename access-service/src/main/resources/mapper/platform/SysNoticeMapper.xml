<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ac.fage.accessmesh.access.platform.mapper.SysNoticeMapper">

    <select id="selectByIdSafe" resultType="cn.ac.fage.accessmesh.access.platform.entity.SysNotice">
        SELECT * FROM sys_notice
        WHERE id = #{id} AND tenant_id = #{tenantId} AND delete_flag = 0
    </select>

    <select id="selectByIdsSafe" resultType="cn.ac.fage.accessmesh.access.platform.entity.SysNotice">
        SELECT * FROM sys_notice
        WHERE tenant_id = #{tenantId} AND delete_flag = 0
        AND id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <!-- XML 分页统一 offset/limit + count 双查询（T-ADMIN-026；id 作 tie-breaker 保证分页稳定） -->
    <select id="selectByTenantPaged" resultType="cn.ac.fage.accessmesh.access.platform.entity.SysNotice">
        SELECT * FROM sys_notice
        WHERE tenant_id = #{tenantId} AND delete_flag = 0
        ORDER BY created_at DESC, id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByTenant" resultType="long">
        SELECT COUNT(*) FROM sys_notice
        WHERE tenant_id = #{tenantId} AND delete_flag = 0
    </select>

    <!-- 受众可见口径（T-ADMIN-029）：status=1 且（ALL 或 USER 且 target_ids JSONB 数组包含该用户）；
         target_ids 为 NULL/空数组的 USER 行不匹配任何人（fail-closed）。
         to_jsonb(bigint) 构造数字标量供 @> 包含判定，[101,102] @> 101 → true -->
    <select id="selectVisibleByUser" resultType="cn.ac.fage.accessmesh.access.platform.entity.SysNotice">
        SELECT * FROM sys_notice
        WHERE tenant_id = #{tenantId} AND delete_flag = 0 AND status = 1
        AND (target_type = 'ALL'
             OR (target_type = 'USER' AND target_ids @> to_jsonb(#{userId})))
        ORDER BY created_at DESC, id DESC
    </select>

    <select id="countVisibleById" resultType="long">
        SELECT COUNT(*) FROM sys_notice
        WHERE tenant_id = #{tenantId} AND delete_flag = 0 AND id = #{id} AND status = 1
        AND (target_type = 'ALL'
             OR (target_type = 'USER' AND target_ids @> to_jsonb(#{userId})))
    </select>

    <!-- 状态机原子转换（T-ADMIN-029 双轨评审 P2-1：条件 UPDATE 下沉判定，消读-判-写竞态窗口；
         影响行数 0 = 前置状态不符 → 服务层 10402 严格拒绝） -->
    <update id="publishFrom">
        UPDATE sys_notice
        SET status = 1, published_at = #{now}, updated_at = #{now}
        WHERE tenant_id = #{tenantId} AND id = #{id} AND delete_flag = 0
        AND status IN (0, 2)
    </update>

    <update id="revokeFrom">
        UPDATE sys_notice
        SET status = 2, updated_at = #{now}
        WHERE tenant_id = #{tenantId} AND id = #{id} AND delete_flag = 0
        AND status = 1
    </update>

    <update id="softDeleteBatch">
        UPDATE sys_notice
        SET delete_flag = id, deleted_at = #{deletedAt}
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND delete_flag = 0
        AND tenant_id = #{tenantId}
    </update>

</mapper>
