<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ac.fage.accessmesh.access.admin.mapper.SysOauth2ClientMapper">

    <update id="softDeleteBatch">
        UPDATE sys_oauth2_client
        SET delete_flag = id, deleted_at = #{deletedAt}
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND delete_flag = 0 AND tenant_id = #{tenantId}
    </update>

    <select id="selectByIdSafe" resultType="cn.ac.fage.accessmesh.access.admin.entity.SysOauth2Client">
        SELECT * FROM sys_oauth2_client
        WHERE id = #{id} AND tenant_id = #{tenantId} AND delete_flag = 0
    </select>

    <select id="selectByClientId" resultType="cn.ac.fage.accessmesh.access.admin.entity.SysOauth2Client">
        SELECT * FROM sys_oauth2_client
        WHERE tenant_id = #{tenantId} AND client_id = #{clientId} AND delete_flag = 0
    </select>

    <!-- XML 分页统一 offset/limit + count 双查询（T-ADMIN-026；id 作 tie-breaker 保证分页稳定） -->
    <select id="selectClientsByCondition" resultType="cn.ac.fage.accessmesh.access.admin.entity.SysOauth2Client">
        SELECT * FROM sys_oauth2_client
        WHERE tenant_id = #{tenantId} AND delete_flag = 0
        <if test="clientName != null and clientName != ''">
            AND client_name LIKE CONCAT('%', CAST(#{clientName} AS VARCHAR), '%')
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY created_at DESC, id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countClientsByCondition" resultType="long">
        SELECT COUNT(*) FROM sys_oauth2_client
        WHERE tenant_id = #{tenantId} AND delete_flag = 0
        <if test="clientName != null and clientName != ''">
            AND client_name LIKE CONCAT('%', CAST(#{clientName} AS VARCHAR), '%')
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
    </select>

    <select id="selectActiveByClientId" resultType="cn.ac.fage.accessmesh.access.admin.entity.SysOauth2Client">
        SELECT * FROM sys_oauth2_client
        WHERE client_id = #{clientId} AND status = 1 AND delete_flag = 0
    </select>

</mapper>
