<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ac.fage.accessmesh.access.permission.mapper.TypeDefinitionMapper">

    <update id="softDeleteBatch">
        UPDATE type_definition
        SET delete_flag = id, deleted_at = #{deletedAt}
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND delete_flag = 0 AND tenant_id = #{tenantId}
    </update>

    <select id="selectByTypeKeyAndCode" resultType="cn.ac.fage.accessmesh.access.permission.entity.TypeDefinition">
        SELECT * FROM type_definition
        WHERE tenant_id = #{tenantId}
          AND type_key = #{typeKey}
          AND type_code = #{typeCode}
          AND delete_flag = 0
    </select>

    <select id="selectByTypeKeyAndValue" resultType="cn.ac.fage.accessmesh.access.permission.entity.TypeDefinition">
        SELECT * FROM type_definition
        WHERE tenant_id = #{tenantId}
          AND type_key = #{typeKey}
          AND type_value = #{typeValue}
          AND delete_flag = 0
    </select>

    <select id="selectByTypeKeyAndCodes" resultType="cn.ac.fage.accessmesh.access.permission.entity.TypeDefinition">
        SELECT * FROM type_definition
        WHERE tenant_id = #{tenantId}
          AND type_key = #{typeKey}
          AND type_code IN
        <foreach collection="codes" item="code" open="(" separator="," close=")">
            #{code}
        </foreach>
          AND delete_flag = 0
    </select>

    <select id="selectByTypeKeyAndValues" resultType="cn.ac.fage.accessmesh.access.permission.entity.TypeDefinition">
        SELECT * FROM type_definition
        WHERE tenant_id = #{tenantId}
          AND type_key = #{typeKey}
          AND type_value IN
        <foreach collection="values" item="value" open="(" separator="," close=")">
            #{value}
        </foreach>
          AND delete_flag = 0
    </select>

    <select id="selectValidById" resultType="cn.ac.fage.accessmesh.access.permission.entity.TypeDefinition">
        SELECT * FROM type_definition
        WHERE id = #{typeDefId}
          AND tenant_id = #{tenantId}
          AND delete_flag = 0
    </select>

    <select id="selectValidByIds" resultType="cn.ac.fage.accessmesh.access.permission.entity.TypeDefinition">
        SELECT * FROM type_definition
        WHERE tenant_id = #{tenantId}
          AND delete_flag = 0
          AND id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <select id="selectByTenantAndTypeKey" resultType="cn.ac.fage.accessmesh.access.permission.entity.TypeDefinition">
        SELECT * FROM type_definition
        WHERE tenant_id = #{tenantId}
          AND type_key = #{typeKey}
          AND delete_flag = 0
    </select>

    <!-- typeValue 自动分配：含软删行取 max（软删不复用是分配语义本身，不加 delete_flag 过滤） -->
    <select id="selectMaxTypeValueAllRows" resultType="java.lang.Integer">
        SELECT MAX(type_value) FROM type_definition
        WHERE tenant_id = #{tenantId}
          AND type_key = #{typeKey}
    </select>

    <!-- keyword 参数必须 CAST AS VARCHAR：JDBC URL 带 stringtype=unspecified（UTC TypeHandler 所需），
         CONCAT(variadic "any") 下 PG 无法推断未定型参数类型会直接 500（T-FE-022 联调暴露，
         同族 LIKE CONCAT 全部 mapper 一并修复） -->
    <sql id="listCondition">
        WHERE tenant_id = #{tenantId}
          AND delete_flag = 0
        <if test="typeKey != null">
          AND type_key = #{typeKey}
        </if>
        <if test="keyword != null and keyword != ''">
          AND (name LIKE CONCAT('%', CAST(#{keyword} AS VARCHAR), '%') OR type_code LIKE CONCAT('%', CAST(#{keyword} AS VARCHAR), '%'))
        </if>
    </sql>

    <select id="countByCondition" resultType="long">
        SELECT COUNT(*) FROM type_definition
        <include refid="listCondition"/>
    </select>

    <select id="selectPageByCondition" resultType="cn.ac.fage.accessmesh.access.permission.entity.TypeDefinition">
        SELECT * FROM type_definition
        <include refid="listCondition"/>
        ORDER BY sort_order, id
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- T-PERM-051：TYPE_DEFINITION 实例门禁按复合业务键 {typeKey}:{typeCode} 批量判定，
         需 (type_key, type_code) 对（格式构造唯一入口 BusinessKeys.typeInstanceBusinessKey，
         禁止在 SQL 内拼串） -->
    <select id="selectValidByTenant" resultType="cn.ac.fage.accessmesh.access.permission.entity.TypeDefinition">
        SELECT * FROM type_definition
        WHERE tenant_id = #{tenantId}
          AND delete_flag = 0
    </select>

</mapper>
