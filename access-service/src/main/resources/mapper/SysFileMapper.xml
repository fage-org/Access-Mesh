<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ac.fage.accessmesh.access.admin.mapper.SysFileMapper">

    <select id="selectValidById" resultType="cn.ac.fage.accessmesh.access.admin.entity.SysFile">
        SELECT * FROM sys_file
        WHERE id = #{id} AND tenant_id = #{tenantId} AND delete_flag = 0
    </select>

    <select id="selectValidByIds" resultType="cn.ac.fage.accessmesh.access.admin.entity.SysFile">
        SELECT * FROM sys_file
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND tenant_id = #{tenantId} AND delete_flag = 0
    </select>

    <!-- XML 分页统一 offset/limit + count 双查询（T-ADMIN-026；id 作 tie-breaker 保证分页稳定）；
         buckets 可见文件夹过滤（T-ADMIN-025 page 按可见文件夹裁剪，null=不过滤） -->
    <select id="selectFilesByCondition" resultType="cn.ac.fage.accessmesh.access.admin.entity.SysFile">
        SELECT * FROM sys_file
        WHERE tenant_id = #{tenantId} AND delete_flag = 0
        <if test="bizType != null">
            AND bucket_name = #{bizType}
        </if>
        <if test="buckets != null">
            AND bucket_name IN
            <foreach collection="buckets" item="bucket" open="(" separator="," close=")">
                #{bucket}
            </foreach>
        </if>
        ORDER BY created_at DESC, id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countFilesByCondition" resultType="long">
        SELECT COUNT(*) FROM sys_file
        WHERE tenant_id = #{tenantId} AND delete_flag = 0
        <if test="bizType != null">
            AND bucket_name = #{bizType}
        </if>
        <if test="buckets != null">
            AND bucket_name IN
            <foreach collection="buckets" item="bucket" open="(" separator="," close=")">
                #{bucket}
            </foreach>
        </if>
    </select>

    <!-- T-ADMIN-025：page 可见文件夹全集（有效文件覆盖的 bucket_name 去重） -->
    <select id="selectDistinctBucketNames" resultType="java.lang.String">
        SELECT DISTINCT bucket_name FROM sys_file
        WHERE tenant_id = #{tenantId} AND delete_flag = 0 AND bucket_name IS NOT NULL
        <if test="bizType != null">
            AND bucket_name = #{bizType}
        </if>
    </select>

    <update id="softDeleteBatch">
        UPDATE sys_file
        SET delete_flag = id, deleted_at = #{deletedAt}
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND delete_flag = 0
        AND tenant_id = #{tenantId}
    </update>

</mapper>
