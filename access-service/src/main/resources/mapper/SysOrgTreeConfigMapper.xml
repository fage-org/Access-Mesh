<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ac.fage.accessmesh.access.admin.mapper.SysOrgTreeConfigMapper">

    <update id="softDeleteBatch">
        UPDATE sys_org_tree_config
        SET delete_flag = id, deleted_at = #{deletedAt}
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND delete_flag = 0 AND tenant_id = #{tenantId}
    </update>

    <select id="selectByIdSafe" resultType="cn.ac.fage.accessmesh.access.admin.entity.SysOrgTreeConfig">
        SELECT * FROM sys_org_tree_config
        WHERE id = #{id} AND tenant_id = #{tenantId} AND delete_flag = 0
    </select>

    <select id="paginateAll" resultType="cn.ac.fage.accessmesh.access.admin.entity.SysOrgTreeConfig">
        SELECT * FROM sys_org_tree_config
        WHERE tenant_id = #{tenantId} AND delete_flag = 0
        ORDER BY created_at DESC
    </select>

    <select id="selectDefaultConfigs" resultType="cn.ac.fage.accessmesh.access.admin.entity.SysOrgTreeConfig">
        SELECT * FROM sys_org_tree_config
        WHERE tenant_id = #{tenantId} AND delete_flag = 0 AND is_default = true
    </select>

    <select id="selectAllValid" resultType="cn.ac.fage.accessmesh.access.admin.entity.SysOrgTreeConfig">
        SELECT * FROM sys_org_tree_config
        WHERE tenant_id = #{tenantId} AND delete_flag = 0
    </select>

    <update id="clearAllDefaults">
        UPDATE sys_org_tree_config
        SET is_default = false, updated_at = #{updatedAt}
        WHERE tenant_id = #{tenantId} AND delete_flag = 0 AND is_default = true
    </update>

</mapper>
