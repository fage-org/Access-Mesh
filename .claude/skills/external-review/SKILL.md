---
name: external-review
description: >-
  外部 AI 评审执行规范（claude / grok / codex 三通道；未点名时使用 CLI 当前配置的默认模型，不检查模型漂移；
  codex 默认配置示例 gpt-6-luna max，点名 gpt-6-sol 封顶 xhigh；grok 默认 grok-4.7；
  全程禁止子代理；本机运行或用户贴回结论两种形态）。
  TRIGGER when: 用户显式要求外部 AI 评审/复评（「用codex/claude/grok评审」「codex 复评」、逐字给出提示词、
  贴回外部 AI 结论要求「核实并修复」）；仅用户触发，收口流程不得自动串联。
disable-model-invocation: true
origin: project
metadata:
  project: AccessMesh
  version: "2.6.0"
---

# 外部 AI 评审（claude / grok / codex；仅用户触发；全程禁止子代理）

## 触发纪律（2026-09-06 用户定案）

- **仅当用户显式指令时执行**：如「用codex评审」「claude/grok 复评」、用户逐字给出提示词、用户贴回外部 AI 结论并要求核实修复（此形态不必本机运行 CLI）。
- **通道由用户点名，模型与推理档优先采用用户本次指定值**：可单通道也可多通道并行；未指定的项沿用对应 CLI 当前配置，不用技能中的示例覆盖配置。Codex 默认配置示例为 `gpt-6-luna` + `max`，用户可指定 `gpt-6-sol`（封顶 `xhigh`）；Grok 默认模型为 `grok-4.7`。
- **不再检查模型漂移（2026-09-26 用户定案）**：不比对配置、启动 banner 与会话返回的模型身份，不为此运行探针、停机、重跑或要求用户决策。报告按调用通道及已指定参数说明即可，不把配置名宣称为已验证的后端模型身份。本条取代原强制注入默认模型、启动核对及返回模型身份核验要求。
- 收口流程（`dual-track-local-review`）不得自动串联本轨道；复评是否续跑、跑几轮**由用户拍板**，无自动收敛轮数——每轮报告后停下等指令，不自行发起下一轮。

## 禁止子代理（2026-09-12 用户定案，三通道统一硬约束）

外部评审全程禁止子代理：子代理上下文不继承注入的豁免段与专项清单，结论不可归因、过程不可追溯。三个 CLI 均有官方开关，一律**开关硬关 + 提示词禁令**双保险（禁令已内置模板「硬性纪律」节，开头即声明）：

| 通道   | 硬关开关                  | 说明                                                                                       |
| ------ | ------------------------- | ------------------------------------------------------------------------------------------ |
| codex  | `--disable multi_agent`   | = `-c features.multi_agent=false`（`codex features list` 实证 multi_agent 默认 stable/true）；resume 用 `--disable`/`-c` 均可 |
| claude | `--disallowedTools Task Workflow` | 拒绝 Task（子代理派生）与 Workflow（多代理编排扇出）两工具；plan 模式系统指示会主动要求并行 Explore 子代理（2026-09-12 外评会话实证），双拒后其余入口由提示词禁令兜底 |
| grok   | `--no-subagents`          | Disable subagent spawning                                                                  |

- 原「codex 实跑测试时 spawn_agent 委派子代理隔离长输出」手法**退役**：子代理禁令下测试长命令只能落主上下文（占额度），大输出测试优先本机自跑、评审只引用落盘报告。
- 本地双轨评审（`dual-track-local-review`）自 2026-09-26 起同样不使用子代理（主代理直跑两轨）——子代理禁令已覆盖全部评审通道。

## 三通道调用形态（大提示词 stdin/文件投喂；输出整文件落盘）

- **提示词一律 stdin/文件投喂**（v2 常规评审 ~5-8KB，送评大上下文场景仍可能更大）：argv `"$(cat file)"` 形式在 Git Bash 报 `Argument list too long` **静默零输出**。
- 三通道可同批并行：各自后台跑 + 输出整文件落盘，互不干扰。
- **后台跑的 stdin 语义（codex）**：argv 传短提示词且后台运行时必须 `< /dev/null`（run_in_background 的 stdin 是永不关闭的管道，codex 打印 `Reading additional input from stdin...` 后无限等待）；用 `- < prompt.md` 投喂时 stdin 即提示词文件、EOF 正常到达，无需 `< /dev/null`。

### codex（未点名时沿用当前配置；点名形态如 sol xhigh）

未点名模型与推理档时，不传 `-m` 或 `model_reasoning_effort` 覆盖项。当前配置为 luna 时，保留既有上下文参数：

```bash
codex exec -c model_context_window=872000 --disable multi_agent -s read-only --color never - < prompt.md
```

#### 模型与上下文

- `gpt-6-luna` + reasoning effort `max` 是默认配置示例；「luna max」不是单独的模型 ID。实际选择以当前 CLI 配置或用户显式指定为准，不强制改回示例值。
- 上下文参数按本次使用的配置模型或点名模型区分；读取配置用于选择调用参数，不核验后端返回身份：

  | 模型 | reasoning effort | 上下文注入 |
  | ---- | ---------------- | ---------- |
  | luna（`gpt-6-luna`） | 未点名沿用配置（默认示例 `max`） | `-c model_context_window=872000`（800k 级写法） |
  | sol（`gpt-6-sol`） | 点名时**封顶 `xhigh`**，用户指定更低档则按指定值 | **不注入** |
  | 其他当前配置模型 | 沿用配置或用户指定值 | 不套用 luna 覆盖参数 |

- 点名 sol 的调用示例：

  ```bash
  codex exec -m gpt-6-sol -c 'model_reasoning_effort="xhigh"' --disable multi_agent -s read-only --color never - < prompt.md
  ```

- luna 上下文仍用 `872000`，不是 `800000`：按既有 95% 有效窗口折扣，分别为 828.4k 与 760k。sol 仍沿用用户确认的封顶 `xhigh`、不注入上下文分档；不因工具支持更高档而自行解封。

#### 运行注意

- **进程清理**：清理 codex.exe「残留」前先查父进程——Codex 桌面应用常驻 app-server 会持续再生 codex.exe，那是用户自己的进程，勿误杀；仅 TaskStop 后日志停止增长但进程存活的情况才需要 taskkill。
- **404 故障窗口**：chatgpt.com 后端 404 可持续数十分钟后自愈，先最小探针确认，别急着归因本机。

#### 沙箱与额度

| 场景                     | 选择                                                                                                                                                                                     |
| ------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 纯评审轮（预期不跑测试） | `--sandbox read-only`（Maven/Testcontainers 跑不了，只能引用既有 surefire 报告，验证靠本地自跑）                                                                                          |
| 要 codex 实跑测试        | `-s workspace-write` + 提示词约束逻辑只读（禁改任何 git 跟踪文件、结束 git status 须与开始一致）；测试长命令在主上下文跑（子代理禁令），大输出优先本机自跑后引用落盘报告                    |

- 跑完必须实核 `git status` 干净。
- **额度中断 → resume 续跑不从头**（用户定规省额度）：`codex exec resume <session-id> "继续…"`；resume 子命令不认 `--color`/`-s`（feature 开关 `--disable` 与 `-c` 均可用）——模型仅在用户点名时用 `-m`（未点名沿用当前配置）、沙箱用 `-c sandbox_mode="workspace-write"`、上下文按模型分档（luna 用 `-c model_context_window=872000`、sol 不注入，见「模型与上下文」）、子代理禁令用 `--disable multi_agent` 或 `-c features.multi_agent=false`，`-o <file>` 可把最终报告单独落盘。**先报告中断并等用户定续跑时刻**（codex 报的重试窗口未必等于实际额度重置点）；续跑前先最小探针确认额度已重置（一句话探针数秒返回服务端判定，口头「应该重置了」不作数）；session-id 在日志头 `session id:` 行。
- 沙箱环境限制要澄清勿误采信：workspace-write 无 Docker（Testcontainers 全 skip）、esbuild spawn EPERM（vitest/build 起不来）——「无法复验」≠声明造假，以本机实跑记录为准、本地补跑定向测试。

### claude

```bash
cat prompt.md | claude -p --permission-mode plan --output-format text --disallowedTools Task Workflow
```

- 未点名时使用当前 CLI 配置的默认模型；用户点名则显式指定。不额外核验配置与返回模型身份。
- headless plan 模式 ExitPlanMode 不可用时完整产出落 `~/.claude/plans/*.md`、stdout 只有摘要——**必须去读 plan 文件**。
- stderr 的 `claude.ai connectors are disabled` 警告为良性噪声。

### grok

```bash
~/.grok/bin/grok --prompt-file prompt.md --always-approve --sandbox read-only --max-turns 40 --no-subagents
```

- **headless 后台跑禁用 plan/dontAsk 权限模式**：无 TTY 下工具审批无人应答，会话存档实证工具调用全部「User cancelled」（2026-09-12 plan/dontAsk 两形态复现；dontAsk 语义=仅预批工具的严格白名单）——免审批用 `--always-approve`、只读用 `--sandbox read-only`（读全盘、只写 ~/.grok+temp，产品定位即 code review）双闸，跑完实核 `git status` 干净兜底。
- 默认模型配置为 `grok-4.7`；未点名时沿用 CLI 当前配置的模型与推理档，不传覆盖参数。用户点名时用 `-m` / `--reasoning-effort` 指定对应项。
- grok 是独立 CLI，**不在 codex models_cache 里**；stdout 会混排中途叙述与最终报告，落盘后取报告段。

## 提示词构建标准（2026-09-12 v2 定案：短提示词；复评感知）

三通道共用模板和三个槽位，目标体量约 5–8KB。提供相同评审对象版本和初始范围，相关约束通过当前规范位置定位，不注入历史总表；评审者可独立扩展读取。治理统一见 `docs/design/project-rules.md` 文档治理。

槽位与填法：

1. **评审对象**：首轮 = 具体 commit hash + diff 范围（重点末提交）；复评轮 = 上轮收口后的增量 commit + 上轮范围作背景，并注明「复核上轮修复 + 增量 + 残余扫描」。
2. **定案与边界**：列本任务相关当前规范的文件/章节、适用范围与有效例外，及任务验收安排；区分已采纳目标与当前实现，不能列无条件禁报清单。
3. **专项清单**（复评轮必设，首轮可选）：
   - 全部**写入口 × 全部读入口**枚举（不是「上轮改过的方法」——按修复清单枚举漏入口是三轮 P1 主因）；
   - 不变量**逐分支对照实现**（回退/fail-open 语义逐分支镜像，不能只写一句）；
   - 本任务**可能破坏的既有测试/入口**清单；
   - **复核上一轮外部评审处置**——附上轮 P0~P3 结论清单与处置状态（已修/已撤回/待定），撤回项附撤回依据；
   - 给评审写的核查清单必须先自己逐条过一遍实现（五轮实证：清单点名过仍照样漏）。

追加项：送评对象未定稿时，槽位 2 注明「用户尚未确认定稿，本轮结论直接影响是否定稿，勿因前轮已处置而降低强度」，并要求评审末尾给出「可定稿 / 需修订（列必改项）」明确判断（配合「方案定稿以用户确认为准」定规）。多通道并行评审共用同一提示词文件（同评审对象 + 同专项清单）。

## 评审提示词模板（三通道共用；除三个 `{{槽位}}` 外固定逐字使用）

```markdown
你是一名资深软件工程师和代码审查专家，请对「〇」节指定的变更做严格、面向生产环境的评审。你的目标不是总结代码或找格式/风格问题，而是发现会导致真实故障、数据错误、安全问题、兼容性问题或维护风险的缺陷。

## 硬性纪律（全程适用）

- 全程在你的主会话内完成：禁止使用任何子代理/多代理/并行代理机制（如 spawn_agent、Task、Workflow、子代理会话）执行评审的任何部分。
- 只读评审：不修改任何文件与 git 状态，不主动运行构建或测试；验证仅用只读命令（git log/show/diff、搜索、读文件、CLI --help 类查询）；测试结论只引用仓库既有 surefire/vitest 报告，不重新构建。
- 开始前读 `docs/design/decision-registry.md` 定案入口，根据任务引用、修改与调用范围、业务概念定位当前规范及例外，不默认整读历史。报告候选前按 project-rules 文档治理复核；已知取舍、恢复机制、范围调整和冲突追溯原决定及替代关系。相同范围/前提不重复决策；已修不豁免回归，范围外问题和前提变化仍核实。报告列出相关依据，撤回争议项说明具体决定与匹配范围。

## 〇、评审范围与背景

### 评审对象

{{评审对象：具体 commit hash + diff 范围（如 <base>..<head>）+ 重点范围 + 首轮/复评轮标记}}

### 任务相关定案与已知边界

{{定案与边界：当前规范文件/章节+适用范围和有效例外+任务验收安排；说明目标、现状与迁移差异}}

### 本次专项核查清单（复评轮必填；首轮可留空）

{{专项清单：写入口×读入口枚举 / 不变量逐分支对照 / 可能破坏的既有测试与入口 / 复核上一轮外部评审处置（含上轮结论与处置状态）/ 其他}}

## 一、评审方法

- **不要只看 diff**：先理解变更目的与修改前后行为、受影响的输入与调用路径；主动追调用链（调用方 → 本次修改 → 下游 → 数据库/缓存 → 返回值 → 后续使用方）；检查相关调用方/被调方/DTO/配置/相似实现/测试/文档；与库内成熟实现的差异要分析是否合理。
- **主动推断隐含约束**：租户隔离、所有权校验、状态转换顺序、事务边界、缓存同步刷新、唯一性/非空约束、删除的关联数据处理、接口兼容、异常上抛 vs 吞掉、服务端权限校验等——能从代码/测试/DDL/调用方式找到证据的优先报告。
- **逐类检查**：正确性（条件/分支遗漏/边界/off-by-one/返回值预期）；空值与异常处理（null/空集/超时/外部失败/异常吞掉）；数据一致性（脏数据/部分成功/幂等/事务/约束违反）；并发（竞态/check-then-act/非原子更新/锁范围）；安全（权限绕过/越权/IDOR/租户隔离失效/注入/敏感数据泄露）；兼容性（API 契约/字段含义/schema/配置变化对既有调用方影响）；性能（只报实际影响：N+1/无界循环/重复查询/锁持有膨胀，不报理论微优化）；设计重量（过度设计）：新增抽象/配置项/边界分支无真实触发场景或调用方、可被既有机制（DomainService/公共工具/公共组件）覆盖、为假想场景预留——按「输出要求」单独列「过度设计可裁剪项」节，不计入 P0-P3 分级与总结计数；测试覆盖（缺口不单独成条，仅在问题成立时随「建议」指出缺失场景）。

## 二、避免误报

报告前自查：问题真的会发生吗？是否已由上游/数据库约束/框架/其他代码保证？是否有明确代码证据？结论若依赖框架或数据库内部行为（ORM 的 null 字段处理、索引是否命中、两种写法是否归一等），必须引用框架源码、官方文档或既有测试作为证据——纯推断不得作为正式问题输出。无法确认的「理论上可能」不报；可以继续搜索代码找证据。

## 三、严重程度（仅用四档）

- **P0**：严重安全漏洞 / 大范围数据损坏 / 系统不可用 / 权限体系被绕过
- **P1**：很可能导致生产故障 / 数据错误 / 明显安全问题 / 关键功能不可用
- **P2**：特定条件下功能错误 / 边界情况失败 / 一致性问题 / 明显兼容性问题
- **P3**：真实存在但影响有限（仅日志内容、极端内部路径、无数据副作用的边界表现）

不要为了填充结果制造问题。

## 四、输出要求

- 中文，按严重度从高到低排序；同根因多个受影响位置合并为一条发现（「位置」中列全部文件与行号，不按位置拆条）。
- 每条问题使用以下格式：

### [P0/P1/P2/P3] 简短标题

**位置：** `文件路径:行号` 或具体方法（同根因多处时全部列出）
**问题：** 哪里错了。
**触发条件：** 什么情况下会发生。
**影响：** 实际后果。
**证据：** 代码/调用方/接口/约束/测试/既有实现依据。
**建议：** 具体修复方向（优先复用既有机制；同类问题多点位出现时优先公共架构层统一解——DomainService/公共工具/公共组件/框架层一处修复、全部调用方受益，并列出可统一的调用点全集）。

- 「过度设计可裁剪项」单独成节：每条列位置、无真实触发场景/可被哪个既有机制覆盖的证据、裁剪方向；本节不计入 P0-P3 分级与总结计数，无可裁剪项时输出「过度设计可裁剪项：无」。
- 与本次变更无关的存量问题放文末「存量观察」一节（每条一行：位置 + 一句话）；仅当本次变更扩大其影响或使其暴露时，才按正常分级报告并注明「既有问题被本次变更放大」。
- 末尾「总结」节列 P0~P3 各级数量；没有值得报告的真实问题时，明确输出**「未发现具有明确证据的生产级缺陷。」**

## 五、执行节奏

- 不要发现前几个问题后就停止。
- 完成第一轮检查后，进行第二轮**换方法**复查——不是把第一轮清单再过一遍：若〇节有专项核查清单，逐条执行并报告执行结果；无清单（或未覆盖）时按默认透镜：列出本次变更新增/修改的全部写入口，逐个反查全部调用方是否经过同一约束；列出全部新增/变更 API 入口，逐个走一遍认证→授权→租户隔离链；列出全部数据库写入点，逐个反查所在事务边界与失败回滚路径。
- 两轮检查完成后再输出最终评审结果。
```
## 结论处置

- 逐条代码级核实：与有效接受边界完全相同且无新影响时引用当前规则说明；历史修复事实不能自动撤回回归。撤回需核对原范围和前提；来源冲突或证据不足须说明具体未核实项。事实性最小修正按当前授权执行，设计取舍再按决策提问协议处理。
- **属实结论即类推扫描**（2026-09-20 用户定规）：核实属实的结论在同模式调用点、同款 helper 副本、同模板页面/组件、兄弟能力包同层排查同类问题，命中并入本轮处置——评审对象限定 diff 范围，范围外同类问题不会自来。
- 外部修法建议对照任务卡/契约权威裁定，可只部分采纳（先例：checkAndThrow 统一 99999 与任务卡定稿冲突，只取测试装配部分）。**采纳修法优先公共架构层统一解决**（2026-09-20 用户定规）：同类问题多点位时先评估上移 DomainService/公共工具/公共组件/框架层一处修复、全部调用方受益；统一方案构成大范围重构时交用户定夺。
- **多通道结论矛盾处由主代理亲核代码裁决**，不偏信任何一家。
- 存疑项按 `.claude/rules/decision-question-protocol.md` 举例提问，一次一条等表态。
- 收敛判定参考：P1 降至个位且全为机械修零决策项 = 接近收敛；每轮仍有新边界分支被发现 = 未收敛。**是否继续跑由用户决定。**
