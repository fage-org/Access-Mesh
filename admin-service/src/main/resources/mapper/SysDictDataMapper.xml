<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ac.fage.accessmesh.admin.mapper.SysDictDataMapper">

    <select id="selectByIdSafe" resultType="cn.ac.fage.accessmesh.admin.entity.SysDictData">
        SELECT * FROM sys_dict_data
        WHERE id = #{id} AND tenant_id = #{tenantId} AND delete_flag = 0
    </select>

    <select id="selectDistinctDictTypesByTenantAndTypes" resultType="cn.ac.fage.accessmesh.admin.entity.SysDictData">
        SELECT dict_type FROM sys_dict_data
        WHERE tenant_id = #{tenantId} AND delete_flag = 0
        AND dict_type IN
        <foreach collection="dictTypes" item="dictType" open="(" separator="," close=")">
            #{dictType}
        </foreach>
        GROUP BY dict_type
    </select>

    <select id="selectByTenantAndDictTypes" resultType="cn.ac.fage.accessmesh.admin.entity.SysDictData">
        SELECT * FROM sys_dict_data
        WHERE tenant_id = #{tenantId} AND delete_flag = 0
        AND dict_type IN
        <foreach collection="dictTypes" item="dictType" open="(" separator="," close=")">
            #{dictType}
        </foreach>
        ORDER BY sort_order ASC
    </select>

    <select id="selectByTenantAndDictType" resultType="cn.ac.fage.accessmesh.admin.entity.SysDictData">
        SELECT * FROM sys_dict_data
        WHERE tenant_id = #{tenantId} AND delete_flag = 0
        AND dict_type = #{dictType}
        ORDER BY sort_order ASC
    </select>

</mapper>
