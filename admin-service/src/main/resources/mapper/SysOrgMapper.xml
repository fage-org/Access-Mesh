<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ac.fage.accessmesh.admin.mapper.SysOrgMapper">

    <select id="paginateOrgs" resultType="cn.ac.fage.accessmesh.admin.entity.SysOrg">
        SELECT * FROM sys_org
        WHERE tenant_id = #{tenantId} AND delete_flag = 0
        <if test="orgName != null">
            AND name LIKE CONCAT('%', #{orgName}, '%')
        </if>
        <if test="orgType != null">
            AND org_type = #{orgType}
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY sort_order ASC, created_at ASC
    </select>

    <select id="selectOrgsForTree" resultType="cn.ac.fage.accessmesh.admin.entity.SysOrg">
        SELECT * FROM sys_org
        WHERE tenant_id = #{tenantId} AND delete_flag = 0
        <if test="orgType != null">
            AND org_type = #{orgType}
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY sort_order ASC, created_at ASC
    </select>

    <update id="softDeleteBatch">
        UPDATE sys_org
        SET delete_flag = id, deleted_at = #{deletedAt}
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND delete_flag = 0 AND tenant_id = #{tenantId}
    </update>

    <!-- CTE recursive query to get all descendant IDs (excluding self) -->
    <select id="selectDescendantIds" resultType="java.lang.Long">
        WITH RECURSIVE descendants AS (
            SELECT id, parent_id FROM sys_org
            WHERE id = #{orgId} AND tenant_id = #{tenantId} AND delete_flag = 0
            UNION ALL
            SELECT o.id, o.parent_id FROM sys_org o
            INNER JOIN descendants d ON o.parent_id = d.id
            WHERE o.tenant_id = #{tenantId} AND o.delete_flag = 0
        )
        SELECT id FROM descendants WHERE id != #{orgId}
    </select>

    <!-- CTE recursive query to get all descendant IDs including self -->
    <select id="selectDescendantIdsIncludingSelf" resultType="java.lang.Long">
        WITH RECURSIVE descendants AS (
            SELECT id, parent_id FROM sys_org
            WHERE id = #{orgId} AND tenant_id = #{tenantId} AND delete_flag = 0
            UNION ALL
            SELECT o.id, o.parent_id FROM sys_org o
            INNER JOIN descendants d ON o.parent_id = d.id
            WHERE o.tenant_id = #{tenantId} AND o.delete_flag = 0
        )
        SELECT id FROM descendants
    </select>

    <!-- Batch CTE recursive query to get descendants for multiple starting points (performance fix: avoid N+1) -->
    <select id="selectBatchDescendantIds" resultType="cn.ac.fage.accessmesh.admin.mapper.SysOrgMapper$DescendantResult">
        WITH RECURSIVE
        starting_points AS (
            SELECT id AS root_id FROM sys_org
            WHERE id IN
            <foreach collection="orgIds" item="orgId" open="(" separator="," close=")">
                #{orgId}
            </foreach>
            AND tenant_id = #{tenantId} AND delete_flag = 0
        ),
        descendants AS (
            SELECT sp.root_id, o.id AS descendant_id, o.parent_id
            FROM sys_org o
            INNER JOIN starting_points sp ON o.id = sp.root_id
            WHERE o.tenant_id = #{tenantId} AND o.delete_flag = 0
            UNION ALL
            SELECT d.root_id, o.id AS descendant_id, o.parent_id
            FROM sys_org o
            INNER JOIN descendants d ON o.parent_id = d.descendant_id
            WHERE o.tenant_id = #{tenantId} AND o.delete_flag = 0
        )
        SELECT root_id AS orgId, descendant_id AS descendantId FROM descendants
    </select>

    <select id="selectValidById" resultType="cn.ac.fage.accessmesh.admin.entity.SysOrg">
        SELECT * FROM sys_org
        WHERE id = #{orgId} AND tenant_id = #{tenantId} AND delete_flag = 0
    </select>

    <select id="selectValidByIds" resultType="cn.ac.fage.accessmesh.admin.entity.SysOrg">
        SELECT * FROM sys_org
        WHERE tenant_id = #{tenantId} AND delete_flag = 0
        AND id IN
        <foreach collection="orgIds" item="orgId" open="(" separator="," close=")">
            #{orgId}
        </foreach>
    </select>

    <select id="selectByIdsForAncestors" resultType="cn.ac.fage.accessmesh.admin.entity.SysOrg">
        SELECT * FROM sys_org
        WHERE tenant_id = #{tenantId} AND delete_flag = 0
        AND id IN
        <foreach collection="orgIds" item="orgId" open="(" separator="," close=")">
            #{orgId}
        </foreach>
    </select>

    <select id="countChildren" resultType="long">
        SELECT COUNT(*) FROM sys_org
        WHERE tenant_id = #{tenantId} AND parent_id = #{orgId} AND delete_flag = 0
    </select>

    <select id="selectByCode" resultType="cn.ac.fage.accessmesh.admin.entity.SysOrg">
        SELECT * FROM sys_org
        WHERE tenant_id = #{tenantId} AND code = #{code} AND delete_flag = 0
    </select>

    <select id="selectExistingByCodes" resultType="cn.ac.fage.accessmesh.admin.entity.SysOrg">
        SELECT * FROM sys_org
        WHERE tenant_id = #{tenantId} AND delete_flag = 0
        AND code IN
        <foreach collection="codes" item="code" open="(" separator="," close=")">
            #{code}
        </foreach>
    </select>

</mapper>