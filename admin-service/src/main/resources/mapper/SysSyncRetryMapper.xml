<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ac.fage.accessmesh.admin.mapper.SysSyncRetryMapper">

    <select id="selectByMessageKey" resultType="cn.ac.fage.accessmesh.admin.entity.SysSyncRetry">
        SELECT * FROM sys_sync_retry
        WHERE tenant_id = #{tenantId}
          AND message_key = #{messageKey}
          AND delete_flag = 0
    </select>

    <select id="selectByIdSafe" resultType="cn.ac.fage.accessmesh.admin.entity.SysSyncRetry">
        SELECT * FROM sys_sync_retry
        WHERE id = #{id}
          AND tenant_id = #{tenantId}
          AND delete_flag = 0
    </select>

    <select id="selectPendingRetries" resultType="cn.ac.fage.accessmesh.admin.entity.SysSyncRetry">
        SELECT * FROM sys_sync_retry
        WHERE status = 'pending'
          AND retry_count &lt; max_retries
          AND next_retry_at &lt;= #{now}
          AND tenant_id = #{tenantId}
          AND delete_flag = 0
        ORDER BY created_at ASC
    </select>

    <select id="paginateByTenantId" resultType="cn.ac.fage.accessmesh.admin.entity.SysSyncRetry">
        SELECT * FROM sys_sync_retry
        WHERE tenant_id = #{tenantId}
          AND delete_flag = 0
        ORDER BY created_at DESC
    </select>

</mapper>
