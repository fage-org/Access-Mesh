<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ac.fage.accessmesh.admin.mapper.SysJobMapper">

    <select id="selectValidById" resultType="cn.ac.fage.accessmesh.admin.entity.SysJob">
        SELECT * FROM sys_job
        WHERE id = #{id} AND tenant_id = #{tenantId} AND delete_flag = 0
    </select>

    <select id="selectEnabledJobs" resultType="cn.ac.fage.accessmesh.admin.entity.SysJob">
        SELECT * FROM sys_job
        WHERE tenant_id = #{tenantId} AND status = 1 AND delete_flag = 0
    </select>

    <select id="selectValidByIds" resultType="cn.ac.fage.accessmesh.admin.entity.SysJob">
        SELECT * FROM sys_job
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND tenant_id = #{tenantId} AND delete_flag = 0
    </select>

    <select id="paginateJobs" resultType="cn.ac.fage.accessmesh.admin.entity.SysJob">
        SELECT * FROM sys_job
        WHERE tenant_id = #{tenantId} AND delete_flag = 0
        <if test="jobGroup != null">
            AND job_group = #{jobGroup}
        </if>
        ORDER BY created_at DESC
    </select>

    <update id="softDeleteBatch">
        UPDATE sys_job
        SET delete_flag = id, deleted_at = #{deletedAt}
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND delete_flag = 0
        AND tenant_id = #{tenantId}
    </update>

</mapper>
